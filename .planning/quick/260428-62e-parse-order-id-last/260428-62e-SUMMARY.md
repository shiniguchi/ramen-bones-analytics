---
quick_id: 260428-62e
slug: parse-order-id-last
date: 2026-04-28
status: complete
commits:
  - bc67dc6
---

# Quick Task 260428-62e — Summary

## What changed

- **`scripts/ingest/parse.ts`** — moved `'order_id'` from index 24 to last position in `ORDERBIRD_COLUMNS`. Total length unchanged (29).
- **`tests/ingest/fixtures/sample.csv`** — reordered the `order_id` column from position 25 to position 29 in the header and all 30 data rows so the fixture continues to satisfy the strict parser.

## Why

The new source-of-truth file `orderbird_data/5-JOINED_DATA/ramen_bones_order_items_20260428.csv` emits `order_id` as the last column. Pre-edit, the parser threw `CSV column mismatch at position 23: expected "order_id", got "wl_card_number"`. Aligning the parser with the joiner's output is durable; massaging every CSV drop is not.

## Verification

- `npx vitest run tests/ingest/normalize.test.ts tests/ingest/idempotency.test.ts tests/ingest/loader.test.ts tests/ingest/backfill.test.ts tests/ingest/hash.test.ts` → **34/34 passed**.
- `tests/ingest/schema.test.ts` has 1 unrelated failure (`column transactions_filterable_v.wl_issuing_country does not exist`) — that test hits a remote DB view and is independent of the parse-layer change.

## Out of scope (handled outside this quick task)

- Uploading the new CSV to Supabase Storage and running `npm run ingest`.
- Visual review at `localhost:5173`.

## Commit

- `bc67dc6` — fix(ingest): align ORDERBIRD_COLUMNS with new joined-CSV order_id-last format
