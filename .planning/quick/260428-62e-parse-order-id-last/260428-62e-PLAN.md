---
quick_id: 260428-62e
slug: parse-order-id-last
date: 2026-04-28
description: Move order_id to last position in ORDERBIRD_COLUMNS to match new joined-CSV format
status: pending
---

# Quick Task 260428-62e — Parser column-order alignment

## Why

The new source-of-truth file `orderbird_data/5-JOINED_DATA/ramen_bones_order_items_20260428.csv` emits `order_id` as the **last** column (after the four `wl_*` columns). The existing strict parser at `scripts/ingest/parse.ts:7-37` expects `order_id` at position 24 (between `is_cash` and `wl_card_number`) and throws `CSV column mismatch at position 23` otherwise.

Aligning `ORDERBIRD_COLUMNS` with the joiner's emitted order is the durable fix — beats massaging every new CSV drop.

## Tasks

### 1. Update `scripts/ingest/parse.ts`

- Move the string literal `'order_id'` from index 24 (line 32) to the end of the `ORDERBIRD_COLUMNS` array (after `'wl_issuing_country'`).
- Total length stays 29 — no other changes.

**Verify:** `grep -n "'order_id'" scripts/ingest/parse.ts` shows `order_id` on the line after `'wl_issuing_country'`.

### 2. Update fixture `tests/ingest/fixtures/sample.csv`

Reorder columns so `order_id` is last (matches new parser expectation). Apply to header AND all 30 data rows. No quoted fields in this fixture, simple awk OK.

**Verify:** `head -1 tests/ingest/fixtures/sample.csv` ends with `,wl_issuing_country,order_id`.

### 3. Run the ingest test suite

`npm run test:integration` (or `vitest run tests/ingest`) — confirm `tests/ingest/normalize.test.ts`, `loader.test.ts`, `idempotency.test.ts` still pass with the reordered fixture.

**Verify:** All ingest tests green.

## must_haves

- `scripts/ingest/parse.ts:ORDERBIRD_COLUMNS` ends with `wl_issuing_country, order_id`.
- `tests/ingest/fixtures/sample.csv` header matches the new column order.
- Existing ingest tests still pass.
- New CSV `ramen_bones_order_items_20260428.csv` parses without error (will be exercised by the post-quick ingest run, not this task).

## Out of scope

- Uploading the new CSV to Supabase Storage (handled outside `/gsd:quick`).
- Running `npm run ingest` (handled outside `/gsd:quick`).
