---
phase: quick-260417-29v
plan: 01
subsystem: security-ssr-headers
tags: [security, ssr, cloudflare, headers, csp]
type: summary
requires: []
provides:
  - SSR handle hook that sets 5 security headers on every Response
affects:
  - src/hooks.server.ts
tech-stack:
  added: []
  patterns:
    - "await resolve() + response.headers.set() for SSR header injection"
key-files:
  created: []
  modified:
    - src/hooks.server.ts
decisions:
  - "SSR headers live in handle() hook, not middleware — adapter-cloudflare only applies _headers to static assets, so defense-in-depth requires setting them on the resolved Response"
  - "CSP/header values mirror _headers verbatim — any tightening is a separate task to keep this change strictly gap-closing"
  - "filterSerializedResponseHeaders option preserved; no change to Supabase session/cookie flow"
requirements:
  - SEC-SSR-HEADERS-01
metrics:
  duration: "~1 min"
  tasks-completed: 1
  files-touched: 1
  completed-date: "2026-04-17"
---

# Quick 260417-29v: Apply Security Headers to SSR Responses Summary

SSR responses from SvelteKit now ship with the same 5 security headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, CSP) that `_headers` applies to static assets — closing the adapter-cloudflare gap where SSR-rendered HTML was previously header-less in production.

## What Changed

**File:** `src/hooks.server.ts`

- Changed `return resolve(event, {...})` → `const response = await resolve(event, {...})`
- Added 5 `response.headers.set(...)` calls with values VERBATIM from `_headers`:
  - `X-Frame-Options: DENY`
  - `X-Content-Type-Options: nosniff`
  - `Referrer-Policy: strict-origin-when-cross-origin`
  - `Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=(), usb=()`
  - `Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self' https://*.supabase.co wss://*.supabase.co; frame-ancestors 'none'; base-uri 'self'; form-action 'self'`
- Added a junior-friendly comment block explaining the adapter-cloudflare SSR gap
- `filterSerializedResponseHeaders` preserved (Supabase serialization filter intact)
- `_headers` file untouched (static-asset defense-in-depth stays)

## Verification

### Automated

**Unit tests:** `npm run test:unit` → 88/88 passed (10 files, 10.08s).

```
 Test Files  10 passed (10)
      Tests  88 passed (88)
```

**Source contains all 5 header names:** `grep -c` across `X-Frame-Options|X-Content-Type-Options|Referrer-Policy|Permissions-Policy|Content-Security-Policy` → 5 matches.

**Local curl (dev server already running on :5173):**

```
$ curl -sI http://localhost:5173/ | grep -iE "^(x-frame-options|x-content-type-options|referrer-policy|permissions-policy|content-security-policy):"

content-security-policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self' https://*.supabase.co wss://*.supabase.co; frame-ancestors 'none'; base-uri 'self'; form-action 'self'
permissions-policy: geolocation=(), microphone=(), camera=(), payment=(), usb=()
referrer-policy: strict-origin-when-cross-origin
x-content-type-options: nosniff
x-frame-options: DENY
```

All 5 headers present on the SSR response (a 303 redirect from `/` → `/login`, meaning the handle hook is being hit and headers are applied before the response is returned). CSP value is byte-exact match with `_headers` line 6.

### Manual / Human Verification

Not required for this change — purely additive header-set calls on top of an existing resolved Response. Unit tests (88/88) + live curl output above cover behavior.

## Deviations from Plan

None — plan executed exactly as written. CSP value copied byte-for-byte from `_headers` line 6, all 5 headers applied, `filterSerializedResponseHeaders` preserved, `_headers` untouched, tests green.

## Success Criteria

- [x] `src/hooks.server.ts` sets all 5 security headers on every SSR Response
- [x] Values mirror `_headers` VERBATIM (no tightening, no modification)
- [x] `filterSerializedResponseHeaders` option preserved
- [x] `_headers` file untouched (defense-in-depth for static assets)
- [x] `npm run test:unit` passes 88/88
- [x] `curl -I http://localhost:5173/` confirms all 5 headers present on SSR response
- [x] Junior-friendly comment explains the adapter-cloudflare gap

## Commits

- `11e85b9` — feat(quick-260417-29v): apply 5 security headers to SSR responses

## Self-Check: PASSED

- File exists: `src/hooks.server.ts` — FOUND
- Commit exists: `11e85b9` — FOUND
- Header names in source: 5/5 present
- Unit tests: 88/88 passing
- Curl output: 5/5 headers verified on SSR response
