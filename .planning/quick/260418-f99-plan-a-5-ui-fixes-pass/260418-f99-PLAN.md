---
quick_id: 260418-f99
mode: quick
title: "Plan A: 5 UI fixes pass"
type: execute
wave: 1
autonomous: true
files_modified:
  - src/lib/components/SegmentedToggle.svelte
  - src/lib/components/CohortRetentionCard.svelte
  - src/lib/components/CalendarRevenueCard.svelte
  - src/lib/components/CalendarCountsCard.svelte
  - src/lib/components/CalendarItemsCard.svelte
  - src/lib/components/CohortAvgLtvCard.svelte
  - src/lib/components/LtvHistogramCard.svelte
  - src/lib/components/KpiTile.svelte
  - .claude/memory/feedback_layerchart_mobile_scroll.md
must_haves:
  truths:
    - "Y-axis currency labels (€1,2M etc.) render fully; no clipping on 5 BarCharts"
    - "SegmentedToggle 'Takeaway' label no longer wraps/overflows; FilterBar scrolls if needed"
    - "KPI big number scales down on <sm screens and falls back to compact format at ≥ €100k"
    - "PC trackpad vertical scroll works over all 6 chart cards; mobile horizontal chart scroll still works"
    - "Retention tooltip paints visibly on hover/tap (div content no longer trapped inside SVG namespace)"
  artifacts:
    - path: "src/lib/components/SegmentedToggle.svelte"
      provides: "flex-1 min-w-0 removed from button class"
    - path: "src/lib/components/CohortRetentionCard.svelte"
      provides: "Tooltip.Root moved outside Svg; touchEvents: 'auto'"
    - path: "src/lib/components/KpiTile.svelte"
      provides: "Responsive big number + formatEURShort fallback at ≥ €100k"
    - path: ".claude/memory/feedback_layerchart_mobile_scroll.md"
      provides: "Appended 2026-04-18 pan-x→auto rule-of-thumb update"
  key_links:
    - from: "5 BarChart cards"
      to: "LayerChart <BarChart> padding prop"
      via: "props.padding (if accepted) or top-level padding fallback"
      pattern: "padding:.*left: 40"
    - from: "KpiTile"
      to: "$lib/format.formatEURShort"
      via: "import"
      pattern: "import.*formatEURShort.*from.*\\$lib/format"
---

<objective>
Apply 5 locked UI fixes + 1 memory update to the dashboard. Each fix ships as its own atomic commit for easy revert. Prescription is locked by the user (Plan A from deepsearch) — do NOT re-evaluate approach or substitute alternatives.

Purpose: Fix 5 concrete UI issues the user hit while using the live dashboard — chart label clipping, overflow on the segmented toggle, KPI tile overflow on small screens, PC trackpad vertical-scroll regression on charts, and the retention tooltip that paints nothing on hover.

Output: 6 atomic commits on the current branch; 198 unit tests still green; svelte-check stays at the 17-error baseline; DEV still renders.
</objective>

<execution_context>
Quick mode — target ≤30% context. No discovery. Prescription in `<source_of_truth>` above is the spec.

Rules from CLAUDE.md:
- Always verify against DEV after each commit (push → deploy → Chrome MCP / curl).
- Never add `Co-authored-by: Claude <noreply@anthropic.com>` to commits.
- Minimal changes; edit existing files only.
- One task = one commit. No cross-task coupling.
</execution_context>

<context>
@.planning/STATE.md
@.claude/memory/feedback_layerchart_mobile_scroll.md

Files under edit:
@src/lib/components/SegmentedToggle.svelte
@src/lib/components/CohortRetentionCard.svelte
@src/lib/components/CalendarRevenueCard.svelte
@src/lib/components/CalendarCountsCard.svelte
@src/lib/components/CalendarItemsCard.svelte
@src/lib/components/CohortAvgLtvCard.svelte
@src/lib/components/LtvHistogramCard.svelte
@src/lib/components/KpiTile.svelte
@src/lib/format.ts
</context>

<risks>
Surfaced from reading the files — resolve during execution, do NOT silently change scope:

1. **LayerChart `padding` location (Fix 1):** `<BarChart>` is the high-level wrapper; `CohortRetentionCard` uses `<Chart padding={...}>` at the Chart level. Whether `<BarChart props={{ padding: {...} }}>` forwards through to the inner `<Chart>` needs a quick source check. If it does NOT, the fallback is `<BarChart padding={...}>` as a top-level prop on BarChart, mirroring how `CohortRetentionCard` does it on `<Chart>`. Pick whichever path actually renders padded Y-axis labels. Document chosen path in SUMMARY.

2. **Fix 4 on `CohortRetentionCard` (outer wrapper):** The retention card's outer wrapper is `class="mt-4 h-64 chart-touch-safe"` — no `touch-pan-x` or `overflow-x-auto` (retention doesn't scroll horizontally). For this card, ONLY change the inner `tooltipContext` from `'pan-x'` → `'auto'`. Do NOT add `touch-auto` to this wrapper; there's nothing to replace. The outer-wrapper class change applies only to the 5 BarChart cards.

3. **Fix 3 import list:** `KpiTile.svelte` currently imports only `formatEUR`. Add `formatEURShort` to the same import line, not a new import statement.

4. **Fix 5 import list:** `CohortRetentionCard.svelte` already imports `Tooltip` from layerchart (line 11). No new import needed — only JSX restructuring.

5. **Mobile horizontal-scroll regression on `touch-auto` (Fix 4 safety gate):** If after `pan-x → auto` the phone loses horizontal chart scroll (browser takes both axes for page scroll), fall back to the CSS override in `src/app.css`: `.chart-touch-safe :global(.lc-tooltip-context) { touch-action: manipulation; }`. Do NOT apply preemptively — try `'auto'` first, document in SUMMARY.
</risks>

<tasks>

<task type="auto" id="T1">
  <name>Task 1 — Fix 2: SegmentedToggle Takeaway overflow (1-line)</name>
  <files>src/lib/components/SegmentedToggle.svelte</files>
  <action>
On the `<button>` class string (line ~17), remove the `flex-1 min-w-0` fragment. Keep `whitespace-nowrap`. Result: buttons size to their content instead of forcing equal widths, so "Takeaway" no longer truncates. FilterBar's parent row already has `overflow-x-auto` so the bar scrolls on narrow viewports.

Before:
```
class="min-h-11 min-w-0 flex-1 rounded-md px-3 py-1.5 text-sm font-medium whitespace-nowrap transition-colors
```

After:
```
class="min-h-11 rounded-md px-3 py-1.5 text-sm font-medium whitespace-nowrap transition-colors
```

Do NOT touch `GrainToggle.svelte` — it's a separate component and was not in Plan A's scope.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose tests/unit 2>&1 | tail -20</automated>
Then open DEV dashboard via Chrome MCP, screenshot the FilterBar at 375px width. Confirm:
- "Takeaway" label renders in full on its button
- No visible clipping or ellipsis
- Buttons no longer stretch to equal widths
  </verify>
  <done>
- `flex-1 min-w-0` removed from SegmentedToggle button class
- Unit tests pass (198 green baseline)
- DEV screenshot shows Takeaway full text
- Commit: `fix(quick-260418-f99): remove flex-1 min-w-0 from SegmentedToggle so labels size to content`
  </done>
</task>

<task type="auto" id="T2">
  <name>Task 2 — Fix 5: Move retention Tooltip.Root outside Svg</name>
  <files>src/lib/components/CohortRetentionCard.svelte</files>
  <action>
In the `<Chart>…</Chart>` block (lines ~135-169), restructure so `<Tooltip.Root>` is a SIBLING of `<Svg>` inside `<Chart>`, not a child of `<Svg>`. This matches LayerChart's own `DefaultTooltip.svelte` / `PieChart.svelte` / `ArcChart.svelte` convention. Currently the `<div>` elements inside `<Tooltip.Header>` / `<Tooltip.List>` get placed in the SVG namespace and never paint.

Target structure:
```svelte
<Chart
  data={...}
  x={xKey}
  y="retention_rate"
  xScale={scaleLinear()}
  yScale={scaleLinear()}
  xDomain={[0, xDomainMax]}
  yDomain={[0, 1]}
  padding={{ left: 32, bottom: 24, top: 8, right: 8 }}
  tooltipContext={{ mode: 'bisect-x', touchEvents: 'pan-x' }}
>
  <Svg>
    <Axis placement="left" format={(v: number) => `${Math.round(v * 100)}%`} grid />
    <Axis placement="bottom" label={xLabel} />
    {#each series as s, i}
      <Spline
        data={s.rows}
        x={xKey}
        y="retention_rate"
        class="stroke-2"
        stroke={palette[i % palette.length]}
      />
    {/each}
    <Highlight points lines />
  </Svg>
  <Tooltip.Root let:data>
    <Tooltip.Header>
      {data?.cohort_month ?? data?.cohort_week} · {xTipKey} {data?.[xKey]}
    </Tooltip.Header>
    <Tooltip.List>
      <Tooltip.Item label="Retention" value={`${Math.round((data?.retention_rate ?? 0) * 100)}%`} />
      <Tooltip.Item label="Cohort size" value={`${data?.cohort_size_month ?? data?.cohort_size_week ?? 0} customers`} />
    </Tooltip.List>
  </Tooltip.Root>
</Chart>
```

Preserve the exact Tooltip.Header / Tooltip.List / Tooltip.Item contents verbatim — only position relative to `<Svg>` changes. Keep `tooltipContext={{ mode: 'bisect-x', touchEvents: 'pan-x' }}` AS-IS in this task; the `'pan-x' → 'auto'` change lands in Task 4 so the tooltip-paint fix is isolated from the touch-action change.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose tests/unit 2>&1 | tail -20 &amp;&amp; npx svelte-check --threshold error 2>&amp;1 | tail -10</automated>
Open DEV dashboard, hover/tap the retention chart. Confirm:
- Tooltip div paints visibly (previously was empty / invisible)
- Header shows cohort label + week/month index
- List shows Retention % and Cohort size
- Bisect-x behavior (single tooltip follows nearest-x) still works
  </verify>
  <done>
- `<Tooltip.Root>` is sibling of `<Svg>` inside `<Chart>`, not child
- Svelte-check at ≤17 error baseline
- Unit tests pass
- DEV screenshot with visible tooltip content
- Commit: `fix(quick-260418-f99): move retention Tooltip.Root outside Svg so div subtree paints`
  </done>
</task>

<task type="auto" id="T3">
  <name>Task 3 — Fix 1: Y-axis padding on 5 BarCharts</name>
  <files>
src/lib/components/CalendarRevenueCard.svelte,
src/lib/components/CalendarCountsCard.svelte,
src/lib/components/CalendarItemsCard.svelte,
src/lib/components/CohortAvgLtvCard.svelte,
src/lib/components/LtvHistogramCard.svelte
  </files>
  <action>
On each of the 5 `<BarChart>` elements, add padding so compact currency labels like `€1,2M` don't clip. LayerChart's `defaultChartPadding` is only `left: 20`, which isn't enough for formatted Y-axis ticks.

**Primary attempt** — nest padding inside `props` (same slot as `xAxis` / `yAxis`):
```
props={{ xAxis: { ticks: MAX_X_TICKS }, yAxis: { format: <formatter> }, padding: { left: 40, right: 8, top: 8, bottom: 24 } }}
```

**If `props.padding` does NOT take effect** (inspect Y-axis label clipping on DEV after first attempt), fall back to `<BarChart padding={{ left: 40, right: 8, top: 8, bottom: 24 }} ...>` as a top-level prop on `<BarChart>` (same shape `<Chart>` accepts in CohortRetentionCard.svelte line 143). Quick-check path:
```
grep -n "padding" node_modules/layerchart/dist/charts/BarChart.svelte | head -20
```

Apply the same padding shape to all 5 files. Preserve the card-specific formatter (`formatEURShort` on CalendarRevenueCard / CohortAvgLtvCard; `formatIntShort` on CalendarCountsCard / CalendarItemsCard / LtvHistogramCard).

Do NOT touch `CohortRetentionCard.svelte` — it already has `padding={{ left: 32, bottom: 24, top: 8, right: 8 }}` on `<Chart>`.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose tests/unit 2>&1 | tail -20 &amp;&amp; npx svelte-check --threshold error 2>&amp;1 | tail -10</automated>
On DEV:
- Load dashboard with a date range that yields 6-figure revenue (triggers €1,2M-style compact labels)
- Inspect each of the 5 chart cards — Y-axis labels fully visible, not clipped on the left
- Screenshot each chart at 375px width via Chrome MCP
  </verify>
  <done>
- All 5 BarCharts render padded Y-axis labels without clipping
- Which padding path (props.padding vs top-level padding prop) was used is documented in the commit body
- Unit tests pass, svelte-check at baseline
- Commit: `fix(quick-260418-f99): pad Y-axis on 5 BarCharts so €1,2M labels don't clip`
  </done>
</task>

<task type="auto" id="T4">
  <name>Task 4 — Fix 4: touch-action 'auto' on 6 charts + outer wrapper swap on 5</name>
  <files>
src/lib/components/CalendarRevenueCard.svelte,
src/lib/components/CalendarCountsCard.svelte,
src/lib/components/CalendarItemsCard.svelte,
src/lib/components/CohortAvgLtvCard.svelte,
src/lib/components/LtvHistogramCard.svelte,
src/lib/components/CohortRetentionCard.svelte
  </files>
  <action>
Relaxes the mobile-scroll override from `pan-x` to `auto` so PC trackpad vertical scroll routes to the page body instead of being swallowed by the chart. Rationale in memory 2026-04-17.

**All 6 chart cards** — replace the inner `tooltipContext` touch-events value:
- From: `tooltipContext={{ touchEvents: 'pan-x' }}` (5 BarCharts)
- From: `tooltipContext={{ mode: 'bisect-x', touchEvents: 'pan-x' }}` (CohortRetentionCard)
- To:   `tooltipContext={{ touchEvents: 'auto' }}` (5 BarCharts)
- To:   `tooltipContext={{ mode: 'bisect-x', touchEvents: 'auto' }}` (CohortRetentionCard — mode preserved)

**5 BarChart cards ONLY** — outer wrapper div class swap:
- From: `class="... overflow-x-auto touch-pan-x overscroll-x-contain chart-touch-safe"`
- To:   `class="... overflow-x-auto touch-auto overscroll-x-contain chart-touch-safe"`

The 5 cards share the same outer wrapper shape (`mt-4 h-64 overflow-x-auto touch-pan-x overscroll-x-contain chart-touch-safe`); keep every other class verbatim, only `touch-pan-x → touch-auto`.

**CohortRetentionCard outer wrapper** is `class="mt-4 h-64 chart-touch-safe"` — no `touch-pan-x` or `overflow-x-auto` present (retention doesn't horizontally scroll). Do NOT add `touch-auto` here; leave the class untouched. Only the inner `tooltipContext` changes.

**SAFETY GATE** (do NOT apply preemptively):
If after this change mobile horizontal chart scroll regresses (phone browser steals horizontal gestures for page scroll instead of routing to the chart's `overflow-x-auto` wrapper), the CSS fallback is:
```css
/* src/app.css */
.chart-touch-safe :global(.lc-tooltip-context) { touch-action: manipulation; }
```
Try `'auto'` first. Only if verification shows regression, apply the CSS fallback and note it in SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose tests/unit 2>&1 | tail -20 &amp;&amp; grep -rn "touch-pan-x\|touchEvents:.'pan-x'" src/lib/components/ || echo "clean"</automated>
On DEV:
- Desktop: scroll the dashboard page with trackpad vertical gesture while mouse hovers over each of the 6 charts. Page should scroll normally (this is the primary regression being fixed).
- Mobile (Chrome DevTools 375px or real phone): swipe horizontally over each of the 5 BarCharts with overflow — chart wrapper should scroll horizontally. If the page scrolls instead, apply the `.chart-touch-safe :global(.lc-tooltip-context) { touch-action: manipulation; }` fallback in src/app.css and re-verify.
- Mobile vertical swipe over any chart → page scrolls as expected.
- Retention card tooltip still works on hover/tap (no mode regression).
  </verify>
  <done>
- All 6 charts use `touchEvents: 'auto'` (retention preserves `mode: 'bisect-x'`)
- 5 BarChart outer wrappers use `touch-auto` (not `touch-pan-x`)
- CohortRetentionCard outer wrapper unchanged
- Grep confirms no `touch-pan-x` or `touchEvents: 'pan-x'` remain in src/lib/components/
- PC trackpad vertical scroll confirmed working on DEV
- Mobile horizontal chart scroll confirmed working (or CSS fallback applied + documented)
- Commit: `fix(quick-260418-f99): relax chart touch-action pan-x → auto so PC vertical scroll works`
  </done>
</task>

<task type="auto" id="T5">
  <name>Task 5 — Fix 3: KpiTile responsive + compact €100k+ fallback</name>
  <files>src/lib/components/KpiTile.svelte</files>
  <action>
Two changes to the big-number rendering:

**(a) Responsive type size** (line ~64):
Change the `<p>` class for the big number:
- From: `class="mt-1 text-[32px] leading-[1.1] font-semibold tabular-nums text-zinc-900"`
- To:   `class="mt-1 text-2xl sm:text-[32px] leading-[1.1] font-semibold tabular-nums text-zinc-900"`

Small screens downshift from 32px → text-2xl (24px); `sm:` breakpoint (640px+) keeps 32px.

**(b) Compact EUR fallback at €100k+:**
Import additionally:
```ts
import { formatEUR, formatEURShort } from '$lib/format';
```
(Current import is `import { formatEUR } from '$lib/format';` — extend it, don't add a second import line.)

Update the `display` $derived.by so `eur-int` branches on magnitude:
```ts
const display = $derived.by(() => {
  if (value === null) return null;
  if (format === 'eur-int') {
    // Compact form (€100k+) to prevent big-number overflow on narrow tiles.
    // formatEURShort takes EUR, not cents — divide here.
    if (value / 100 >= 100000) return formatEURShort(value / 100);
    return formatEUR(value);
  }
  if (format === 'eur-dec') return formatEUR(value, true);
  return value.toLocaleString('de-DE');
});
```

Rules:
- `eur-dec` path unchanged — still `formatEUR(value, true)` (shows cents precision)
- `int` path unchanged — still `value.toLocaleString('de-DE')`
- `value === null` guard stays — leads to `EmptyState` render, unchanged
- Delta caption formatting (lines ~39-55) is NOT touched
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose tests/unit 2>&1 | tail -40</automated>
On DEV:
- Load dashboard on ≥ €100k range (e.g. full-year date range) — revenue KPI tile shows `€1,2M` style instead of `€1.234.567`
- Load dashboard on small range (today) where revenue < €100k → tile shows full `formatEUR` form
- Switch viewport to mobile 375px → big-number font is 24px (text-2xl)
- Switch to sm+ (≥640px) → big-number font is 32px
- Delta caption text unchanged ("▲ +X%", "▼ −X%", "— flat vs …", "— no prior data")
  </verify>
  <done>
- `text-2xl sm:text-[32px]` applied to big-number `<p>`
- `formatEURShort` imported on the existing import line
- `display` $derived.by branches `eur-int` on `value / 100 >= 100000`
- `eur-dec` and `int` paths unchanged
- Delta caption untouched
- Unit tests pass (KpiTile-specific suite green)
- DEV screenshot at 375px and ≥640px showing correct type scale
- Commit: `fix(quick-260418-f99): KpiTile responsive type + compact €100k+ fallback`
  </done>
</task>

<task type="auto" id="T6">
  <name>Task 6 — Fix 6: Update LayerChart mobile-scroll memory</name>
  <files>.claude/memory/feedback_layerchart_mobile_scroll.md</files>
  <action>
Append (do NOT replace) a dated note to the existing file body. Keep frontmatter and existing paragraphs intact.

Append below the existing bulleted list:

```markdown

## Update 2026-04-18

Relaxed the default rule from `'pan-x'` → `'auto'` after the PC trackpad vertical-scroll regression surfaced on the full dashboard. On desktop, `'pan-x'` was blocking the browser from routing vertical trackpad gestures up to the page scroll container — charts effectively swallowed vertical intent.

Updated rule-of-thumb:
- Default to `tooltipContext={{ touchEvents: 'auto' }}` on LayerChart charts that live inside horizontally-scrollable wrappers.
- Only fall back to `'pan-x'` if `'auto'` actively causes the tooltip-on-swipe regression on a real mobile device (symptom: horizontal swipe shows tooltip instead of scrolling).
- Nuclear CSS fallback if neither works: `.chart-touch-safe :global(.lc-tooltip-context) { touch-action: manipulation; }` in `src/app.css` — only apply when the component-level prop fails.
- Applied across 6 dashboard charts in quick-260418-f99.
```

Then add the one-line index entry to `.claude/memory/MEMORY.md` — NO new memory file needed; the existing `feedback_layerchart_mobile_scroll.md` pointer already lists this topic, so MEMORY.md index is unchanged. Leave MEMORY.md alone.
  </action>
  <verify>
    <automated>tail -20 .claude/memory/feedback_layerchart_mobile_scroll.md</automated>
Visual read — confirm:
- Frontmatter intact
- Original body preserved verbatim
- New "Update 2026-04-18" section appended at bottom
- MEMORY.md index unchanged (existing entry still accurate)
  </verify>
  <done>
- `feedback_layerchart_mobile_scroll.md` has appended 2026-04-18 update
- Original content untouched above the new section
- Commit: `docs(quick-260418-f99): update LayerChart mobile-scroll memory — pan-x → auto default`
  </done>
</task>

</tasks>

<verification>
Overall phase checks (run once after Task 6):
- `npx vitest run` — 198 tests green (baseline)
- `npx svelte-check --threshold error` — ≤17 errors (baseline)
- `git log --oneline -7` — 6 atomic commits on this branch, none with `Co-authored-by: Claude` trailer
- DEV dashboard at 375px + 1440px renders without console errors
- Manual UX checklist:
  - Takeaway button shows full label (Fix 2)
  - Retention chart tooltip paints visible content (Fix 5)
  - All 5 BarChart Y-axis labels fully visible, no clipping (Fix 1)
  - PC trackpad vertical scroll works over all 6 charts (Fix 4)
  - KPI big number downshifts at 375px and uses €1,2M form at ≥€100k (Fix 3)
  - Memory file has 2026-04-18 update section (Fix 6)
</verification>

<success_criteria>
- 6 atomic commits landed on current branch in task order (T1 → T6)
- Every fix from the locked prescription is in exactly one commit (no cross-task coupling)
- 198 unit tests green; svelte-check at 17-error baseline
- DEV deploy succeeds; 5 manual UX checks pass
- No `Co-authored-by` trailer in any commit
- SUMMARY.md captures: which padding path (Fix 1) was used, whether the mobile-scroll CSS fallback (Fix 4) was needed, and any files that differed from the prescription (should be none — if it happens, flag it)
</success_criteria>

<output>
After all 6 tasks ship, write `.planning/quick/260418-f99-plan-a-5-ui-fixes-pass/260418-f99-SUMMARY.md` capturing:
- Commit SHAs per fix
- Fix 1 path taken (`props.padding` vs top-level `<BarChart padding>`)
- Fix 4 CSS fallback — applied Y/N, why
- Test counts before/after
- Any deviations from the prescription (flag, do not silently apply)
</output>
