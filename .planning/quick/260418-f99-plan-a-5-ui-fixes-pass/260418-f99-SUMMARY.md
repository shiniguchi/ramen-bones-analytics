---
quick_id: 260418-f99
status: complete
date: 2026-04-18
commits:
  - bec629c  # T1: SegmentedToggle
  - 73d96b7  # T2: Retention Tooltip.Root outside Svg (incomplete — see T2b)
  - ec905d4  # T3: BarChart Y-axis padding
  - 02f39f3  # T4: touch-action auto
  - 60ef822  # T5: KpiTile responsive
  - e45fc20  # T6: memory append
  - c53cb24  # T2b: Tooltip.Root needs {#snippet children} not let:data (Svelte 5) — caught by Chrome MCP QA
merge_commit: 6d61a36
qa_verified: 2026-04-18T09:28:00Z
---

# Quick Task 260418-f99 — Plan A: 5 UI fixes pass

## Tasks shipped

| Task | Commit | Shipped |
|---|---|---|
| T1 — SegmentedToggle Takeaway overflow | `bec629c` | Removed `flex-1 min-w-0` from button class — buttons size to content |
| T2 — Retention tooltip renders | `73d96b7` | Moved `<Tooltip.Root>` out of `<Svg>`; it's now a sibling inside `<Chart>` so the div subtree paints |
| T3 — Y-axis padding on 5 BarCharts | `ec905d4` | Added `padding={{ left: 40, right: 8, top: 8, bottom: 24 }}` to 5 `<BarChart>` components |
| T4 — touch-action pan-x → auto | `02f39f3` | All 6 charts: `tooltipContext.touchEvents: 'auto'`. 5 BarChart outer wrappers: `touch-pan-x` → `touch-auto`. CohortRetentionCard wrapper untouched (never had `touch-pan-x`) |
| T5 — KpiTile responsive + €100k+ compact | `60ef822` | `text-2xl sm:text-[32px]` + `formatEURShort` fallback when `value/100 >= 100_000` |
| T6 — Memory append | `e45fc20` | `.claude/memory/feedback_layerchart_mobile_scroll.md` — appended 2026-04-18 rule update |
| T2b — Svelte 5 snippet syntax | `c53cb24` | `<Tooltip.Root let:data>` → `<Tooltip.Root>{#snippet children({ data })}...{/snippet}`. Caught by Chrome MCP QA; unit tests passed but the runtime threw `invalid_default_snippet` on every interaction |

## Fix 1 — padding path taken

**Top-level `<BarChart padding={{...}}>`**, not `props.padding`. Reason: BarChart's `props` slot is a keyed dispatch (`xAxis`/`yAxis`/`bars`/`tooltip`/`brush`) and does not forward arbitrary keys through to the underlying `<Chart>`. The top-level `padding` prop mirrors how `CohortRetentionCard` already calls it on `<Chart>` directly. Executor confirmed via LayerChart source.

## Fix 4 — CSS fallback

**Not applied.** `touchEvents: 'auto'` alone is in place. The `.chart-touch-safe :global(.lc-tooltip-context) { touch-action: manipulation; }` escape hatch in `src/app.css` was NOT added. If mobile horizontal-scroll regression surfaces during DEV QA, that's the fallback to apply.

## Test counts

| | Before | After |
|---|---|---|
| `npx vitest run tests/unit` | 198 / 198 green | 198 / 198 green |
| `npx svelte-check --threshold error` | 17 errors (pre-existing in `tests/unit/dashboardStore.test.ts`) | 17 errors (unchanged) |

The executor's completion note mentioned 19 errors via `git stash`; re-run on merged HEAD shows 17. No new errors were introduced either way.

## Deviations from prescription

None. All 6 fixes landed as written. One documented path-choice (Fix 1 top-level vs `props.padding`) — covered in "Fix 1" section above and was explicitly permitted by the plan ("pick whichever path actually renders").

## DEV QA — verified in Chrome MCP (post-deploy 2026-04-18T09:28Z)

URL: `https://ramen-bones-analytics.pages.dev/?range=all&grain=month`
Viewport: 339×734 (mobile emulation in Chrome MCP)

- [x] **Takeaway button** — `scrollWidth=88 clientWidth=88 overflow=false`. Text fits; buttons size to content.
- [x] **Retention chart tooltip** — paints on hover. Verified text content: `"2025-06-01 · Month 5 Retention 4% Cohort size 347 customers"` — all expected fields render. **Fix required an additional commit (`c53cb24`) after `let:data` threw `invalid_default_snippet` at runtime.**
- [x] **5 BarChart Y-axis labels** — all 5 have `g.lc-layout-svg-g transform="translate(40, 8)"`. Visible in screenshots: `800, 600, 400, 200, 0` (Transactions) and `1,5K / 1K / 500 / 0` (Items) render fully with left gutter.
- [x] **PC trackpad vertical scroll** — cannot simulate directly in Chrome MCP (no physical trackpad), but verified via CSS: all 6 `.lc-tooltip-context` elements show `touch-action: auto` (was `pan-x`); 5 BarChart outer wrappers show class `touch-auto`. CohortRetentionCard wrapper unchanged (never had `touch-pan-x`) as designed. Behavior is governed entirely by these CSS values.
- [ ] **Mobile horizontal chart scroll** — Chrome MCP runs with `pointerType:mouse`, not a real touch device, so real-phone horizontal swipe routing cannot be definitively verified here. User should eyeball on a real phone. Fallback (if regressed): add `.chart-touch-safe :global(.lc-tooltip-context) { touch-action: manipulation; }` to `src/app.css`.
- [x] **KPI tile at ≥ €100k** — `€203,3K` rendered on the "All" range revenue tile (raw value was 20,3M cents). Compact formatter engaged correctly.
- [x] **KPI tile on mobile (<640px)** — both KPI tiles measured at `fontSize: 24px` (= `text-2xl`) at 339px viewport. `sm:text-[32px]` will engage at ≥640px.

**Bonus find:** console was showing `invalid_default_snippet` exceptions. Root cause was the Svelte 4 `let:data` syntax carried through on the Tooltip.Root fix; Svelte 5 requires `{#snippet children({ data })}`. Fixed in `c53cb24`. Post-fix redeploy: zero console exceptions.

## Worktree notes

Executor ran in an isolated worktree (`agent-a9aea6e4`). Merge commit `6d61a36` brought all 6 commits back. One cleanup hiccup: initial `git worktree remove --force` left the worktree locked; manual `git worktree unlock && remove` resolved it. Vitest picked up stale `.test.ts` files from the leftover worktree path during one intermediate run — not a real failure, cleared after full worktree removal.
