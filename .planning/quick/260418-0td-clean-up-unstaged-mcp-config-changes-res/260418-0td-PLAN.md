---
quick_id: 260418-0td
description: Clean up unstaged MCP config changes — restore Context7 + claude-in-chrome to .mcp.json, document mcp-servers/, commit in logical chunks
created: 2026-04-17
mode: quick
must_haves:
  truths:
    - .mcp.json must contain all three servers (Context7, claude-in-chrome, supabase-dev) so a fresh clone gets full MCP wiring
    - .mcp.json.template is removed (single source of truth = .mcp.json)
    - mcp-servers/ has a README documenting what postgres.ts is and how it loads SUPABASE_DB_URL
    - No secrets committed (.mcp.json references no credentials directly; secrets stay in .env which is gitignored)
  artifacts:
    - .mcp.json (3-server config)
    - mcp-servers/README.md (purpose + setup notes)
    - .env.example (updated SUPABASE_DB_URL doc — already in WT)
  key_links:
    - .mcp.json
    - .mcp.json.template (deleted)
    - mcp-servers/postgres.ts
    - .env.example
    - .planning/quick/260417-o8a-fix-postgrest-1000-row-cap-on-transactio/260417-o8a-PLAN.md (orphaned, commit separately)
---

## Decision (locked from user)

Track `.mcp.json` in git. Delete `.mcp.json.template`. Forkers get MCP setup out of the box; secrets stay in `.env`.

## Tasks

### Task 1 — Restore servers to .mcp.json + add mcp-servers/README.md
- **files:** `.mcp.json`, `mcp-servers/README.md` (new)
- **action:**
  - Update `.mcp.json` to include Context7, claude-in-chrome, supabase-dev (preserve existing supabase-dev entry; add the two from old template)
  - Create `mcp-servers/README.md` documenting postgres.ts, the SUPABASE_DB_URL env requirement, and how `.mcp.json` references it
- **verify:** `cat .mcp.json` shows 3 servers; `ls mcp-servers/README.md`
- **done:** committed as `chore(mcp): consolidate MCP config into .mcp.json + document mcp-servers/`

### Task 2 — Stage .mcp.json.template deletion + .env.example doc update
- **files:** `.mcp.json.template` (deleted in WT), `.env.example` (modified in WT)
- **action:** Stage and commit alongside Task 1 in the same logical chunk (all relate to the MCP config consolidation)
- **verify:** `git status` shows no remaining MCP-related changes
- **done:** included in Task 1 commit

### Task 3 — Commit orphaned quick-260417-o8a PLAN.md
- **files:** `.planning/quick/260417-o8a-fix-postgrest-1000-row-cap-on-transactio/260417-o8a-PLAN.md`
- **action:** Stage and commit as a docs commit — it's a leftover artifact from a prior quick task
- **verify:** `git status` clean except for new STATE.md changes from this task
- **done:** committed as `docs(quick-260417-o8a): commit lingering plan artifact`

### Task 4 — Update STATE.md and final commit
- **files:** `.planning/STATE.md`, all 260418-0td artifacts
- **action:** Append row to Quick Tasks Completed table, update Last activity, commit
- **verify:** `git status` clean
- **done:** committed as `docs(quick-260418-0td): MCP config cleanup`
