# Plan 19-04: Phase-final QA + planning-docs drift gate

**Goal:** Verify all three implementation plans (19-01, 19-02, 19-03) on localhost and DEV; confirm build metrics; close the planning-docs drift gate so Phase 19 can ship via `gsd-ship`.

**Phase:** 19 (Cold-Start Trim)
**Status:** Pending
**Effort:** ~1 hour
**Depends on:** 19-01, 19-02, 19-03 all complete
**Blocks:** Nothing (phase-final)

---

## QA checklist

### A. Build metrics (run before pushing)

```bash
npm run build
ls -lh .svelte-kit/cloudflare/_app/immutable/chunks/
```

Expected:
- [ ] 5+ async chunks named roughly `CalendarCountsCard-*.js`, `CalendarRevenueCard-*.js`, `CalendarItemsCard-*.js`, `CalendarItemRevenueCard-*.js`, `MdeCurveCard-*.js` (from 19-01)
- [ ] 2 async chunks for `item-counts`/`benchmark` endpoints (from 19-02)
- [ ] 4 async chunks for `de`, `ja`, `es`, `fr` dict modules (from 19-03)
- [ ] `npm run check` exits 0
- [ ] `npm run test:unit` exits 0 (including new `lazy-mount-loader.test.ts`)

### B. Localhost smoke — 375 × 667 (mobile viewport)

Start dev server (`npm run dev`) and open http://localhost:5173 in the Playwright MCP browser.

**Cold-load check:**
- [ ] Page paints above-fold KPI tiles without the 5 deferred chart cards
- [ ] Skeletons visible for all deferred card positions
- [ ] No `invalid_default_snippet` console warning (per `feedback_svelte5_tooltip_snippet.md`)
- [ ] No IntersectionObserver errors in console

**Scroll check (Network panel filtered to `chunk-`):**
- [ ] Scrolling to `CalendarCountsCard` position → a new `*.js` chunk request appears in Network
- [ ] Scrolling to `CalendarItemsCard` position → `/api/item-counts?from=…&to=…` request fires
- [ ] Scrolling to `CohortRetentionCard` position → `/api/benchmark` request fires alongside `/api/retention`
- [ ] Scrolling to `MdeCurveCard` position → a new chunk appears

**Locale check:**
- [ ] Set cookie `rb_locale=ja` → reload → Japanese strings render correctly (no raw keys)
- [ ] Network tab shows `de.js`/`ja.js` chunk downloads on locale switch, not the full `messages.ts` equivalent

**Filter-change check (regression):**
- [ ] Click a date-range chip → KPI tiles update → deferred cards remain mounted (no re-skeleton)
- [ ] 5 locale test: cycle through EN / DE / JA / ES / FR using the language switcher in DashboardHeader → each locale renders without raw key strings

### C. DEV deploy + final QA

```bash
git push origin feature/phase-19-cold-start-trim
gh workflow run deploy.yml --ref feature/phase-19-cold-start-trim
```

Wait for deploy. Then via Playwright MCP, navigate to the DEV URL.

- [ ] Playwright `browser_navigate` to DEV URL → `browser_screenshot` shows page loads without JS error overlay
- [ ] `browser_console_messages` → no `ERROR` lines
- [ ] LCP via DevTools → target ≤ 2.5 s on simulated 4G (Fast 3G in Chrome DevTools)
- [ ] Deferred chunks arrive in Network panel on scroll (same as localhost)

---

## Planning-docs drift gate

### 1. Update `STATE.md`

Edit `.planning/STATE.md` frontmatter:
- `milestone: v1.4` → `v1.5`
- `milestone_name: Coverage Map` → `Cold-Start Trim`
- `status: Complete` → `In Progress`
- `stopped_at: Phase 19 in progress`
- `last_updated`: current ISO datetime
- `progress.total_phases: 21` → `22`
- `progress.total_plans: 123` → `127`
- `completed_phases: 21` (unchanged — Phase 19 not yet complete when this plan runs; update to 22 when merged)
- `completed_plans: 111` (unchanged — will update when each sub-plan is marked complete)

Body: add Phase 19 status entry explaining all 4 sub-plans and their goals.

### 2. Update `ROADMAP.md`

- Update Coverage line: add `v1.5` requirements count
- Add new `<details>` section: `v1.5 Cold-Start Trim (Phase 19) — In Progress`
- Add Phase 19 detail block with goals, success criteria, and plan list (4 plans)

### 3. Run validator

```bash
.claude/scripts/validate-planning-docs.sh
```

Expected: exits 0 with no `⚠️ Planning docs drift` warnings.

If the validator counts PLAN.md files, expected count is 127 (123 existing + 4 new Phase 19 plans).

---

## Definition of done

Phase 19 is done when:

1. All 4 sub-plans complete (19-01 through 19-04)
2. `npm run build` shows 11+ async chunks that were previously synchronous
3. `npm run check` + `npm run test:unit` both exit 0
4. Localhost QA PASS (skeletons on cold load, chunks arrive on scroll, locale switch works)
5. DEV QA PASS (Playwright screenshot + console scan)
6. `validate-planning-docs.sh` exits 0
7. PR merged to `main`
8. `19-SUMMARY.md` written with per-task QA evidence (required per `docs/workflow.md` artifact layout)
