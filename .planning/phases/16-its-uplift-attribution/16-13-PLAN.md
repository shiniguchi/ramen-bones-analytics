---
plan: 13
phase: 16
title: forecast-refresh.yml workflow extension + DEV smoke test
wave: 4
depends_on: [05, 06, 07]
files_modified:
  - .github/workflows/forecast-refresh.yml
  - .github/workflows/migrations.yml
files_created: []
requirements: [UPL-02, UPL-04, UPL-07]
threats_mitigated: []
autonomous: false
type: execute
must_haves:
  truths:
    - "forecast-refresh.yml weekly cron runs run_all.py with --track=both (or default), invokes cumulative_uplift.py last, then refreshes MVs"
    - "Workflow completes in <10 min on ubuntu-latest (CONTEXT.md C-02 / Phase 14 budget)"
    - "Existing 0 7 * * 1 UTC cron schedule unchanged (Phase 15 D-16)"
    - "migrations.yml supports workflow_dispatch on feature branches (per .claude/memory/feedback_migrations_workflow_dispatch.md)"
    - "End-to-end smoke test: gh workflow run forecast-refresh.yml --ref feature/phase-16-its-uplift-attribution executes the full BAU→CF→cumulative_uplift→MV refresh cascade"
  artifacts:
    - path: .github/workflows/forecast-refresh.yml
      provides: "Track-B fits + cumulative_uplift step appended after BAU"
    - path: .github/workflows/migrations.yml
      provides: "workflow_dispatch trigger added so feature-branch QA can apply migrations to DEV"
  key_links:
    - from: "forecast-refresh.yml"
      to: "scripts.forecast.cumulative_uplift"
      via: "python -m invocation in new step after BAU+CF fits"
      pattern: "python -m scripts.forecast.cumulative_uplift"
---

<objective>
Wire Track-B fits and `cumulative_uplift.py` into the existing weekly forecast-refresh workflow per CONTEXT.md C-02 + RESEARCH §5. Extend `forecast-refresh.yml` with two new steps; do NOT add a new cron schedule (Guard 8 cascade-gap stays clean).

Also extend `migrations.yml` with `workflow_dispatch` trigger so the feature branch can apply Phase 16 migrations to DEV without merging — per `.claude/memory/feedback_migrations_workflow_dispatch.md`.

Finally, smoke test the full cascade on the feature branch via `gh workflow run`.

`autonomous: false` because the smoke test requires the executor to drive `gh` CLI and read workflow logs.

Output: 2 modified workflow files + smoke test result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-its-uplift-attribution/16-CONTEXT.md
@.planning/phases/16-its-uplift-attribution/16-RESEARCH.md
@.github/workflows/forecast-refresh.yml
@.github/workflows/migrations.yml
@scripts/forecast/run_all.py
@scripts/forecast/cumulative_uplift.py
@.claude/memory/feedback_migrations_workflow_dispatch.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend forecast-refresh.yml with cumulative_uplift step</name>
  <files>.github/workflows/forecast-refresh.yml</files>
  <read_first>
    - .github/workflows/forecast-refresh.yml (existing Phase 14 + 15 workflow — find the "Run forecast pipeline" step and the MV-refresh step)
    - .planning/phases/16-its-uplift-attribution/16-RESEARCH.md (§5 — exact YAML diff)
    - .planning/phases/16-its-uplift-attribution/16-CONTEXT.md (C-02 — keep 0 7 * * 1 UTC cron)
    - **MUST run before editing**: `grep -n 'Refresh forecast MVs\|refresh_forecast_mvs' .github/workflows/forecast-refresh.yml` — record the existing step name + line number; if absent, the new step is the only refresh; if present, MOVE it in place rather than insert a duplicate. The verify command asserts at most one refresh step in the final YAML.
  </read_first>
  <action>
    Modify `.github/workflows/forecast-refresh.yml`:

    1. Verify the existing "Run forecast pipeline" step uses `python -m scripts.forecast.run_all` (no args, or with the existing `--models` flag). Per Plan 05 Task 2, `run_all.py` defaults `--track=both`. **No edit to that step needed for BAU+CF orchestration** — auto-picks up.

    2. AFTER the existing `Run forecast pipeline` step, BEFORE any MV refresh step (or replacing/wrapping it), insert two steps per RESEARCH §5:

    ```yaml
          - name: Run cumulative uplift
            # Phase 16 (UPL-04 / UPL-07): aggregates Track-B forecasts into
            # campaign_uplift_v + fires off-week reminder via atomic UPDATE on
            # feature_flags. Runs LAST in the cascade — after BAU+CF fits.
            env:
              SUPABASE_URL: ${{ secrets.DEV_SUPABASE_URL }}
              SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.DEV_SUPABASE_SERVICE_ROLE_KEY }}
            run: |
              set -euo pipefail
              python -m scripts.forecast.cumulative_uplift

          - name: Refresh forecast MVs
            # Existing Phase 14 step — moved AFTER cumulative_uplift since
            # campaign_uplift_v's backing table (Plan 07) was just populated.
            env:
              SUPABASE_URL: ${{ secrets.DEV_SUPABASE_URL }}
              SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.DEV_SUPABASE_SERVICE_ROLE_KEY }}
            run: |
              set -euo pipefail
              python -c "
              from scripts.forecast.db import make_client
              c = make_client()
              c.rpc('refresh_forecast_mvs', {}).execute()
              "
    ```

    Note: the existing `Refresh forecast MVs` step likely already exists somewhere in the workflow. If so, MOVE it to AFTER `Run cumulative uplift`. Do not duplicate.

    3. Verify `on:` block keeps existing schedule:
    ```yaml
    on:
      schedule:
        - cron: '0 7 * * 1'  # Monday 07:00 UTC (Phase 15 D-16)
      workflow_dispatch:
        inputs:
          run_date:
            description: 'YYYY-MM-DD (defaults to today)'
            required: false
    ```

    The `workflow_dispatch` input lets the executor trigger from the feature branch via `gh workflow run forecast-refresh.yml --ref feature/phase-16-its-uplift-attribution`.
  </action>
  <verify>
    <automated>grep -q "python -m scripts.forecast.cumulative_uplift" .github/workflows/forecast-refresh.yml && grep -q "0 7 \* \* 1" .github/workflows/forecast-refresh.yml && grep -q "workflow_dispatch" .github/workflows/forecast-refresh.yml && [[ $(grep -c 'refresh_forecast_mvs\|Refresh forecast MVs' .github/workflows/forecast-refresh.yml) -le 1 ]] && python3 scripts/ci-guards/check-cron-schedule.py</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/forecast-refresh.yml` contains step `Run cumulative uplift` invoking `python -m scripts.forecast.cumulative_uplift`
    - The `Refresh forecast MVs` step appears AFTER `Run cumulative uplift`
    - `on.schedule` cron is unchanged: `'0 7 * * 1'`
    - `workflow_dispatch` block is present
    - **Exactly one** refresh step exists in the file (no duplicates): `grep -c 'refresh_forecast_mvs\|Refresh forecast MVs' .github/workflows/forecast-refresh.yml` returns ≤1
    - `python3 scripts/ci-guards/check-cron-schedule.py` exits 0 (Guard 8 cascade-gap still passes — no new cron file added)
  </acceptance_criteria>
  <done>Workflow YAML extended.</done>
</task>

<task type="auto">
  <name>Task 2: Add workflow_dispatch trigger to migrations.yml (if not present)</name>
  <files>.github/workflows/migrations.yml</files>
  <read_first>
    - .github/workflows/migrations.yml (existing trigger config)
    - .claude/memory/feedback_migrations_workflow_dispatch.md (rationale: feature-branch QA needs migrations applied to DEV before /api/* visual QA)
  </read_first>
  <action>
    Inspect `.github/workflows/migrations.yml` `on:` block:

    - If `workflow_dispatch` is already present → no change needed; document in summary that the trigger pre-existed.
    - If absent → add it as a top-level trigger:
    ```yaml
    on:
      push:
        branches: [main]
        paths:
          - 'supabase/migrations/**'
      workflow_dispatch: {}
    ```

    Per `feedback_migrations_workflow_dispatch.md`: when triggering from a feature branch, run:
    ```bash
    gh workflow run migrations.yml --ref feature/phase-16-its-uplift-attribution
    ```

    This applies pending migrations to DEV against the feature ref so visual QA against DEV doesn't 500 on missing schema.
  </action>
  <verify>
    <automated>grep -q "workflow_dispatch" .github/workflows/migrations.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/migrations.yml` `on:` block contains `workflow_dispatch` (either pre-existing or newly added)
  </acceptance_criteria>
  <done>migrations.yml manually triggerable.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Smoke test full cascade on feature branch</name>
  <what-built>
    `forecast-refresh.yml` now runs BAU + CF + cumulative_uplift + MV-refresh end-to-end per Phase 16 cascade. `migrations.yml` triggerable on feature ref.
  </what-built>
  <how-to-verify>
    1. Push the feature branch:
    ```bash
    git push origin feature/phase-16-its-uplift-attribution
    ```

    2. Apply migrations to DEV (in case any pending haven't shipped via earlier db push tasks):
    ```bash
    gh workflow run migrations.yml --ref feature/phase-16-its-uplift-attribution
    gh run watch  # follow the run
    ```

    3. Trigger the forecast-refresh workflow on the feature ref:
    ```bash
    gh workflow run forecast-refresh.yml --ref feature/phase-16-its-uplift-attribution
    gh run watch
    ```

    4. Inspect logs (`gh run view <run-id> --log` or via GitHub UI). Verify:
       - `Run forecast pipeline` step exits 0 with both BAU and CF fits attempted
       - `Run cumulative uplift` step exits 0; logs say "Computed uplift for N campaigns × M models"
       - `Refresh forecast MVs` step exits 0
       - Total wall time < 10 min (RESEARCH § Phase 14 budget)

    5. Verify on DEV:
    ```bash
    psql $DEV_DB_URL -c "SELECT campaign_id, model_name, window_kind, cumulative_uplift_eur, ci_lower_eur, ci_upper_eur FROM campaign_uplift WHERE campaign_id='friend-owner-2026-04-14' ORDER BY computed_at DESC LIMIT 10"
    ```
    Expected: 5 models × 2 window_kinds = 10 rows with non-null CI bounds and a populated `computed_at`.

    6. Verify pipeline_runs:
    ```bash
    psql $DEV_DB_URL -c "SELECT step_name, status, fit_train_end FROM pipeline_runs WHERE step_name LIKE 'cf_%' OR step_name = 'cumulative_uplift' ORDER BY started_at DESC LIMIT 20"
    ```
    Expected: ~5 `cf_<model>` rows with `fit_train_end='2026-04-07'`, plus 1 `cumulative_uplift` row.

    7. State `Smoke test: PASS/FAIL` with the workflow run URL.
  </how-to-verify>
  <resume-signal>State `Smoke test: PASS` with the workflow run URL, or describe failures (e.g., specific step exit code + log excerpt).</resume-signal>
  <acceptance_criteria>
    - `gh workflow run forecast-refresh.yml --ref feature/phase-16-its-uplift-attribution` triggers a run that exits 0
    - All 3 new/changed steps (Run forecast pipeline → Run cumulative uplift → Refresh forecast MVs) green
    - Total wall time < 10 min
    - `campaign_uplift` table on DEV populated with ≥5 rows for the friend campaign
    - `pipeline_runs` has ≥5 cf_* rows with `fit_train_end='2026-04-07'`
  </acceptance_criteria>
  <done>End-to-end Phase 16 cascade verified on DEV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GHA workflow → Supabase | Service-role key in secrets; standard Phase 14 pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (no new threats) | — | — | accept | Workflow extension reuses existing service-role secret + Phase 14 patterns; no new attack surface. |
</threat_model>

<verification>
- Guard 8 (cron schedule) still green
- Workflow runs end-to-end on feature branch
- DEV data populated correctly
</verification>

<success_criteria>
- forecast-refresh.yml extended; weekly cron unchanged
- migrations.yml workflow_dispatch in place
- Full cascade smoke test PASS
- UPL-02 + UPL-04 + UPL-07 all wired to nightly automation
</success_criteria>

<output>
After completion, create `.planning/phases/16-its-uplift-attribution/16-13-SUMMARY.md`
</output>
