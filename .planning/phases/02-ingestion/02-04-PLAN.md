---
phase: 02-ingestion
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - tests/ingest/loader.test.ts
  - tests/ingest/idempotency.test.ts
  - tests/ingest/fixtures/README.md
  - README.md
autonomous: false
requirements: [ING-01, ING-02, ING-05]
must_haves:
  truths:
    - "loader.test.ts passes GREEN against TEST Supabase project using the fixture"
    - "idempotency.test.ts passes GREEN — double-run produces zero diffs"
    - "Real CSV ingestion report is captured in a SUMMARY with actual row counts and missing_worldline_rows value"
    - "Founder has a human-verify checkpoint confirming ≥20 real rows match the loader's interpretation (ING-05)"
    - "README has a docs section explaining npm run ingest prerequisites and report fields"
  artifacts:
    - path: tests/ingest/loader.test.ts
      provides: "ING-01 integration: staging row count matches CSV, transactions count matches unique positive invoices"
      contains: "runIngest"
    - path: tests/ingest/idempotency.test.ts
      provides: "ING-02 proof: zero-diff re-run"
      contains: "runIngest"
    - path: tests/ingest/fixtures/README.md
      provides: "ING-05 artifact: founder's sign-off of which real-row semantics the fixture mirrors"
  key_links:
    - from: "loader.test.ts"
      to: "TEST Supabase project"
      via: "tests/helpers/supabase.ts adminClient()"
      pattern: "adminClient"
    - from: "idempotency.test.ts"
      to: "runIngest()"
      via: "double invocation + row count diff"
      pattern: "runIngest"
---

<objective>
Turn the remaining RED integration tests GREEN, run the loader against the REAL CSV, and checkpoint with the founder for ING-05 sign-off.

Purpose: Close out Phase 2 with proof that the loader works on synthetic AND real data, with a human confirmation that the semantic interpretation matches business reality. This is the phase's UAT.
Output: Two GREEN integration tests + a documented real-run report + README docs + founder checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ingestion/02-CONTEXT.md
@.planning/phases/02-ingestion/02-RESEARCH.md
@.planning/phases/02-ingestion/02-03-SUMMARY.md
@tests/ingest/fixtures/sample.csv
@tests/ingest/loader.test.ts
@tests/ingest/idempotency.test.ts
@scripts/ingest/index.ts
@tests/helpers/supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn loader + idempotency integration tests GREEN against TEST project</name>
  <files>tests/ingest/loader.test.ts, tests/ingest/idempotency.test.ts</files>
  <action>
Both test files already exist as stubs from Plan 02. This task fills in the implementations so they execute against the TEST Supabase project.

**Setup pattern** (both files):
1. Before each test: truncate `public.transactions` and `public.stg_orderbird_order_items` for the TEST `restaurant_id` using `adminClient()` (bypasses RLS via service role key).
2. Upload `tests/ingest/fixtures/sample.csv` to the TEST storage bucket at path `test/sample.csv` (use `supabase.storage.from('orderbird-raw').upload(path, file, { upsert: true })`).
3. Set env vars inline for the test (`process.env.ORDERBIRD_CSV_BUCKET = 'orderbird-raw'`, etc.) pointing at TEST credentials.
4. Call `runIngest()` from `scripts/ingest/index.ts`.

**loader.test.ts assertions** (ING-01):
- `report.rows_read` equals fixture data row count (24+)
- `stg_orderbird_order_items` `SELECT count(*)` equals `report.rows_read` (proves synthetic row_index PK worked on the T-3 split-bill scenario)
- `transactions` `SELECT count(*)` equals unique positive-total invoices (T-5 negative excluded)
- `SELECT sum(tip_cents) FROM transactions` equals the hand-calculated fixture tip total (proves D-12 tip-from-first-row, NOT sum)
- `SELECT count(*) FROM transactions WHERE card_hash IS NOT NULL` equals count of card invoices with wl_card_number populated (T-1 and others, excluding T-6)
- `report.missing_worldline_rows` = 1 (the T-6 scenario)

**idempotency.test.ts assertions** (ING-02):
- Run `runIngest()` once, snapshot row counts
- Run `runIngest()` second time
- `report2.transactions_new` = 0
- `SELECT count(*)` unchanged on both tables
- `SELECT md5(string_agg(source_tx_id || gross_cents::text || coalesce(card_hash,''), ',' ORDER BY source_tx_id))` identical across runs (proves byte-exact zero-diff)

If TEST project env vars missing, use `it.skip` with a clear message (Phase 1 pattern). CI workflow applies migrations to TEST before running tests so the schema is there.
  </action>
  <verify>
    <automated>npx vitest run tests/ingest/loader.test.ts tests/ingest/idempotency.test.ts</automated>
  </verify>
  <done>Both integration tests GREEN (or explicitly skipped with clear reason when TEST env missing). Full `npm run test` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Run loader against REAL CSV and capture report + README docs</name>
  <files>README.md, .planning/phases/02-ingestion/02-04-REAL-RUN.md</files>
  <action>
**2a — Real run:**
1. Confirm `orderbird_data/5-JOINED_DATA_20250611_20260411/ramen_bones_order_items.csv` exists locally.
2. Upload it to the DEV Supabase orderbird-raw bucket at `dev/ramen_bones_order_items.csv` via supabase CLI: `supabase storage cp ./orderbird_data/5-JOINED_DATA_20250611_20260411/ramen_bones_order_items.csv orderbird-raw/dev/ramen_bones_order_items.csv --experimental` (or the current syntax).
3. Export env vars pointing at DEV + the v1 RESTAURANT_ID from `0005_seed_tenant.sql`.
4. Run `npm run ingest -- --dry-run` first. Capture JSON output.
5. Run `npm run ingest` (write mode). Capture JSON output.
6. Write `.planning/phases/02-ingestion/02-04-REAL-RUN.md` containing both JSON reports, the real `missing_worldline_rows` count, and a table of row counts per table.
7. Query DEV `transactions` for the top-5 highest invoices and spot-check `gross_cents`, `tip_cents`, `card_hash NOT NULL`, `payment_method` canonical form — paste results into the real-run doc.

**2b — README.md docs section** — Append an `## Ingestion` section with:
- Prerequisites (env vars list from D-19, link to pii-columns.txt)
- How to run: `npm run ingest -- --dry-run` then `npm run ingest`
- Report field meanings (especially `missing_worldline_rows` — why it matters, what founder should do if it grows)
- Idempotency guarantee statement
- Pointer to `tests/ingest/fixtures/README.md` for semantic reference
  </action>
  <verify>
    <automated>test -f .planning/phases/02-ingestion/02-04-REAL-RUN.md &amp;&amp; grep -q "rows_read" .planning/phases/02-ingestion/02-04-REAL-RUN.md &amp;&amp; grep -q "## Ingestion" README.md</automated>
  </verify>
  <done>Real-run doc exists with actual numbers from DEV; README has ingestion section; founder can read either and understand the loader.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Founder ING-05 sign-off — review ≥20 real rows</name>
  <what-built>
Phase 2 loader end-to-end. Real CSV loaded into DEV with full report in `02-04-REAL-RUN.md`. Fixture README documents the 11 semantic scenarios.
  </what-built>
  <how-to-verify>
1. Open Supabase DEV SQL editor and run:
   ```sql
   select source_tx_id, occurred_at, card_hash is not null as has_card,
          gross_cents / 100.0 as gross_eur, tip_cents / 100.0 as tip_eur,
          payment_method, sales_type
   from transactions
   order by occurred_at desc
   limit 25;
   ```
2. For each of the top 25 rows, cross-check against the real CSV rows for the same `invoice_number`:
   - Does `gross_eur` match the CSV's `invoice_total_eur`?
   - Does `tip_eur` match the CSV's `tip_eur` (NOT multiplied by line-item count)?
   - Is `payment_method` the canonical casing?
   - Is `card_hash` non-null for card transactions and null for cash?
   - Does `occurred_at` convert to the correct Europe/Berlin wall-clock time?
3. Review `.planning/phases/02-ingestion/02-04-REAL-RUN.md` `missing_worldline_rows` count. Is it within expectations (~772 per RESEARCH Pitfall 2)?
4. Review `tests/ingest/fixtures/README.md` scenario list — does each scenario match your understanding of the business semantics?
  </how-to-verify>
  <resume-signal>Type "approved" if all 25 rows match and the missing_worldline_rows count is acceptable. Type "revise: {what's wrong}" to block and require loader fix.</resume-signal>
</task>

</tasks>

<verification>
- `npm run test` full suite GREEN
- `02-04-REAL-RUN.md` contains actual DEV row counts
- Founder checkpoint approved
- ING-01 through ING-05 all satisfied
</verification>

<success_criteria>
Phase 2 complete: loader works on fixture and real CSV, idempotency proven, founder has confirmed semantic correctness on real rows. Phase 3 (Analytics SQL) can start reading from `transactions` and `stg_orderbird_order_items` with confidence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion/02-04-SUMMARY.md` documenting:
- Real CSV row counts and any surprises
- Founder sign-off timestamp and any revisions requested
- Outstanding issues (if any) to carry into Phase 3
</output>
