---
phase: 02-ingestion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/ingest/fixtures/sample.csv
  - tests/ingest/loader.test.ts
  - tests/ingest/normalize.test.ts
  - tests/ingest/hash.test.ts
  - tests/ingest/idempotency.test.ts
autonomous: true
requirements: [ING-01, ING-02, ING-03, ING-04, ING-05]
must_haves:
  truths:
    - "Fixture CSV exists with 24+ rows covering every D-21 edge case + DST + missing-wl_card_number"
    - "Four vitest test files exist with RED stubs (describe/it scaffolding) for ING-01..05"
    - "Test stubs reference the loader entry point that Plan 03 will create"
    - "npm run test -- tests/ingest/ executes the new files (even if they skip/fail until Plan 03)"
  artifacts:
    - path: tests/ingest/fixtures/sample.csv
      provides: "24+ row synthetic CSV covering all D-21 scenarios"
      min_lines: 25
    - path: tests/ingest/loader.test.ts
      provides: "ING-01 stub: CSV→staging upsert test skeleton"
      contains: "describe.*loader"
    - path: tests/ingest/normalize.test.ts
      provides: "ING-03 stub: invoice-grain reducer tests (tips/brutto/correction pairs)"
      contains: "describe.*normalize"
    - path: tests/ingest/hash.test.ts
      provides: "ING-04 stub: sha256 card_hash + cash NULL tests"
      contains: "describe.*hash"
    - path: tests/ingest/idempotency.test.ts
      provides: "ING-02, ING-05 stub: zero-diff re-run test"
      contains: "describe.*idempoten"
  key_links:
    - from: "tests/ingest/*.test.ts"
      to: "scripts/ingest/ (created in Plan 03)"
      via: "import { ... } from '../../scripts/ingest/...'"
      pattern: "from.*scripts/ingest"
---

<objective>
Wave 0: create the deterministic test surface BEFORE the loader exists. The fixture CSV and vitest stubs make every subsequent task verifiable with an automated command. This plan runs in parallel with Plan 01 (schema) because it only touches `tests/ingest/*`.

Purpose: Satisfy the Nyquist requirement that every task in Plans 03 and 04 has an `<automated>` verify command that points to a real test file. Also produces the ING-05 artifact: the synthetic fixture is the "≥20 real rows reviewed" sign-off — it is hand-built from real CSV observations but committable because synthetic.
Output: Fixture CSV + 4 test files with stubs that import from `scripts/ingest/*` (Plan 03 fills in the implementations).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ingestion/02-CONTEXT.md
@.planning/phases/02-ingestion/02-RESEARCH.md
@.planning/phases/02-ingestion/02-VALIDATION.md
@tests/helpers/supabase.ts
@tests/setup.ts

<interfaces>
Vitest pattern (established in Phase 1):
- `describe('thing', () => { it('does X', async () => { ... }) })`
- Test files live under `tests/` and are picked up by `vitest run`
- `tests/helpers/supabase.ts` exports `adminClient()` using TEST_SUPABASE_* env
- `tests/setup.ts` enforces DEV-safety (refuses to run against DEV)

Loader module shapes this plan's stubs MUST import from (Plan 03 creates them):
- `scripts/ingest/hash.ts`      → `export function hashCard(wlCardNumber: string | null, restaurantId: string): string | null`
- `scripts/ingest/parse.ts`     → `export function parseCsv(text: string): Record<string,string>[]`
- `scripts/ingest/normalize.ts` → `export function toStagingRows(rows, restaurantId, sourceFile): StagingRow[]` and `export function toTransactions(staging, restaurantId): TxRow[]`
- `scripts/ingest/index.ts`     → `export async function runIngest(opts?: { dryRun?: boolean }): Promise<IngestReport>`

IngestReport shape (Plan 03 locks):
```ts
type IngestReport = {
  rows_read: number;
  invoices_deduped: number;
  staging_upserted: number;
  transactions_new: number;
  transactions_updated: number;
  cash_rows_excluded: number;
  missing_worldline_rows: number;
  errors: number;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build fixture CSV covering all D-21 edge cases</name>
  <files>tests/ingest/fixtures/sample.csv</files>
  <action>
Hand-build a synthetic 24+ row CSV with the 29-column header EXACTLY matching the real file (see interfaces in Plan 01). All values synthetic — no real customer data. The fixture is the ING-05 artifact: it documents every semantic the loader must handle.

Required scenarios (each a distinct invoice unless noted):
1. **Normal invoice, INHOUSE, card, tip>0** — invoice `T-1`, 2 item rows, Visa, tip=3.00, wl_card_number=`482510xxxxxxxxx0001`
2. **Normal invoice, TAKEAWAY, cash, tip=0** — invoice `T-2`, 1 row, payment_method=Bar, wl_card_number blank, is_cash=True
3. **Split-bill same invoice (duplicate item rows)** — invoice `T-3`, 3 rows where two rows have IDENTICAL (item_name='Ramen', quantity=1, item_gross_amount_eur=15.00) but different order_id UUIDs. This is the Pitfall 6 case — proves synthetic row_index PK is required.
4. **Correction pair (positive + negative lines net to 0)** — invoice `T-4`, 4 rows: Kimchi +5/-5, Ramen +15/-15. invoice_total_eur on all 4 rows = 0.00. Must survive staging and be filtered from transactions per D-11.
5. **Correction pair NEGATIVE-total invoice (must be dropped from transactions)** — invoice `T-5`, 1 row with invoice_total_eur = -8.00. Filtered by `WHERE invoice_total_eur >= 0`.
6. **Card row with MISSING Worldline enrichment** — invoice `T-6`, 1 row, payment_method=MasterCard, wl_card_number blank, card_txn_id filled. Must land in staging but produce NULL card_hash per D-08 (Pitfall 2) and increment `missing_worldline_rows` counter.
7. **DST transition row** — invoice `T-7`, date=2025-10-26 time=02:30:00 (Berlin CEST→CET fall-back). occurred_at must resolve to a valid UTC (first-occurrence convention).
8. **Case-variant payment_method** — invoice `T-8`, payment_method=`MASTERCARD` (uppercase). Must normalize to `MasterCard` in transactions but stay uppercase in staging.
9. **Blank is_cash (empty string)** — invoice `T-9`, is_cash=`` empty. Loader infers cash from payment_method=Bar (Pitfall 7).
10. **Invoice_number with space+parens** — invoice `T-10 (ex-T-4)`, 1 row (recovered-invoice case from real file). Stored verbatim.
11. **Tip ONLY on invoice (not per item)** — invoice `T-11`, 3 item rows, tip_eur=5.00 on ALL 3 rows. Loader must take tip from first row only (total tip in transactions = 5.00, not 15.00). Pitfall = D-12 violation.

Minimum row count: 24 (scenarios above expand to at least 2 rows per multi-line invoice).

Header line EXACT:
```
date,time,item_name,quantity,item_price_eur,category_name,category_kind,table_name,tab_name,party_name,invoice_number,tax_rate_pct,sales_type,item_gross_amount_eur,invoice_total_eur,payment_method,processor,tip_eur,given_eur,change_eur,card_type,card_last4,card_txn_id,is_cash,order_id,wl_card_number,wl_card_type,wl_payment_type,wl_issuing_country
```

Use dot-decimals (`5.00` not `5,00`), YYYY-MM-DD dates, HH:MM:SS times, `True`/`False`/empty for is_cash.

After writing the file, add a companion `tests/ingest/fixtures/README.md` (≤40 lines) listing each invoice and which D-XX decision + RESEARCH pitfall it exercises. This README IS the founder's ING-05 "≥20 rows reviewed" sign-off artifact.
  </action>
  <verify>
    <automated>test $(wc -l &lt; tests/ingest/fixtures/sample.csv) -ge 25 &amp;&amp; head -1 tests/ingest/fixtures/sample.csv | tr ',' '\n' | wc -l | grep -q '^29$'</automated>
  </verify>
  <done>sample.csv has ≥25 lines (1 header + 24+ data rows), header has exactly 29 columns, fixture README lists all 11 scenarios with D-XX + pitfall tags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test stubs for hash, normalize, loader, idempotency</name>
  <files>
    tests/ingest/hash.test.ts,
    tests/ingest/normalize.test.ts,
    tests/ingest/loader.test.ts,
    tests/ingest/idempotency.test.ts
  </files>
  <behavior>
    - hash.test.ts: hashCard(null, rid) returns null (D-08); hashCard('', rid) returns null; hashCard('482510x7567', rid) returns deterministic sha256 hex; same input twice returns same hash
    - normalize.test.ts: toStagingRows assigns row_index 1..N per invoice group; toTransactions dedupes to one row per invoice_number; filters invoice_total_eur < 0; takes tip_eur from first row of group (not sum); normalizes MASTERCARD → MasterCard; DST row 2025-10-26 02:30 Berlin resolves to a valid UTC; correction-pair invoice T-4 produces one tx row with total 0.00; invoice T-11 3-row tip=5.00 produces tx tip_cents=500 (not 1500)
    - loader.test.ts: runIngest() against TEST project reads fixture, writes staging row count = CSV row count (proving synthetic PK works on split-bill case T-3), writes deduped transactions count = unique invoices minus negative-total invoices, report.missing_worldline_rows = 1 (scenario 6), report.cash_rows_excluded ≥ 1
    - idempotency.test.ts: runIngest() twice in a row; second run reports transactions_new=0, transactions_updated=0; staging row count unchanged; SELECT COUNT(*) from both tables is identical run-to-run
  </behavior>
  <action>
Create 4 test files as RED stubs — they MUST fail (or skip with a clear TODO) until Plan 03 implements `scripts/ingest/*`. Use `it.todo()` OR `it(..., async () => { ... })` that imports from `scripts/ingest/*` and will throw `Cannot find module` until Plan 03 lands. The goal is that `npm run test -- tests/ingest/` EXECUTES these files today (even if red) so wave 0 is satisfiable.

**tests/ingest/hash.test.ts** — unit, no DB:
```ts
import { describe, it, expect } from 'vitest';
import { hashCard } from '../../scripts/ingest/hash';

const RID = '00000000-0000-0000-0000-000000000001';

describe('hashCard (ING-04)', () => {
  it('returns null for null wl_card_number (D-08 cash)', () => {
    expect(hashCard(null, RID)).toBeNull();
  });
  it('returns null for empty string wl_card_number', () => {
    expect(hashCard('', RID)).toBeNull();
  });
  it('returns deterministic sha256 hex for real wl_card_number', () => {
    const h1 = hashCard('482510xxxxxxxxx0001', RID);
    const h2 = hashCard('482510xxxxxxxxx0001', RID);
    expect(h1).toEqual(h2);
    expect(h1).toMatch(/^[a-f0-9]{64}$/);
  });
  it('differs across restaurant_ids for the same card (tenant salt)', () => {
    const h1 = hashCard('482510xxxxxxxxx0001', RID);
    const h2 = hashCard('482510xxxxxxxxx0001', '00000000-0000-0000-0000-000000000002');
    expect(h1).not.toEqual(h2);
  });
});
```

**tests/ingest/normalize.test.ts** — unit, no DB. Load fixture via `readFileSync('tests/ingest/fixtures/sample.csv')`, call `parseCsv` + `toStagingRows` + `toTransactions`, assert on each scenario tag.

Must assert:
- T-3 split-bill produces 3 staging rows with row_index 1,2,3 (no collision)
- T-4 correction-pair produces 1 transaction row with gross_cents=0
- T-5 negative-total invoice produces 0 transaction rows
- T-11 tip=5.00 repeated on 3 rows produces tx.tip_cents=500
- T-8 MASTERCARD normalizes to 'MasterCard' in transactions
- T-7 DST row produces a valid ISO timestamp (not NaN/Invalid Date)
- T-6 missing-wl row produces staging row but NULL card_hash in transactions

**tests/ingest/loader.test.ts** — integration, uses `tests/helpers/supabase.ts` `adminClient()` against TEST project. Must:
- Upload fixture to TEST storage bucket (or set ORDERBIRD_CSV_OBJECT to local path if loader supports)
- Call `runIngest()`
- Query `stg_orderbird_order_items` — row count equals CSV data-row count
- Query `transactions` — row count equals unique-positive-invoice count
- Assert report.missing_worldline_rows = 1

Mark with `it.skip` if TEST project env is missing (follow Phase 1 pattern).

**tests/ingest/idempotency.test.ts** — integration, same helper. Call `runIngest()` twice, snapshot row counts, assert zero diff.

After all 4 files exist, run `npm run test -- tests/ingest/`. Expected outcome: tests fail with `Cannot find module '../../scripts/ingest/hash'` (or equivalent). That is the Wave 0 success state — the automated harness executes and produces a deterministic RED signal that Plan 03 will turn GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/ingest/ 2>&amp;1 | grep -E 'Cannot find module|Test Files' | head -5</automated>
  </verify>
  <done>Four test files exist, import from `scripts/ingest/*` module paths, and `vitest run tests/ingest/` executes them producing RED output pointing at the missing loader modules.</done>
</task>

</tasks>

<verification>
- `ls tests/ingest/fixtures/sample.csv tests/ingest/*.test.ts` lists 5 files
- Fixture has 29-column header and ≥24 data rows
- Fixture README enumerates all 11 scenarios with D-XX tags
- `vitest run tests/ingest/` executes all 4 files (expected RED, proving scaffold is wired)
</verification>

<success_criteria>
Every task in Plan 03 and Plan 04 can point its `<automated>` verify command at one of these 4 test files. ING-05 founder-review artifact (fixture README) exists and is committable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion/02-02-SUMMARY.md` documenting:
- The 11 fixture scenarios and which D-XX + pitfall each exercises
- Expected RED test output as baseline for Plan 03 to beat
</output>
