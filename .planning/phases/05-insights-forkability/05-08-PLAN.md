---
phase: 05-insights-forkability
plan: 08
type: execute
wave: 5
depends_on: []
files_modified:
  - .planning/phases/05-insights-forkability/05-08-SUMMARY.md
autonomous: false
gap_closure: true
requirements: [INS-03, INS-05]
must_haves:
  truths:
    - "The friend has a real Supabase Auth user in the DEV project"
    - "A memberships row links the friend's user_id to restaurant ba1bf707-aae9-46a9-8166-4b6459e6c2fd with role='owner'"
    - "Signing in as the friend lands on the dashboard, NOT /not-provisioned"
    - "The JWT minted for the friend contains restaurant_id via custom_access_token_hook"
    - "Credentials have been shared with the friend via a secure channel"
  artifacts:
    - path: .planning/phases/05-insights-forkability/05-08-SUMMARY.md
      provides: "Evidence of user creation, membership insert, JWT claim verification, and sign-in success (no /not-provisioned redirect)"
  key_links:
    - from: auth.users (friend row)
      to: public.memberships
      via: "user_id foreign key + role='owner' + restaurant_id=ba1bf707-aae9-46a9-8166-4b6459e6c2fd"
      pattern: "role.*owner"
    - from: custom_access_token_hook
      to: JWT claims
      via: "restaurant_id claim injected from memberships lookup"
      pattern: "restaurant_id"
---

<objective>
Close **Gap 2** from 05-HUMAN-UAT.md: the friend's Supabase Auth account is not provisioned. Phase 1 D-10 said "founder pre-creates the friend's user via the Supabase dashboard and shares credentials" but this was never executed — the only seeded user is `iguchise@gmail.com`, the founder's dev account. Without a real Auth user + a `memberships` row linking that user to restaurant `ba1bf707-aae9-46a9-8166-4b6459e6c2fd`, the friend's sign-in will land on `/not-provisioned`.

This plan is mostly **manual** (Supabase Dashboard UI for auth user creation and secure credential sharing) with automated SQL verification steps.

Purpose: Unblocks 05-06 Task 3. The friend cannot sign off on the InsightCard if they cannot sign in.
Output: A provisioned friend account that successfully signs in against the DEV environment (or the Task-05-07 production URL) and lands on the dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@supabase/migrations/0001_tenancy_schema.sql
@supabase/migrations/0002_auth_hook.sql
@supabase/migrations/0005_seed_tenant.sql
@supabase/migrations/0015_auth_hook_security_definer.sql
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/05-insights-forkability/05-HUMAN-UAT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create the friend's Auth user in Supabase Dashboard</name>
  <what-built>A real Supabase Auth user for the friend in the DEV project. Done via Dashboard UI because (a) creating users via SQL bypasses Supabase's password hashing / email confirmation flow, and (b) the founder needs the Dashboard-generated invite or password to share securely.</what-built>
  <how-to-verify>
    1. Open https://supabase.com/dashboard → select the DEV project → Authentication → Users.
    2. Click "Add user" → "Create new user".
    3. Email: the friend's real email address (confirm with founder out-of-band; do NOT put it in plans or commits).
    4. Password: generate a strong random password (use `openssl rand -base64 16`). Auto-confirm the user (tick "Auto Confirm User" if present, else use "Send invite" flow — Dashboard will prompt).
    5. Click "Create user". Note the generated `user_id` (UUID) from the user row that appears — you will need it in Task 2.
    6. Record the email + user_id (but NOT the password) in a scratchpad — they go into the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "user created: user_id=<uuid> email=<email>" once the user exists in the Dashboard, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Insert memberships row linking friend to restaurant</name>
  <files></files>
  <read_first>
    - supabase/migrations/0001_tenancy_schema.sql (confirm memberships table shape: user_id, restaurant_id, role)
    - supabase/migrations/0005_seed_tenant.sql (confirm restaurant_id literal ba1bf707-aae9-46a9-8166-4b6459e6c2fd exists)
    - Task 1 resume note (friend's user_id UUID)
  </read_first>
  <action>
Using the Supabase Dashboard SQL Editor against the DEV project, run (substituting `<FRIEND_USER_ID>` from Task 1):

```sql
-- Idempotent insert: ON CONFLICT DO NOTHING in case this plan is re-run
INSERT INTO public.memberships (user_id, restaurant_id, role)
VALUES (
  '<FRIEND_USER_ID>'::uuid,
  'ba1bf707-aae9-46a9-8166-4b6459e6c2fd'::uuid,
  'owner'
)
ON CONFLICT (user_id, restaurant_id) DO UPDATE SET role = EXCLUDED.role;

-- Verify
SELECT m.user_id, m.restaurant_id, m.role, u.email, r.name
FROM public.memberships m
JOIN auth.users u ON u.id = m.user_id
JOIN public.restaurants r ON r.id = m.restaurant_id
WHERE m.user_id = '<FRIEND_USER_ID>'::uuid;
```

Expected: exactly one row returned with `role='owner'`, the friend's email, and the restaurant's name.

If `memberships` has different column names (e.g. `membership_role` instead of `role`), read `0001_tenancy_schema.sql` first to adapt the SQL. Do NOT guess — read the schema.

If the `ON CONFLICT` key is different from `(user_id, restaurant_id)`, adapt to match the actual UNIQUE constraint. Use `\d public.memberships` in SQL Editor to confirm.
  </action>
  <verify>
    <automated>MISSING — this task runs SQL through the Supabase Dashboard SQL editor; capture the SELECT output into 05-08-SUMMARY.md as the verification artifact. Executor should paste the returned row verbatim.</automated>
  </verify>
  <acceptance_criteria>
    - The SELECT query returns exactly 1 row
    - That row's `role` column equals `'owner'` (or the equivalent per 0001_tenancy_schema.sql)
    - That row's `restaurant_id` equals `ba1bf707-aae9-46a9-8166-4b6459e6c2fd`
    - That row's `email` matches the friend's email from Task 1
    - The SELECT output is captured in 05-08-SUMMARY.md verbatim
  </acceptance_criteria>
  <done>Friend's user_id is linked to the founder's restaurant as an owner.</done>
</task>

<task type="auto">
  <name>Task 3: Verify JWT mints restaurant_id claim + test end-to-end sign-in</name>
  <files></files>
  <read_first>
    - supabase/migrations/0002_auth_hook.sql (confirm custom_access_token_hook reads from memberships)
    - supabase/migrations/0015_auth_hook_security_definer.sql (confirm hook is SECURITY DEFINER)
    - .cloudflare/pages-project.md (from 05-07 — the deployed URL the friend will use)
  </read_first>
  <action>
Two verifications:

**A. JWT claim verification (SQL Editor):**

```sql
-- Simulate what the access token hook returns for the friend
SELECT public.custom_access_token_hook(
  jsonb_build_object(
    'user_id', '<FRIEND_USER_ID>',
    'claims', jsonb_build_object('sub', '<FRIEND_USER_ID>')
  )
);
```

Expected: JSON output whose `claims` object contains `"restaurant_id": "ba1bf707-aae9-46a9-8166-4b6459e6c2fd"`. If the hook function signature differs, read `0002_auth_hook.sql` and adapt the call site.

**B. End-to-end sign-in smoke test:**

Open the deployed URL from 05-07 (`https://ramen-bones-analytics.pages.dev/login`) in a clean browser profile (or incognito). Sign in with the friend's email + the password generated in Task 1.

Expected:
- Redirect lands on `/` (dashboard), NOT `/not-provisioned`
- At least one card renders (even if InsightCard shows the degenerate "€0" row — Gap 3 closes that)
- No console errors mentioning "restaurant_id" or "JWT"

**C. Secure credential handoff:**

Share the friend's credentials via a secure channel (Signal, 1Password sharing link, etc.). Do NOT commit the password to git, do NOT paste it in the SUMMARY, do NOT send via plain email or SMS. In the SUMMARY, record only:
- The channel used (e.g. "Signal")
- The date shared
- Confirmation from the founder that the friend received them

If the friend tests sign-in themselves and confirms dashboard loads, that's the strongest signal — record the confirmation timestamp.
  </action>
  <verify>
    <automated>MISSING — manual browser sign-in flow. Capture in 05-08-SUMMARY.md: (1) the custom_access_token_hook SELECT output showing restaurant_id claim, (2) a screenshot or verbatim description of the dashboard page loading after sign-in (URL is `/`, not `/not-provisioned`), (3) confirmation of secure credential handoff.</automated>
  </verify>
  <acceptance_criteria>
    - `custom_access_token_hook` SELECT returns JSON containing `"restaurant_id": "ba1bf707-aae9-46a9-8166-4b6459e6c2fd"`
    - Signing in as the friend at `https://ramen-bones-analytics.pages.dev/login` redirects to `/` (dashboard), not `/not-provisioned`
    - Credential handoff channel + date recorded in SUMMARY
    - Password is NOT in any committed file
  </acceptance_criteria>
  <done>Friend can sign in to the deployed app and see the dashboard.</done>
</task>

</tasks>

<verification>
- Auth user exists in DEV Supabase project (verified in Dashboard UI)
- `memberships` row exists with role='owner' and the correct restaurant_id (verified via SQL SELECT)
- JWT hook mints `restaurant_id` claim (verified via SELECT on `custom_access_token_hook`)
- End-to-end sign-in against the deployed URL (from 05-07) lands on the dashboard
- Credentials shared via secure channel
</verification>

<success_criteria>
- Friend can sign in and see the dashboard
- No `/not-provisioned` redirect
- Gap 2 from 05-HUMAN-UAT.md is closed
- 05-06 Task 3 is unblocked from the "friend cannot auth" side
</success_criteria>

<output>
Create `.planning/phases/05-insights-forkability/05-08-SUMMARY.md` with:
- Friend's user_id (UUID is not secret — the email/password are)
- Friend's email (if founder is comfortable — else "shared privately")
- memberships SELECT output (verbatim)
- custom_access_token_hook SELECT output showing restaurant_id claim
- Sign-in verification result (dashboard loaded / not-provisioned)
- Secure credential handoff channel + date
- Explicit note: password is NOT in this file or any committed file
</output>
