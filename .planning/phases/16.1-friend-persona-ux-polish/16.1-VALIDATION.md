---
phase: 16.1
slug: friend-persona-ux-polish
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-04
---

# Phase 16.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Source-of-truth for the planner's per-task `<automated>` / `<manual>` references.

Derived from `16.1-RESEARCH.md` §"Validation Architecture" (lines 496-540). All assertions in this file are owned by the planner; this document is the contract the plan-checker enforces.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Backend framework** | Pytest 7.x (existing — `scripts/forecast/tests/`) |
| **Frontend framework** | Vitest + svelte-check (existing — `vite.config.ts`) |
| **Visual gate** | Chrome MCP at `http://localhost:5173` (per `.claude/CLAUDE.md` localhost-first rule) |
| **Backend config file** | `scripts/forecast/pyproject.toml` / `pytest.ini` |
| **Frontend config file** | `vite.config.ts` |
| **Quick run command (backend)** | `pytest scripts/forecast/tests/test_grain_helpers.py -x` |
| **Quick run command (frontend)** | `npm run check 2>&1 \| tail -30` |
| **Full suite command** | `pytest scripts/forecast/tests/ && npm run check && npm run build` |
| **Estimated runtime** | backend ~2s, frontend ~25s, full ~60s |

---

## Sampling Rate

- **After every task commit (16.1-04 / backend tasks):** `pytest scripts/forecast/tests/test_grain_helpers.py -x`
- **After every task commit (Wave 2 / UI tasks):** `npm run check 2>&1 | tail -30`
- **After every plan wave:** `pytest scripts/forecast/tests/ && npm run check && npm run build`
- **Before `/gsd-verify-work`:** Full suite green + Chrome MCP localhost verify in ja+en for ALL modified `.svelte` files
- **Max feedback latency (per-task):** ~25s (frontend `check`); ~2s (backend `pytest -x`)

---

## Per-Task Verification Map

> Plan IDs and task IDs are placeholders pending the planner's final task graph. The mapping below is **what must be verified**, not where it lives in the task tree.

| Verification | Plan | Wave | Requirement | Threat / Carry-fwd | Test Type | Automated Command | File Exists | Status |
|--------------|------|------|-------------|---------------------|-----------|-------------------|-------------|--------|
| D-14 day-grain math = `window_start_for_grain(date(2026,4,27), 'day') == date(2026,4,20)` | 16.1-04 | 1 | UPL-05 / FUI-09 | C-01 (superseded) | unit | `pytest scripts/forecast/tests/test_grain_helpers.py::test_window_start_day -x` | ❌ W0 | ⬜ pending |
| D-14 week-grain math = `window_start_for_grain(date(2026,4,27), 'week') == date(2026,3,23)` (CW13 Mon) | 16.1-04 | 1 | UPL-05 / FUI-09 | — | unit | `pytest scripts/forecast/tests/test_grain_helpers.py::test_window_start_week -x` | ❌ W0 | ⬜ pending |
| D-14 month-grain math = `window_start_for_grain(date(2026,4,30), 'month') == date(2026,1,1)` | 16.1-04 | 1 | UPL-05 / FUI-09 | — | unit | `pytest scripts/forecast/tests/test_grain_helpers.py::test_window_start_month -x` | ❌ W0 | ⬜ pending |
| D-14 cold-start (no actuals) doesn't crash | 16.1-04 | 1 | UPL-05 / FUI-09 | — | unit | `pytest scripts/forecast/tests/test_grain_helpers.py::test_window_start_today_anchor -x` | ❌ W0 | ⬜ pending |
| D-15 `pred_dates_for_grain(window_start=W)` includes window_start in output | 16.1-04 | 1 | UPL-05 / FUI-09 | — | unit | `pytest scripts/forecast/tests/test_grain_helpers.py::test_pred_dates_with_window -x` | ❌ W0 | ⬜ pending |
| D-15 acceptance — `forecast_with_actual_v` row count matches `today + horizon - window_start + 1` per (granularity, model, kpi) | 16.1-04 | 1 | UPL-05 / FUI-09 | C-01 (superseded) | integration | manual SQL via Supabase Studio after `gh workflow run forecast-refresh.yml --ref feature/phase-16-its-uplift-attribution` | N/A | ⬜ manual |
| D-16 tooltip top + bottom render when bar + forecast both exist | 16.1-01 | 2 | UPL-05 / FUI-09 | C-02 | manual | Chrome MCP localhost:5173 — hover Calendar* bar at bucket with `target_date < lastActualDate` | N/A | ⬜ manual |
| D-16 tooltip top-only when bar exists but no forecast row (bucket outside `[window_start, today + horizon]`) | 16.1-01 | 2 | UPL-05 / FUI-09 | C-02 | manual | Chrome MCP localhost:5173 — hover bar before window_start | N/A | ⬜ manual |
| D-16 tooltip bottom-only when forecast row exists but no bar (future bucket) | 16.1-01 | 2 | UPL-05 / FUI-09 | C-02 | manual | Chrome MCP localhost:5173 — hover future bucket | N/A | ⬜ manual |
| D-17 forecast-card scroll-to-today lands at ~60% viewport on first paint | 16.1-05 | 2 | UPL-05 / FUI-09 | — | manual | Chrome MCP localhost:5173 at 375×667 — load page, screenshot, measure scrollerRef.scrollLeft | N/A | ⬜ manual |
| D-17 `totalSlots = past_buckets + bar_buckets + future_buckets` after D-15 lands | 16.1-05 | 2 | UPL-05 / FUI-09 | — | grep | `grep -n "totalSlots" src/lib/components/RevenueForecastCard.svelte src/lib/components/InvoiceCountForecastCard.svelte` returns the new helper call | grep gate ✓ | ⬜ pending |
| D-18 supportive labels render: hero subtitle + Y-axis label + X-axis caption + counterfactual baseline marker | 16.1-03 | 2 | UPL-06 / FUI-09 | — | manual | Chrome MCP localhost:5173 at 375×667 — visual check all 4 labels visible | N/A | ⬜ manual |
| D-18 i18n ja AND en — no raw `{key}` fallthrough | 16.1-03 | 2 | UPL-06 / FUI-09 | C-06 | manual | Chrome MCP localhost:5173 toggle locale; visually verify all D-18 labels translated | N/A | ⬜ manual |
| C-02 invariant — Tooltip.Root uses `{#snippet children}` form (no `let:data`) on ALL modified files | 16.1-01, -03, -05 | 2 | FUI-09 | C-02 | grep | `grep -c "let:data" src/lib/components/Calendar*.svelte src/lib/components/*Forecast*.svelte src/lib/components/CampaignUpliftCard.svelte` returns `0` | grep gate ✓ | ⬜ pending |
| C-03 invariant — `tooltipContext: { touchEvents: 'auto' }` on every Chart in modified files | 16.1-01, -05 | 2 | FUI-09 | C-03 | grep | `grep -c "touchEvents:" src/lib/components/Calendar*.svelte src/lib/components/*Forecast*.svelte` returns ≥1 per file | grep gate ✓ | ⬜ pending |
| C-05 invariant — localhost-first verification BEFORE DEV push for every modified .svelte | 16.1-01, -03, -05 | 2 | FUI-09 | C-05 | manual | Chrome MCP localhost:5173 — declared `Visual verification: PASS` in transcript before push | N/A | ⬜ per-task gate |
| Planning-docs drift gate clean | (phase gate) | — | — | — | script | `.claude/scripts/validate-planning-docs.sh` exits 0 | script gate ✓ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `scripts/forecast/tests/test_grain_helpers.py` — failing tests for D-14 day/week/month/cold-start math + D-15 `pred_dates_for_grain(window_start=…)` shape. **Must exist before 16.1-04 Task 1 lands** (TDD-RED gate).
- [ ] No framework install — pytest, vitest, svelte-check, and Chrome MCP are all already configured.

*If `scripts/forecast/tests/` doesn't exist yet, plan 16.1-04 Task 1 creates it (with `__init__.py` if Python conventions require it) before the helper edit.*

---

## Manual-Only Verifications

All Wave 2 acceptance gates run via Chrome MCP at `http://localhost:5173` (375×667 viewport, ja AND en locales, per `.claude/memory/feedback_localhost_first_ui_verify.md`):

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Friend-persona acceptance — owner reads card, states what it tells her | D-20 / FUI-09 | Subjective; requires owner-in-loop | After all Wave 2 plans verify-clean: deploy to DEV, owner Chrome MCP review at her phone resolution |
| D-15 row-count integration | D-15 | Production-DB only — no fixture DB on DEV | Run `forecast-refresh.yml workflow_dispatch` on feature branch; SELECT COUNT(DISTINCT target_date) ... in Supabase Studio matches expected window |
| Calendar* card past + future Spline visual continuity (no gap at `lastActualDate`) | D-01..D-04 | Visual gestalt; no automated pixel diff for v1.3 | Chrome MCP localhost screenshot at day grain with `last_actual_date < today`; visual check seam |
| Forecast cards horizontal-scroll behavior + scroll-to-today position | D-17 | Touch interaction; viewport-relative | Chrome MCP localhost at 375×667; scrollerRef.scrollLeft within ±5% of `chartW * 0.6` |
| CampaignUpliftCard regime tier × CI matrix produces correct hero string | D-05..D-07 | Owner-readable copy gate | Chrome MCP localhost; verify hero string matches D-06 matrix for n_days fixture data |
| CampaignUpliftCard disclosure panel collapsed by default; opens on click | D-09..D-11 | DOM-state interaction gate | Chrome MCP localhost; click trigger; assert panel content visible |
| CampaignUpliftCard supportive labels (D-18) render in ja AND en | D-18 | i18n surface gate | Chrome MCP localhost; toggle locale; verify all 4 labels translated |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (Wave 2 plans must interleave grep gates / svelte-check between manual Chrome MCP gates)
- [ ] Wave 0 covers all MISSING references (`scripts/forecast/tests/test_grain_helpers.py`)
- [ ] No watch-mode flags in any `<automated>` command (use `pytest -x`, `npm run check` once)
- [ ] Feedback latency < 30s per task
- [ ] `nyquist_compliant: true` set in frontmatter (after planner self-attests post-plan-write)

**Approval:** pending (will be approved by gsd-plan-checker on verification pass)
