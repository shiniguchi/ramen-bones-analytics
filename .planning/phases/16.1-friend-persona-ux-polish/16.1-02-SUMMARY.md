---
phase: 16.1-friend-persona-ux-polish
plan: 02
subsystem: ui
tags: [i18n, sveltekit, svelte5, typescript, messages, campaign-uplift-card]

# Dependency graph
requires:
  - phase: 16-its-uplift-attribution
    provides: CampaignUpliftCard.svelte (the consumer in Plan 16.1-03), `headline.row.n_days` API field (drives D-05 maturity tier)
  - phase: 15-forecast-backtest-overlay
    provides: t() helper + Record<MessageKey, string> compile-time i18n contract
provides:
  - 13 i18n keys covering CampaignUpliftCard plain-language regime (D-06 hero matrix, D-08 secondary, D-09 disclosure trigger, D-10 disclosure panel labels)
  - JA natural translations matching owner-persona register
  - DE/ES/FR placeholder copies (= EN verbatim) + v1.4 translation backlog stub
affects: [16.1-03 (consumes the keys), v1.4-i18n-polish (de/es/fr translation work)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "i18n key append pattern: divider comment + EN canonical + 4 placeholder mirrors keeps Record<MessageKey, string> compile-clean"
    - "Single-brace placeholder syntax {date}/{weeks}/{point}/{lo}/{hi} compatible with interpolate() at messages.ts:927"

key-files:
  created:
    - .planning/backlog/i18n-campaign-uplift-card-de-es-fr.md
  modified:
    - src/lib/i18n/messages.ts

key-decisions:
  - "DE/ES/FR placeholder = EN verbatim per CONTEXT.md C-05 (owner only verifies ja + en) — minimum-friction path that still satisfies Record<MessageKey, string> compile-time enforcement"
  - "JA translations are real, owner-persona register (banking-domain not stats-domain) — matches CONTEXT.md friend-owner persona"
  - "Single-brace placeholder syntax mandated by interpolate() regex /\\{(\\w+)\\}/g at messages.ts:927 — no double-brace mistakes"

patterns-established:
  - "Phase 16.1 i18n divider header: `// --- Campaign uplift card (Phase 16.1[: D-* notes]) ---` distinguishes new keys from prior phases"
  - "Placeholder-locale comment marker: `— DE placeholder = EN; v1.4 polish backlog item filed` flags un-translated locale blocks for the v1.4 polish wave"

requirements-completed: [UPL-06, FUI-09]

# Metrics
duration: 3min
completed: 2026-05-04
---

# Phase 16.1 Plan 02: i18n keys for CampaignUpliftCard plain-language regime — Summary

**13 new i18n keys land in all 5 locale blocks of `src/lib/i18n/messages.ts` covering D-06 hero tier×CI matrix, D-08 plain-language secondary, D-09 disclosure trigger, D-10 detail panel — JA gets natural owner-persona translations; DE/ES/FR get EN placeholders + v1.4 backlog stub.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-04T09:05:30Z
- **Completed:** 2026-05-04T09:08:22Z
- **Tasks:** 1
- **Files modified:** 1 (`src/lib/i18n/messages.ts`)
- **Files created:** 1 (`.planning/backlog/i18n-campaign-uplift-card-de-es-fr.md`)

## Accomplishments

- 13 keys × 5 locale blocks = 65 entries appended to `messages.ts` with TypeScript `Record<MessageKey, string>` compile-time completeness preserved
- JA translations are owner-friendly natural Japanese (e.g. `判断するにはまだ早すぎます`, `どうやって計算しているの？`) — non-statistical register matches PROJECT.md friend-owner persona
- DE/ES/FR placeholder copies + v1.4 translation backlog stub filed for proper translation
- Plan 16.1-03 unblocked: every `t(page.data.locale, 'uplift_*')` call site now has type-safe key references with no runtime fallback to raw key string

## Task Commits

1. **Task 1: Append 13 new keys to all 5 locale blocks** — `be961b2` (feat)

**Plan metadata:** [final commit hash recorded after this summary commit]

## Files Created/Modified

- `src/lib/i18n/messages.ts` — +80 lines / -5 lines (4 sections: EN canonical with `as const`, DE/ES/FR placeholder mirrors with EN copy, JA with natural translations); diff stats `git diff --stat HEAD~1 HEAD -- src/lib/i18n/messages.ts` shows `1 file changed, 80 insertions(+), 5 deletions(-)` (the -5 is the trailing-comma + newline reflow from the prior `repeater_cohort_description` line)
- `.planning/backlog/i18n-campaign-uplift-card-de-es-fr.md` — new v1.4 stub: scope, owner TBD, ~1.5h effort

## Verification Output

### Grep loop — every key appears 5x (one per locale block)

```
uplift_card_title_with_date: 5
uplift_card_computing: 5
uplift_hero_too_early: 5
uplift_hero_early_not_measurable: 5
uplift_hero_early_added: 5
uplift_hero_early_reduced: 5
uplift_hero_mature_no_lift: 5
uplift_hero_mature_added: 5
uplift_hero_mature_reduced: 5
uplift_secondary_plain: 5
uplift_details_trigger: 5
uplift_details_anticipation_plain: 5
uplift_details_divergence_plain: 5
```

13/13 keys × 5/5 locales = clean.

### Regression guards

- `^} as const;` count: **1** (EN block `as const` preserved → `MessageKey = keyof typeof en` widens correctly)
- `Record<MessageKey, string>` count: **5** (4 locale block declarations + 1 in `export const messages: Record<Locale, Record<MessageKey, string>>`)
- `export const messages: Record<Locale, Record<MessageKey, string>> = { en, de, ja, es, fr };` — present on line 922
- JA CJK presence: `grep -c "効果ありましたか" src/lib/i18n/messages.ts` → **1** (real Japanese, not placeholder)

### `npm run check` (svelte-check, tsconfig.json)

```
1777885631784 COMPLETED 4696 FILES 7 ERRORS 0 WARNINGS 3 FILES_WITH_PROBLEMS
```

7 errors are **all pre-existing** (vite.config.ts test-config typing, hooks.server.ts implicit-any, CalendarRevenueCard.svelte index possibly undefined). **Zero errors related to messages.ts** — verified via `grep -i "messages.ts\|MessageKey\|uplift_"` on full check output (returns no matches).

### `npm run build`

```
✓ built in 17.47s
> Using @sveltejs/adapter-cloudflare
  ✔ done
```

Production build green.

## Decisions Made

- **Placeholder copies in DE/ES/FR (= EN verbatim)** — per CONTEXT.md C-05 (owner only verifies ja + en) and the planner's pre-locked snippet. Compile-clean without committing to unreviewed translations. Section divider comment in each placeholder block (`— DE/ES/FR placeholder = EN; v1.4 polish backlog item filed`) flags the un-translated state for the v1.4 polish wave.
- **Single-brace placeholders** for `{date}`, `{weeks}`, `{point}`, `{lo}`, `{hi}` — matches existing `interpolate()` regex `/\{(\w+)\}/g` at messages.ts:927. No double-brace mistakes.
- **Section divider styling** — `// --- Campaign uplift card (Phase 16.1[: D-* notes]) ---` follows the existing `// --- Foo bar ---` convention (e.g., Forecast hover popup, MdeCurveCard sections).

## Deviations from Plan

None — plan executed exactly as written. The 13 EN strings, 13 JA strings, and the placeholder rule for DE/ES/FR were all specified verbatim in the plan; copying them in was a mechanical edit. Snippet-form acceptance criteria (`as const`, `Record<MessageKey, string>` count, JA CJK presence, single-brace syntax, grep-loop 5/5) all pass on first try.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required. UI-only data work.

## Next Phase Readiness

- **Plan 16.1-03 unblocked** — every `t(page.data.locale, 'uplift_hero_*' / 'uplift_details_*' / 'uplift_card_*' / 'uplift_secondary_*')` call site in CampaignUpliftCard.svelte (Plan 03) now resolves to a real key with TypeScript completeness guarantee.
- **No UI verification at this stage** (per plan output spec) — keys are not yet consumed. Plan 16.1-03 wires them into CampaignUpliftCard and runs the Chrome MCP localhost-first gate.
- **Backlog item `i18n-campaign-uplift-card-de-es-fr.md`** ready for v1.4 polish wave when proper DE/ES/FR translations are commissioned.

## Self-Check: PASSED

- `src/lib/i18n/messages.ts`: FOUND
- `.planning/backlog/i18n-campaign-uplift-card-de-es-fr.md`: FOUND
- Commit `be961b2`: FOUND in `git log --oneline`
- All 13 keys appear 5x each in messages.ts (grep-loop output above)
- `} as const;` preserved (1 occurrence)
- `Record<MessageKey, string>` preserved (5 occurrences)
- `export const messages: Record<Locale, ...> = { en, de, ja, es, fr };` preserved
- JA CJK characters present (`効果ありましたか` grep returns 1)
- `npm run check`: zero new TypeScript errors related to messages.ts
- `npm run build`: succeeds (`✓ built in 17.47s`)

---
*Phase: 16.1-friend-persona-ux-polish*
*Plan: 02*
*Completed: 2026-05-04*
