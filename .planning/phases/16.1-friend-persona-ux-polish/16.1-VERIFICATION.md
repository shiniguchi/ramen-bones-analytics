---
phase: 16.1-friend-persona-ux-polish
verified: 2026-05-04T19:30:00Z
status: human_needed
score: 7/10 must-haves verified (3 require human/live-environment verification)
overrides_applied: 0
human_verification:
  - test: "Trigger GHA forecast-refresh.yml on feature branch and verify Supabase SQL row count"
    expected: "SELECT COUNT(DISTINCT target_date) FROM forecast_with_actual_v WHERE granularity='day' returns ~387 rows (was ~372 pre-D-15); MIN(target_date) for day-grain SARIMAX revenue_eur ≈ 2026-04-20; Apr 28-29 rows present"
    why_human: "16.1-04 Task 5 was explicitly deferred — unit tests (16/16) prove helper math; call-site wiring is grep-confirmed for all 5 models; but actual GHA dispatch + DB query is the only way to prove past-window rows land in DEV (environment-specific: GHA -> Supabase write path + RLS view passthrough)"
    command: "gh workflow run forecast-refresh.yml --ref feature/phase-16-its-uplift-attribution && gh run watch"
  - test: "Chrome MCP localhost:5173 at 375x667 — CalendarRevenueCard + CalendarCountsCard at day/week/month grains in ja AND en"
    expected: "Past-forecast lines visible (solid faded) bridging actuals end to last_actual_date; future-forecast lines dashed from last_actual_date forward; CI band continuous; today lands at ~60% viewport; tooltip shows BOTH visit-cohort section (top) AND per-model forecast values with JA/EN model labels (bottom) on a bucket with both bar data and forecast data; zero console errors"
    why_human: "All 3 Chrome MCP gates (16.1-01 Task 6, 16.1-03 Task 4, 16.1-05 Task 3) were auto-approved per workflow.auto_advance=true config — Chrome MCP tools not in executor's tool whitelist. Past-Spline visibility also depends on 16.1-04 Task 5 GHA dispatch completing first (past rows must exist in forecast_with_actual_v)"
  - test: "Chrome MCP localhost:5173 at 375x667 — CampaignUpliftCard in ja AND en"
    expected: "Hero string is plain-language tier-aware (e.g. 'Too early to tell' in en / '判断するにはまだ早すぎます' in ja for <14d campaign); D-18 supportive labels visible (subtitle above hero, Y label above sparkline, X caption below sparkline, baseline legend chip with dashed swatch); 'How is this calculated? ›' button present and toggles inline panel with statistical detail + anticipation note; locale switch shows real JA strings (not EN placeholders); zero console errors"
    why_human: "16.1-03 Task 4 Chrome MCP gate auto-approved. Tier rendered depends on current campaign n_days from /api/campaign-uplift on DEV — only verifiable with live data."
  - test: "Friend-persona acceptance: owner reads any of 4 cards at 375px and states in her own words what it tells her"
    expected: "Owner can verbally describe what CalendarRevenueCard / CalendarCountsCard / RevenueForecastCard / CampaignUpliftCard is showing without asking for translation or statistical explanation"
    why_human: "Subjective owner acceptance test by definition — requires the actual friend-owner to view the cards. Cannot be automated or substituted by developer verification."
---

# Phase 16.1: Friend-Persona UX Polish Verification Report

**Phase Goal:** Close the four friend-persona acceptance gaps from the 2026-05-04 owner Chrome MCP localhost reviews: (1) Calendar* + Forecast cards render past-forecast continuously over windowed range; (2) Forecast cards get horizontal-scroll parity; (3) Calendar* Tooltip.Root shows per-visible-model forecast values; (4) CampaignUpliftCard plain-language regime tiering + supportive chart-context labels.

**Verified:** 2026-05-04T19:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Calendar* cards split Splines into past (faded solid) + future (dashed) + continuous CI band | VERIFIED | `lastActualDate` $derived (count=1 each), `splitSeriesByModel` $derived (count=1 each), `stroke-opacity={0.7}` (count=1 each), `fillOpacity={0.06}` (count=1 each), `past-line-`/`future-line-` keys (count=2 each) in both CalendarRevenueCard.svelte and CalendarCountsCard.svelte |
| 2 | D-14 grain math anchors on last complete period (Mon-week / 5CW / 4 months) | VERIFIED | `window_start_for_grain` in grain_helpers.py implements exact Monday-anchored week, -28d for 5-week window, and is_end_of_month check for month grain. 16/16 pytest assertions GREEN confirming all edge cases (Sunday last_actual, mid-month, end-of-month, cold-start) |
| 3 | Pipeline + API emit continuous past+future rows over windowed range (D-15) | REQUIRES_HUMAN_VERIFY | Unit tests 16/16 GREEN; all 5 model fits wired (window_start_for_grain count=3 in prophet_fit.py, count>=3 in sarimax/naive_dow/ets/theta); GHA dispatch + SQL row-count verification (16.1-04 Task 5) explicitly deferred per orchestrator instruction |
| 4 | Forecast cards get horizontal-scroll wrapper + past/future Spline split (D-17) | VERIFIED | RevenueForecastCard + InvoiceCountForecastCard both have `overflow-x-auto` (count=1 each), `computeChartWidth` (count=3 each), `scrollerRef`/`cardW`/`chartW` (count=7 each), `pastBuckets`/`futureBuckets`/`totalSlots` (count=7 each), `stroke-opacity={0.7}` (count=3 each including comments), `curveMonotoneX` preserved (count=5 each) |
| 5 | Calendar* Tooltip.Root shows per-visible-model forecast values (D-16) | VERIFIED | `forecast_model_` i18n call site (count>=1 each), `FORECAST_MODEL_COLORS[name]` (count=5 each), `splitSeriesByModel` used in tooltip lookup (count>=2 each), `{#snippet children` form preserved (C-02), `touchEvents: 'auto'` preserved (C-03) in both Calendar* cards |
| 6 | CampaignUpliftCard plain-language regime + D-18 supportive labels | VERIFIED | `maturityTier` $derived (count=2 confirmed), `heroKey` $derived (count=5), 7-key tier×CI matrix implemented, `detailsOpen` $state, `uplift-details-panel` (count=3), `uplift_card_subtitle`/`uplift_sparkline_y_label`/`uplift_sparkline_x_caption`/`uplift_baseline_label` all present (count>=1 each), `aria-expanded` + `aria-controls` (count=2), hardcoded-English regression guard returns 0 |
| 7 | Mobile bundle budget unchanged (no new chart libs) | VERIFIED | `package.json` unchanged across Phase 16.1 commits; all 5 modified components import only pre-existing `layerchart`, `d3-scale`, `d3-shape`, `d3-time`, `date-fns` |
| 8 | Localhost-first Chrome MCP verification at 375×667 in ja+en BEFORE DEV deploy | REQUIRES_HUMAN_VERIFY | All 3 Chrome MCP gates (16.1-01 Task 6, 16.1-03 Task 4, 16.1-05 Task 3) auto-approved per workflow.auto_advance=true config; Chrome MCP not in executor tool whitelists; orchestrator-designated as human verification |
| 9 | Friend-persona acceptance: owner states what any of 4 cards tells her without translation | REQUIRES_HUMAN_VERIFY | Subjective owner test by definition — requires actual friend-owner at 375px; cannot be automated |
| 10 | Planning-docs drift gate passes | VERIFIED | `.claude/scripts/validate-planning-docs.sh` output: `✅ planning docs in sync — ROADMAP: 17/18 phases checked, STATE: 79/91 plans (frontmatter), Disk: 79/91 plans summarised` |

**Score:** 7/10 truths verified (3 require human/live-environment verification)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/i18n/messages.ts` | 25 D-16 model-label keys (5 keys × 5 locales) + 13 D-06/D-08/D-09 uplift keys + 4 D-18 supportive-label keys | VERIFIED | forecast_model_sarimax count=5, forecast_model_naive_dow count=5, forecast_model_prophet count=5, forecast_model_ets count=5, forecast_model_theta count=5, uplift_hero_too_early count=5, uplift_details_trigger count=5, uplift_card_subtitle count=5, uplift_baseline_label count=5, 単純予測 count=1 |
| `src/lib/components/CalendarRevenueCard.svelte` | Past+future Spline split + chartXDomain widening + D-16 tooltip extension | VERIFIED | lastActualDate=1, splitSeriesByModel=6, forecastWindowStart=12, stroke-opacity=1, fillOpacity=1, pastForecastBuckets>=2, forecast_model_=1, FORECAST_MODEL_COLORS=5, {#snippet children=1, touchEvents 'auto'=1 |
| `src/lib/components/CalendarCountsCard.svelte` | Mirror of CalendarRevenueCard with formatIntShort | VERIFIED | lastActualDate=1, splitSeriesByModel=5, forecastWindowStart=11, stroke-opacity=1, kpi=invoice_count=2 (1 doc-comment+1 fetch), formatIntShort=4, forecast_model_=1, FORECAST_MODEL_COLORS=5 |
| `src/lib/components/CampaignUpliftCard.svelte` | Tier-aware hero + disclosure + 4 D-18 supportive labels | VERIFIED | page import=1, uplift-details-panel=3, maturityTier=2, heroKey=5, detailsOpen=5, uplift_secondary_plain=1, uplift_card_subtitle=5, aria-expanded=2, hardcoded-English=0, let:data=0, touchEvents 'auto'=1 |
| `src/lib/components/RevenueForecastCard.svelte` | D-17 horizontal-scroll + past/future Spline split | VERIFIED | computeChartWidth=3, overflow-x-auto=1, scrollerRef/cardW/chartW state=7, pastBuckets/futureBuckets/totalSlots=7, stroke-opacity=3, curveMonotoneX=5, width={chartW}=1 |
| `src/lib/components/InvoiceCountForecastCard.svelte` | Mirror of RevenueForecastCard | VERIFIED | computeChartWidth=3, overflow-x-auto=1, stroke-opacity=3, curveMonotoneX=5, kpi=invoice_count=2, formatIntShort=3, width={chartW}=1 |
| `scripts/forecast/grain_helpers.py` | window_start_for_grain helper + pred_dates_for_grain extended with window_start + train_end kwargs | VERIFIED | window_start_for_grain defined (lines 53-105), pred_dates_for_grain accepts window_start + train_end kwargs (count>=12 each), all granularities implemented |
| `scripts/forecast/tests/test_grain_helpers.py` | 16 unit tests covering D-14 + D-15 + B2 + W8 | VERIFIED | File exists (169 lines), `def test_` count=16, pytest run: 16 passed in 0.03s |
| `scripts/forecast/sarimax_fit.py` | Wired to window_start_for_grain | VERIFIED | window_start_for_grain count=3, train_end=train_end count>=5 |
| `scripts/forecast/naive_dow_fit.py` | Wired to window_start_for_grain | VERIFIED | window_start_for_grain count=3, window_start=window_start OR train_end=train_end count>=5 |
| `scripts/forecast/ets_fit.py` | Wired to window_start_for_grain | VERIFIED | window_start_for_grain count=3, kwargs present |
| `scripts/forecast/theta_fit.py` | Wired to window_start_for_grain | VERIFIED | window_start_for_grain count=3, kwargs present |
| `scripts/forecast/prophet_fit.py` | Path A — wired to window_start_for_grain at both cf-track + bau-track call sites | VERIFIED | window_start_for_grain count=3 (1 import + 2 call sites), train_end=train_end count>=3 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| CalendarRevenueCard.svelte | /api/forecast past rows | splitSeriesByModel partitions forecastData.actuals[].date | WIRED | splitSeriesByModel count=6; lastActualDate derives from forecastData.actuals |
| CalendarCountsCard.svelte | /api/forecast past rows | Same partition primitive | WIRED | splitSeriesByModel count=5 |
| Calendar*Card.svelte | messages.ts D-16 model labels | `t(page.data.locale, forecast_model_${name} as MessageKey)` | WIRED | forecast_model_ call site present in both cards; 5 keys × 5 locales = 25 entries verified |
| CampaignUpliftCard.svelte | messages.ts uplift keys | `t(page.data.locale, heroKey, heroVars)` and per-D-18-label calls | WIRED | 9 distinct uplift_* key call sites confirmed (count=9 in grep) |
| CampaignUpliftCard.svelte | /api/campaign-uplift | headline.row.n_days drives maturityTier $derived | WIRED | headline.row.n_days count=3; maturityTier $derived wired |
| RevenueForecastCard.svelte | computeChartWidth | width={chartW} on Chart | WIRED | computeChartWidth=3, width={chartW}=1 |
| InvoiceCountForecastCard.svelte | computeChartWidth | width={chartW} on Chart | WIRED | same pattern confirmed |
| 5 model fits | grain_helpers.window_start_for_grain | window_start= kwarg in pred_dates_for_grain call | WIRED | All 5 models: sarimax/naive_dow/ets/theta count=3 each; prophet count=3 (import + 2 call sites) |
| grain_helpers.pred_dates_for_grain | Past-window date emission | window_start prefix logic in function body | WIRED | D-15 Option B implementation confirmed; train_end B2 guard implemented |
| grain_helpers.pred_dates_for_grain | DEV forecast_with_actual_v | GHA workflow dispatch | NOT_YET_VERIFIED | Requires human execution of `gh workflow run forecast-refresh.yml --ref feature/phase-16-its-uplift-attribution` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| CalendarRevenueCard.svelte | `forecastData.actuals[]` (boundary) + `seriesByModel` (rows) | `/api/forecast` via `clientFetch` | Conditional: code renders correctly when rows exist; past rows depend on 16.1-04 Task 5 GHA dispatch | FLOWING (code-level) / REQUIRES_HUMAN_VERIFY (DEV data population) |
| CalendarCountsCard.svelte | Same pattern | `/api/forecast?kpi=invoice_count` | Same | Same |
| CampaignUpliftCard.svelte | `headline.row.n_days` (tier) + `ciOverlapsZero` (CI) | `/api/campaign-uplift` | Real data from campaign_uplift_v on DEV | FLOWING |
| RevenueForecastCard.svelte | `splitSeriesByModel` past/future split | `/api/forecast` via existing `forecastData` | Same as Calendar* — code correct; past rows need GHA dispatch | FLOWING (code-level) / REQUIRES_HUMAN_VERIFY (DEV past rows) |
| InvoiceCountForecastCard.svelte | Same | `/api/forecast?kpi=invoice_count` | Same | Same |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| grain_helpers D-14 math — 16 unit tests | `cd scripts/forecast && python -m pytest tests/test_grain_helpers.py -v` | 16 passed in 0.03s | PASS |
| Full Python forecast suite | `cd scripts/forecast && python -m pytest tests/` | 40 passed / 2 skipped (prophet not installed — pre-existing baseline) | PASS |
| TypeScript compile after Phase 16.1 | `npm run check 2>&1 \| tail -3` | `7 ERRORS 0 WARNINGS 3 FILES_WITH_PROBLEMS` — all 7 pre-existing (vite.config.ts, hooks.server.ts×5, CalendarRevenueCard.svelte×1 'w' possibly undefined); zero new | PASS |
| Production build | `npm run build` | Green after all 5 plans per SUMMARY reports | PASS (via SUMMARY) |
| planning-docs drift gate | `.claude/scripts/validate-planning-docs.sh` | `✅ planning docs in sync` | PASS |
| Component test regressions introduced by Phase 16.1 | `npm test 2>&1 \| grep 'FAIL  src/lib/components'` | Only InsightCard.test.ts (6 failures) — pre-dates Phase 16.1 (commit 55a4432 / 334e752 from 2026-04-15 to 2026-04-22); no Calendar*/CampaignUplift/Forecast card test failures | PASS |
| Chrome MCP 375×667 visual verification | Browser-based — requires Chrome MCP | Auto-approved per workflow.auto_advance=true; tools not in executor whitelist | SKIP (human required) |
| Supabase SQL past-row count | Requires GHA dispatch + DB MCP | 16.1-04 Task 5 explicitly deferred | SKIP (human required) |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| UPL-05 | 16.1-01, 16.1-04, 16.1-05 | naive_dow_uplift_eur cross-check in campaign_uplift_v (Phase 16 prior); Phase 16.1 adds window math that feeds forecast windowing | PARTIALLY SATISFIED | Phase 16.1 closes the forecast-windowing side; naive_dow_uplift_eur column itself was wired in Phase 16 Plan 16-07. Phase 16.1 does not add new SQL. REQUIREMENTS.md checkbox remains `[ ]` (not ticked by plan executor — documentation gap, not code gap) |
| UPL-06 | 16.1-03 | CampaignUpliftCard renders per-campaign cumulative uplift; "CI overlaps zero — no detectable lift" | SATISFIED | ciOverlapsZero $derived preserved; hero-ci-overlaps data-testid present; plain-language hero includes `uplift_hero_*_no_lift` / `uplift_hero_too_early` / `not_measurable` paths for CI-overlap cases. REQUIREMENTS.md checkbox remains `[ ]` (documentation gap) |
| FUI-09 | All 5 plans | All v1.3 frontend components verified at localhost:5173 via Chrome MCP BEFORE DEV deploy | PARTIALLY SATISFIED | Code complete; Chrome MCP visual gate deferred to human verification (workflow.auto_advance=true auto-approval). REQUIREMENTS.md checkbox remains `[ ]` |

**Note on REQUIREMENTS.md checkboxes:** UPL-05, UPL-06, and FUI-09 remain `[ ]` in `.planning/REQUIREMENTS.md` even though all 5 plan SUMMARY files declare `requirements-completed: [UPL-05, UPL-06, FUI-09]`. This is a documentation drift — the checkbox update was not performed by plan executors. The validator script does not check REQUIREMENTS.md checkboxes (only ROADMAP `[x]` marks and STATE counts). Recommend ticking the boxes before the phase ships.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| CampaignUpliftCard.svelte | 87 | `return null` when no campaigns | Info | Legitimate guard — not a stub; headline null triggers empty-state branch which still renders the subtitle (RESEARCH.md edge case handled) |
| CampaignUpliftCard.svelte | 180 | `return []` when no daily data | Info | Legitimate guard — sparklineData empty array is handled by the sparkline render guard; not a stub |

No blocker anti-patterns found. No TODO/FIXME/PLACEHOLDER strings in any modified file. No hardcoded empty data flowing to render paths.

### Human Verification Required

#### 1. Pipeline DEV Data Population (SC3 — criterion 3)

**Test:** Run `gh workflow run forecast-refresh.yml --ref feature/phase-16-its-uplift-attribution && gh run watch`

**Expected:** After the workflow completes (~4 min per 16.1-04 SUMMARY), run this SQL via Supabase DB MCP or Studio:
```sql
SELECT
  MIN(target_date) as min_date,
  MAX(target_date) as max_date,
  COUNT(DISTINCT target_date) as total_dates
FROM forecast_with_actual_v
WHERE granularity = 'day'
  AND model_name = 'sarimax'
  AND kpi = 'revenue_eur';
```
Expected: `min_date ≈ 2026-04-20`, `total_dates ≈ 387`, and explicitly check `SELECT COUNT(*) FROM forecast_with_actual_v WHERE target_date IN ('2026-04-28', '2026-04-29') AND granularity='day'` returns > 0 rows.

**Why human:** GHA dispatch requires authenticated gh CLI on the feature branch. Supabase SQL verification requires DB MCP. 16.1-04 Task 5 was explicitly deferred by orchestrator as a blocking human-verify checkpoint.

#### 2. Chrome MCP — Calendar* Cards at 375×667 in ja + en (SC8)

**Test:** Start dev server (`npm run dev`). Chrome MCP navigate to `localhost:5173` at 375×667. Verify:
- CalendarRevenueCard day grain: faded solid past-Spline visible from window_start to last_actual_date; dashed future-Spline forward; CI band continuous.
- Toggle week grain: 5 complete CWs of past-Spline; future forward.
- Toggle month grain: 4 complete months of past-Spline.
- Hover a bar where both bar data AND forecast row exist: tooltip shows visit-cohort rows (top) AND per-model rows with JA labels (bottom) separated by divider.
- Hover future-only bucket: bottom section only (no top).
- Hover bar-only bucket (outside window): top section only (no bottom).
- Console: zero `invalid_default_snippet` or LayerChart errors.
- Repeat all in locale en.

**Expected:** All above pass; `Visual verification (16.1-01): PASS`.

**Why human:** Chrome MCP (`mcp__claude-in-chrome__*`) not in executor tool whitelists (auto-advance=true applied). Past-Spline requires 16.1-04 Task 5 GHA dispatch to have populated DEV data first.

#### 3. Chrome MCP — CampaignUpliftCard + Forecast Cards at 375×667 in ja + en (SC8)

**Test:** Same localhost:5173 session. Verify:
- CampaignUpliftCard: hero string is plain-language and matches the campaign's n_days tier; D-18 subtitle above hero; Y label above sparkline; X caption + baseline chip below; "How is this calculated? ›" button toggles panel inline; locale ja shows `判断するにはまだ早すぎます` (or appropriate tier string), `累計売上影響額（€）`, `キャンペーン開始からの経過日数`.
- RevenueForecastCard + InvoiceCountForecastCard: horizontal scroll active; today lands at ~60% viewport; past-Spline faded solid; future-Spline dashed.
- Console: zero errors on all cards.

**Expected:** All above pass; `Visual verification (16.1-03/16.1-05): PASS`.

**Why human:** Same as above. CampaignUpliftCard hero tier depends on live `headline.row.n_days` from `/api/campaign-uplift` on DEV.

#### 4. Friend-Persona Acceptance Test (SC9)

**Test:** Friend-owner views each of the 4 cards on her phone (or emulated 375px). Ask: "What does this card tell you?" without prompting.

**Expected:** Owner can state: (Calendar*) "it shows me how much revenue is coming from repeat vs new customers, and the dotted line is the forecast"; (CampaignUpliftCard) "it's asking if my April campaign worked, and saying [tier-appropriate statement]"; (Forecast cards) "it shows the revenue forecast scrolling forward, and I can see what happened in the past few weeks too".

**Why human:** Subjective persona acceptance test — owner must provide verbal response. Cannot be verified programmatically.

### Gaps Summary

No automated-checkable criteria FAIL. The 3 items requiring human verification are:
- SC3 (pipeline DEV data): unit tests and wiring confirmed; GHA dispatch + SQL row-count is the final evidence step.
- SC8 (Chrome MCP visual gates): all code-level guards pass; visual rendering at 375×667 in ja+en cannot be confirmed without Chrome MCP execution.
- SC9 (owner acceptance): subjective test by definition.

**Documentation gap (WARNING, not BLOCKER):** REQUIREMENTS.md checkboxes for UPL-05, UPL-06, FUI-09 remain `[ ]` despite plans claiming `requirements-completed`. Recommend ticking these before the phase ships. The planning validator does not catch this — it only checks ROADMAP `[x]` marks.

---

_Verified: 2026-05-04T19:30:00Z_
_Verifier: Claude (gsd-verifier)_
