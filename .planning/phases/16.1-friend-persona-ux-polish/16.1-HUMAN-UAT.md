---
status: partial
phase: 16.1-friend-persona-ux-polish
source: [16.1-VERIFICATION.md]
started: 2026-05-04T22:00:00.000Z
updated: 2026-05-05T00:10:00.000Z
---

## Current Test

SC3 PASSED 2026-05-05; SC8 PASSED 2026-05-05 (with inline strokeOpacity fix `ec6cf65`); SC9 pending owner persona test.

## Tests

### 1. SC3 — Pipeline DEV data populated via manual GHA dispatch (16.1-04 Task 5) — **PASSED 2026-05-05**

**Result:** ✅ **PASSED.** GHA run `25346033979` completed in 4m34s on `feature/phase-16-its-uplift-attribution`. Supabase DEV verified:
- SARIMAX day-grain revenue_eur: `min_target=2026-04-20`, `max_target=2027-05-09`, `distinct_dates=385` ✓ (window_start anchor honored; D-15 Option B + B2 train_end fix both effective)
- Apr 28-29 gap-fill: 5 rows per date (sarimax + naive_dow + prophet + ets + theta), all non-NULL `yhat` ✓ (friend-owner's verbatim Apr 28-29 gripe closed)
- naive_dow span: identical to SARIMAX (`min=2026-04-20`, `max=2027-05-09`, 385 dates) ✓
- **Prophet Path A confirmed live** — past-yhat rows present alongside the other 4 simulate-style models (Risk 2 contingency resolved favorably, matching the local pytest decision)

---

**Original test definition (kept for traceability):**
expected: After running `gh workflow run forecast-refresh.yml --ref feature/phase-16-its-uplift-attribution` and waiting for completion (5-10 min), the following SQL queries against DEV Supabase return the expected row counts:

```sql
-- Day grain — SARIMAX revenue_eur should have ~387 distinct dates (was ~372 pre-D-15)
SELECT
  MIN(target_date) AS min_target,
  MAX(target_date) AS max_target,
  COUNT(DISTINCT target_date) AS distinct_dates
FROM forecast_with_actual_v
WHERE granularity = 'day'
  AND model_name = 'sarimax'
  AND kpi_name = 'revenue_eur';
```
- `min_target` ≈ `2026-04-20` (window_start when last_actual=Apr 27); was today+1 pre-D-15.
- `max_target` ≈ today + 372d.
- `distinct_dates` ≈ 386–387.

```sql
-- Apr 28-29 explicit gap-fill check — owner's verbatim friend-persona gripe
SELECT target_date, model_name, yhat
FROM forecast_with_actual_v
WHERE granularity = 'day'
  AND kpi_name = 'revenue_eur'
  AND target_date IN ('2026-04-28', '2026-04-29')
ORDER BY target_date, model_name;
```
- Expected: 5 rows per date (sarimax + naive_dow + prophet + ets + theta) with non-NULL `yhat` values.

```sql
-- Naive DoW cross-check — same span as SARIMAX (Path A confirmation)
SELECT MIN(target_date), MAX(target_date), COUNT(DISTINCT target_date)
FROM forecast_with_actual_v
WHERE granularity = 'day' AND model_name = 'naive_dow' AND kpi_name = 'revenue_eur';
```
- Expected: identical date span to SARIMAX above.

result: passed (2026-05-05 — see PASSED block above for SQL evidence)

### 2. SC8 — Chrome MCP localhost visual gates (consolidated from 16.1-01 Task 6 + 16.1-03 Task 4 + 16.1-05 Task 3) — **PASSED 2026-05-05** (with 1 inline fix)

**Result:** ✅ **PASSED.** Driven by orchestrator Chrome MCP at localhost:5173, viewport 375×667, both JA + EN locales.

**Inline fix landed during QA (commit `ec6cf65`):** Past-Spline elements rendered at full opacity instead of the designed `stroke-opacity={0.7}` faded treatment because layerchart's `Path.svelte` destructures camelCase `strokeOpacity`/`strokeWidth` from props and silently overrides any kebab-case spread from `restProps`. Renamed `stroke-opacity={0.7}` → `strokeOpacity={0.7}` and `stroke-width={...}` → `strokeWidth={...}` in the past/future Spline blocks of all 4 cards. Pre-existing `fillOpacity={0.06}` (CI band) had used the camelCase convention all along — 16.1-01 / 16.1-05 inadvertently mixed kebab-case for stroke props only.

**Post-fix verification (DOM + screenshot evidence):**
- **CalendarRevenueCard + CalendarCountsCard** (16.1-01): 1 fadedSolid past-Spline (sarimax, stroke-opacity=0.7) + 3 dashed future-Splines per card; 2 CI band Areas (fill-opacity=0.06) per card; chartXDomain widened LEFT to forecastWindowStart (window_start visible at chart leftmost). Visual: scrolled to "May 1" axis; bars + faded past-Spline + dashed future-Spline + continuous light-blue CI band all overlaid as designed.
- **RevenueForecastCard + InvoiceCountForecastCard** (16.1-05): 1 fadedSolid past-Spline + 3 dashed future-Splines per card; horizontal-scroll wrapper engaged (scrollWidth=10828px, clientWidth=434px); past+future visually distinct (Apr 21 - Apr 27 SOLID faded for past, Apr 28+ DASHED for future). Visual screenshot: actuals overlay also visible. Scroll-to-today RAF observed to land near today at first paint.
- **CampaignUpliftCard** (16.1-03):
  - JA hero: `[data-testid="hero-ci-overlaps"]` renders "まだはっきりとした効果は見えていません" (one of 7 D-06 plain-language strings; matches early-tier × CI-overlap matrix).
  - EN hero: same testid renders "Probably not measurable yet".
  - 4 D-18 supportive labels all present in both locales: subtitle "Comparing your actual revenue since launch against what the model predicted without the campaign." / "キャンペーン開始後の実際の売上と…"; Y label "Cumulative revenue impact (€)" / "累計売上影響額（€）"; X caption "Days since campaign launch" / "キャンペーン開始からの経過日数"; baseline chip "Dashed line = no campaign baseline" / "点線 = キャンペーンなしの基準".
  - Disclosure trigger expand+collapse end-to-end: aria-expanded false→true on click; chevron `›`→`⌄`; panel renders 3 sections: dim-point-estimate ("−€565 (95% CI −€3.745 … +€2.298)"), plain anticipation note (full sentence in both locales), divergence-warning in amber (plain copy: "Two of our checks disagree…" / "2つのチェック方法で結果が違いました…").
  - Plain-language secondary line: "Best estimate: ~−€565 compared to expected. Range: −€3.745 to +€2.298 — that's normal day-to-day noise." / "目安: 予想と比べて−€565ぐらい。範囲…" — point estimate ALWAYS paired with CI bounds (UPL-06 invariant satisfied).
- **Cross-cutting (D — Console + Locale)**:
  - Hard-reload console error sweep matching `error|warn|invalid_default_snippet|layerchart|derived_inert`: ZERO matches.
  - HMR-only `derived_inert` warnings observed during edit cycle but disappeared after hard reload (dev-mode artifact).
  - Locale switch en→ja and ja→en both seamlessly re-render all D-18 labels with real translations (not placeholders).
  - DE/ES/FR placeholders verified in source (= EN verbatim) per backlog stub.

**One known minor cosmetic leftover deferred:** Lines `RevenueForecastCard.svelte:274` + `InvoiceCountForecastCard.svelte:274` still pass `stroke-width={2}` (kebab-case) to the actuals-overlay Spline — actuals line will render at default width=1 instead of intended 2. Subtle; not blocking past-Spline visibility. Tracked for follow-up commit if owner notices.

**Empty-state branch coverage:** Verified subtitle renders even when headline=null (CONTEXT.md edge case honored — empty-state shell does not read as orphaned).

---

**Original test definition (kept for traceability):**
expected: Open `npm run dev` (localhost:5173) and verify in Chrome MCP at viewport 375×667 in BOTH `ja` and `en` locales:

**A. CalendarRevenueCard + CalendarCountsCard (16.1-01)**
- Past-forecast Spline lines render with `stroke-opacity={0.7}` (faded) covering Apr 20-27 range when last_actual=Apr 27.
- Future-forecast Spline lines render with `stroke-dasharray='4 4'` (dashed) from today+1 forward.
- CI Area band continuous (no break at today line) at `fillOpacity={0.06}`.
- Chart canvas widens to accommodate `pastForecastBuckets + bars + futureForecastBuckets` slots; horizontal scroll engages on viewports < required width.
- Scroll-to-today RAF lands today bucket at ~60% viewport on first paint.
- D-16 Tooltip.Root shows: top visit-cohort rows (existing) + bottom per-visible-model section (new) with i18n model labels (`forecast_model_sarimax`, `forecast_model_naive_dow`, etc.); revenue card uses `formatEUR(yhat_mean*100)`, counts card uses `formatIntShort(yhat_mean)`.

**B. CampaignUpliftCard (16.1-03)**
- Hero copy is one of 7 plain-language strings depending on `n_days` tier × CI overlap (e.g., "Too early to tell" for n_days<14; "Looks the same as expected weeks" for mature + ciOverlap; etc.).
- D-18 hero subtitle visible BELOW the title in BOTH empty-state branch AND known-state branch.
- Sparkline supportive labels: Y label `<p>` ABOVE Chart (W4 LOCKED placement); X caption `<p>` BELOW Chart; baseline legend chip with dashed `<span>` swatch BELOW X caption.
- "How is this calculated? ›" disclosure trigger present, aria-expanded=false initially. Click → chevron flips to `⌄`, aria-expanded=true, panel expands inline (no animation per KISS) with 3 sections: verbatim statistical line / plain anticipation note / (conditional) plain divergence note in amber.

**C. RevenueForecastCard + InvoiceCountForecastCard (16.1-05)**
- Horizontal-scroll wrapper engages on narrow viewports (`overflow-x-auto`).
- Past-Spline (faded solid `stroke-opacity={0.7}`) + future-Spline (dashed `'4 4'`) per visible model.
- `curveMonotoneX` preserved on both branches.
- Scroll-to-today lands today at ~60% viewport.
- C-03 invariant: two-finger trackpad scroll on the card scrolls the page vertically (NOT trapped horizontally).

**D. Cross-cutting (all 4 cards)**
- Console-clean: zero `invalid_default_snippet` warnings; zero LayerChart errors; zero Svelte 5 runtime warnings.
- Locale switch en → ja shows real JA strings (e.g., `累計売上影響額`, `最近の傾向に基づくと…`) — NOT placeholder English text.
- Empty-state branch (when `headline=null` or `daily=[]`): subtitle still renders, no orphan blank shell.
- DE/ES/FR locales render placeholder text (= EN verbatim) gracefully — backlog item `i18n-campaign-uplift-card-de-es-fr.md` tracks the v1.4 translation work.

**Prerequisite:** Test #1 (GHA dispatch) MUST complete first so past-window forecast rows actually exist in DEV — otherwise the past-Spline branches will render empty.

result: passed (2026-05-05 — see PASSED block above for full evidence; one minor cosmetic actuals-overlay stroke-width leftover deferred)

### 3. SC9 — Friend-persona acceptance test
expected: The friend-owner views all 4 modified cards (CalendarRevenueCard, CalendarCountsCard, CampaignUpliftCard, RevenueForecastCard, InvoiceCountForecastCard — 5 cards counting both Forecast variants) at her phone (375×667) in `ja` locale and **states in her own words what each card is telling her** without asking for translation or jargon clarification.

Specific persona acceptance signals:
- She does NOT ask "what's CI?" or "what does 'overlaps zero' mean?" — the plain-language hero on CampaignUpliftCard hides that jargon by default.
- She does NOT ask "where's Apr 28-29?" — the past-forecast Splines visually fill the gap on Calendar* cards.
- She CAN scroll horizontally on Forecast cards without the page bouncing (C-03 mobile scroll fix preserved).
- She optionally clicks "How is this calculated? ›" if curious — the disclosure panel reveals the statistical detail without dominating the default read.

result: [pending]

## Summary

total: 3
passed: 2
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
