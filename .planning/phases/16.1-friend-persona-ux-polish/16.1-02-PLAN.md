---
phase: 16.1-friend-persona-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n/messages.ts
  - .planning/backlog/i18n-campaign-uplift-card-de-es-fr.md
autonomous: true
requirements: [UPL-06, FUI-09]
must_haves:
  truths:
    - "messages.ts gains 11 new keys covering: 7 hero strings (D-06 tier × CI matrix), 1 plain-language secondary-line template (D-08), 1 disclosure trigger label (D-09), 2 disclosure panel labels (D-10 anticipation + divergence), 1 'Did the {date} campaign work?' headline template, and 1 'Counterfactual is computing…' empty-state body — total 13 NEW keys"
    - "Every new key exists in ALL 5 locale blocks (en, de, ja, es, fr) — TypeScript compile-time enforcement via `Record<MessageKey, string>` (messages.ts:204, 384, 563, 743)"
    - "Japanese (ja) translations are real natural translations (per CONTEXT.md owner persona); de/es/fr translations duplicate the EN string verbatim as placeholders, with a one-line v1.4 backlog note filed for proper translation"
    - "Headline template uses {date} placeholder and the no-lift mature-tier template uses {weeks} placeholder — both compatible with the existing interpolate() helper at messages.ts:924-928 (regex /\\{(\\w+)\\}/g)"
    - "EN block keeps `as const` assertion (messages.ts:201) — `MessageKey = keyof typeof en` widens correctly to include the new keys"
    - "A new section divider comment marks the new keys as belonging to Phase 16.1 (D-06 / D-08 / D-09)"
  artifacts:
    - path: "src/lib/i18n/messages.ts"
      provides: "13 new i18n keys covering CampaignUpliftCard plain-language regime"
      contains: "uplift_hero_too_early"
      contains_2: "uplift_details_trigger"
  key_links:
    - from: "src/lib/i18n/messages.ts"
      to: "src/lib/components/CampaignUpliftCard.svelte (consumed in Plan 16.1-03)"
      via: "t(page.data.locale, 'uplift_hero_*' / 'uplift_details_*' / 'uplift_campaign_*')"
      pattern: "uplift_hero_|uplift_details_|uplift_campaign_"
---

<objective>
Append 13 new i18n keys to `src/lib/i18n/messages.ts` across all 5 locale blocks (en, de, ja, es, fr) covering the CampaignUpliftCard plain-language rewrite (UPL-06) consumed by Plan 16.1-03.

**Why this plan lands first (Wave 1, parallel to Plan 01):** TypeScript enforces `Record<MessageKey, string>` on every non-EN locale (messages.ts:204+); a card calling `t(locale, 'new_key')` against a missing key would compile-fail. Landing the keys first means Plan 16.1-03 can reference them on first write without juggling temporary fallbacks.

**Owner persona constraint:** ja gets a real, natural translation matching the friend-owner's reading level (non-statistical, banking-domain not stats-domain). de/es/fr get EN-string placeholders + a v1.4 backlog item — the friend only verifies ja + en per CONTEXT.md C-05 / ROADMAP SC#6.

Purpose: Foundation for Plan 16.1-03's plain-language rewrite. Self-contained; no UI verification at this stage (keys are not yet consumed).

Output: One modified .ts file. No runtime behavior change until Plan 16.1-03 wires the keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16.1-friend-persona-ux-polish/16.1-CONTEXT.md
@.planning/phases/16.1-friend-persona-ux-polish/16.1-PATTERNS.md
@.planning/backlog/campaign-uplift-card-plain-language.md
@CLAUDE.md
@.claude/CLAUDE.md
@src/lib/i18n/messages.ts

<interfaces>
<!-- Existing structure (verbatim from messages.ts). Append to each locale block; do NOT change existing keys. -->

```ts
// messages.ts:13 — EN block (canonical; `as const` makes it the typing source)
const en = {
  // ... ~190 existing keys ...
  repeater_cohort_description: '…'
} as const;

// messages.ts:201
export type MessageKey = keyof typeof en;

// messages.ts:204, 384, 563, 743 — non-EN locales typed `Record<MessageKey, string>`
const de: Record<MessageKey, string> = { /* every key from en */ };
const ja: Record<MessageKey, string> = { /* every key from en */ };
const es: Record<MessageKey, string> = { /* every key from en */ };
const fr: Record<MessageKey, string> = { /* every key from en */ };

// messages.ts:922
export const messages: Record<Locale, Record<MessageKey, string>> = { en, de, ja, es, fr };

// messages.ts:924-928 — interpolation helper (regex /\{(\w+)\}/g — single braces)
function interpolate(template: string, vars?: Record<string, string | number>): string {
  if (!vars) return template;
  return template.replace(/\{(\w+)\}/g, (_, k) =>
    k in vars ? String(vars[k]) : `{${k}}`
  );
}
```

**The 13 NEW keys (planner-locked, drawn from CONTEXT.md D-06 / D-08 / D-09 / D-10 + PATTERNS.md):**

EN (canonical strings — copy verbatim into the EN block, with section divider comment):

```ts
// --- Campaign uplift card (Phase 16.1: D-06 plain-language hero / D-08 secondary line / D-09 disclosure / D-10 detail panel) ---

// Empty/computing state (replaces the hardcoded line at CampaignUpliftCard:158-163)
uplift_card_title_with_date: 'Did the {date} campaign work?',
uplift_card_computing: 'We\'re still calculating — first result lands tomorrow morning.',

// 7 hero strings (D-06 tier × CI matrix — see CONTEXT.md table)
uplift_hero_too_early: 'Too early to tell',
uplift_hero_early_not_measurable: 'Probably not measurable yet',
uplift_hero_early_added: 'Looks like the campaign added revenue, but more weeks of data would confirm',
uplift_hero_early_reduced: 'Looks like the campaign reduced revenue, but more weeks of data would confirm',
uplift_hero_mature_no_lift: 'No measurable lift after {weeks} weeks',
uplift_hero_mature_added: 'Yes, your campaign appears to have added revenue',
uplift_hero_mature_reduced: 'Yes, your campaign appears to have reduced revenue',

// 1 plain-language secondary line (D-08; numbers come from formatEur, see Plan 03)
uplift_secondary_plain: 'Best estimate: ~{point} compared to expected. Range: {lo} to {hi} — that\'s normal day-to-day noise.',

// 1 disclosure trigger label (D-09)
uplift_details_trigger: 'How is this calculated?',

// 2 disclosure panel labels (D-10 sections 2 + 3; section 1 is reusable from formatEur output)
uplift_details_anticipation_plain: 'We compare your actual revenue against what the model predicted from data 7+ days before the campaign launched, so any pre-launch anticipation isn\'t counted as campaign uplift.',
uplift_details_divergence_plain: 'Two of our checks disagree — we\'d want more weeks of data before drawing conclusions.',
```

JA (natural Japanese — owner-persona register, banking-domain not stats-jargon):

```ts
// --- Campaign uplift card (Phase 16.1) ---
uplift_card_title_with_date: '{date}のキャンペーンは効果ありましたか？',
uplift_card_computing: '計算中です — 最初の結果は明日の朝に出ます。',
uplift_hero_too_early: '判断するにはまだ早すぎます',
uplift_hero_early_not_measurable: 'まだはっきりとした効果は見えていません',
uplift_hero_early_added: '売上が伸びたように見えますが、もう少しデータが必要です',
uplift_hero_early_reduced: '売上が下がったように見えますが、もう少しデータが必要です',
uplift_hero_mature_no_lift: '{weeks}週間経っても、はっきりとした効果は出ていません',
uplift_hero_mature_added: 'はい、キャンペーンで売上が伸びたようです',
uplift_hero_mature_reduced: 'はい、キャンペーンで売上が下がったようです',
uplift_secondary_plain: '目安: 予想と比べて{point}ぐらい。範囲: {lo} 〜 {hi} — これくらいの差は普段の変動の範囲内です。',
uplift_details_trigger: 'どうやって計算しているの？',
uplift_details_anticipation_plain: 'キャンペーン開始の7日以上前のデータでモデルが予測した売上と、実際の売上を比べています。開始前の期待感による売上の動きはキャンペーンの効果には含めません。',
uplift_details_divergence_plain: '2つのチェック方法で結果が違いました — 結論を出す前にもう少しデータを集めたいところです。',
```

DE / ES / FR (placeholder = EN string verbatim per Claude's discretion + CONTEXT.md note that owner only verifies ja + en):

Use the EXACT EN string for each of the 13 keys in each of the 3 locale blocks. This satisfies `Record<MessageKey, string>` typing without committing to a translation that hasn't been reviewed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append 13 new keys to all 5 locale blocks in messages.ts</name>
  <files>src/lib/i18n/messages.ts</files>
  <read_first>
    - src/lib/i18n/messages.ts (full file — confirm structure: en `as const` at line 13-199, MessageKey type at 201, de at 204, ja at 384, es at 563, fr at 743, messages export at 922, interpolate helper at 924-928)
    - .planning/phases/16.1-friend-persona-ux-polish/16.1-CONTEXT.md D-06 (the tier × CI matrix table — source of truth for hero strings)
    - .planning/phases/16.1-friend-persona-ux-polish/16.1-CONTEXT.md D-08, D-09, D-10
    - .planning/phases/16.1-friend-persona-ux-polish/16.1-PATTERNS.md (messages.ts pattern section)
    - .planning/backlog/campaign-uplift-card-plain-language.md (side-by-side current↔plain-language sketch — verify the EN strings above match the backlog's plain-language column)
  </read_first>
  <behavior>
    - Test 1: `grep -c "uplift_hero_too_early" src/lib/i18n/messages.ts` returns 5 (one per locale block).
    - Test 2: `grep -c "uplift_details_trigger" src/lib/i18n/messages.ts` returns 5.
    - Test 3: `npm run check` passes — TypeScript does NOT report missing keys in any non-EN locale.
    - Test 4: `npm run build` succeeds.
    - Test 5: `t(locale, 'uplift_hero_mature_no_lift', { weeks: 4 })` interpolates correctly to "No measurable lift after 4 weeks" (en) / "4週間経っても、はっきりとした効果は出ていません" (ja). Verified later in Plan 16.1-03; here we only confirm the placeholder syntax matches `\{(\w+)\}` (single braces, e.g. `{weeks}` not `{{weeks}}`).
  </behavior>
  <action>
    1. Open `src/lib/i18n/messages.ts`. Locate the END of the EN block (just before `} as const;` at line 199 area).

    2. Insert this section divider + 13 keys IMMEDIATELY before the `} as const;` closer of the EN block. Use a trailing comma on the last key (the existing keys do this; preserve the style):

       ```ts
         // --- Campaign uplift card (Phase 16.1: D-06 plain-language hero / D-08 secondary line / D-09 disclosure / D-10 detail panel) ---
         uplift_card_title_with_date: 'Did the {date} campaign work?',
         uplift_card_computing: 'We\'re still calculating — first result lands tomorrow morning.',
         uplift_hero_too_early: 'Too early to tell',
         uplift_hero_early_not_measurable: 'Probably not measurable yet',
         uplift_hero_early_added: 'Looks like the campaign added revenue, but more weeks of data would confirm',
         uplift_hero_early_reduced: 'Looks like the campaign reduced revenue, but more weeks of data would confirm',
         uplift_hero_mature_no_lift: 'No measurable lift after {weeks} weeks',
         uplift_hero_mature_added: 'Yes, your campaign appears to have added revenue',
         uplift_hero_mature_reduced: 'Yes, your campaign appears to have reduced revenue',
         uplift_secondary_plain: 'Best estimate: ~{point} compared to expected. Range: {lo} to {hi} — that\'s normal day-to-day noise.',
         uplift_details_trigger: 'How is this calculated?',
         uplift_details_anticipation_plain: 'We compare your actual revenue against what the model predicted from data 7+ days before the campaign launched, so any pre-launch anticipation isn\'t counted as campaign uplift.',
         uplift_details_divergence_plain: 'Two of our checks disagree — we\'d want more weeks of data before drawing conclusions.',
       ```

       Confirm the existing trailing-comma + closer pattern is preserved (`} as const;` immediately follows). DO NOT remove `as const` — it is load-bearing for `MessageKey = keyof typeof en`.

    3. Locate the END of the JA block (just before its closing `};` at line ~563 area). Insert:

       ```ts
         // --- Campaign uplift card (Phase 16.1) ---
         uplift_card_title_with_date: '{date}のキャンペーンは効果ありましたか？',
         uplift_card_computing: '計算中です — 最初の結果は明日の朝に出ます。',
         uplift_hero_too_early: '判断するにはまだ早すぎます',
         uplift_hero_early_not_measurable: 'まだはっきりとした効果は見えていません',
         uplift_hero_early_added: '売上が伸びたように見えますが、もう少しデータが必要です',
         uplift_hero_early_reduced: '売上が下がったように見えますが、もう少しデータが必要です',
         uplift_hero_mature_no_lift: '{weeks}週間経っても、はっきりとした効果は出ていません',
         uplift_hero_mature_added: 'はい、キャンペーンで売上が伸びたようです',
         uplift_hero_mature_reduced: 'はい、キャンペーンで売上が下がったようです',
         uplift_secondary_plain: '目安: 予想と比べて{point}ぐらい。範囲: {lo} 〜 {hi} — これくらいの差は普段の変動の範囲内です。',
         uplift_details_trigger: 'どうやって計算しているの？',
         uplift_details_anticipation_plain: 'キャンペーン開始の7日以上前のデータでモデルが予測した売上と、実際の売上を比べています。開始前の期待感による売上の動きはキャンペーンの効果には含めません。',
         uplift_details_divergence_plain: '2つのチェック方法で結果が違いました — 結論を出す前にもう少しデータを集めたいところです。',
       ```

    4. Locate the END of the DE block (line ~384 area, just before its closing `};`). Insert the 13 keys with the EXACT SAME EN strings (placeholder copy — duplicate the EN strings verbatim). Add a comment header:

       ```ts
         // --- Campaign uplift card (Phase 16.1) — DE placeholder = EN; v1.4 polish backlog item filed ---
         uplift_card_title_with_date: 'Did the {date} campaign work?',
         uplift_card_computing: 'We\'re still calculating — first result lands tomorrow morning.',
         uplift_hero_too_early: 'Too early to tell',
         // ... (all 13 keys with EN values — exact copy)
       ```

    5. Repeat step 4 for ES (line ~563 area) and FR (line ~743 area). Same comment header pattern, EN-string placeholder values.

    6. **Sanity grep:** confirm each of the 13 new keys appears exactly 5 times across the file:
       ```bash
       for key in uplift_card_title_with_date uplift_card_computing uplift_hero_too_early uplift_hero_early_not_measurable uplift_hero_early_added uplift_hero_early_reduced uplift_hero_mature_no_lift uplift_hero_mature_added uplift_hero_mature_reduced uplift_secondary_plain uplift_details_trigger uplift_details_anticipation_plain uplift_details_divergence_plain; do
         count=$(grep -c "^[[:space:]]*${key}:" src/lib/i18n/messages.ts)
         echo "$key: $count"
       done
       ```
       Each must report 5. If any reports < 5, you missed a locale; if > 5, you accidentally duplicated.

    7. **DO NOT modify** any existing key, the EN block's `as const`, or the `messages` export at line 922.

    8. **File a v1.4 backlog item** for the de/es/fr translation work. Append (or create if absent) `.planning/backlog/i18n-campaign-uplift-card-de-es-fr.md` with:
       ```markdown
       # Translate Phase 16.1 CampaignUpliftCard keys (de / es / fr)

       **Owner:** TBD
       **Source:** Phase 16.1 plan 02 — 13 new keys landed with EN placeholders in de/es/fr blocks per CONTEXT.md "owner only verifies ja + en" decision.
       **Scope:** Translate the 13 `uplift_*` keys in `src/lib/i18n/messages.ts` for the de, es, fr locale blocks. EN canonical and JA reference are already in the file.
       **Effort:** ~30 min × 3 locales = ~1.5 h.
       ```
  </action>
  <verify>
    <automated>cd /Users/shiniguchi/development/ramen-bones-analytics &amp;&amp; npm run check 2>&amp;1 | tail -20 &amp;&amp; for key in uplift_card_title_with_date uplift_card_computing uplift_hero_too_early uplift_hero_early_not_measurable uplift_hero_early_added uplift_hero_early_reduced uplift_hero_mature_no_lift uplift_hero_mature_added uplift_hero_mature_reduced uplift_secondary_plain uplift_details_trigger uplift_details_anticipation_plain uplift_details_divergence_plain; do count=$(grep -c "^[[:space:]]*${key}:" src/lib/i18n/messages.ts); echo "$key: $count"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 13 keys appear exactly 5 times each in `src/lib/i18n/messages.ts` (one occurrence per locale block; verified by the grep loop in `<verify>`)
    - File still contains `} as const;` for the EN block (regression guard for `MessageKey = keyof typeof en`)
    - File still contains the type declarations: `Record<MessageKey, string>` count ≥ 4 (de, ja, es, fr — grep returns ≥ 4)
    - File still contains `export const messages: Record<Locale, Record<MessageKey, string>> = { en, de, ja, es, fr }` (regression guard for the export)
    - `npm run check` reports zero new TypeScript errors related to messages.ts (in particular, NO `Property 'uplift_*' is missing in type Record<MessageKey, string>` errors)
    - `npm run build` succeeds
    - Backlog file `.planning/backlog/i18n-campaign-uplift-card-de-es-fr.md` exists
    - Each new EN string uses single-brace placeholder syntax (e.g. `{date}`, `{weeks}`, `{point}`, `{lo}`, `{hi}`) — no `{{double}}` braces (regex compat with messages.ts:927)
    - JA strings are non-empty and visibly Japanese (grep confirms presence of CJK characters: `grep -c "効果ありましたか" src/lib/i18n/messages.ts` returns 1)
  </acceptance_criteria>
  <done>
    13 new i18n keys land in all 5 locale blocks. svelte-check + build green. ja translations are real; de/es/fr are EN placeholders with v1.4 backlog item filed. Keys are now consumable by Plan 16.1-03.
  </done>
</task>

</tasks>

<verification_criteria>

**Goal-backward verification (this plan):**

1. **Truth: every new key exists in all 5 locales** — grep loop confirms 5/5 for each of the 13 keys. ✓ Task 1 acceptance criteria.

2. **Truth: TypeScript compile is clean** — `Record<MessageKey, string>` enforces completeness; `npm run check` passes. ✓ Task 1 verify command.

3. **Truth: JA is real, not placeholder** — visual / CJK character grep confirms genuine translation. ✓ Task 1 acceptance criteria.

4. **Truth: placeholder syntax is interpolate-compatible** — single-brace `{name}` matches the existing regex at messages.ts:927. ✓ Task 1 acceptance criteria.

5. **Truth: v1.4 translation backlog item exists** — `.planning/backlog/i18n-campaign-uplift-card-de-es-fr.md` filed. ✓ Task 1 action step 8.

</verification_criteria>

<success_criteria>

This plan is complete when:

- [ ] 13 new keys appended to all 5 locale blocks in `messages.ts`
- [ ] svelte-check + build green
- [ ] Backlog item for de/es/fr translation filed
- [ ] All acceptance criteria pass
- [ ] No existing keys modified

This plan is foundation-only: no UI verification at this stage (keys aren't consumed yet). Plan 16.1-03 wires them and runs the Chrome MCP gate.

</success_criteria>

<output>
After completion, create `.planning/phases/16.1-friend-persona-ux-polish/16.1-02-SUMMARY.md` recording:
- Diff stats (line additions per locale block)
- Grep-loop output confirming 5/5 for every new key
- svelte-check + build clean output
- Path to the backlog item filed
</output>
