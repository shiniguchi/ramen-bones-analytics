---
phase: 16.3-dashboard-cleanup-events-everywhere
plan: 02
subsystem: ui
tags: [svelte, typescript, events, palette, design-tokens]

# Dependency graph
requires:
  - phase: 16.3-01
    provides: 3 forecast components deleted; +page.svelte slots removed; build green
  - phase: 15-forecast-backtest-overlay
    provides: EventMarker.svelte source-of-truth palette + forecastEventClamp.ts EVENT_PRIORITY
provides:
  - "src/lib/eventTypeColors.ts: single typed source of truth for the 5 event-type → hex color values"
  - "Re-export of EVENT_PRIORITY from forecastEventClamp.ts so EventBadgeStrip can do single-import"
  - "Unblocks Plan 16.3-03 (popup CSS util / ChartHoverPopup) and Plan 16.3-04+ (EventBadgeStrip + per-card wiring)"
  - "Deletion path for EventMarker.svelte in Plan 16.3-07 (no longer the only place the palette lives)"
affects: [16.3-03, 16.3-04, 16.3-05, 16.3-06, 16.3-07, 16.3-08, EventBadgeStrip, ChartHoverPopup]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Design-token module pattern: hex literals live in $lib/eventTypeColors.ts only — components import the constant, never inline hex"
    - "Type-only import + value re-export for ergonomic single-import (EventType type + EVENT_PRIORITY value from one source module)"

key-files:
  created:
    - "src/lib/eventTypeColors.ts (24 lines, 1 typed Record + 1 re-export)"
  modified: []

key-decisions:
  - "Hex values lifted byte-for-byte from EventMarker.svelte:60-113 (campaign_start=#dc2626, holiday=#16a34a, recurring_event=#eab308, transit_strike=#dc2626) plus school_holiday=#5eead4 from EventMarker.svelte:51"
  - "EVENT_PRIORITY re-exported from same module per CONTEXT.md D-04 step 2 + plan must_have C-13 — keeps EventBadgeStrip consumer to one import path"
  - "EventType imported as type-only from $lib/forecastEventClamp — no duplicate type declaration, type and runtime palette stay in lockstep"
  - "EventMarker.svelte preserved on disk (deletion deferred to Plan 16.3-07 after all 5 cards swap to EventBadgeStrip)"
  - "forecastEventClamp.ts unchanged (read-only this plan)"

patterns-established:
  - "Single source of truth for palette: any future event-type color change happens in one file"
  - "Re-export ergonomics pattern: a sibling module can re-export a constant to give consumers a single import surface without owning the data"

requirements-completed: [UPL-05]

# Metrics
duration: 2min
completed: 2026-05-06
---

# Phase 16.3 Plan 02: Event-Type Color Palette Module Summary

**`src/lib/eventTypeColors.ts` ships the 5 event-type → hex palette and re-exports EVENT_PRIORITY from forecastEventClamp.ts — single typed import for downstream EventBadgeStrip and beyond.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-06T08:33:35Z
- **Completed:** 2026-05-06T08:34:49Z
- **Tasks:** 1
- **Files modified:** 0 (pure addition)
- **Files created:** 1

## Accomplishments

- Created `src/lib/eventTypeColors.ts` (24 lines) exporting `EVENT_TYPE_COLORS: Record<EventType, string>` with all 5 keys
- Hex values match EventMarker.svelte source-of-truth byte-for-byte
- Re-exported `EVENT_PRIORITY` from `forecastEventClamp.ts` so consumers do one import for both palette + priority order
- `EventType` imported as `type` only — no runtime type duplication
- Verified `EventMarker.svelte` and `forecastEventClamp.ts` untouched
- `npm run check` baseline preserved: 7 pre-existing errors (vite.config.ts, hooks.server.ts, CalendarRevenueCard.svelte) — zero new errors introduced

## Task Commits

1. **Task 1: Create eventTypeColors.ts with palette + priority re-export** — `3162b85` (feat)

**Plan metadata:** _this commit_ (docs: complete plan)

## Files Created

| Path                        | Purpose                                                                                                              |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `src/lib/eventTypeColors.ts` | Single source of truth: 5-key `EVENT_TYPE_COLORS` palette + re-exported `EVENT_PRIORITY` from `forecastEventClamp.ts` |

### Final hex values

| Event type        | Hex     | Source of truth                  |
| ----------------- | ------- | -------------------------------- |
| `campaign_start`  | #dc2626 | EventMarker.svelte:67 (red line) |
| `transit_strike`  | #dc2626 | EventMarker.svelte:108 (red top bar) |
| `school_holiday`  | #5eead4 | EventMarker.svelte:51 (teal bg)  |
| `holiday`         | #16a34a | EventMarker.svelte:78 (green dashed line) |
| `recurring_event` | #eab308 | EventMarker.svelte:90 (yellow line) |

### Confirmation: source files untouched

- `src/lib/components/EventMarker.svelte` STILL EXISTS on disk (deletion deferred to Plan 16.3-07).
- `src/lib/forecastEventClamp.ts` UNCHANGED (read-only this plan).

### Pointers

- **Plan 16.3-03** (popup CSS util / ChartHoverPopup): consumes nothing from this module directly, but `EventBadgeStrip` will use both popups + palette together — keeping them on adjacent waves makes integration clean.
- **Plan 16.3-07** (per-card consumer wiring + `EventMarker.svelte` deletion): once all 5 calendar cards import the palette via `EventBadgeStrip`, this module becomes the only place the palette exists and `EventMarker.svelte` can be removed.

## Decisions Made

None new — followed CONTEXT.md D-04 (LOCKED 2026-05-06) and the plan's `<action>` block verbatim. Both decisions in scope for this plan (palette extraction + EVENT_PRIORITY re-export) were already locked upstream.

## Deviations from Plan

None — plan executed exactly as written.

The plan declared `tdd="true"` but its `<action>` and `<verify>` blocks specify a constants-only module verified via grep + `npm run check` (no separate test file). Followed the plan's explicit verification surface; no separate `.test.ts` file was added because the module exposes a single typed literal and a re-export, both of which would duplicate the file content if asserted via a test.

**Total deviations:** 0
**Impact on plan:** None.

## Issues Encountered

- `npm run check` reports 7 errors. All 7 are pre-existing on `HEAD~1` (verified by `git stash` baseline run): `vite.config.ts:7` (vitest config typing), `hooks.server.ts:23-38` (Supabase SSR cookie handler types), `CalendarRevenueCard.svelte:195` (`'w' possibly undefined`). My change adds 1 new file (`eventTypeColors.ts`) and 0 new errors. Per the orchestrator's plan-specific note, pre-existing errors unrelated to this plan are acceptable. Logged for downstream awareness — not actioned (out of scope).

## Threat Surface

T-16.3-02-01 (palette drift between EventMarker.svelte and eventTypeColors.ts) mitigated by:
1. Hex values lifted verbatim with file:line citations in code comments
2. Plan 16.3-07 will delete EventMarker.svelte once all consumers use the new module — collapses the two-source surface to one

No new trust boundaries introduced (pure module addition; no auth, no DB, no input). ASVS L1 unchanged.

## User Setup Required

None — no external service configuration.

## Next Phase Readiness

- **Plan 16.3-03 (popup CSS util):** unblocked
- **Plan 16.3-04 (EventBadgeStrip component):** unblocked — can `import { EVENT_TYPE_COLORS, EVENT_PRIORITY } from '$lib/eventTypeColors'`
- **Plan 16.3-07 (per-card wiring + EventMarker deletion):** palette no longer load-bearing in EventMarker.svelte; deletion safe once consumers swap

## Self-Check: PASSED

- [x] FOUND: `src/lib/eventTypeColors.ts`
- [x] FOUND commit: `3162b85`
- [x] FOUND: `src/lib/components/EventMarker.svelte` (preserved, per plan)
- [x] FOUND unchanged: `src/lib/forecastEventClamp.ts`
- [x] All 5 hex/key pairs present (grep composite count = 5)
- [x] `EVENT_PRIORITY` re-export present
- [x] `EventType` imported as type-only

---
*Phase: 16.3-dashboard-cleanup-events-everywhere*
*Completed: 2026-05-06*
