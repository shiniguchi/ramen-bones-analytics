---
phase: 16.3-dashboard-cleanup-events-everywhere
plan: 04
type: execute
wave: 2
depends_on: [16.3-01]
autonomous: true
requirements: [FUI-09]
files_modified:
  - src/lib/i18n/messages.ts
  - .planning/backlog/i18n-event-strip-de-es-fr.md
must_haves:
  truths:
    - "messages.ts gains 8 new keys × 5 locales = 40 entries (5 event-type labels + popup_event_count + popup_show_all_events + popup_show_fewer + event_strip_open_popup)"
    - "Per CONTEXT.md C-05: en + ja get real translations; de/es/fr placeholder = en verbatim per the 16.1-02 pattern (translation backlog stub created)"
    - "Translation backlog file `.planning/backlog/i18n-event-strip-de-es-fr.md` created listing the 8 new keys + en source strings for de/es/fr translators"
    - "JA translations are natural restaurant-owner Japanese, NOT engineer Japanese (per CONTEXT.md specifics §: friend uses ja locale at 375×667)"
    - "All 8 keys present in every locale block — TypeScript MessageKey enforces this; missing-key drift will fail npm run check"
    - "popup_event_count + popup_show_all_events accept `{n}` placeholder; event_strip_open_popup accepts `{date}` + `{count}`"
    - "C-05 carry-forward satisfied: i18n via src/lib/i18n/messages.ts; no separate JSON file"
    - "No schema push (UI strings only)"
    - "npm run check exits 0"
  artifacts:
    - path: "src/lib/i18n/messages.ts"
      provides: "8 new keys × 5 locales = 40 entries appended to each locale's block"
      contains: "event_type_campaign_start"
    - path: ".planning/backlog/i18n-event-strip-de-es-fr.md"
      provides: "Translation backlog stub for v1.4 — lists the 8 keys with en source strings; de/es/fr placeholder pattern matches 16.1-02 backlog stub"
      contains: "event_type_campaign_start"
  key_links:
    - from: "src/lib/i18n/messages.ts"
      to: "(ja block)"
      via: "Record<MessageKey, string>"
      pattern: "event_type_campaign_start.*['\"][^'\"]+['\"]"
---

<objective>
Wave 2 — add the 8 new i18n keys × 5 locales = 40 entries that EventBadgeStrip + ChartHoverPopup (Plan 16.3-03) consume:

**Event-type labels (5 keys — used by ChartHoverPopup type column AND EventBadgeStrip aria-labels):**
- `event_type_campaign_start`
- `event_type_transit_strike`
- `event_type_school_holiday`
- `event_type_holiday`
- `event_type_recurring_event`

**Popup chrome (3 keys):**
- `popup_event_count` — header summary, e.g. "{n} events" / "{n}件のイベント"
- `popup_show_all_events` — show-all expander button, e.g. "Show all {n}" / "{n}件すべて表示"
- `popup_show_fewer` — collapse button, e.g. "Show fewer" / "折りたたむ"

**EventBadgeStrip aria-label (1 key):**
- `event_strip_open_popup` — accessible name for badge button, e.g. "{count} events on {date}"

Per CONTEXT.md C-05: en + ja real translations; de/es/fr placeholder = en verbatim (16.1-02 pattern). Translation backlog stub for de/es/fr created at `.planning/backlog/i18n-event-strip-de-es-fr.md`.

Purpose: closes the i18n half of SC3 + SC6 (mobile QA in ja AND en locales). Without this plan, EventBadgeStrip's `t(locale, key)` calls return raw keys at runtime AND TypeScript's `MessageKey = keyof typeof messages.en` enforcement fails npm run check on the consumer side. This plan therefore unblocks the svelte-check pass on Plans 16.3-03 + 16.3-07.

Output: 40-entry expansion of `messages.ts` (8 keys × 5 locales) + 1 backlog stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-CONTEXT.md
@src/lib/i18n/messages.ts
@.planning/phases/16.1-friend-persona-ux-polish/16.1-02-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static strings only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.3-04-01 | Information disclosure | i18n placeholder strings | accept | All strings are static UI labels; no PII, no secrets |
| T-16.3-04-02 | Tampering | Locale drift causing TypeScript MessageKey fail | mitigate | All 5 locale blocks gain identical 8 keys in one commit; npm run check enforces |

ASVS L1: no high-severity threats.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Append 8 new keys × 5 locales = 40 entries to messages.ts</name>
  <read_first>
    - src/lib/i18n/messages.ts (full file — 1175 lines; locale blocks at line 13 (en), 255 (de), 486 (ja), 716 (es), 947 (fr); section delimiter pattern is `// --- <Section> ----`)
    - .planning/phases/16.1-friend-persona-ux-polish/16.1-02-SUMMARY.md (precedent for "ja + en real, de/es/fr placeholder" pattern + backlog stub)
    - .planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-CONTEXT.md (C-05 + specifics § "JA translations are natural restaurant-owner Japanese")
  </read_first>
  <action>
    Edit `src/lib/i18n/messages.ts`. After the existing `// --- Forecast hover popup (Phase 15 FUI-04) ----` section (currently ends ~line 45 in the en block), insert a new section for each locale.

    **For the `en` block (after the existing forecast popup section ~line 45):**

    ```ts
      // --- Event badge strip + popup (Phase 16.3 D-03/D-04/SC3-SC6) ----------
      event_type_campaign_start:    'Campaign',
      event_type_transit_strike:    'Transit strike',
      event_type_school_holiday:    'School holiday',
      event_type_holiday:           'Public holiday',
      event_type_recurring_event:   'Recurring event',
      popup_event_count:            '{n} events',
      popup_show_all_events:        'Show all {n}',
      popup_show_fewer:             'Show fewer',
      event_strip_open_popup:       '{count} events on {date}',
    ```

    **For the `ja` block (insert at the structurally identical position — after the popup section):**

    ```ts
      // --- Event badge strip + popup (Phase 16.3 D-03/D-04/SC3-SC6) ----------
      event_type_campaign_start:    'キャンペーン',
      event_type_transit_strike:    '交通機関の運休',
      event_type_school_holiday:    '学校休暇',
      event_type_holiday:           '祝日',
      event_type_recurring_event:   '定期イベント',
      popup_event_count:            '{n}件のイベント',
      popup_show_all_events:        '{n}件すべて表示',
      popup_show_fewer:             '折りたたむ',
      event_strip_open_popup:       '{date}に{count}件のイベント',
    ```

    The JA strings above use natural restaurant-owner Japanese:
    - キャンペーン (campaign — borrowed katakana, owner-natural)
    - 交通機関の運休 (transit shutdown — formal but readable; owner asked for clarity over slang)
    - 学校休暇 (school holiday — direct)
    - 祝日 (public holiday — single character standard)
    - 定期イベント (recurring event — natural)
    - 件 (counter for events — owner-natural)
    - 折りたたむ (collapse — standard UI)

    **For the `de`, `es`, `fr` blocks: paste the en block VERBATIM** (placeholder per C-05 + 16.1-02 precedent — owner only verifies ja + en). Insert at the same structural position in each block's flow.

    Verification grep at the end:
    - `grep -c "event_type_campaign_start" src/lib/i18n/messages.ts` should return 5 (one per locale block).

    Commit message: `feat(16.3-04): add event-strip + popup i18n keys (8 keys × 5 locales = 40 entries; ja+en real, de/es/fr placeholder)`. NO `Co-authored-by: Claude` trailer.
  </action>
  <verify>
    <automated>test "$(grep -c 'event_type_campaign_start' src/lib/i18n/messages.ts)" = "5" &amp;&amp; test "$(grep -c 'event_type_transit_strike' src/lib/i18n/messages.ts)" = "5" &amp;&amp; test "$(grep -c 'event_type_school_holiday' src/lib/i18n/messages.ts)" = "5" &amp;&amp; test "$(grep -c 'event_type_holiday' src/lib/i18n/messages.ts)" = "5" &amp;&amp; test "$(grep -c 'event_type_recurring_event' src/lib/i18n/messages.ts)" = "5" &amp;&amp; test "$(grep -c 'popup_event_count' src/lib/i18n/messages.ts)" = "5" &amp;&amp; test "$(grep -c 'popup_show_all_events' src/lib/i18n/messages.ts)" = "5" &amp;&amp; test "$(grep -c 'popup_show_fewer' src/lib/i18n/messages.ts)" = "5" &amp;&amp; test "$(grep -c 'event_strip_open_popup' src/lib/i18n/messages.ts)" = "5" &amp;&amp; grep -q "キャンペーン" src/lib/i18n/messages.ts &amp;&amp; grep -q "祝日" src/lib/i18n/messages.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 8 new keys appears EXACTLY 5 times across messages.ts (one per locale block)
    - JA block has real Japanese strings: キャンペーン, 交通機関の運休, 学校休暇, 祝日, 定期イベント, 件, 折りたたむ
    - de / es / fr blocks have the same 8 keys with en source values (placeholder pattern)
    - `npm run check` exits 0 (TypeScript MessageKey shape consistent across all 5 blocks)
    - Existing 16.1-* keys + Phase 15 keys unchanged
  </acceptance_criteria>
  <done>40 entries land in messages.ts; svelte-check green; Plans 16.3-03 + 16.3-07 unblocked.</done>
</task>

<task type="auto">
  <name>Task 2: Create translation backlog stub for de/es/fr</name>
  <read_first>
    - .planning/backlog/i18n-campaign-uplift-card-de-es-fr.md (16.1-02 precedent — copy structure exactly)
  </read_first>
  <action>
    Create `.planning/backlog/i18n-event-strip-de-es-fr.md`:

    ```markdown
    # i18n Backlog: Event Strip + Popup (Phase 16.3 — de / es / fr)

    **Created:** 2026-05-06
    **Source phase:** 16.3-04
    **Reason:** Per CONTEXT.md C-05 + 16.1-02 precedent, the friend-owner only verifies ja + en at 375×667. de / es / fr were seeded with en-verbatim placeholder values to keep the TypeScript `MessageKey` shape consistent.

    ## Keys to translate

    | Key | EN source |
    |-----|-----------|
    | `event_type_campaign_start` | `Campaign` |
    | `event_type_transit_strike` | `Transit strike` |
    | `event_type_school_holiday` | `School holiday` |
    | `event_type_holiday` | `Public holiday` |
    | `event_type_recurring_event` | `Recurring event` |
    | `popup_event_count` | `{n} events` |
    | `popup_show_all_events` | `Show all {n}` |
    | `popup_show_fewer` | `Show fewer` |
    | `event_strip_open_popup` | `{count} events on {date}` |

    ## Translation guidance

    - Match the existing tone of the locale (formal restaurant-owner persona, not engineer)
    - Preserve `{n}`, `{date}`, `{count}` placeholders exactly
    - Singular vs plural: en source already uses plural-only ("events"); if the target language requires plural inflection on `popup_event_count`, add a separate `_one` variant key in a follow-up commit (precedent: `popup_horizon_days_one` / `popup_horizon_days_many`)

    ## Status

    - [ ] de translated
    - [ ] es translated
    - [ ] fr translated

    Resolved when all 3 ticked AND messages.ts updated.
    ```

    Stage this file together with the messages.ts changes from Task 1 in the same commit.
  </action>
  <verify>
    <automated>test -f .planning/backlog/i18n-event-strip-de-es-fr.md &amp;&amp; grep -c "event_type_campaign_start\|event_type_transit_strike\|event_type_school_holiday\|event_type_holiday\|event_type_recurring_event\|popup_event_count\|popup_show_all_events\|popup_show_fewer\|event_strip_open_popup" .planning/backlog/i18n-event-strip-de-es-fr.md | grep -q "^9$"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/backlog/i18n-event-strip-de-es-fr.md` exists
    - Lists all 9 keys (8 translated + 1 redundancy with the strip-aria) — actually 9 keys total per Task 1 spec
    - Pattern matches 16.1-02 backlog precedent (Status checklist of 3 locales)
  </acceptance_criteria>
  <done>v1.4 translation backlog tracked.</done>
</task>

</tasks>

<verification>
**Goal-backward verification (Wave 2 prerequisite for SC3 + SC6):**

1. SC3 — i18n consumed by EventBadgeStrip lands; ChartHoverPopup type-column labels display correctly in ja AND en locales
2. SC6 — mobile QA can verify ja AND en locales because both have real translations (de/es/fr placeholder = en is acceptable per C-05)
3. Pattern continuity — backlog stub matches 16.1-02 precedent
4. Type safety — TypeScript MessageKey enforces all 9 keys × 5 locales; npm run check green
</verification>

<success_criteria>
- 9 new keys × 5 locales = 45 entries (8 type/popup keys + 1 strip aria-label) appended (count of 9 entries × 5 = 45 line additions plus section delimiter comments × 5 = 5 extra lines; total ~50 line diff to messages.ts)
- Backlog stub created
- npm run check green
- Single commit lands; no `Co-authored-by: Claude` trailer
</success_criteria>

<output>
After completion, append to `16.3-04-SUMMARY.md`:
- Final list of 9 keys + the JA verbatim strings (so Wave 3 mobile QA can spot-check pixel-perfect rendering)
- Backlog stub created at `.planning/backlog/i18n-event-strip-de-es-fr.md`
- Pointer to Plan 16.3-03 (consumes these keys) + 16.3-07 (per-card wiring)
</output>
