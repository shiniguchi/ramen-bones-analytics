---
phase: 16.3-dashboard-cleanup-events-everywhere
plan: 04
subsystem: i18n
tags: [i18n, event-strip, popup, ja, en, placeholder-locales]
requires: [16.3-01]
provides: [event-strip-i18n-keys, popup-chrome-i18n-keys, event-aria-label-i18n-key]
affects: [src/lib/i18n/messages.ts]
tech-stack:
  added: []
  patterns: ["ja+en real / de+es+fr placeholder = en verbatim (16.1-02 backlog-stub precedent)"]
key-files:
  created:
    - .planning/backlog/i18n-event-strip-de-es-fr.md
  modified:
    - src/lib/i18n/messages.ts
decisions:
  - "Appended new section AT END of each locale block (not after popup section as plan literal-text suggested) — preserves the chronological-by-phase pattern established by 16.1 + 16.2 sections; semantically equivalent for TypeScript MessageKey enforcement"
metrics:
  duration_minutes: 8
  completed: 2026-05-06
  tasks: 2
  files_changed: 2
  insertions: 93
  deletions: 5
---

# Phase 16.3 Plan 04: i18n Event-Strip + Popup Keys Summary

**One-liner:** Added 9 i18n keys × 5 locales (45 entries) to `messages.ts` for `EventBadgeStrip` + `ChartHoverPopup` consumers; ja + en real, de/es/fr placeholder = en (v1.4 backlog filed).

## What Landed

### Task 1: 45 new entries in `src/lib/i18n/messages.ts`

9 keys appended to every locale block (en, de, ja, es, fr):

| Key | EN | JA |
|-----|-----|-----|
| `event_type_campaign_start` | `Campaign` | `キャンペーン` |
| `event_type_transit_strike` | `Transit strike` | `交通機関の運休` |
| `event_type_school_holiday` | `School holiday` | `学校休暇` |
| `event_type_holiday` | `Public holiday` | `祝日` |
| `event_type_recurring_event` | `Recurring event` | `定期イベント` |
| `popup_event_count` | `{n} events` | `{n}件のイベント` |
| `popup_show_all_events` | `Show all {n}` | `{n}件すべて表示` |
| `popup_show_fewer` | `Show fewer` | `折りたたむ` |
| `event_strip_open_popup` | `{count} events on {date}` | `{date}に{count}件のイベント` |

de / es / fr blocks: en verbatim (placeholder per CONTEXT.md C-05 + 16.1-02 precedent).

JA tone notes (restaurant-owner natural, NOT engineer Japanese):
- `キャンペーン` — borrowed katakana, owner-natural
- `交通機関の運休` — formal but readable; owner asked for clarity over slang
- `学校休暇` — direct
- `祝日` — single-character standard
- `定期イベント` — natural
- `件` — counter for events (owner-natural)
- `折りたたむ` — standard UI verb

### Task 2: Translation backlog stub

Created `.planning/backlog/i18n-event-strip-de-es-fr.md` listing all 9 keys with EN sources, translation guidance (placeholder preservation, plural-inflection note referencing `popup_horizon_days_one`/`_many` precedent), and a 3-locale checkbox status block. Pattern matches `i18n-campaign-uplift-card-de-es-fr.md` (16.1-02 precedent).

## Verification

| Check | Result |
|-------|--------|
| 9 keys × 5 locales (45 entries) | ✅ All 9 keys grep to exactly 5 occurrences each |
| JA strings present | ✅ 13 distinct JA strings (キャンペーン / 交通機関の運休 / 学校休暇 / 定期イベント / 折りたたむ + variants) |
| Backlog stub exists | ✅ `.planning/backlog/i18n-event-strip-de-es-fr.md` created with 9-row keys table |
| `npm run check` zero new errors | ✅ 7 errors at HEAD baseline; 7 errors after edits — zero introduced (all pre-existing in vite.config.ts / hooks.server.ts / CalendarRevenueCard.svelte 195:28 — outside this plan's scope per SCOPE BOUNDARY) |
| TypeScript MessageKey shape consistent | ✅ Same 7 errors before vs. after = no MessageKey drift |
| No `Co-authored-by: Claude` trailer | ✅ Commit message verified |

## Deviations from Plan

### Minor structural placement (no functional impact)

**1. [Rule 3 — Pattern continuity] New section appended at END of each locale block, not mid-block after the popup section.**
- **Plan literal text:** "After the existing `// --- Forecast hover popup (Phase 15 FUI-04) ----` section (currently ends ~line 45 in the en block), insert a new section."
- **What was done:** New section appended at the END of each locale block, after the `// --- Model availability disclosure (Phase 16.2 polish)` section.
- **Why:** Examining `messages.ts` showed every prior phase (16.1-02, 16.1-03, 16.1-D-16, 16.1-D-18, 16.2 polish) appended its new section at the END of each block, not mid-block. Mid-block insertion would have broken the chronological-by-phase pattern that makes the diff trivial to review. Semantically equivalent — TypeScript MessageKey enforcement is identical regardless of position within the object literal.
- **Files modified:** `src/lib/i18n/messages.ts` (5 locale blocks)
- **Commit:** `ddee2bb`

### Auth gates

None.

## Threat Flags

None — static UI strings only, no new trust boundary.

## Pointers (downstream consumers)

- **Plan 16.3-03** (EventBadgeStrip + ChartHoverPopup): consumes all 9 keys. Without this plan, `t(locale, key)` would return raw keys at runtime AND the consumer's `npm run check` would fail on `MessageKey` lookup.
- **Plan 16.3-07** (per-card wiring): renders `EventBadgeStrip` + `ChartHoverPopup` into 5 dashboard cards (Calendar* + RepeaterCohortCount). Same i18n surface.

## Commits

| Hash | Message | Files |
|------|---------|-------|
| `ddee2bb` | feat(16.3-04): add event-strip + popup i18n keys (9 keys × 5 locales = 45 entries; ja+en real, de/es/fr placeholder) | `src/lib/i18n/messages.ts`, `.planning/backlog/i18n-event-strip-de-es-fr.md` |

## Self-Check: PASSED

- [x] `src/lib/i18n/messages.ts` modified — confirmed via `git log -1 --name-only`
- [x] `.planning/backlog/i18n-event-strip-de-es-fr.md` created — confirmed via `test -f`
- [x] Commit `ddee2bb` exists — confirmed via `git rev-parse --short HEAD`
- [x] All 9 keys × 5 locales = 45 entries present — confirmed via per-key grep loop
- [x] JA strings natural restaurant-owner Japanese — verified content
- [x] de/es/fr placeholder = en verbatim — verified content
- [x] `npm run check` shows zero new errors vs. HEAD baseline (7 = 7)
