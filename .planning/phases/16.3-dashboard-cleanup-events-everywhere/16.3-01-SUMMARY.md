---
phase: 16.3-dashboard-cleanup-events-everywhere
plan: 01
subsystem: dashboard-page
tags: [delete, dashboard, forecast-cards, wave-1]
dependency_graph:
  requires:
    - "Phase 16+16.1+16.2 PR merged into main (forecast pipeline frozen)"
  provides:
    - ".planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md (popup CSS source-of-truth for Plan 16.3-03)"
    - "src/routes/+page.svelte without RevenueForecastCard / InvoiceCountForecastCard / ForecastHoverPopup imports or slots"
  affects:
    - "Plan 16.3-02 (eventTypeColors.ts palette extraction reads EventMarker.svelte still on disk — preserved through Wave 1)"
    - "Plan 16.3-03 (popup util reads .planning/.../16.3-popup-css-lift.md instead of the deleted ForecastHoverPopup.svelte)"
    - "Plan 16.3-07 (EventBadgeStrip wiring — the 5 dashboard chart cards no longer have to coexist with deleted-but-still-imported references)"
tech_stack:
  added: []
  patterns:
    - "Pure deletion: 3 components + 3 unit-test files removed; 4 lines + 1 narrative comment block trimmed from src/routes/+page.svelte"
key_files:
  created:
    - .planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md
    - .planning/phases/16.3-dashboard-cleanup-events-everywhere/deferred-items.md
  modified:
    - src/routes/+page.svelte
  deleted:
    - src/lib/components/RevenueForecastCard.svelte
    - src/lib/components/InvoiceCountForecastCard.svelte
    - src/lib/components/ForecastHoverPopup.svelte
    - tests/unit/RevenueForecastCard.test.ts
    - tests/unit/InvoiceCountForecastCard.test.ts
    - tests/unit/ForecastHoverPopup.test.ts
decisions:
  - "Friend-owner verbal feedback 2026-05-06: the 2 dedicated forecast cards don't drive business decisions — delete (CONTEXT.md origin §)"
  - "ForecastHoverPopup.svelte CSS lifted to 16.3-popup-css-lift.md BEFORE deletion (D-07): popup util in Plan 16.3-03 reads the lift artifact, not the deleted source"
  - "EventMarker.svelte deliberately preserved through Wave 1; Plan 16.3-02 reads its hard-coded hex palette before its deletion in Plan 16.3-07"
  - "Forecast pipeline preserved end-to-end: /api/forecast, forecast_daily, forecast_with_actual_v, forecast_quality cron, forecastOverlay.svelte.ts, ForecastLegend.svelte, ModelAvailabilityDisclosure.svelte, forecastEventClamp.ts (Calendar* cards continue to render past-forecast Splines)"
  - "Test files for the 3 deleted components removed in same commit (Rule 3 — auto-fix blocking issues caused by the current task's changes)"
metrics:
  duration_minutes: ~6
  completed_date: 2026-05-06
  tasks_completed: 3
  files_changed: 9
  lines_inserted: 121
  lines_deleted: 1293
---

# Phase 16.3 Plan 01: Delete Forecast Cards Summary

Wave 1 dashboard cleanup: deleted the 3 dedicated forecast components (`RevenueForecastCard.svelte`, `InvoiceCountForecastCard.svelte`, `ForecastHoverPopup.svelte`) and their imports + `<LazyMount>` slot blocks from `src/routes/+page.svelte`. Forecast pipeline preserved.

## What Changed

### Deleted from disk (6 files)

**3 forecast components:**
1. `src/lib/components/RevenueForecastCard.svelte`
2. `src/lib/components/InvoiceCountForecastCard.svelte`
3. `src/lib/components/ForecastHoverPopup.svelte`

**3 unit-test files (Rule 3 auto-fix — they import deleted modules):**
4. `tests/unit/RevenueForecastCard.test.ts`
5. `tests/unit/InvoiceCountForecastCard.test.ts`
6. `tests/unit/ForecastHoverPopup.test.ts`

### Modified (1 file)

**`src/routes/+page.svelte`:**
- Lines 21-22 (pre-16.3 source): 2 component imports removed (`RevenueForecastCard`, `InvoiceCountForecastCard`).
- Lines 274-294 (pre-16.3 source): 2 `<LazyMount>` slot blocks removed (the wrappers for the 2 deleted cards).
- Lines 96-102 (pre-16.3 source): 7-line narrative comment block referencing `RevenueForecastCard` self-fetch behavior — trimmed (component no longer exists; comment was stale).

### Created (2 planning artifacts)

1. `.planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md`
   Verbatim popup-shell CSS classes lifted from `ForecastHoverPopup.svelte` BEFORE deletion. Single source-of-truth for Plan 16.3-03's popup util (component or CSS-only — planner's discretion per D-07).

2. `.planning/phases/16.3-dashboard-cleanup-events-everywhere/deferred-items.md`
   Out-of-scope discoveries: `tests/e2e/forecast-event-markers.spec.ts` (skipped E2E, comment-only references), pre-existing svelte-check errors in unrelated files (`vite.config.ts`, `src/hooks.server.ts`, `src/lib/components/CalendarRevenueCard.svelte`), and 6 source files with comment-only mentions of the deleted components.

## Pipeline Preservation (SC2 verified)

All 8 preserved-pipeline assets confirmed on disk:

| Asset | Status |
|---|---|
| `src/lib/components/EventMarker.svelte` | Preserved (deletion deferred to Plan 16.3-07 after palette extracted in 16.3-02) |
| `src/lib/forecastEventClamp.ts` | Preserved |
| `src/lib/forecastOverlay.svelte.ts` | Preserved |
| `src/lib/components/ForecastLegend.svelte` | Preserved |
| `src/lib/components/ModelAvailabilityDisclosure.svelte` | Preserved |
| `src/routes/api/forecast/+server.ts` | Preserved |
| `forecast_daily` materialized view | Untouched (no SQL change) |
| `forecast-refresh.yml` cron | Untouched |

## Verification Results

### `npm run check` (svelte-check)

```
1778056146275 COMPLETED 4694 FILES 7 ERRORS 0 WARNINGS 3 FILES_WITH_PROBLEMS
```

**7 errors — ALL pre-existing, none introduced by this plan:**
- `vite.config.ts:7:3` — `'test' does not exist in type 'UserConfigExport'` (vitest type union shadowing)
- `src/hooks.server.ts:23,24,38` — implicit-any cookies parameters + JwtPayload `claims` access
- `src/lib/components/CalendarRevenueCard.svelte:195:28` — `'w' is possibly 'undefined'`

Pre-existence verified by `git stash && npm run check` on clean HEAD producing identical 7-error output. Out of scope per SCOPE BOUNDARY rule (pre-existing, unrelated files).

### `npm run build`

```
✓ built in 18.04s
> Using @sveltejs/adapter-cloudflare
  ✔ done
```

Build green. All Vite/SvelteKit chunks emit successfully. Adapter-cloudflare adapter completes cleanly. Mobile bundle budget unchanged (no new dependencies added).

## Commit

| Hash | Message |
|---|---|
| `88f29cc` | `feat(16.3-01): delete dedicated forecast cards + popup; remove from dashboard grid` |

Single atomic commit covering all 3 tasks (Task 1 popup-CSS lift artifact + Task 2 file deletions + Task 3 +page.svelte clean-up + Rule 3 test-file cleanup).

Diff stat: 9 files changed, 121 insertions(+), 1293 deletions(-).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Removed 3 unit-test files that imported deleted components**
- **Found during:** Task 3 `npm run check`
- **Issue:** `tests/unit/RevenueForecastCard.test.ts`, `tests/unit/InvoiceCountForecastCard.test.ts`, `tests/unit/ForecastHoverPopup.test.ts` imported the now-deleted `.svelte` modules. svelte-check reported 3 NEW "Cannot find module" errors directly caused by Task 2 deletions.
- **Fix:** Deleted the 3 test files. They tested deleted UI surfaces and had no preservation requirement.
- **Files removed:** `tests/unit/{RevenueForecastCard,InvoiceCountForecastCard,ForecastHoverPopup}.test.ts`
- **Commit:** `88f29cc`

**2. [Rule 1 - Bug] Trimmed stale narrative comment in src/routes/+page.svelte**
- **Found during:** Task 3 grep cross-check (`grep -q "RevenueForecastCard" src/routes/+page.svelte` returned matches in comments).
- **Issue:** A 7-line comment block at lines 96-102 (pre-16.3 source) described `RevenueForecastCard.self-fetch behavior — but the component no longer exists post-deletion, so the comment was misleading.
- **Fix:** Trimmed the stale comment block. The acceptance criteria literal `grep -q "RevenueForecastCard" src/routes/+page.svelte` now returns no match.
- **Files modified:** `src/routes/+page.svelte`
- **Commit:** `88f29cc`

## Auth Gates

None.

## Known Stubs

None — pure deletion plan; no new UI rendered.

## Threat Flags

None — pure deletion in client-side rendering. No new network endpoints, auth paths, file access patterns, or schema changes. Threat T-16.3-01-03 (DoS / availability via pipeline preservation) mitigated: `npm run build` green; Calendar* cards untouched, still consume `createForecastOverlay()`.

## Wave 2 Hand-off Notes

- **Plan 16.3-02 (`eventTypeColors.ts` palette):** `EventMarker.svelte` is on disk at `src/lib/components/EventMarker.svelte` with the 5 hex values at lines 60-113 (verified in pre-deletion read). Read those values verbatim into the new shared lib, then 16.3-07 deletes `EventMarker.svelte` after Wave 2 wires the new strip everywhere.
- **Plan 16.3-03 (popup util):** Read `.planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md` for the verbatim Tailwind classes + the `contained="window"` auto-flip directive. Do NOT attempt to read the deleted `ForecastHoverPopup.svelte`.
- **Plan 16.3-04 — 16.3-07 (per-card EventBadgeStrip wiring):** The 5 wired charts (`CalendarCountsCard`, `CalendarRevenueCard`, `CalendarItemsCard`, `CalendarItemRevenueCard`, `RepeaterCohortCountCard`) all rendered cleanly in `npm run build` after Wave 1; their host structures (`<Chart>` + `bind:this={scrollerRef}` + `bind:clientWidth={cardW}`) are untouched, ready for badge-strip slot insertion.

## Self-Check: PASSED

- File `src/lib/components/RevenueForecastCard.svelte` — MISSING (deleted as planned)
- File `src/lib/components/InvoiceCountForecastCard.svelte` — MISSING (deleted as planned)
- File `src/lib/components/ForecastHoverPopup.svelte` — MISSING (deleted as planned)
- File `src/lib/components/EventMarker.svelte` — FOUND (preserved as required)
- File `src/lib/forecastEventClamp.ts` — FOUND
- File `src/lib/forecastOverlay.svelte.ts` — FOUND
- File `src/lib/components/ForecastLegend.svelte` — FOUND
- File `src/lib/components/ModelAvailabilityDisclosure.svelte` — FOUND
- File `src/routes/api/forecast/+server.ts` — FOUND
- File `.planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md` — FOUND
- File `.planning/phases/16.3-dashboard-cleanup-events-everywhere/deferred-items.md` — FOUND
- File `src/routes/+page.svelte` — FOUND (no references to deleted components)
- Commit `88f29cc` — FOUND in git log
- `npm run check` — only pre-existing errors remain (verified by stash diff)
- `npm run build` — exits 0
