---
phase: 16.3-dashboard-cleanup-events-everywhere
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [UPL-05, FUI-09]
files_modified:
  - src/routes/+page.svelte
  - src/lib/components/RevenueForecastCard.svelte
  - src/lib/components/InvoiceCountForecastCard.svelte
  - src/lib/components/ForecastHoverPopup.svelte
must_haves:
  truths:
    - "RevenueForecastCard.svelte does not exist on disk after this plan"
    - "InvoiceCountForecastCard.svelte does not exist on disk after this plan"
    - "ForecastHoverPopup.svelte does not exist on disk after this plan (CSS already lifted in 16.3-02 — but for Wave 1 ordering, the file is deleted here; popup util in Wave 2 references the lifted CSS that 16.3-02 will copy from this file BEFORE deletion)"
    - "src/routes/+page.svelte does not import RevenueForecastCard, InvoiceCountForecastCard, or ForecastHoverPopup"
    - "src/routes/+page.svelte does not contain the `<RevenueForecastCard />` slot or the `<InvoiceCountForecastCard />` slot or their `<LazyMount>` wrappers (lines 274-294 in pre-16.3 source)"
    - "EventMarker.svelte is preserved on disk in Wave 1 (consumers RevenueForecastCard / InvoiceCountForecastCard are deleted, but EventMarker stays until 16.3-07 finishes palette migration in Wave 2)"
    - "npm run check (svelte-check) exits 0 — no type / import errors"
    - "npm run build exits 0 — Vite/SvelteKit build green"
    - "No new schema push required (UI deletion only)"
    - "Closes residual UPL-05 (event-overlay scope outside forecast cards) and FUI-09 (mobile readability gate persists in Wave 3 QA) friend-persona-acceptance gaps"
    - "Per CONTEXT.md D-10: Wave 1 = deletion + page wiring + svelte-check green; EventMarker.svelte deleted in 16.3-07 after palette extracted"
  artifacts:
    - path: "src/lib/components/RevenueForecastCard.svelte"
      provides: "DELETED — friend says doesn't drive business decisions (per CONTEXT.md origin §)"
    - path: "src/lib/components/InvoiceCountForecastCard.svelte"
      provides: "DELETED — friend says doesn't drive business decisions"
    - path: "src/lib/components/ForecastHoverPopup.svelte"
      provides: "DELETED — CSS pattern (popup body shell + auto-flip) is lifted into Wave 2 popup util BEFORE deletion (16.3-03 reads this file's CSS first)"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "(removed) RevenueForecastCard / InvoiceCountForecastCard imports"
      via: "import statement deletion (lines 21-22 in pre-16.3 source)"
      pattern: "(?!.*RevenueForecastCard).*(?!.*InvoiceCountForecastCard)"
    - from: "src/routes/+page.svelte main grid"
      to: "(removed) <RevenueForecastCard /> + <InvoiceCountForecastCard /> slots"
      via: "<LazyMount> block deletion (lines 274-294 in pre-16.3 source)"
      pattern: "RevenueForecastCard|InvoiceCountForecastCard"
---

<objective>
Wave 1 — delete the 3 dedicated forecast components (`RevenueForecastCard.svelte`, `InvoiceCountForecastCard.svelte`, `ForecastHoverPopup.svelte`) and remove their imports + slots from `src/routes/+page.svelte`. The forecast pipeline (`/api/forecast`, `forecast_daily`, cron, `forecast_quality`, `forecastOverlay.svelte.ts`, `ForecastLegend.svelte`, `ModelAvailabilityDisclosure.svelte`, `forecastEventClamp.ts`) is preserved — only the dedicated forecast CARDS are removed because the friend-owner says they don't drive business decisions (CONTEXT.md origin §). `CalendarRevenueCard` + `CalendarCountsCard` continue to render past-forecast Splines via `createForecastOverlay()` unchanged. CampaignUpliftCard's counterfactual baseline unchanged.

Purpose: closes the trivial "scope reduction" half of Phase 16.3. Wave 2 plans depend on this wave landing first so that wired-up cards (CalendarRevenueCard etc.) don't have to coexist with deleted-but-still-imported references during the EventBadgeStrip rollout. EventMarker.svelte is INTENTIONALLY preserved through Wave 1 because Wave 2's `eventTypeColors.ts` palette extraction (Plan 16.3-02) reads its 5 hard-coded hex values as source-of-truth before deletion in Plan 16.3-07.

Output:
- 3 component files removed from `src/lib/components/`
- 4 lines (2 imports + 2 slot deletions) cleaned from `src/routes/+page.svelte`
- `npm run check` + `npm run build` both green
- Wave 1 commit: `feat(16.3-01): delete dedicated forecast cards + popup; remove from dashboard grid`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-CONTEXT.md
@CLAUDE.md
@.claude/CLAUDE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ SvelteKit SSR | Page render path; deletions strictly remove client-side components — no new untrusted input crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.3-01-01 | Tampering | `+page.svelte` slot deletion | accept | Pure UI removal; no auth, no data, no RLS scope change |
| T-16.3-01-02 | Information disclosure | Deleted ForecastHoverPopup.svelte | accept | Component held no secrets; CSS pattern (Wave 2 lift) carries no security info |
| T-16.3-01-03 | DoS / availability | Forecast pipeline preservation | mitigate | npm run check + npm run build both green; manual smoke-load on localhost confirms Calendar* cards still render forecast Splines |

ASVS L1: no high-severity threats. Wave 3 mobile QA covers visual regression.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Lift ForecastHoverPopup.svelte CSS into a planning artifact (read-only) BEFORE deletion</name>
  <read_first>
    - .planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-CONTEXT.md (D-07 popup positioning + auto-flip pattern)
    - src/lib/components/ForecastHoverPopup.svelte (full file — 149 lines; the CSS classes on lines 76-149 are the "above/below auto-flip" pattern Wave 2 popup util reuses per D-07)
  </read_first>
  <action>
    Create `.planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md` capturing the verbatim popup-shell CSS classes from `ForecastHoverPopup.svelte`:

    - Outer wrapper: `min-w-[200px] max-w-[280px] rounded-lg border border-zinc-200 bg-white p-3 shadow-md`
    - Header: `flex items-baseline justify-between gap-2 border-b border-zinc-100 pb-1.5`
    - Header label: `text-xs font-semibold text-zinc-900`
    - Header date: `text-[10px] tabular-nums text-zinc-500`
    - Field block: `mt-2 space-y-0.5` / `flex items-baseline justify-between gap-2 text-[11px]`
    - Subtle row: `text-[10px] text-zinc-500`
    - Divider: `border-t border-zinc-100 pt-1.5`
    - Auto-flip note (parent owns wrapper): "Auto-flip on right-edge overflow is handled by the parent wrapper's built-in `contained=\"window\"` prop (CalendarRevenueCard precedent)" — Plan 16.3-03 (popup util) reuses this directive in its docstring.

    This artifact is the single source-of-truth that Plan 16.3-03 reads to build `ChartHoverPopup.svelte` OR `chartPopup.css` (D-07 — planner's discretion in 16.3-03 frontmatter). After this task, `ForecastHoverPopup.svelte` itself can be deleted in Task 2.

    Per `.claude/CLAUDE.md` "**🚨 NEVER add `Co-authored-by: Claude <noreply@anthropic.com>` to git commits**" — do not include that trailer in any commit on this plan or any 16.3 plan.
  </action>
  <verify>
    <automated>test -f .planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md &amp;&amp; grep -c "min-w-\[200px\] max-w-\[280px\]" .planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md | grep -q "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md` exists
    - Contains the 7 verbatim CSS class strings listed in &lt;action&gt;
    - Contains the `contained="window"` auto-flip directive note
  </acceptance_criteria>
  <done>Popup CSS lifted to planning artifact; ForecastHoverPopup.svelte safe to delete in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the 3 forecast component files from disk</name>
  <read_first>
    - src/routes/+page.svelte (lines 21-22 imports, 274-294 slot block — confirm exact line numbers before edit)
    - .planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-CONTEXT.md (SC1 + D-04: EventMarker.svelte preserved through Wave 1; deleted in 16.3-07)
    - .planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md (Task 1 artifact — confirms ForecastHoverPopup CSS already captured)
  </read_first>
  <action>
    Use `rm` (or Bash file deletion) to remove these 3 files from disk:

    1. `src/lib/components/RevenueForecastCard.svelte`
    2. `src/lib/components/InvoiceCountForecastCard.svelte`
    3. `src/lib/components/ForecastHoverPopup.svelte`

    DO NOT delete `src/lib/components/EventMarker.svelte` in this plan — it is preserved through Wave 1 because Plan 16.3-02 reads its hard-coded hex values (lines 60-113) as the source of truth for the `eventTypeColors.ts` palette before deletion in Plan 16.3-07 (per CONTEXT.md D-04 + Wave 2 ordering).

    DO NOT delete `src/lib/forecastEventClamp.ts`, `src/lib/forecastOverlay.svelte.ts`, `src/lib/components/ForecastLegend.svelte`, or `src/lib/components/ModelAvailabilityDisclosure.svelte` — all preserved per SC2.

    DO NOT touch `/api/forecast` server, `forecast_daily`, `forecast_with_actual_v`, `forecast_quality`, or `forecast-refresh.yml` — preserved per SC2.

    No commit yet — Task 3 runs together with this in one atomic commit.
  </action>
  <verify>
    <automated>! test -f src/lib/components/RevenueForecastCard.svelte &amp;&amp; ! test -f src/lib/components/InvoiceCountForecastCard.svelte &amp;&amp; ! test -f src/lib/components/ForecastHoverPopup.svelte &amp;&amp; test -f src/lib/components/EventMarker.svelte &amp;&amp; test -f src/lib/forecastEventClamp.ts &amp;&amp; test -f src/lib/forecastOverlay.svelte.ts &amp;&amp; test -f src/lib/components/ForecastLegend.svelte &amp;&amp; test -f src/lib/components/ModelAvailabilityDisclosure.svelte &amp;&amp; test -f src/routes/api/forecast/+server.ts</automated>
  </verify>
  <acceptance_criteria>
    - 3 deleted files do not exist
    - 8 preserved-pipeline files DO exist (EventMarker, forecastEventClamp, forecastOverlay, ForecastLegend, ModelAvailabilityDisclosure, /api/forecast/+server.ts)
  </acceptance_criteria>
  <done>3 component files removed; pipeline intact.</done>
</task>

<task type="auto">
  <name>Task 3: Remove imports + slot blocks from src/routes/+page.svelte</name>
  <read_first>
    - src/routes/+page.svelte (full file — but specifically lines 21-22 for imports and lines 274-294 for the two LazyMount slot blocks; line numbers per pre-16.3 source captured in CONTEXT.md canonical_refs §)
  </read_first>
  <action>
    Edit `src/routes/+page.svelte`:

    **(a) Delete 2 import lines (originally at line 21-22):**
    ```
    import RevenueForecastCard from '$lib/components/RevenueForecastCard.svelte';
    import InvoiceCountForecastCard from '$lib/components/InvoiceCountForecastCard.svelte';
    ```

    **(b) Delete 2 LazyMount slot blocks (originally lines 274-294):**

    The full block to remove:
    ```
        <!-- Phase 15 D-01: RevenueForecastCard slots immediately after the
             InsightCard narrative ... -->
        <LazyMount minHeight="320px">
          {#snippet children()}
            <RevenueForecastCard />
          {/snippet}
        </LazyMount>

        <!-- Phase 15-15 / D-18: InvoiceCountForecastCard mirrors the revenue card ... -->
        <LazyMount minHeight="320px">
          {#snippet children()}
            <InvoiceCountForecastCard />
          {/snippet}
        </LazyMount>
    ```

    **(c) PRESERVE** the CampaignUpliftCard `<LazyMount minHeight="180px">` block immediately after — it stays.

    **(d) PRESERVE** all 5 charts that Wave 2 Plan 16.3-07 will wire EventBadgeStrip into: `CalendarCountsCard`, `CalendarRevenueCard`, `CalendarItemsCard`, `CalendarItemRevenueCard`, `RepeaterCohortCountCard`. None of their imports or slots change in this plan.

    Reading order on the page after the edit:
    1. InsightCard
    2. CampaignUpliftCard (preserved)
    3. KPI tiles (Revenue + Transactions)
    4. DailyHeatmapCard
    5. CalendarCountsCard
    6. CalendarRevenueCard
    7. CalendarItemsCard + CalendarItemRevenueCard
    8. CohortRetentionCard
    9. RepeaterCohortCountCard
    10. MdeCurveCard

    Commit message: `feat(16.3-01): delete dedicated forecast cards + popup; remove from dashboard grid` (NO `Co-authored-by: Claude` trailer per `.claude/CLAUDE.md`).

    Stage `src/routes/+page.svelte` deletion + the 3 file deletions from Task 2 + `.planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md` from Task 1 in ONE commit.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; ! grep -q "RevenueForecastCard\|InvoiceCountForecastCard\|ForecastHoverPopup" src/routes/+page.svelte &amp;&amp; ! grep -rln "RevenueForecastCard\|InvoiceCountForecastCard\|ForecastHoverPopup" src/ 2&gt;/dev/null | grep -v "^$"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `npm run build` exits 0
    - `grep -q "RevenueForecastCard" src/routes/+page.svelte` returns no match
    - `grep -q "InvoiceCountForecastCard" src/routes/+page.svelte` returns no match
    - `grep -q "ForecastHoverPopup" src/routes/+page.svelte` returns no match
    - `grep -rln "RevenueForecastCard\|InvoiceCountForecastCard\|ForecastHoverPopup" src/` returns nothing (zero references project-wide)
    - Forecast pipeline files preserved (EventMarker, forecastOverlay, ForecastLegend, ModelAvailabilityDisclosure, forecastEventClamp, /api/forecast)
    - Single git commit lands all changes; commit message starts with `feat(16.3-01):`; commit message does NOT contain "Co-authored-by: Claude"
  </acceptance_criteria>
  <done>+page.svelte cleaned; svelte-check + build green; Wave 1 ready for Wave 2 dependents.</done>
</task>

</tasks>

<verification>
**Goal-backward verification (Wave 1 slice of SC1 + SC2):**

1. SC1 partial — 3 forecast cards deleted: `! test -f src/lib/components/RevenueForecastCard.svelte`, `! test -f src/lib/components/InvoiceCountForecastCard.svelte`, `! test -f src/lib/components/ForecastHoverPopup.svelte`
2. SC1 partial — imports + slots gone from +page.svelte: `! grep -q "RevenueForecastCard\|InvoiceCountForecastCard\|ForecastHoverPopup" src/routes/+page.svelte`
3. SC1 — svelte-check green: `npm run check` exits 0
4. SC1 — build green: `npm run build` exits 0
5. SC2 — pipeline preserved: all 8 preserved-pipeline files still exist (verified in Task 2 acceptance)
6. EventMarker.svelte preserved through Wave 1 (deletion in 16.3-07 after palette extracted in 16.3-02)
</verification>

<success_criteria>
- 3 component files removed from disk
- 2 import lines + 2 slot blocks removed from `src/routes/+page.svelte`
- `npm run check` exits 0; `npm run build` exits 0
- Forecast pipeline files preserved (8 files listed in Task 2 acceptance)
- Single atomic commit on `feature/phase-16.3-dashboard-cleanup-events-everywhere` branch
- Wave 2 plans (16.3-02 through 16.3-06) can proceed in parallel where their dependencies allow
</success_criteria>

<output>
After completion, create `.planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-01-SUMMARY.md` capturing:
- The 3 files deleted
- The 4 lines removed from +page.svelte (line numbers in pre-16.3 source)
- npm run check + npm run build outputs (paste tail)
- Commit SHA + message
- Confirmation that EventMarker.svelte is preserved (deletion deferred to 16.3-07)
- Pointer to `.planning/phases/16.3-dashboard-cleanup-events-everywhere/16.3-popup-css-lift.md` for Wave 2 popup util consumers
</output>
