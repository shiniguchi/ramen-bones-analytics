---
phase: 16.3-dashboard-cleanup-events-everywhere
verified: 2026-05-06T11:35:00Z
status: human_needed
score: 8/9 must-haves verified
overrides_applied: 1
overrides:
  - must_have: "SC2 — EventMarker.svelte preserved on disk"
    reason: "CONTEXT.md D-04 (locked 2026-05-06) re-interprets the ROADMAP wording as 'forecast pipeline preserves the visual encoding it provided'; palette extracted to src/lib/eventTypeColors.ts (single source of truth) and the file deleted. Verified: the 5 hex values match the deleted EventMarker palette byte-for-byte; no consumers remain (the 2 deleted forecast cards were the only consumers)."
    accepted_by: "phase-orchestrator (D-04 locked 2026-05-06)"
    accepted_at: "2026-05-06"
human_verification:
  - test: "SC8 — Friend-persona tap test on 2026-04-14 campaign"
    expected: "Owner opens dashboard on her phone (375×667, ja locale), scrolls to CalendarRevenueCard at day grain, taps the badge on 2026-04-14, popup shows campaign name + date + type. Three deleted forecast cards (RevenueForecastCard, InvoiceCountForecastCard, ForecastHoverPopup) are absent from the scroll path AND owner verbally confirms she does not miss them."
    why_human: "Subjective owner-acceptance signal — codebase verifier cannot speak for the friend-owner. Plan 16.3-08 explicitly defers this to the post-/gsd-ship sign-off cycle; HUMAN-UAT.md SC8 carries PENDING status by design."
---

# Phase 16.3: Dashboard Cleanup + Events Everywhere — Verification Report

**Phase Goal:** Delete 3 forecast components owner says don't drive business decisions; build mobile-first `EventBadgeStrip` (one badge per bucket) wired into 5 date-axis chart cards; preserve forecast pipeline; keep Phase 17 unaffected.

**Verified:** 2026-05-06T11:35:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (SC1–SC9 from CONTEXT.md)

| #   | Truth                                                                       | Status                  | Evidence                                                                                                                                                                                                                                  |
| --- | --------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SC1 | Three forecast cards deleted; no broken imports                             | VERIFIED                | `ls` confirms `RevenueForecastCard.svelte`, `InvoiceCountForecastCard.svelte`, `ForecastHoverPopup.svelte` all `No such file or directory`. `grep -rln` in `src/` returns only the documented design-context comment in `ChartHoverPopup.svelte:10`. `+page.svelte` zero refs. svelte-check baseline 7 errors (all pre-existing — none in 16.3-modified surface). |
| SC2 | Forecast pipeline preserved (EventMarker re-interpreted via palette)       | VERIFIED (override)     | All 5 listed files present: `forecastEventClamp.ts`, `forecastOverlay.svelte.ts`, `ForecastLegend.svelte`, `ModelAvailabilityDisclosure.svelte`, `src/routes/api/forecast/+server.ts`. EventMarker.svelte deletion accepted via D-04 override; palette lives in `eventTypeColors.ts` byte-for-byte from the original `EventMarker.svelte:60-113` palette (campaign=#dc2626, holiday=#16a34a, recurring=#eab308, school_holiday=#5eead4, transit_strike=#dc2626). |
| SC3 | `eventTypeColors.ts` palette + EVENT_PRIORITY exports                       | VERIFIED                | `src/lib/eventTypeColors.ts` (24L) exports `EVENT_TYPE_COLORS` (5 entries match palette spec) and re-exports `EVENT_PRIORITY` from `forecastEventClamp.ts`. Imported by EventBadgeStrip + ChartHoverPopup + 5 wired cards.            |
| SC4 | `EventBadgeStrip` + `ChartHoverPopup` primitives substantive + safe         | VERIFIED                | EventBadgeStrip 175 lines (≥80), one-badge-per-bucket logic in `eventsByBucket` derived map + `highestPriorityType()` helper + 44px MIN_TAP_PX promotion + corner-count `5+` rollup at threshold 4 + auto-flip via `anchorRect.top < 60`. ChartHoverPopup 106 lines (≥50) with `min-w-[200px] max-w-[280px]` shell + month-grain Show-all expander at threshold 10. Zero `{@html}` in either file. 13/13 vitest tests passing locally. |
| SC5 | EventBadgeStrip wired into 5 date-axis cards; 4 excluded cards NOT wired    | VERIFIED                | `grep -c EventBadgeStrip` per card: CalendarRevenueCard=4, CalendarCountsCard=5, CalendarItemsCard=6, CalendarItemRevenueCard=6, RepeaterCohortCountCard=6 (all ≥4). CohortRetentionCard=0, DailyHeatmapCard=0, MdeCurveCard=0, CampaignUpliftCard=0. C-12 (no inline hex) + C-02 (`touchEvents:'auto'`) preserved in all 5 wired cards. |
| SC6 | Mobile QA at 375×667 in ja+en                                                | VERIFIED                | 16.3-08-SUMMARY: localhost:5173 root reload returned `body { overflow-x: hidden }`, 10 ja-locale h2 cards rendered, 4 EventBadgeStrip mounts in immediate viewport (5th behind LazyMount, user-confirmed). Per memory `feedback_chrome_mcp_on_request.md`, Wave 3 plan structure was the explicit ask. |
| SC7 | Localhost-first gate; zero console errors                                    | VERIFIED                | 16.3-08-SUMMARY: zero matches in console pattern `error\|invalid_default_snippet\|warn\|Uncaught\|TypeError\|ReferenceError\|Failed`. User sign-off captured: "i confirm all the changes are reflected. move on." (2026-05-06). |
| SC8 | Friend-persona acceptance — campaign tap test + scan test                   | UNCERTAIN (human_needed) | Documented PENDING in HUMAN-UAT.md SC8 row. Per Wave 3 plan, owner sign-off happens post-/gsd-ship via DEV branch URL — this is the planned acceptance path, not a gap. Routed to `human_verification` per verifier instruction. |
| SC9 | Planning-docs drift gate (validate-planning-docs.sh exits 0)                 | VERIFIED                | Live validator run: `✅ planning docs in sync` → ROADMAP 19/20 phases, STATE 94/106 plans, Disk 94/106 plans, exit 0. ROADMAP line 52 ticked `[x]`; STATE.md frontmatter `completed_phases=19`, `completed_plans=94`. |

**Score:** 8/9 truths verified (SC8 routed to human verification per the documented post-merge sign-off path; not a gap).

### Required Artifacts

| Artifact                                                            | Expected                                                                                            | Status        | Details                                                                                                |
| ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------ |
| `src/lib/eventTypeColors.ts`                                        | Exports EVENT_TYPE_COLORS (5 hex values from EventMarker palette) + re-exports EVENT_PRIORITY       | VERIFIED      | 24L; palette byte-exact; 5 imports across components.                                                  |
| `src/lib/components/EventBadgeStrip.svelte`                         | ≥80L; one-badge-per-bucket; highest-priority color; corner count `5+`; 44px tap; auto-flip; no @html | VERIFIED      | 175L; all spec features present; XSS gate clean.                                                       |
| `src/lib/components/ChartHoverPopup.svelte`                         | ≥50L; per-bucket popup; show-all expander on month grain; no @html                                  | VERIFIED      | 106L; SHOW_ALL_THRESHOLD=10 with month-grain gate; XSS gate clean.                                     |
| `src/routes/api/forecast/+server.ts` (range_start surface)          | Optional `?range_start=YYYY-MM-DD`; 400 on malformed or future date; backward compatible            | VERIFIED      | Lines 78–98: regex validation, future-date rejection, fallback to `todayStr` when omitted.            |
| `src/lib/forecastOverlay.svelte.ts` (rangeStart input + typed events) | `rangeStart?: () => string \| null`; `events: ForecastEvent[]` typed                                 | VERIFIED      | Lines 33, 60–61, 75, 121, 125 — getter wired through to `&range_start=...` URL param + cache-key.   |
| `src/lib/components/CalendarRevenueCard.svelte`                     | Imports + renders EventBadgeStrip; `touchEvents:'auto'` preserved; no inline hex                    | VERIFIED      | 4 EventBadgeStrip refs; touchEvents at line 268; 0 inline hex.                                         |
| `src/lib/components/CalendarCountsCard.svelte`                      | (same)                                                                                              | VERIFIED      | 5 refs; touchEvents preserved; 0 inline hex.                                                            |
| `src/lib/components/CalendarItemsCard.svelte`                       | (same)                                                                                              | VERIFIED      | 6 refs; touchEvents preserved; 0 inline hex.                                                            |
| `src/lib/components/CalendarItemRevenueCard.svelte`                 | (same)                                                                                              | VERIFIED      | 6 refs; touchEvents preserved; 0 inline hex.                                                            |
| `src/lib/components/RepeaterCohortCountCard.svelte`                 | (same)                                                                                              | VERIFIED      | 6 refs; touchEvents preserved; 0 inline hex.                                                            |
| `src/lib/components/RevenueForecastCard.svelte`                     | DELETED                                                                                             | VERIFIED      | Absent from disk.                                                                                      |
| `src/lib/components/InvoiceCountForecastCard.svelte`                | DELETED                                                                                             | VERIFIED      | Absent from disk.                                                                                      |
| `src/lib/components/ForecastHoverPopup.svelte`                      | DELETED (CSS lifted into ChartHoverPopup)                                                           | VERIFIED      | Absent from disk; CSS lift recorded in `16.3-popup-css-lift.md`.                                       |
| `src/lib/components/EventMarker.svelte`                             | DELETED (palette extracted per D-04)                                                                | VERIFIED (override) | Absent from disk; palette in `eventTypeColors.ts`. Override accepted via D-04 (CONTEXT.md line 109).   |

### Key Link Verification

| From                                  | To                                       | Via                                                            | Status   | Details                                                                                                                  |
| ------------------------------------- | ---------------------------------------- | -------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------ |
| 5 wired cards                         | `EventBadgeStrip.svelte`                 | `import EventBadgeStrip from './EventBadgeStrip.svelte'`       | WIRED    | All 5 imports listed by `grep -rln`; usage counts 4–6 per file (component instantiations).                              |
| `EventBadgeStrip.svelte`              | `ChartHoverPopup.svelte`                 | `import ChartHoverPopup` + `<ChartHoverPopup events={...} />`  | WIRED    | Line 51 import; line 170 instantiation gated by `openBucketIso === b.iso`.                                              |
| `EventBadgeStrip.svelte`              | `eventTypeColors.ts`                     | `import { EVENT_TYPE_COLORS, EVENT_PRIORITY }`                 | WIRED    | Line 49; consumed at lines 92 (priority) + 142 (background-color).                                                      |
| 5 wired cards                         | `forecastOverlay.svelte.ts` `rangeStart` | `rangeStart: () => string \| null` getter passed through       | WIRED    | Forecast overlay reads getter at line 121, builds `&range_start=...` query param at line 125.                            |
| `forecastOverlay.svelte.ts`           | `/api/forecast?range_start=...`          | `fetch('/api/forecast?...&range_start=' + encodeURIComponent)` | WIRED    | Line 125 URL construction; server validates at lines 86–97.                                                              |
| `/api/forecast/+server.ts`            | events feed widening                     | `eventsStart = rangeStart ?? todayStr`                          | WIRED    | Line 115 — caller-supplied range backs through to events query lower bound.                                              |
| `ChartHoverPopup.svelte`              | i18n labels for 5 event types            | `t(page.data.locale, 'event_type_<name>')`                     | WIRED    | 45 i18n keys present (9 keys × 5 locales) covering all event-type / popup labels.                                       |

### Data-Flow Trace (Level 4)

| Artifact                              | Data Variable     | Source                                                           | Produces Real Data | Status      |
| ------------------------------------- | ----------------- | ---------------------------------------------------------------- | ------------------ | ----------- |
| EventBadgeStrip.svelte                | `events` prop     | Caller card → `forecastOverlay.events` → `/api/forecast` events feed → real DB query of holidays / school_holidays / recurring_events / campaign_calendar / transit_alerts | YES (DB-backed)    | FLOWING     |
| ChartHoverPopup.svelte                | `events` prop     | Forwarded from EventBadgeStrip's `bucketEvents` slice            | YES                | FLOWING     |
| 5 wired cards (EventBadgeStrip slot)  | overlay state     | `createForecastOverlay({ rangeStart: () => earliest_bucket_iso })` reactive store; events typed `ForecastEvent[]` | YES                | FLOWING     |

### Behavioral Spot-Checks

| Behavior                                                            | Command                                                                  | Result                                                              | Status |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------- | ------ |
| EventBadgeStrip + ChartHoverPopup unit tests pass                   | `npx vitest run src/lib/components/{EventBadgeStrip,ChartHoverPopup}.test.ts` | 13 passed, 0 failed (Test Files 2 passed, Duration 2.42s)           | PASS   |
| svelte-check baseline preserved (no NEW errors in 16.3 surface)     | `npx svelte-check --tsconfig ./tsconfig.json --threshold error`           | 7 errors total, all pre-existing (vite.config.ts, hooks.server.ts × 5, CalendarRevenueCard:233 — pre-16.3 issue) | PASS   |
| validate-planning-docs.sh drift gate                                | `bash .claude/scripts/validate-planning-docs.sh`                          | `✅ planning docs in sync` → exit 0                                | PASS   |
| Deleted forecast components have 0 references in src/ (excl. doc-comment exception) | `grep -rln 'RevenueForecastCard\|InvoiceCountForecastCard\|ForecastHoverPopup\|EventMarker' src/` | 1 match — `ChartHoverPopup.svelte` line 10 (intentional design-context comment per orchestrator note) | PASS   |
| `range_start` query param validation in /api/forecast                | `grep -n range_start src/routes/api/forecast/+server.ts`                  | 9 lines including 400 returns for malformed + future dates           | PASS   |

### Requirements Coverage

| Requirement | Source Plan(s)                                       | Description                                                                                                                                              | Status                                | Evidence                                                                                                                                                                                                                       |
| ----------- | ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| UPL-05      | 16.3-01, 16.3-02, 16.3-03, 16.3-05, 16.3-06, 16.3-07, 16.3-08 | `naive_dow_uplift_eur` cross-check column already closed by Phase 16.1-03 (REQUIREMENTS.md row line 398 = "Closed"). 16.3 references it as a friend-persona-acceptance carry-forward (event visibility outside forecast cards). | SATISFIED (no regression introduced) | UPL-05 closed pre-16.3; 16.3 made no campaign_uplift_v changes. Friend-persona event visibility now extended to 5 calendar/cohort cards via EventBadgeStrip (SC1–SC5).                                                       |
| FUI-09      | 16.3-01, 16.3-03, 16.3-04, 16.3-07, 16.3-08          | "All v1.3 frontend components verified at localhost:5173 via Chrome MCP per .claude/CLAUDE.md localhost-first rule BEFORE any DEV deploy"               | SATISFIED                             | SC7 verified (zero console errors at localhost:5173 ja-locale). Wave 3 (16.3-08) Chrome MCP smoke test in 16.3-08-SUMMARY confirms every modified component verified before /gsd-ship. REQUIREMENTS.md row stays "Pending" because FUI-09 is a rolling cross-phase requirement — not a 16.3-specific close. |

### Anti-Patterns Found

| File                                                  | Line                          | Pattern                                                                                                | Severity | Impact                                                                                              |
| ----------------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------ | -------- | --------------------------------------------------------------------------------------------------- |
| `src/lib/components/ChartHoverPopup.svelte`           | 10–11                         | Reference to deleted `ForecastHoverPopup.svelte` in design-context comment                              | Info     | Documented intentional exception per verifier instruction + Wave 3 Task 1 PARTIAL gate explanation. |
| `src/lib/components/CalendarRevenueCard.svelte`       | 233:28                        | `'w' is possibly 'undefined'` (svelte-check error)                                                     | Info     | Pre-existing error from Phase 16.1; not introduced by 16.3 wiring.                                  |
| `src/hooks.server.ts`                                 | 23, 24, 38                    | Implicit `any` types on cookies handler                                                                 | Info     | Pre-existing errors; not in 16.3 surface.                                                            |
| `vite.config.ts`                                      | 7                             | `Object literal may only specify known properties, and 'test' does not exist`                          | Info     | Pre-existing error; not in 16.3 surface.                                                             |

No 16.3-introduced anti-patterns or stubs found. No `TODO/FIXME/PLACEHOLDER` markers. No empty handlers. No hardcoded empty data on rendered props. No `console.log`-only implementations.

### Human Verification Required

#### 1. Friend-persona acceptance (SC8)

**Test:** Owner opens dashboard on her phone (375×667, `ja` locale), scrolls to `CalendarRevenueCard` at day grain, taps the badge on 2026-04-14, reads the campaign name + date + type in the popup. Three deleted forecast cards (`RevenueForecastCard`, `InvoiceCountForecastCard`, `ForecastHoverPopup`) are absent from the scroll path AND owner verbally confirms she does not miss them.

**Expected:** Tap-test → popup shows "2026-04-14" header + campaign name + localized type label ("キャンペーン"). Scan-test → owner confirms 3 forecast cards gone and not missed.

**Why human:** Codebase verifier cannot speak for the friend-owner. Plan 16.3-08 explicitly defers SC8 to the post-`/gsd-ship` sign-off cycle (HUMAN-UAT.md SC8 row carries PENDING by design). DEV branch URL must exist for owner to test on her phone.

### Gaps Summary

No structural gaps. All 8 codebase-verifiable success criteria pass. The single remaining item (SC8 friend-persona acceptance) is a documented owner-side sign-off scheduled for post-`/gsd-ship` — not a code defect.

Two intentional deviations are accepted via overrides:

1. **EventMarker.svelte deletion (SC2 wording).** ROADMAP SC2 listed `EventMarker.svelte` as preserved-on-disk; CONTEXT.md D-04 (locked 2026-05-06) re-interpreted this as "the forecast pipeline preserves the visual encoding it provided" via the new `eventTypeColors.ts` module. Palette byte-exact match verified.
2. **ChartHoverPopup.svelte design-context comment (lines 10–11).** Per Wave 3 Task 1 PARTIAL gate explanation in 16.3-08-SUMMARY and the verifier instruction, this single residual reference to the deleted `ForecastHoverPopup.svelte` is a maintainer-aid comment, not a stale import. Kept by design.

---

_Verified: 2026-05-06T11:35:00Z_
_Verifier: Claude (gsd-verifier)_
