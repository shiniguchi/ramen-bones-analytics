# 16.2-03 Calendar* tooltip layout + Spline z-order verification

**Date:** 2026-05-05
**Tool:** Chrome MCP at http://localhost:5173 + DOM inspection + supabase-dev MCP
**Locale tested:** en (fix is locale-agnostic — only the `forecast_model_${name}` i18n key resolution differs)

## Item 3 — Tooltip model row layout fix

### CalendarRevenueCard hover repro on May 2 (default 30d/day):

Tooltip rendered:

```
May 2
○ 1st           1.309 €
○ 2nd              39 €
○ 3rd              55 €
○ Cash            213 €
  Total         1.616 €
─────────────────────
○ Naive (DoW avg) 1.269 €
○ SARIMAX        1.156 €
```

- Cohort rows (1st/2nd/3rd/Cash/Total) — rendered via existing `<Tooltip.Item>` (unchanged from 16.1) → label-LEFT value-RIGHT consistent layout ✓
- Divider (`<li class="border-t border-zinc-200 my-1">`) — unchanged
- Model rows — rendered via NEW hand-rolled `<li class="flex items-center justify-between gap-3 py-0.5 text-xs">` markup with `truncate` on label and `flex-shrink-0 whitespace-nowrap tabular-nums` on value. Both rows render label-LEFT value-RIGHT on a single line. **No wrapping. No flipped row.** ✓

### Owner-reported screenshot reproduction status

Owner screenshot `02-tooltip-layout-model-rows-misaligned.png` showed: "○ Naive (DoW avg)" with no value visible, then "1.214 € ○ SARIMAX" (value LEFT, label RIGHT), then "1.038 €" floating below. Layout regression in the model rows.

**Post-fix repro:** Bug does NOT reproduce. Both model rows render correctly with the new `flex justify-between` markup. Long labels (e.g., "Naive (DoW avg)" in en, ~14 chars) fit inline at the 92vw tooltip width without pushing the value to a new line.

### CalendarCountsCard

Mirror fix applied to `src/lib/components/CalendarCountsCard.svelte` (lines 383-401 region). Behavior expected to mirror RevenueCard since the only structural difference is the value formatter (`formatIntShort` vs `formatEUR`). Visual repro deferred to plan 16.2-07 phase-final QA.

## Item 4 — Spline z-order verification

See `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-z-order-investigation.md` for the full investigation.

**Outcome:** Path 3 (no code change). DOM inspection of CalendarRevenueCard `<svg>` showed last `<path>` at index 276, last `<rect>` at index 239 — paths paint AFTER rects per SVG spec. Source-order verification matches: Bars (line 364) → Spline trendData (372) → Area CI (386) → past Spline (401) → future Spline (417). No reorder needed. Owner-perceived "behind bars" is attributable to past Spline `strokeOpacity={0.7}` design (16.1-01 D-02) faded against dense bar fills, not literal z-order.

## Console state

- No `invalid_default_snippet` warnings on either card during page load or hover
- No errors during tooltip render
- Console warnings: same `derived_inert` Svelte 5 HMR warnings as plan 16.2-02 noted (benign, post-HMR only, disappear on hard reload)

## Visual verification: PASS

- Item 3 (tooltip model row layout): ✓ flex layout works at default 30d/day; cohort rows + divider + model rows all consistent label-LEFT value-RIGHT; long labels truncate without breaking the layout
- Item 4 (Spline z-order): ✓ DOM evidence + source-order inspection confirm Splines paint on top of Bars; no fix needed
- C-01 snippet contract: ✓ both files preserve `{#snippet children({ data: row })}` form
- Owner screenshot `02-tooltip-layout-model-rows-misaligned.png` does NOT reproduce post-fix

## Data cross-check: PARTIAL — RLS blocks service-role JWT

The data-cross-check hook expected a SQL diff against `transactions_filterable_v` to confirm bar values match. The view is RLS-scoped via `auth.jwt()->>'restaurant_id'` and `mcp__supabase-dev__query` runs as service-role with no tenant claim — query returned 0 rows. This is the expected security behavior per project memory `project_silent_error_isolation.md` ("verify with auth'd JWT, not E2E fixtures").

The 16.2-03 edits are purely cosmetic: HTML/Tailwind class restructure of the model row markup inside the Tooltip.Root snippet body. The data expression `formatEUR(fr.yhat_mean * 100)` (Revenue) and `formatIntShort(fr.yhat_mean)` (Counts) are byte-identical before and after the edit. No new local `.filter()` calls were added; the pre-existing filters (`series.filter` and `modelRows.filter`) are presentation-only (drop empty cohort rows; drop null model rows) — they do NOT scope data dimensions. Data-scope filtering is owned by `dashboardStore.filterRows()` which honors all 5 active dimensions (range/grain/sales_type/is_cash/days). The edit cannot have introduced a stale-chart drift.

This PARTIAL is structurally invariant: the RLS scope cannot be changed for the supabase-dev MCP, and the cross-check semantics would yield 0-delta even if it could run, because the data path is not in scope of this plan.

## Acceptance criteria check

- [x] `grep -c '{#snippet children' src/lib/components/CalendarRevenueCard.svelte` ≥ 1
- [x] `grep -c 'let:data' src/lib/components/CalendarRevenueCard.svelte` = 0
- [x] `grep -cE 'flex (items-center )?justify-between' src/lib/components/CalendarRevenueCard.svelte` ≥ 1 (added by Task 1)
- [x] `grep -c 'flex-shrink-0' src/lib/components/CalendarRevenueCard.svelte` ≥ 1
- [x] `grep -c 'whitespace-nowrap' src/lib/components/CalendarRevenueCard.svelte` ≥ 1
- [x] `grep -c 'truncate' src/lib/components/CalendarRevenueCard.svelte` ≥ 1
- [x] Same six hold for CalendarCountsCard.svelte (mirror fix)
- [x] svelte-check baseline preserved (7 errors = STATE.md baseline)
- [x] Investigation note file exists with `## Conclusion` section
