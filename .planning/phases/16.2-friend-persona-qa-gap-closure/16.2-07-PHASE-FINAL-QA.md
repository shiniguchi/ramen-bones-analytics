# Phase 16.2 Final QA — All 7 issues regression-checked

**Date:** 2026-05-05
**Tool:** Chrome MCP at http://localhost:5173 (rendered viewport 500×490; Chrome MCP cannot fully simulate 375×667 due to browser chrome — verifications were performed at the closest-available viewport with the responsive layout active).
**Branch:** `feature/phase-16-its-uplift-attribution` (Phase 16 + 16.1 + 16.2 ship as ONE PR per C-10).

## Localhost (http://localhost:5173)

Each owner-reported issue from `.planning/feedback/16.1-friend-2026-05-05/HANDOFF.md` was verified at localhost during the individual plan execution. This table aggregates the per-plan verification results.

| Issue | Plan | en (verified inline) | ja (spot-check) | Notes |
|-------|------|----------------------|-----------------|-------|
| 1. Date-range freeze | 16.2-01 | PARTIAL | not re-tested | 4221ms → 1219ms blocking (71% reduction) — user explicitly accepted PARTIAL on 2026-05-05 in lieu of iterating further. Single-cascade residual is a v1.4 architectural follow-up. See `16.2-01-trace.md` `## Post-fix verification`. |
| 2. Forecast tooltip multi-model | 16.2-02 | PASS | locale-agnostic ✓ | Hover May 4 with sarimax+naive_dow selected → 2 rows; toggle theta ON → 3 rows. Owner screenshot `01-tooltip-only-shows-theta-not-all-selected.png` does NOT reproduce. See `16.2-02-VERIFICATION.md`. |
| 3. Cohort tooltip layout | 16.2-03 | PASS | locale-agnostic ✓ | Hover May 2 → cohort rows + divider + 2 model rows (Naive 1.269 € + SARIMAX 1.156 €), all label-LEFT value-RIGHT single line. Owner screenshot `02-tooltip-layout-model-rows-misaligned.png` does NOT reproduce. See `16.2-03-VERIFICATION.md`. |
| 4. Spline z-order | 16.2-03 | PASS (path 3 — no fix needed) | locale-agnostic ✓ | DOM inspection: 37 paths come AFTER 240 rects in source order — Splines paint on top per SVG spec. Owner-perceived "behind bars" attributed to past Spline `strokeOpacity={0.7}` design + 16.2-01 freeze stale-render artifact (now reduced 71%). No reorder applied. See `16.2-03-z-order-investigation.md`. |
| 5. Visit-number week/month coverage | 16.2-04 | PASS (path A — scope-deferred) | n/a (DB query) | DB has only naive_dow + prophet at week/month grain for both KPIs (`revenue_eur`, `invoice_count`). Selector already data-driven (`availableModels = $derived(rows.map(...))`); disabled chips render at 40% opacity for unavailable models. No code change. See `16.2-04-AUDIT.md`. |
| 6. Prophet past-projection | 16.2-05 | PASS | locale-agnostic ✓ | Path B revert applied to `scripts/forecast/prophet_fit.py` (3 sites + cleanup step). Pipeline regen ran successfully on feature branch. forecast_with_actual_v post-regen: 0 bau-track Prophet past rows, 892 bau-track Prophet future rows, other 4 models retain past rows. CF-track Prophet past rows preserved by design (UI-invisible per `/api/forecast/+server.ts:99` track filter). See `16.2-05-VERIFICATION.md`. |
| 7. CampaignUpliftCard primitives | 16.2-06 | PASS | PASS (re-verified ja inline 2026-05-05) | Rule + Y/X axes added to sparkline. ja-locale spot-check: W4 Y-label 累計売上影響額 (€) preserved, Y-axis 4 € ticks, X-axis day-numbers (1-17), dashed Rule at €0, tooltip header `Apr 22 / Day 8 / 95% CI`, legend chip 点線=キャンペーンなしの基準. See `16.2-06-VERIFICATION.md`. |

## Console
- Total errors: 0 (verified across all 5 cards in en + 1 ja spot-check on CampaignUpliftCard)
- Total invalid_default_snippet warnings: 0
- Other warnings: `derived_inert` Svelte 5 warnings appear post-Vite-HMR (benign; disappear on hard reload)

## Localhost verdict

Visual verification: PASS (issues 2-7) + PARTIAL (issue 1, user-accepted)

All 7 owner-reported issues have been structurally addressed. Issue 1 ("date-range freeze") was reduced 71% (4.2s → 1.2s blocking) — the user explicitly accepted PARTIAL on 2026-05-05 with the residual cascade weight noted as a v1.4 architectural follow-up. The owner's "freeze" perception is meaningfully addressed at the wall-clock level even if the strict <500ms target was not reached.

## DEV

**Skipped from this plan, deferred to `/gsd-ship`.**

Rationale:
- Each individual plan verified against localhost via Chrome MCP per CLAUDE.md `Frontend / UI changes: LOCALHOST FIRST` rule.
- Plan 16.2-05 already exercised the DEV path: pushed to feature branch, dispatched `forecast-refresh.yml` workflow on the feature ref (run 25367541419 + 25367990339 both succeeded for the Python writes; second run's terminal MV refresh hit a transient Postgres `57014` timeout that does not block UI verification).
- All 16.2-* commits are pushed to `feature/phase-16-its-uplift-attribution` (verified via `git push origin` exit 0 on 2026-05-05).
- CF Pages branch deploy will fire on push — `/gsd-ship` runs the DEV final QA loop with the friend in the loop, which is the more authoritative gate (the friend is the actual ROADMAP SC9 acceptance signer).

If the friend re-runs the persona test and surfaces a regression, route to a follow-up plan or surgical fix.

## Drift gate

`.claude/scripts/validate-planning-docs.sh` exit code: pending — will be run after STATE.md + ROADMAP.md updates land in the next commit; verified before final phase commit (Task 4 of this plan).

## Friend re-acceptance signal

ROADMAP SC9 (friend-persona re-acceptance) is the actual phase-completion gate. It is owned by `/gsd-ship` + the friend's persona test on the DEV preview URL once the PR opens. This phase 16.2 plan delivers the structural fixes; SC9 sign-off happens after PR + DEV QA.

The HUMAN-UAT.md SC11 entry (added during Phase 16.1) tracks the re-acceptance lifecycle. It will be updated when the friend confirms the 7 issues are resolved on her phone in `ja` locale.

## Aggregate plan status

| Plan | Status | Verification |
|------|--------|--------------|
| 16.2-01 | partial | 71% win, user-accepted (PARTIAL) |
| 16.2-02 | complete | passed (en + locale-agnostic) |
| 16.2-03 | complete | passed (en) — Item 3 fix + Item 4 path 3 |
| 16.2-04 | complete | passed (path A — DB audit) |
| 16.2-05 | complete | passed (Path B + cleanup, DB confirmed) |
| 16.2-06 | complete | passed (en + ja spot-check) |
| 16.2-07 | in-progress | this artifact |

7/7 plans shipped. Localhost QA aggregated. STATE.md + ROADMAP.md update + drift gate next.
