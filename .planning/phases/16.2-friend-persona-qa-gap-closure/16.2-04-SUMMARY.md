---
phase: 16.2-friend-persona-qa-gap-closure
plan: 04
status: complete
verification: passed
started: 2026-05-05
completed: 2026-05-05
---

# 16.2-04 SUMMARY — Visit-number week/month forecast coverage audit

## Audit result

DB state verified via `mcp__supabase-dev__query` against Supabase DEV `forecast_with_actual_v`:

| KPI | Grain | Models present |
|---|---|---|
| revenue_eur | week | naive_dow, prophet |
| revenue_eur | month | naive_dow, prophet |
| invoice_count | week | naive_dow, prophet |
| invoice_count | month | naive_dow, prophet |

`sarimax`, `ets`, `theta` rows do NOT exist for week/month at either KPI. Owner's observation is correct — pipeline ships only `naive_dow` + `prophet` at non-day grain by design.

## Conclusion path: A — scope-deferred

The pipeline only produces 2 of the 5 models for non-day granularity, by design. The visit-number forecast cards correctly render only those 2 models when grain is week/month.

## Follow-up

**None.** Selector is already data-driven:

- `availableModels = $derived(Array.from(new Set(rows.map(r => r.model_name))))` at `RevenueForecastCard.svelte:73`, `CalendarRevenueCard.svelte:151`, `CalendarCountsCard.svelte:141`.
- `ForecastLegend.svelte:43-82` disables chips not in `availableModels` (40% opacity, click no-ops).

Owner-experienced behavior matches the data state by construction. No code change in 16.2.

## Key files

- `created`: `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-04-AUDIT.md`
- `created`: `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-04-SUMMARY.md`
- `modified`: none

## Self-Check: PASSED

- Both audit queries run successfully against Supabase DEV
- AUDIT.md committed with both query/result tables, "Models present" summary, named Path A conclusion, and explicit "no selector edit needed (data-driven)" note
- Plan acceptance criteria met: file exists, ≥6 H2 sections, named Path A, no follow-up plan needed (selector data-driven)
- Existing v1.4 deferred entry in 16.2-CONTEXT.md `<deferred>` covers the future-pipeline backlog
