---
phase: 16.2-friend-persona-qa-gap-closure
plan: 02
type: execute
wave: 2
depends_on: [16.2-01]
autonomous: false
requirements: [SC-2]
files_modified:
  - src/lib/components/RevenueForecastCard.svelte
  - src/lib/components/InvoiceCountForecastCard.svelte
  - src/lib/components/ForecastHoverPopup.svelte
must_haves:
  truths:
    - "Owner hovers any bucket on RevenueForecastCard with sarimax + naive_dow selected (theta NOT selected) and the tooltip shows TWO rows — one for sarimax, one for naive_dow — and ZERO rows for theta or any unselected model"
    - "Owner hovers any bucket on InvoiceCountForecastCard with multiple models selected and the tooltip shows ALL selected models with values, not just one"
    - "The Highlight dot for each selected model lands ON its corresponding Spline path within ±1px (verified by DOM inspection at 3 sample x-positions per card)"
    - "No `invalid_default_snippet` runtime warning fires (Tooltip.Root snippet contract honored — C-01)"
    - "Tooltip respects `visibleModels` state from ForecastLegend toggles"
  artifacts:
    - path: "src/lib/components/RevenueForecastCard.svelte"
      provides: "Multi-model tooltip + per-Spline Highlight binding"
      contains: "filter.*selectedModels|visibleModels.has"
    - path: "src/lib/components/InvoiceCountForecastCard.svelte"
      provides: "Mirror of RevenueForecastCard fix"
      contains: "filter.*selectedModels|visibleModels.has"
    - path: "src/lib/components/ForecastHoverPopup.svelte"
      provides: "Either deprecated (replaced by inline Tooltip.List rows) OR refactored to accept multiple model rows"
      exports: "(updated component contract OR deletion)"
  key_links:
    - from: "RevenueForecastCard.svelte Tooltip.Root snippet body"
      to: "splitSeriesByModel + visibleModels"
      via: "filter then map"
      pattern: "data\\?\\..*filter.*model_name|seriesByModel.*has"
    - from: "Per-Spline Highlight"
      to: "modelData (NOT chart-level data)"
      via: "data prop binding"
      pattern: "<Highlight.*data=|<Highlight.*lines.*points"
---

<objective>
Item 2 — Fix the forecast tooltip on `RevenueForecastCard.svelte` + `InvoiceCountForecastCard.svelte` so it (a) renders rows for EVERY selected model on every hovered bucket where data exists (not just one), (b) filters to only models the owner has toggled on, and (c) the per-Spline `<Highlight>` dot lands on its corresponding Spline path within ±1px.

Owner reported (verbatim 2026-05-05): *"i can somehow only see individually theta and ets popup bubbles but not sarimax and naive even those they are the ones selected and ets and theta are not selected. you need to only show the datapoints which the forecast methods are selected, and you must show all the available datapoints if they are shown."*

Screenshot evidence: `01-tooltip-only-shows-theta-not-all-selected.png` shows tooltip with only "theta" 804€ + 95% CI when sarimax + naive_dow were selected.

Purpose: ROADMAP SC2. Friend-persona acceptance gate cannot pass while the forecast tooltip is misleading the owner about which models are predicting which value.

Output: Both forecast cards' tooltips render multi-model `<Tooltip.Item>` rows filtered to `visibleModels`, with per-Spline `<Highlight>` dots correctly bound. Honors C-01 (`{#snippet children({ data })}` form, never `let:data`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md
@.planning/feedback/16.1-friend-2026-05-05/HANDOFF.md
@.planning/feedback/16.1-friend-2026-05-05/01-tooltip-only-shows-theta-not-all-selected.png
@src/lib/components/CalendarRevenueCard.svelte
@CLAUDE.md
@.claude/CLAUDE.md

<interfaces>
<!-- Pattern to mimic — CalendarRevenueCard.svelte tooltip body (lines 442-479, the GOOD pattern landed in 16.1-01). -->
<!-- This pattern correctly: (a) finds matching rows per visible model via splitSeriesByModel.entries(), (b) filters by ISO date match, (c) renders one Tooltip.Item per matching row. -->

<!-- CalendarRevenueCard.svelte:444-475 (reference): -->
<!--   {#snippet children({ data: row })}                                       -->
<!--     {@const bucketIso = fullRow?.bucket_d instanceof Date ? format(...) : null} -->
<!--     {@const modelRows = bucketIso === null ? [] : Array.from(splitSeriesByModel.entries()) -->
<!--       .map(([name, split]) => {                                            -->
<!--         const r = split.past.find((x) => x.target_date === bucketIso)      -->
<!--                ?? split.future.find((x) => x.target_date === bucketIso);   -->
<!--         return r ? { name, row: r } : null;                                -->
<!--       })                                                                    -->
<!--       .filter((x): x is { name: string; row: ForecastRow } => x !== null)} -->
<!--     {#if modelRows.length > 0}                                              -->
<!--       <Tooltip.Header>{fullRow?.bucket}</Tooltip.Header>                   -->
<!--       <Tooltip.List>                                                        -->
<!--         {#each modelRows as { name, row: fr } (`mr-${name}`)}              -->
<!--           <Tooltip.Item label={t(...)} color={FORECAST_MODEL_COLORS[name]} value={...}/> -->
<!--         {/each}                                                             -->
<!--       </Tooltip.List>                                                       -->
<!--     {/if}                                                                   -->
<!--   {/snippet}                                                                -->

<!-- ForecastHoverPopup.svelte (lines 1-100, current state) renders ONE row per call (single hoveredRow). -->
<!-- This is the bug: even with multi-model selection, only one model gets passed to the popup. -->
<!-- Fix options: -->
<!--   (a) Replace ForecastHoverPopup with inline Tooltip.List + per-model Tooltip.Item rows (matches CalendarRevenueCard pattern) -->
<!--   (b) Refactor ForecastHoverPopup to accept hoveredRows[] and iterate -->
<!-- Implementer chooses based on minimal-diff principle. (a) preferred per CalendarRevenueCard precedent. -->

<!-- splitSeriesByModel structure (existing in both Forecast cards from 16.1-05): -->
<!--   Map<string, { past: ForecastRow[]; future: ForecastRow[] }> -->
<!--   Keyed by model_name; each entry has past + future arrays. -->

<!-- visibleModels structure: -->
<!--   Set<string> of model names from ForecastLegend toggles. -->
<!--   Already drives which Splines render in the {#each visibleModels as m} block. -->

<!-- Per-Spline Highlight binding (D-05): -->
<!--   Currently: a single <Highlight points lines /> at chart level (line 287 in RevenueForecastCard). -->
<!--   Bug: bisect-x returns closest data point regardless of model, so dots drift. -->
<!--   Fix: move <Highlight> INSIDE the {#each visibleModels as m} block, bind to that model's data. -->
<!--   Pattern: <Highlight data={modelData} points lines /> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix multi-model tooltip rows + verify C-01 snippet contract</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md (D-04, D-05, D-06, C-01)
    - .planning/feedback/16.1-friend-2026-05-05/HANDOFF.md (Item 2 verbatim)
    - .planning/feedback/16.1-friend-2026-05-05/01-tooltip-only-shows-theta-not-all-selected.png (visual ground truth)
    - .claude/memory/feedback_svelte5_tooltip_snippet.md (C-01 contract)
    - src/lib/components/CalendarRevenueCard.svelte (lines 442-479 — reference pattern that works)
    - src/lib/components/RevenueForecastCard.svelte (lines 1-310, current Tooltip.Root + Highlight + ForecastHoverPopup state)
    - src/lib/components/InvoiceCountForecastCard.svelte (lines 1-310, mirror)
    - src/lib/components/ForecastHoverPopup.svelte (current single-row consumer)
  </read_first>
  <files>src/lib/components/RevenueForecastCard.svelte, src/lib/components/InvoiceCountForecastCard.svelte</files>
  <action>
    Open `src/lib/components/RevenueForecastCard.svelte`. Locate the `<Tooltip.Root>` block (line ~290).

    Replace its current snippet body (which calls `<ForecastHoverPopup hoveredRow={...}>` with a single model row) with a multi-model `Tooltip.List` pattern modeled directly on `CalendarRevenueCard.svelte:444-479`:

    ```svelte
    <Tooltip.Root contained="window" class="max-w-[92vw]">
      {#snippet children({ data })}
        {@const bucketIso = data?.target_date_d instanceof Date
          ? format(data.target_date_d, 'yyyy-MM-dd')
          : null}
        {@const modelRows = bucketIso === null
          ? []
          : Array.from(splitSeriesByModel.entries())
              .filter(([name]) => visibleModels.has(name))   // D-04: filter to selected models only
              .map(([name, split]) => {
                const r = split.past.find((x) => x.target_date === bucketIso)
                       ?? split.future.find((x) => x.target_date === bucketIso);
                return r ? { name, row: r } : null;
              })
              .filter((x): x is { name: string; row: ForecastRow } => x !== null)}
        {#if modelRows.length > 0}
          <Tooltip.Header>{format(data.target_date_d as Date, 'MMM d')}</Tooltip.Header>
          <Tooltip.List>
            {#each modelRows as { name, row: fr } (`mr-${name}`)}
              <Tooltip.Item
                label={t(page.data.locale, `forecast_model_${name}` as MessageKey)}
                color={FORECAST_MODEL_COLORS[name]}
                value={formatEUR((fr.yhat_mean as number) * 100)}
              />
            {/each}
          </Tooltip.List>
        {/if}
      {/snippet}
    </Tooltip.Root>
    ```

    Verify imports at top of file include: `Tooltip` (already imported per line 7), `format` from date-fns (likely already), `formatEUR` from `$lib/format`, `FORECAST_MODEL_COLORS` from `$lib/chartPalettes`, `t` + `MessageKey` from `$lib/i18n/messages`, `page` from `$app/state`. Add any missing imports.

    Apply byte-equivalent fix to `src/lib/components/InvoiceCountForecastCard.svelte` — substitute `formatEUR((fr.yhat_mean) * 100)` with `formatIntShort(fr.yhat_mean as number)` (counts are integers, not cents). Reuse the same multi-model row pattern.

    Confirm C-01: every `Tooltip.Root` body in the modified files uses `{#snippet children({ data })}` form — `let:data` is forbidden.

    Do NOT delete `ForecastHoverPopup.svelte` yet — leave the file in place; mark it as "deprecated, kept for diff legibility" via a top-of-file comment. (Cleanup deferred to a v1.4 polish task.)
  </action>
  <acceptance_criteria>
    - `grep -c '{#snippet children' src/lib/components/RevenueForecastCard.svelte` returns ≥1
    - `grep -c 'let:data' src/lib/components/RevenueForecastCard.svelte` returns 0
    - `grep -c 'visibleModels.has' src/lib/components/RevenueForecastCard.svelte` returns ≥1 (D-04 filter applied)
    - `grep -c 'splitSeriesByModel' src/lib/components/RevenueForecastCard.svelte` returns ≥1 (using the established Map structure)
    - `grep -c 'Tooltip.Item' src/lib/components/RevenueForecastCard.svelte` returns ≥1 (multi-row rendering)
    - Same 5 conditions hold for InvoiceCountForecastCard.svelte:
      - `grep -c '{#snippet children' src/lib/components/InvoiceCountForecastCard.svelte` returns ≥1
      - `grep -c 'let:data' src/lib/components/InvoiceCountForecastCard.svelte` returns 0
      - `grep -c 'visibleModels.has' src/lib/components/InvoiceCountForecastCard.svelte` returns ≥1
      - `grep -c 'splitSeriesByModel' src/lib/components/InvoiceCountForecastCard.svelte` returns ≥1
      - `grep -c 'Tooltip.Item' src/lib/components/InvoiceCountForecastCard.svelte` returns ≥1
    - svelte-check baseline preserved: `npm run check 2>&1 | grep -cE 'error' | tail -1` ≤ baseline (7 per STATE.md)
  </acceptance_criteria>
  <verify>
    <automated>npm run check 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Both Forecast cards render multi-model Tooltip.Item rows filtered by visibleModels. Snippet contract honored. svelte-check green.</done>
</task>

<task type="auto">
  <name>Task 2: Per-Spline Highlight binding fix (dot-on-line alignment)</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md (D-05)
    - src/lib/components/RevenueForecastCard.svelte (after Task 1 edit; current `<Highlight points lines />` at line ~287)
    - src/lib/components/InvoiceCountForecastCard.svelte (mirror at line ~287)
    - LayerChart Highlight docs (Context7: query "layerchart Highlight per-series data binding") — only if pattern unclear from existing code
  </read_first>
  <files>src/lib/components/RevenueForecastCard.svelte, src/lib/components/InvoiceCountForecastCard.svelte</files>
  <action>
    The current single chart-level `<Highlight points lines />` (line ~287 in both Forecast cards) uses the chart's default `bisect-x` to find the closest data point — which doesn't know about per-model splits, so the dot drifts off the per-Spline path.

    Move `<Highlight>` INSIDE the per-model `{#each visibleModels as m}` block(s) so each visible Spline gets its OWN Highlight bound to its OWN data. Pattern (modeled on LayerChart Highlight docs + the existing per-model Spline structure):

    ```svelte
    {#each Array.from(splitSeriesByModel.entries()) as [modelName, split] (`hl-${modelName}`)}
      {#if visibleModels.has(modelName)}
        {@const modelData = [...split.past, ...split.future].map(r => ({ ...r, d: parseISO(r.target_date) }))}
        {#if split.past.length > 0 || split.future.length > 0}
          <Highlight
            data={modelData}
            x={(r) => r.d}
            y={(r) => r.yhat_mean}
            points
            lines={false}
            color={FORECAST_MODEL_COLORS[modelName]}
          />
        {/if}
      {/if}
    {/each}
    ```

    Delete the existing chart-level `<Highlight points lines />` line.

    **PRE-EDIT GATE: Verify LayerChart Highlight supports per-series binding via Context7 BEFORE writing the loop above.** Query `mcp__Context7__resolve-library-id` for `layerchart`, then `mcp__Context7__query-docs` with topic `Highlight component data prop per-series binding`. Required to land on N-instance pattern (one `<Highlight>` per visible model, each bound to its model's `data` array). Do NOT rely on tribal knowledge — confirm the API supports this shape.

    **If Context7 confirms per-series binding works:** ship the loop above (preferred path; satisfies SC2(b) for ALL selected models).

    **If Context7 confirms per-series binding is NOT supported:** ship N parallel `<Highlight>` instances by hand-instantiating one component invocation per visible model with its own `data` binding (still N dots, just no `{#each}` sugar — semantically equivalent). Do NOT fall back to a single Highlight bound to one model — that would only satisfy SC2(b) for the first model and leave 4 of 5 dots drifting (goal-coverage failure). If neither N-loop nor N-parallel can be made to work after a documented Context7 + LayerChart source-read effort, escalate to user with a concrete reproduction; do not silently degrade.

    Apply byte-equivalent fix to InvoiceCountForecastCard.svelte.
  </action>
  <acceptance_criteria>
    - Per-model Highlight count matches `visibleModels.size` at any given hover state. Verify via DOM inspection at 3 sample x-positions: every selected model shows a Highlight dot on its Spline path within ±1px (NOT just the first model — explicit goal-coverage gate per SC2(b))
    - Either: per-model Highlight loop exists — `grep -c '<Highlight' src/lib/components/RevenueForecastCard.svelte` returns ≥1 AND that Highlight is INSIDE a `{#each ... visibleModels` or `{#each ... splitSeriesByModel.entries()` block (verify by inspecting line numbers)
    - OR: N parallel hand-instantiated `<Highlight>` invocations (one per active model) with each bound to its own model's `data` array — `grep -c '<Highlight' src/lib/components/RevenueForecastCard.svelte` returns ≥N where N is the count of forecast models (sarimax, naive_dow, ets, theta, prophet → 5)
    - chart-level standalone `<Highlight points lines />` (line 287 form) is REMOVED: `grep -nE '<Highlight points lines />$' src/lib/components/RevenueForecastCard.svelte` returns 0
    - Same checks hold for InvoiceCountForecastCard.svelte
    - svelte-check baseline preserved
  </acceptance_criteria>
  <verify>
    <automated>npm run check 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Each visible model's Spline has its own Highlight (or a justified fallback). svelte-check green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Localhost-first Chrome MCP verification at 375×667 in ja AND en</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md (C-04 localhost-first)
    - .claude/CLAUDE.md §"Frontend / UI changes: LOCALHOST FIRST"
    - .planning/feedback/16.1-friend-2026-05-05/01-tooltip-only-shows-theta-not-all-selected.png (the bug to verify is fixed)
  </read_first>
  <what-built>
    Multi-model tooltip rows + per-Spline Highlight binding on both Forecast cards (Tasks 1+2).
  </what-built>
  <how-to-verify>
    1. Ensure dev server running on http://localhost:5173.
    2. Chrome MCP open at viewport 375×667, locale `ja`.
    3. Navigate to dashboard → scroll to RevenueForecastCard.
    4. Open ForecastLegend; toggle: sarimax ON, naive_dow ON, theta OFF, ets OFF, prophet OFF.
    5. Hover any future bucket where multiple models predict.
    6. Verify tooltip shows TWO `<Tooltip.Item>` rows: one labeled SARIMAX (color matches palette), one labeled Naive (DoW). Verify NO theta / ets / prophet row.
    7. Toggle theta ON; re-hover same bucket; verify tooltip now shows THREE rows (sarimax + naive + theta).
    8. Visually verify each Highlight dot sits on its corresponding Spline line (within ~1px). Take screenshot.
    9. Repeat for InvoiceCountForecastCard.
    10. Switch locale to `en`; repeat steps 4-9.
    11. Open Chrome MCP `read_console_messages`; verify NO `invalid_default_snippet` warnings or errors.
    12. Save screenshots to `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-02-screenshots/` (4 files: revenue-ja, revenue-en, counts-ja, counts-en).
    13. State `Visual verification: PASS/FAIL/PARTIAL` in `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-02-VERIFICATION.md`.
  </how-to-verify>
  <acceptance_criteria>
    - 4 PNGs saved under `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-02-screenshots/`: `ls .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-02-screenshots/ | wc -l` returns ≥4
    - VERIFICATION.md exists with PASS line: `grep -E 'Visual verification: PASS' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-02-VERIFICATION.md` returns ≥1
    - VERIFICATION.md confirms zero `invalid_default_snippet` warnings: `grep -i 'invalid_default_snippet.*0\|no.*invalid_default_snippet\|console: clean' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-02-VERIFICATION.md` returns ≥1
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 3 tooltip rows + dot-alignment + zero console warnings checked across both ja+en. If FAIL, describe which assertion failed.</resume-signal>
</task>

</tasks>

<verification>
- Tooltip multi-row pattern matches CalendarRevenueCard reference
- C-01 snippet contract honored on both files
- visibleModels filter applied (D-04)
- Per-Spline Highlight binding (D-05) — or justified fallback
- D-06: Calendar* tooltip cross-check is OUT OF SCOPE for this plan — handled in 16.2-03 since Calendar* tooltip body lives in those files (already extends C-01 pattern correctly per 16.1-01 ship — should already be passing)
- Chrome MCP localhost verification at 375×667 in ja AND en (Task 3 gate)
</verification>

<success_criteria>
- ROADMAP SC2(a): tooltip renders only and all selected models
- ROADMAP SC2(b): per-Spline Highlight dots land on their lines within ±1px
- C-01 invariant preserved
- localhost-qa-gate.js Stop hook does not block
</success_criteria>

<output>
After completion, create `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-02-SUMMARY.md` with:
- Files modified + line ranges
- Pattern: replaced `ForecastHoverPopup` single-row consumer with multi-row Tooltip.List
- Highlight binding strategy chosen (per-model loop vs justified fallback)
- Owner-reported screenshot reproduction status (passes / does not reproduce)
</output>
