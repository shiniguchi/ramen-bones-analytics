---
phase: 16.2-friend-persona-qa-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [SC-1]
files_modified:
  - src/routes/+page.svelte
  - src/routes/+page.server.ts
  - src/routes/api/forecast/+server.ts
  - src/lib/dashboardStore.svelte.ts
  - src/lib/components/CalendarRevenueCard.svelte
  - src/lib/components/CalendarCountsCard.svelte
must_haves:
  truths:
    - "Owner changes range chip from 30d to April + day-grain on localhost:5173 at 375×667 and chart re-renders in <500ms perceived response (wall-clock target measured via Chrome MCP performance.measure)"
    - "No SvelteKit replaceState→invalidate re-fetch loop fires more than once per filter change (memory: feedback_sveltekit_replacestate_invalidate_gotcha.md)"
    - "Console shows no errors and no `invalid_default_snippet` warnings during the filter transition"
    - "Network panel shows /api/forecast response is either reduced in size OR pre-existing — the trace identifies which"
  artifacts:
    - path: ".planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md"
      provides: "Chrome MCP performance trace findings + identified bottleneck + fix applied"
      contains: "## Repro\n## Trace findings\n## Identified bottleneck\n## Fix applied\n## Verified <500ms"
  key_links:
    - from: "src/routes/+page.svelte filter setters (lines ~224-242)"
      to: "URL state via mergeSearchParams"
      via: "replaceState() OR goto({replaceState, invalidateAll})"
      pattern: "goto\\(.*invalidateAll: true|replaceState\\(mergeSearchParams"
    - from: "src/routes/+page.server.ts dailyRowsP load"
      to: "/api/forecast endpoint OR transactions_filterable_v"
      via: "fetchAll pagination"
      pattern: "fetchAll<.*>\\("
---

<objective>
Item 1 — Eliminate the date-range filter freeze the friend-owner reported on 2026-05-05 (verbatim: "when i change the date ranges, e.g. i wanted to see the data daily from April, the app does not response and freeze"). Investigation-first per D-01: reproduce on localhost, capture Chrome MCP performance trace at 375×667 with April + day-grain, identify the actual bottleneck from D-02 ranked candidates, then apply the fix the trace points to.

Purpose: Friend-persona acceptance gate (SC9) cannot pass while interactive filter changes hang the UI on her phone. The SvelteKit `replaceState`→`invalidate` re-fetch loop is the highest-prior suspect (memory: `feedback_sveltekit_replacestate_invalidate_gotcha.md`) — but the trace, not the suspicion, drives the fix.

Output: `16.2-01-trace.md` artifact documenting repro + trace findings + identified bottleneck + fix applied + post-fix <500ms verification. Code patch on `feature/phase-16-its-uplift-attribution`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md
@.planning/feedback/16.1-friend-2026-05-05/HANDOFF.md
@.claude/CLAUDE.md
@CLAUDE.md

<interfaces>
<!-- Existing replaceState pattern in +page.svelte (lines 200-244, current state — confirmed via grep 2026-05-05): -->
<!--   Line 212: goto(globalThis.window.location.href, { replaceState: true, invalidateAll: true })  ← RANGE-CHIP / GRAIN setter (correct memory pattern) -->
<!--   Line 224: replaceState(mergeSearchParams({ sales_type: v }), {})  ← WITHOUT invalidate (chip filter, client-side store update only) -->
<!--   Line 232: replaceState(mergeSearchParams({ is_cash: v }), {})     ← WITHOUT invalidate (chip filter) -->
<!--   Line 242: replaceState(mergeSearchParams({ days: ... }), {})       ← WITHOUT invalidate (DoW filter) -->
<!-- The pattern at 212 is INTENTIONAL (range/grain need server-side refetch). The pattern at 224/232/242 is INTENTIONAL (chip filters use client-side store). Both can be correct; the trace decides. -->

<!-- /api/forecast endpoint exists at: -->
<!--   src/routes/api/forecast/+server.ts -->
<!-- Suspected to ship ~3850 rows on April-day-grain per D-02. Use Chrome MCP network panel to measure actual response size. -->

<!-- chartXDomain widening landed in 16.1-01 D-03 / 16.1-05 D-15 across CalendarRevenueCard + CalendarCountsCard. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repro the freeze + capture Chrome MCP performance trace</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md (D-01, D-02, D-03)
    - .planning/feedback/16.1-friend-2026-05-05/HANDOFF.md (Item 1 verbatim — "April daily" is the documented hang repro per CONTEXT.md `<specifics>`)
    - .claude/memory/feedback_sveltekit_replacestate_invalidate_gotcha.md (D-02 candidate #2)
    - .claude/memory/feedback_chrome_mcp_ui_qa.md (drive Chrome MCP myself)
    - src/routes/+page.svelte (lines 1-260, current filter setter state)
  </read_first>
  <files>.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md (CREATE)</files>
  <action>
    Start dev server: `npm run dev` in repo root if not already running on :5173.

    Open Chrome MCP at http://localhost:5173 with viewport `375×667`. Sign in with the seed account (or whatever the localhost dev session expects — check src/lib/dashboardStore.svelte.ts and src/routes/+page.server.ts auth flow).

    Reproduce the EXACT owner-reported freeze:
    1. Land on dashboard with default range (likely 30d / day grain)
    2. Switch range chip to "Custom" or "April 2026"
    3. Switch grain to "day"
    4. Observe the freeze (UI unresponsive, possibly several seconds)

    BEFORE switching, instrument with Chrome MCP javascript_tool:
    ```js
    performance.mark('filter-change-start');
    // Then fire the filter change via Chrome MCP click on the range chip
    // After UI stabilizes:
    performance.mark('filter-change-end');
    performance.measure('filter-change', 'filter-change-start', 'filter-change-end');
    performance.getEntriesByName('filter-change')[0].duration;
    ```

    Capture all four signals:
    - **Performance panel:** main-thread script execution timeline during the hang. Note the longest tasks by name (e.g., `chartXDomain recompute`, `splitSeriesByModel`, layerchart render). Use Chrome MCP `mcp__claude-in-chrome__read_console_messages` AFTER the trace to read any logged warnings.
    - **Network panel:** count requests fired during the filter change. Verify whether `/api/forecast` is fetched 1× or N× (N× = D-02 candidate #2 — `replaceState`→`invalidate` re-fetch loop). Record response size of the largest call.
    - **Console:** any errors, particularly `invalid_default_snippet` warnings or RangeError from formatBucketLabel.
    - **DevTools "Sources" / "Performance"** flame chart: identify the longest sync function in user code (not framework code). This is the bottleneck per D-03.

    Write findings to `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md` using this structure:
    ```
    # 16.2-01 Date-range freeze trace
    ## Repro
    - Browser: Chrome MCP, viewport 375×667
    - Locale: ja (or en — record which)
    - Default range: <recorded>
    - Action: Switch to <range> + <grain>
    ## Wall-clock measurement
    - filter-change duration: <ms>
    ## Network panel
    - Total requests during filter change: <N>
    - /api/forecast invocations: <N> (response size: <kb>)
    - Other endpoints: <list>
    ## Performance panel — longest user-code tasks
    1. <function name> — <ms>
    2. ...
    ## Console
    - Errors: <list, or "none">
    - Warnings: <list, or "none">
    ## Identified bottleneck
    - <one of D-02's 4 candidates, OR a new candidate the trace surfaced>
    - <evidence supporting this conclusion>
    ## Fix plan (for Task 2)
    - <concrete change needed>
    ```
  </action>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md` returns 0
    - File contains all 6 sections: `grep -c '^## ' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md` returns ≥6
    - File records a wall-clock duration: `grep -c 'filter-change duration' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md` returns ≥1
    - File names a specific bottleneck (one of: replaceState-invalidate-loop, chartXDomain-recompute, /api/forecast-row-count-blowup, scroll-to-today-RAF-cascade, OR a new specific function name): `grep -E 'replaceState|chartXDomain|forecast.*row|scroll-to-today|<specific function>' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md` returns ≥1
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md &amp;&amp; grep -q 'Identified bottleneck' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md &amp;&amp; grep -q 'Fix plan' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md</automated>
  </verify>
  <done>16.2-01-trace.md exists with repro steps, wall-clock duration, network/perf/console findings, and a named bottleneck. Trace artifact ready for Task 2 to apply the fix.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the fix the trace pointed to</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md (Task 1 output — drives this task)
    - .claude/memory/feedback_sveltekit_replacestate_invalidate_gotcha.md (if bottleneck is replaceState-invalidate-loop, fix pattern is `goto({replaceState: true, invalidateAll: true})` NOT `$app/navigation.replaceState` + manual `invalidate()`)
    - src/routes/+page.svelte (the file to edit if Item 1 is filter-setter related)
    - src/lib/dashboardStore.svelte.ts (alternative edit site)
    - src/routes/api/forecast/+server.ts (if /api/forecast row count is the bottleneck — pagination / aggregation candidate)
    - src/routes/+page.server.ts (if SSR fan-out is the bottleneck)
    - src/lib/components/CalendarRevenueCard.svelte (if chartXDomain or splitSeriesByModel recomputation is the bottleneck)
    - src/lib/components/CalendarCountsCard.svelte (mirror)
  </read_first>
  <files>(determined by Task 1's identified bottleneck — at minimum one of: src/routes/+page.svelte, src/routes/api/forecast/+server.ts, src/lib/dashboardStore.svelte.ts, src/lib/components/CalendarRevenueCard.svelte, src/lib/components/CalendarCountsCard.svelte)</files>
  <action>
    Read 16.2-01-trace.md → "Identified bottleneck" + "Fix plan" sections.

    Apply the minimal fix the trace identified. Common fixes by bottleneck class (apply only the one Task 1 surfaced):

    **A. replaceState→invalidate re-fetch loop:**
    Replace the offending direct `replaceState(mergeSearchParams({...}), {})` call with:
    ```ts
    await goto(mergeSearchParams({...}), { replaceState: true, invalidateAll: true, keepFocus: true });
    ```
    Wire via memory pattern `feedback_sveltekit_replacestate_invalidate_gotcha.md`. Do NOT manually call `invalidate()` after `$app/navigation.replaceState` — that's the documented trap.

    **B. /api/forecast row count blowup:**
    Add server-side aggregation in `src/routes/api/forecast/+server.ts` (e.g., return only visible models from URL filter, OR cap to last N days for day-grain), OR add LazyMount around the cards consuming this endpoint, OR memoize the response.

    **C. chartXDomain quadratic recompute:**
    Convert the offending `$derived` to memoize the `chartXDomain` Date computation by hashing the upstream inputs (e.g., `JSON.stringify([allDates[0], allDates[allDates.length - 1], grain])`). Avoid recomputing on every reactive tick.

    **D. scroll-to-today RAF cascade:**
    Throttle/debounce the RAF on filter change OR guard with a `firstRender` flag so it only fires once per chartW-stabilization.

    Whatever the fix, KEEP IT MINIMAL. The trace identifies the root cause; do not bundle 2-3 candidate fixes "just in case."

    Update `16.2-01-trace.md` "## Fix applied" section with the exact code change applied + 1-line rationale.
  </action>
  <acceptance_criteria>
    - Code change applied to the file Task 1 identified (verify via `git diff feature/phase-16-its-uplift-attribution -- <file>`)
    - 16.2-01-trace.md has a populated `## Fix applied` section: `grep -A 5 '## Fix applied' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md | grep -c '.'` returns ≥3 lines
    - svelte-check baseline preserved (no NEW errors): `npm run check 2>&1 | tail -5` shows error count ≤ baseline (7 per STATE.md)
    - If fix touched +page.svelte filter setters, verify: `grep -c 'goto.*invalidateAll: true' src/routes/+page.svelte` returns ≥1 (i.e., memory pattern applied)
    - If fix touched /api/forecast, verify response shape didn't break: `curl -s http://localhost:5173/api/forecast | head -c 100` returns valid JSON-ish output (not 500)
  </acceptance_criteria>
  <verify>
    <automated>npm run check 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Code change applied, trace artifact updated with `## Fix applied`, svelte-check baseline preserved, build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Localhost-first Chrome MCP verification at 375×667 — fix achieves <500ms target</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md (Tasks 1+2 output — repro steps + fix applied)
    - .claude/CLAUDE.md §"Frontend / UI changes: LOCALHOST FIRST"
    - .claude/memory/feedback_localhost_first_ui_verify.md
  </read_first>
  <what-built>
    Date-range filter freeze fix applied per Task 2 (file determined by Task 1 trace).
  </what-built>
  <how-to-verify>
    1. Ensure dev server running on http://localhost:5173.
    2. Chrome MCP: open http://localhost:5173 at viewport 375×667, locale `ja`.
    3. Reproduce the EXACT same range-change action that hung in Task 1 (April + day grain).
    4. Re-run the `performance.mark` / `performance.measure` instrumentation from Task 1.
    5. Record new wall-clock duration in `16.2-01-trace.md` under a new `## Post-fix verification` section.
    6. Verify Network panel shows the right number of requests (the trace's predicted N, typically 1 for /api/forecast).
    7. Verify Console shows no errors and no `invalid_default_snippet` warnings.
    8. Switch locale to `en` via the language switcher; repeat the range-change action; verify same <500ms behavior.
    9. State `Visual verification: PASS` in the `16.2-01-trace.md` `## Post-fix verification` section IF the wall-clock is <500ms in BOTH locales. Otherwise state `FAIL` + observed duration + suspected secondary bottleneck.

    Expected outcome:
    - filter-change duration: <500ms in both ja and en at 375×667
    - No console errors
    - No re-fetch loop in network panel
    - All 5 modified cards (Calendar* + Forecast* + CampaignUpliftCard) still render correctly post-fix
  </how-to-verify>
  <acceptance_criteria>
    - 16.2-01-trace.md has a `## Post-fix verification` section: `grep -c '## Post-fix verification' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md` returns 1
    - Section records both locale wall-clock durations under 500ms with anchored regex (NO false-match on 4-digit ms values): `grep -E '^(ja|en|JA|EN)( locale)?: (([1-9][0-9]?|[1-4][0-9]{2})ms|< ?500 ?ms)$' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md | wc -l` returns ≥2 (one line per locale; the `[1-9][0-9]?|[1-4][0-9]{2}` alternation matches 1-499ms only — anchored `^` + `$` prevents the previous regex's false-match where `<[1-5]00` could substring-match `4500ms`)
    - Section explicitly records `Visual verification: PASS` as the authoritative textual gate (executor is responsible for stating PASS only when both locale wall-clocks are genuinely <500ms): `grep -E '^Visual verification: (PASS|FAIL|PARTIAL)$' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md` returns ≥1
    - If FAIL: section records `Visual verification: FAIL` + observed duration + suspected secondary bottleneck. Executor returns to Task 2 for iteration; do NOT advance to Wave 2 until PASS
  </acceptance_criteria>
  <resume-signal>Type "approved" if filter-change in both ja+en is <500ms and no regressions surfaced. If FAIL, describe the residual symptom — Task 2 may need to iterate.</resume-signal>
</task>

</tasks>

<verification>
- Chrome MCP localhost verification at 375×667 passed in ja AND en (Task 3 gate)
- Performance trace artifact (16.2-01-trace.md) committed alongside the fix
- svelte-check baseline preserved (≤7 errors per STATE.md)
- No regressions in any of the 5 modified cards in 16.1
- Localhost-qa-gate.js Stop hook does not block (frontend file edit + localhost navigate present in transcript)
</verification>

<success_criteria>
- ROADMAP SC1 satisfied: April + day-grain filter change re-renders in <500ms perceived response on localhost at 375×667
- D-01 honored: investigate-first, then fix
- D-03 honored: minimal fix, not blind multi-candidate patching
- Trace artifact committed to repo for future debugging reference
</success_criteria>

<output>
After completion, create `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-SUMMARY.md` with:
- Identified bottleneck (one sentence)
- Fix applied (file:line + 1-line description)
- Pre-fix vs post-fix wall-clock measurements
- Whether D-02 candidate #1/#2/#3/#4 was confirmed (or a new candidate surfaced)
</output>
