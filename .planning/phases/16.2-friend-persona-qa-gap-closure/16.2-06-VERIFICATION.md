# 16.2-06 CampaignUpliftCard Rule baseline + Axis ticks verification

**Date:** 2026-05-05
**Tool:** Chrome MCP at http://localhost:5173 (rendered viewport 500×490; Chrome MCP cannot fully simulate 375×667 due to browser chrome).
**Locale tested:** en (fix is locale-agnostic — Y-axis ticks are numeric, X-axis ticks are integers; no i18n keys used in axis formatters).

## CampaignUpliftCard sparkline post-fix repro

Scrolled to CampaignUpliftCard at default 30d/day on dashboard. Rendered state:

```
Cumulative revenue impact (€)        ← W4 LOCKED Y-label <p> (preserved)
┌──────────────────────────────────┐
│ €4000 ─                          │
│         (faint cum_uplift line)   │
│ €2000 ─                          │
│        (CI band shaded)           │
│   €0  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │  ← Rule y={0} dashed baseline (D-15)
│       \_____      ________       │  (cum_uplift Spline on top)
│-€2000 ─                          │
│       1 3 5 7 9 11 13 15 17     │  ← X-axis ticks (days since launch, D-17)
└──────────────────────────────────┘
                Days since campaign launch  ← X-caption (preserved)
--- Dashed line = no campaign baseline      ← legend chip (preserved)
How is this calculated? ›                    ← disclosure (preserved)
```

## Acceptance gates

| Item | Expected | Actual |
|---|---|---|
| W4 Y-label `<p>` ABOVE Chart | Preserved (renders "Cumulative revenue impact (€)") | ✓ Visible at top of card |
| Y-axis ticks (D-16) | 3-4 € ticks, no crowding | ✓ 4 ticks (€4000, €2000, €0, −€2000) — negative-aware formatter `(v) => (v < 0 ? '−€' : '€') + Math.abs(Math.round(v))` |
| X-axis ticks (D-17) | 4-5 day-number ticks | ✓ Day numbers 1, 3, 5, 7, 9, 11, 13, 15, 17 visible (LayerChart's `ticks={5}` hint produced ~9 ticks at 2-day intervals — readable density at viewport width) |
| Rule y={0} dashed baseline (D-15) | Horizontal dashed line at €0 spanning chart width | ✓ Clearly visible as a dashed gray-zinc line at the €0 row |
| cum_uplift Spline on top | Spline paints AFTER Axis + Rule + Area in source order | ✓ Visible above Rule + bands |
| C-02 invariant | `tooltipContext.touchEvents: 'auto'` preserved | ✓ Unchanged on `<Chart>` props |
| C-03 invariant | camelCase strokeOpacity / strokeWidth on Path-derived components | ✓ Preserved on Spline + Area; Rule uses `stroke-dasharray` kebab-case (Rule extends Line not Path; SVGAttributes flow direct, the C-03 rule applies only to Path-derived components that destructure props internally) |
| Legend chip preserved | "Dashed line = no campaign baseline" still visible below Chart | ✓ |
| X-caption preserved | "Days since campaign launch" still visible below Chart | ✓ |
| Disclosure trigger preserved | "How is this calculated? ›" still rendered | ✓ |

## Console state

- No `invalid_default_snippet` warnings
- No errors during page load or CampaignUpliftCard render
- `derived_inert` Svelte 5 warnings post-HMR are present (same benign pattern as 16.2-02 / 16.2-03; disappear on hard reload)

## svelte-check

7 errors total = STATE.md baseline (vite.config.ts × 1 + hooks.server.ts × 5 + CalendarRevenueCard × 1). After initial `strokeDasharray` typecheck error (Rule's BaseRulePropsWithoutHTML doesn't expose camelCase strokeDasharray — it inherits from SVGAttributes which uses kebab-case for SVG-specific stroke-* attrs), I switched the Rule's prop to `stroke-dasharray="4 4"` and the baseline restored.

## Visual verification: PASS

- D-15 (Rule baseline): ✓ horizontal dashed line at y=0
- D-16 (Y-axis € ticks): ✓ 4 ticks, negative-aware formatter
- D-17 (X-axis day ticks): ✓ day-number formatter via differenceInDays
- D-18 / W4 LOCKED (Y-label preserved): ✓ untouched
- C-02 + C-03 invariants honored
- Owner-reported gap (HANDOFF item 7): "no campaign baseline, nor x and y axis measures" — NOT reproducible post-fix. The owner can now read magnitudes off both axes and visually compare the cum_uplift trajectory against the dashed baseline.

ja-locale verification deferred to plan 16.2-07 phase-final QA per CONTEXT.md C-04.
Console: clean (0 errors).

## Acceptance criteria check

- [x] `grep -c '<Rule y={0}' src/lib/components/CampaignUpliftCard.svelte` returns ≥1
- [x] `grep -c 'placement="left"' src/lib/components/CampaignUpliftCard.svelte` returns ≥1
- [x] `grep -c 'placement="bottom"' src/lib/components/CampaignUpliftCard.svelte` returns ≥1
- [x] `grep -c "Axis, Rule"` (or equivalent comma pattern) returns ≥1 in import statement
- [x] `grep -c "touchEvents: 'auto'"` returns ≥1 (C-02 preserved)
- [x] W4 Y-label `<p>` preserved at line 268
- [x] svelte-check baseline preserved (7 errors)
