# Phase 16.2: Friend-Persona QA Gap Closure - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-05
**Phase:** 16.2-friend-persona-qa-gap-closure
**Areas discussed:** Item 6 (Prophet fix path), Item 1 (perf approach), Item 5 (verify-only scope)

---

## Context for this discussion

This was a recommendations-locking discussion, not a from-scratch brainstorm. Phase 16.2 scope was effectively pre-decided in `.planning/feedback/16.1-friend-2026-05-05/HANDOFF.md` (saved by the prior session before /clear), which captured 7 verbatim owner-reported issues + per-item triage + recommended fix paths.

Per `.claude/memory/feedback_follow_recs_first.md`, the discuss-phase orchestrator locked in handoff's recommendations as defaults inline rather than running a per-question menu. Only 3 genuinely-undecided gray areas surfaced:
- G1: Item 6 (Prophet) — Path B revert vs sanity clamp post-predict vs investigate-further-first
- G2: Item 1 (perf) — investigate-first vs blind-fix vs defer to /gsd-debug
- G3: Item 5 (visit-number coverage) — verify-only vs include-UI-fix-up-unconditionally vs pure-deferral

All 3 questions presented in a single AskUserQuestion call (Recommended option as first option per follow-recs-first pattern). User selected the Recommended option for all 3.

---

## G1 — Item 6 (Prophet exponential past-curve) fix path

| Option | Description | Selected |
|--------|-------------|----------|
| Path B revert (Recommended) | Drop the window_start kwarg from prophet_fit.py. Past-Spline branch becomes empty for Prophet only; future-Spline keeps rendering. Handoff calls Path A 'broken in practice' — pytest passed but Prophet's predict() projects stationary trend backward onto past dates, exponential artifact visible to owner. Documented in 16.1-04 RESEARCH as the Risk 2 contingency. Lowest risk; small Python edit + pipeline regen. | ✓ |
| Sanity clamp post-predict | Keep Path A wired but add a filter dropping rows where yhat > 2× max(actuals) or yhat < 0. Preserves Prophet past-yhat for backtest gate continuity later. Risk: clamp threshold is arbitrary; can mask real artifact symptoms; harder to explain in the .planning/learnings/ entry. | |
| Investigate further first | Spend 30-60min with /gsd-debug to understand whether predict() backward-projection is fixable via a different DataFrame shape (e.g., stitching past+future in one call) before choosing revert vs clamp. Adds time but might unlock a third path. | |

**User's choice:** Path B revert (Recommended)
**Notes:** Confirmed handoff's recommendation. Path A's failure mode (exponential past-curve visible to owner) is severe enough that the v1.3 default favors honesty over data continuity. Re-evaluate after Phase 17 backtest gate provides a CV harness that could re-validate Path A. Locked as D-12 in CONTEXT.md.

---

## G2 — Item 1 (date-range filter freeze) investigation strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Investigate-first via Chrome perf profiler (Recommended) | First task: reproduce on localhost with April + day grain, capture Chrome MCP performance trace, identify the actual bottleneck. Likely culprits per handoff: forecast-row-count blowup (~3850 rows in /api/forecast), replaceState→invalidate re-fetch loop (memory: feedback_sveltekit_replacestate_invalidate_gotcha.md), or chartXDomain recompute. Then plan the fix. Avoids blind-fix scope creep. | ✓ |
| Blind-fix the suspected causes | Apply all 3 handoff-suspected fixes in one plan: (a) memoize chartXDomain; (b) switch replaceState→goto({invalidateAll}); (c) paginate /api/forecast or move row aggregation server-side. Faster but risks fixing the wrong thing or compounding bugs. | |
| Defer Item 1 to /gsd-debug session | Keep Item 1 OUT of 16.2 scope; spawn a separate /gsd-debug investigation that may turn into 16.3 or a quick-fix. Lets 16.2 ship the easier 6 items faster. | |

**User's choice:** Investigate-first via Chrome perf profiler (Recommended)
**Notes:** Trace-driven fix avoids the cost of blind-fixing the wrong cause. The forecast-row-count + replaceState-loop + chartXDomain memo are 3 independent levers; trace will reveal which actually dominates. Locked as D-01..D-03 in CONTEXT.md.

---

## G3 — Item 5 (visit-number week/month forecast coverage) scope

| Option | Description | Selected |
|--------|-------------|----------|
| Verify-only — SQL audit + document (Recommended) | Run the audit query (DISTINCT model_name, granularity FROM forecast_with_actual_v WHERE granularity IN ('week','month')) early in 16.2 plan execution. If sarimax/ets/theta missing in DB → out of scope, document in deferred + update model selector to hide unavailable options. If present in DB but UI is filtering them out → expand 16.2 scope with a UI fix-up task. Owner already said 'don't worry about this if planned' so verify gates the work. | ✓ |
| Include UI fix-up unconditionally | Assume UI is filtering models out (since DB pipeline writes all 5 per Phase 14) and add a UI fix task to 16.2 scope upfront. Risk: SQL might show models genuinely absent, wasting plan effort. | |
| Pure deferral — out of 16.2 scope | Trust owner's 'don't worry if planned' permission and treat Item 5 as planned scope without verifying. Risk: leaves a real UI bug undetected if DB has the rows. | |

**User's choice:** Verify-only — SQL audit + document (Recommended)
**Notes:** Audit gates the work — cheap SQL query (~30s) determines whether 16.2 absorbs a UI fix-up plan or just documents the planned scope. Aligns with `.claude/memory/feedback_sql_cross_check_per_chart.md` pattern. Locked as D-10..D-11 in CONTEXT.md.

---

## Claude's Discretion

Per CONTEXT.md `### Claude's Discretion`:
- Plan organization (1-to-1 with items vs grouped) — planner's call.
- Item 3 file narrowing (`RepeaterCohortCountCard.svelte` vs `CalendarItemsCard.svelte`) — implementer's call via screenshot DOM inspection.
- Item 7 tick formatter exact strings (`'€500'` vs `'+€500'`, `'Day 7'` vs `'7d'` vs `'7'`) — implementer picks visually cleanest at 375×667.
- Item 1 perf budget tightening (<500ms ceiling but can target <200ms per Phase 9 precedent) — implementer's call after trace.

## Deferred Ideas

Per CONTEXT.md `<deferred>`:
- Prophet Path A as a CV-validated approach (re-evaluate after Phase 17 backtest gate)
- Tooltip pattern generalization across all dashboard cards (v1.4 design system)
- Forecast-row-count budget governance (post-Phase 17 perf phase)
- Disclosure-pattern generalization (v1.4)
- Top-20 menu-item card overlays past-forecast continuity (v1.4 polish bucket)

---

## Locked-in defaults from HANDOFF.md (NOT discussed because handoff's triage was already unambiguous)

These were confirmed implicitly by the user choosing Option A (= "phase 16.2 with the 7-item scope from HANDOFF") rather than Option B (per-item /gsd-debug) or Option C (inline-fix in 16.1). They are recorded here for audit traceability.

| Item | Locked decision | Source |
|------|----------------|--------|
| Item 2 | Filter tooltip rows by selected models; bind per-model `Highlight` to its own Spline data | HANDOFF Item 2 triage |
| Item 3 | flex layout fix on D-16 model section in visit-number card variant | HANDOFF Item 3 triage |
| Item 4 | Reorder `<Spline past>` + `<Spline future>` to render AFTER `<Bars>` (SVG source-order = paint-order) | HANDOFF Item 4 triage |
| Item 7 | Add `<Rule y={0}>` baseline + `<Axis placement="left|bottom">` ticks; preserve W4 LOCKED Y-label-as-`<p>`-ABOVE | HANDOFF Item 7 triage |
| Branch | Continue `feature/phase-16-its-uplift-attribution` (16.1's branch) | STATE.md "ship together as one PR" |
| QA gates | Localhost-first Chrome MCP at 375×667 in ja+en BEFORE DEV; pipeline regen via `forecast-refresh.yml workflow_dispatch` for Item 6 | `.claude/CLAUDE.md` + memory `feedback_migrations_workflow_dispatch.md` |
| Re-acceptance | Owner re-runs friend-persona test in `ja` locale at 375×667 — same conditions that surfaced the bugs | HANDOFF "Recommended next actions" |

---

*Discussion conducted by Claude Opus 4.7 (1M context) in interactive session 2026-05-05.*
*Methodology: 1 AskUserQuestion call with 3 questions in parallel; Recommended option pre-marked per `feedback_follow_recs_first.md` pattern; user accepted all 3 Recommended.*
