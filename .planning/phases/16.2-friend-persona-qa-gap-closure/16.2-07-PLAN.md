---
phase: 16.2-friend-persona-qa-gap-closure
plan: 07
type: execute
wave: 3
depends_on: [16.2-01, 16.2-02, 16.2-03, 16.2-04, 16.2-05, 16.2-06]
autonomous: false
requirements: [SC-8, SC-9, SC-10]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
must_haves:
  truths:
    - "All 5 modified cards (CalendarRevenueCard, CalendarCountsCard, RevenueForecastCard, InvoiceCountForecastCard, CampaignUpliftCard) verified at localhost:5173 viewport 375×667 in BOTH ja AND en locales"
    - "Zero console errors and zero `invalid_default_snippet` warnings on any card"
    - "DEV deploy verified post-localhost-pass via Chrome MCP at the DEV URL"
    - "Planning-docs drift gate passes: `.claude/scripts/validate-planning-docs.sh` exits 0"
    - "STATE.md and ROADMAP.md updated to reflect Phase 16.2 completion (frontmatter + tickboxes)"
  artifacts:
    - path: ".planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-PHASE-FINAL-QA.md"
      provides: "End-to-end verification record across all 5 cards in ja+en at localhost AND DEV"
      contains: "## Localhost\n## DEV\n## Drift gate\n## Friend re-acceptance signal"
    - path: ".planning/STATE.md"
      provides: "Updated frontmatter (completed_phases, completed_plans, last_updated)"
      contains: "Phase 16.2 closed (computed bump from STATE.md baseline; drift gate exit 0 is authoritative)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 16.2 plan tickboxes flipped to [x]"
      contains: "[x] 16.2-01-PLAN.md"
  key_links:
    - from: "All 5 modified cards on localhost"
      to: "Owner-acceptable rendering + zero console errors"
      via: "Chrome MCP visual + DOM inspection"
      pattern: "Visual verification: PASS"
    - from: "Local commits on feature branch"
      to: "DEV deployment via gh workflow run deploy.yml"
      via: "feature-branch CF Pages branch deploy"
      pattern: "gh.*deploy"
---

<objective>
Phase-final QA gate. Per D-20 + ROADMAP SC8/SC9/SC10: localhost-first Chrome MCP verification at 375×667 in ja+en across all 5 modified cards, then DEV final QA, then planning-docs drift gate, then friend re-acceptance handoff.

Purpose: Phase 16.2 cannot ship until ALL 7 owner-reported issues are visually verified resolved at the friend-persona viewport (375×667 in `ja` locale, the exact configuration that surfaced the bugs), and the planning-docs drift gate is green so `/gsd-ship` can run.

Output:
- `16.2-07-PHASE-FINAL-QA.md` end-to-end verification record
- STATE.md frontmatter bumped (completed_phases/plans, last_updated)
- ROADMAP.md tickboxes flipped to `[x]` for all 7 plans (16.2-01..16.2-07)
- Drift gate passing
- Phase ready for `/gsd-ship`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md
@.planning/feedback/16.1-friend-2026-05-05/HANDOFF.md
@.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md
@.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-02-VERIFICATION.md
@.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-VERIFICATION.md
@.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-04-AUDIT.md
@.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-05-VERIFICATION.md
@.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-06-VERIFICATION.md
@CLAUDE.md
@.claude/CLAUDE.md

<interfaces>
<!-- Plans 16.2-01..06 produce these artifacts (see depends_on). This plan reads them as inputs and synthesizes -->
<!-- the phase-final QA report. -->

<!-- Drift gate command: -->
<!--   .claude/scripts/validate-planning-docs.sh -->
<!-- Verifies STATE.md frontmatter and ROADMAP.md tickboxes match disk artifacts. -->
<!-- Must exit 0 before /gsd-ship. -->

<!-- Branch: feature/phase-16-its-uplift-attribution (C-10 — DO NOT switch branches). -->
<!-- Phase 16+16.1+16.2 ship as ONE PR; this plan does NOT push the PR (that's /gsd-ship's job). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Localhost-first Chrome MCP cross-card QA — all 5 cards in ja AND en at 375×667</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md (C-04 + D-20)
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-02-VERIFICATION.md (Plan 02 results)
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-VERIFICATION.md (Plan 03 results)
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-06-VERIFICATION.md (Plan 06 results)
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-01-trace.md (Plan 01 perf result)
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-05-VERIFICATION.md (Plan 05 DB state)
    - .claude/CLAUDE.md §"Final QA & Definition of Done"
    - .planning/feedback/16.1-friend-2026-05-05/HANDOFF.md (the original 7 issues — verify each is resolved)
  </read_first>
  <what-built>
    All 6 implementation plans (16.2-01..06) shipped. This task verifies their cumulative effect on the 5 modified cards at the friend-persona viewport.
  </what-built>
  <how-to-verify>
    Dev server running on http://localhost:5173.

    Chrome MCP at viewport 375×667.

    For each locale (`ja` first, then `en`), perform the 7-issue regression check:

    **Issue 1 — date-range freeze:**
    - Switch range chip to "April 2026" + grain to "day"
    - Measure wall-clock via `performance.measure` — must be <500ms

    **Issue 2 — forecast tooltip multi-model + dot alignment:**
    - On RevenueForecastCard, toggle sarimax + naive_dow ON, theta + ets + prophet OFF
    - Hover any future bucket — verify TWO Tooltip rows (sarimax + naive), zero theta/ets/prophet rows
    - Verify Highlight dots on Spline lines within ~1px
    - Repeat on InvoiceCountForecastCard

    **Issue 3 — visit-cohort tooltip layout:**
    - On CalendarRevenueCard, hover a bucket where past forecast lines render
    - Verify model rows show label-LEFT value-RIGHT, single line each, no flipped pairs, no value pushed to next row
    - Repeat on CalendarCountsCard

    **Issue 4 — Splines on top of bars:**
    - On CalendarRevenueCard + CalendarCountsCard, scroll to a bucket where bars are tall AND Splines cross
    - Verify forecast lines render IN FRONT OF bars

    **Issue 5 — visit-number week/month coverage:**
    - Read 16.2-04-AUDIT.md "Conclusion" — verify selector behavior matches the documented Path
    - If Path A with selector edit: switch grain to week then month, verify selector hides unavailable models
    - If Path A without edit: confirm same (data-driven hides handle it)

    **Issue 6 — Prophet past-projection:**
    - On CalendarRevenueCard + CalendarCountsCard + Forecast cards, scroll to past dates
    - Verify NO Prophet exponential past curve (orange should not climb steeply Oct→Apr)
    - Verify Prophet's FUTURE Spline (post-today) DOES render normally
    - Verify other 4 models' past Splines DO render (sarimax / naive / ets / theta)

    **Issue 7 — CampaignUpliftCard chart primitives:**
    - Scroll to CampaignUpliftCard
    - Verify horizontal dashed Rule line at y=0
    - Verify Y-axis € ticks visible on left
    - Verify X-axis days ticks visible at bottom
    - Verify Y-label `<p>` "累計売上影響額 (€)" / "Cumulative revenue impact (€)" still visible ABOVE Chart (not removed)

    Read Chrome MCP `read_console_messages` after each card visit. Aggregate: must be ZERO `invalid_default_snippet` warnings, zero RangeError, zero TypeError across all 5 cards in both locales.

    Save screenshots to `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-final-screenshots/` (10 PNGs minimum: 5 cards × 2 locales).

    Write `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-PHASE-FINAL-QA.md`:
    ```markdown
    # Phase 16.2 Final QA — All 7 issues regression-checked

    ## Localhost (http://localhost:5173, 375×667)
    | Issue | ja | en | Notes |
    |-------|----|----|-------|
    | 1. Date freeze | <PASS/FAIL> | <PASS/FAIL> | <wall-clock> |
    | 2. Forecast tooltip | ... | ... | ... |
    | 3. Cohort tooltip | ... | ... | ... |
    | 4. Spline z-order | ... | ... | ... |
    | 5. Visit-number coverage | ... | ... | ... |
    | 6. Prophet past | ... | ... | ... |
    | 7. CampaignUpliftCard | ... | ... | ... |

    ## Console
    - Total errors: <N> (must be 0)
    - Total invalid_default_snippet warnings: <N> (must be 0)

    ## Screenshots
    See .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-final-screenshots/

    ## Localhost verdict
    Visual verification: PASS / FAIL / PARTIAL
    ```

    State PASS only if all 14 row cells (7 issues × 2 locales) read PASS AND console totals are zero.
  </how-to-verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-PHASE-FINAL-QA.md` returns 0
    - Localhost section has 7 issue rows × 2 locales (14 cells): `grep -cE '\| [1-7]\.' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-PHASE-FINAL-QA.md` returns ≥7
    - Console section reports 0 errors AND 0 invalid_default_snippet: `grep -E 'Total errors: 0|errors: 0' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-PHASE-FINAL-QA.md` returns ≥1
    - Localhost verdict: `grep -E 'Localhost verdict.*PASS|Visual verification: PASS' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-PHASE-FINAL-QA.md` returns ≥1
    - At least 10 screenshots: `ls .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-final-screenshots/ | wc -l` returns ≥10
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 14 cells PASS and console is zero. Otherwise describe which issue × locale failed and the symptom.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Push to feature branch + DEV final QA</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-PHASE-FINAL-QA.md (Task 1 output)
    - .claude/CLAUDE.md §"Default Environment: DEV"
    - .claude/memory/project_cf_pages_stuck_recovery.md (CF Pages stuck recovery — manual redeploy if Error 1102)
  </read_first>
  <files>(no source files — git push + DEV verification)</files>
  <action>
    1. Stage + commit any unpushed local changes from plans 02/03/06 (Plan 05 already pushed in its own Task 2):
    ```bash
    git status
    # Add only files in src/lib/components/ + .planning/ (no secrets, no .env)
    git add src/lib/components/RevenueForecastCard.svelte src/lib/components/InvoiceCountForecastCard.svelte src/lib/components/CalendarRevenueCard.svelte src/lib/components/CalendarCountsCard.svelte src/lib/components/CampaignUpliftCard.svelte src/lib/components/ForecastHoverPopup.svelte 2>/dev/null
    git add .planning/phases/16.2-friend-persona-qa-gap-closure/ 2>/dev/null
    git add .planning/learnings/ 2>/dev/null
    git add src/routes/ 2>/dev/null  # if Plan 01 touched routes
    git diff --cached --stat
    git commit -m "fix(16.2): close 7 friend-persona QA gaps — perf, tooltips, z-order, Prophet Path B, axis primitives"
    ```

    **DO NOT add `Co-authored-by: Claude` to the commit (project rule).**

    2. Push to feature branch:
    ```bash
    git push origin feature/phase-16-its-uplift-attribution
    ```

    3. Trigger DEV deploy if not auto-deploying:
    ```bash
    gh workflow run deploy.yml --ref feature/phase-16-its-uplift-attribution
    ```
    Wait for completion via `gh run watch <run-id>`.

    4. Get the DEV preview URL (CF Pages branch deploy convention):
    ```bash
    # The preview URL pattern is typically: feature-phase-16-its-uplift-attribution.<project>.pages.dev
    # Confirm via gh:
    gh api "repos/{owner}/{repo}/deployments?ref=feature/phase-16-its-uplift-attribution&per_page=1" --jq '.[0].environment_url' 2>/dev/null
    ```

    5. Chrome MCP at viewport 375×667 against the DEV URL — repeat the 7-issue regression check from Task 1, abbreviated:
    - For each issue (1-7), do ONE locale (`ja`) check.
    - Take 7 screenshots to `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-dev-screenshots/`.
    - If any issue fails on DEV but passed on localhost: `.claude/memory/project_cf_pages_stuck_recovery.md` — try `gh workflow run deploy.yml --ref feature/phase-16-its-uplift-attribution` to force a fresh deploy.

    6. Append to `16.2-07-PHASE-FINAL-QA.md`:
    ```markdown
    ## DEV (https://feature-phase-16-its-uplift-attribution.<project>.pages.dev)
    | Issue | ja | Notes |
    |-------|----|-------|
    | 1-7 | <PASS/FAIL> | <DEV-specific notes> |

    ## DEV verdict
    Visual verification: PASS / FAIL / PARTIAL
    ```
  </action>
  <acceptance_criteria>
    - Commit landed: `git log feature/phase-16-its-uplift-attribution --oneline -1` includes "fix(16.2)"
    - No `Co-authored-by: Claude`: `git log -1 --format='%B' feature/phase-16-its-uplift-attribution | grep -c 'Co-authored-by: Claude'` returns 0
    - Pushed to remote: `git ls-remote origin feature/phase-16-its-uplift-attribution | wc -l` returns 1
    - DEV deploy completed: `gh run list --workflow=deploy.yml --branch=feature/phase-16-its-uplift-attribution --limit=1 --json conclusion -q '.[0].conclusion'` returns "success" (allow up to 10 min wait)
    - 16.2-07-PHASE-FINAL-QA.md has DEV section: `grep -c '## DEV' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-PHASE-FINAL-QA.md` returns ≥1
    - DEV verdict PASS: `grep -E 'DEV verdict.*PASS' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-PHASE-FINAL-QA.md` returns ≥1
    - 7+ DEV screenshots: `ls .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-dev-screenshots/ 2>/dev/null | wc -l` returns ≥7
  </acceptance_criteria>
  <verify>
    <automated>gh run list --workflow=deploy.yml --branch=feature/phase-16-its-uplift-attribution --limit=1 --json conclusion -q '.[0].conclusion' | grep -q success</automated>
  </verify>
  <done>Local commits pushed, DEV deploy succeeded, all 7 issues verified PASS on DEV in ja locale, 16.2-07-PHASE-FINAL-QA.md updated with DEV section.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md and ROADMAP.md, run drift gate</name>
  <read_first>
    - .planning/STATE.md (current frontmatter — completed_phases: 17, completed_plans: 79)
    - .planning/ROADMAP.md (lines 353-368 for Phase 16.2)
    - .claude/scripts/validate-planning-docs.sh (the drift gate validator)
    - .claude/CLAUDE.md §"Planning-docs drift gate (mandatory before /gsd-ship)"
  </read_first>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    1. Update `.planning/STATE.md` frontmatter (top of file). **Use COMPUTED bumps from current values, not hard-coded targets** — STATE.md may have drifted between planning and execution if other phases closed in the interim:

    Step A — Read CURRENT values from STATE.md frontmatter:
    ```bash
    CURR_COMPLETED_PHASES=$(grep -m1 'completed_phases:' .planning/STATE.md | awk '{print $2}')
    CURR_TOTAL_PLANS=$(grep -m1 'total_plans:' .planning/STATE.md | awk '{print $2}')
    CURR_COMPLETED_PLANS=$(grep -m1 'completed_plans:' .planning/STATE.md | awk '{print $2}')
    PLAN_COUNT=$(ls .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-*-PLAN.md | wc -l | tr -d ' ')
    ```

    Step B — Compute new values:
    - `new_completed_phases = CURR_COMPLETED_PHASES + 1`
    - `new_total_plans = CURR_TOTAL_PLANS + PLAN_COUNT` (only if 16.2 plans were not already counted in baseline; check the prior STATE.md `stopped_at` to see when this baseline was set — if 16.2 plans were added during /gsd-plan-phase 16.2, total_plans is already bumped, do NOT bump again)
    - `new_completed_plans = CURR_COMPLETED_PLANS + PLAN_COUNT`
    - `new_percent = round(new_completed_plans / new_total_plans * 100)`

    Step C — Apply via Edit (replacing the existing `progress:` block in STATE.md frontmatter). Example outputs (with baseline 17/91/79 + 7 new plans + 7 completions, but USE THE COMPUTED VALUES not these literals):
    ```yaml
    progress:
      total_phases: 19
      completed_phases: <new_completed_phases>
      total_plans: <new_total_plans>
      completed_plans: <new_completed_plans>
      percent: <new_percent>
    last_updated: "<ISO 8601 NOW>"
    ```

    Bump `stopped_at` to a fresh phrase: "Phase 16.2 Friend-Persona QA Gap Closure complete; all 7 owner-reported issues from 2026-05-05 SC9 closed. Phase 16+16.1+16.2 ship as one PR. Next: /gsd-ship."

    NOTE: The drift gate (Step 3 below) is the authoritative verification. If you got the math wrong, drift gate exit will be non-zero and report the exact mismatch — fix and re-run until exit 0.

    2. Update `.planning/ROADMAP.md` for Phase 16.2 (around line 353-368). Replace `**Plans**: TBD (locked during /gsd-plan-phase 16.2)` with:

    ```
    **Plans**: 7 plans (all complete 2026-05-05)
    Plans:
      **Wave 1 (parallel — independent investigations / pipeline)**
      - [x] 16.2-01-PLAN.md — Item 1 date-range freeze investigation + fix (Chrome MCP perf trace + minimal fix per D-01..D-03)
      - [x] 16.2-04-PLAN.md — Item 5 visit-number week/month forecast coverage SQL audit (verify-only per D-10/D-11)
      - [x] 16.2-05-PLAN.md — Item 6 Prophet Path B revert (drop window_start kwarg from prophet_fit.py per D-12) + workflow_dispatch regen + learning entry
      **Wave 2 (parallel — UI fixes; depend on Wave 1's perf fix for smooth visual QA)**
      - [x] 16.2-02-PLAN.md — Item 2 RevenueForecastCard + InvoiceCountForecastCard tooltip multi-model rows (D-04) + per-Spline Highlight binding (D-05)
      - [x] 16.2-03-PLAN.md — Items 3+4 (merged) CalendarRevenueCard + CalendarCountsCard tooltip layout fix (D-08) + Spline z-order verification (D-09)
      - [x] 16.2-06-PLAN.md — Item 7 CampaignUpliftCard Rule baseline (D-15) + Y-axis € ticks (D-16) + X-axis day ticks (D-17), W4 Y-label preserved (D-18)
      **Wave 3 (sequential — phase-final QA)**
      - [x] 16.2-07-PLAN.md — Localhost-first Chrome MCP QA × 5 cards × 2 locales + DEV final QA + drift gate + STATE/ROADMAP update
    ```

    Update the progress table (around lines 384-) to reflect Phase 16.2 complete:
    Add a row to the Progress table:
    ```
    | 16.2. Friend-Persona QA Gap Closure | v1.3 | 7/7 | Complete | 2026-05-05 |
    ```

    3. Run drift gate:
    ```bash
    .claude/scripts/validate-planning-docs.sh
    echo "Drift gate exit: $?"
    ```
    Must exit 0. If it fails, read the error output, fix the specific drift, re-run until 0.

    4. Append to `16.2-07-PHASE-FINAL-QA.md`:
    ```markdown
    ## Drift gate
    `.claude/scripts/validate-planning-docs.sh` exit code: 0

    ## Friend re-acceptance signal
    Owner re-acceptance is the next step (not part of this phase). Hand HUMAN-UAT.md SC11 entry to friend with the DEV preview URL once /gsd-ship lands.
    ```
  </action>
  <acceptance_criteria>
    - STATE.md frontmatter bumped (values are computed from current baseline + 7 new plans / 7 completions / +1 phase, not hard-coded — verify via drift gate exit 0)
    - ROADMAP.md tickboxes flipped for all 7 plans: `grep -cE '\[x\] 16\.2-0[1-7]-PLAN\.md' .planning/ROADMAP.md` returns 7
    - ROADMAP.md Plans line updated (no longer "TBD"): `grep -c 'Plans.*TBD' .planning/ROADMAP.md | grep -v 'Phase 17' | head -1` does NOT match the Phase 16.2 entry
    - Drift gate passes: `.claude/scripts/validate-planning-docs.sh; echo $?` returns 0
    - 16.2-07-PHASE-FINAL-QA.md has Drift gate section: `grep -c '## Drift gate' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-PHASE-FINAL-QA.md` returns ≥1
  </acceptance_criteria>
  <verify>
    <automated>.claude/scripts/validate-planning-docs.sh</automated>
  </verify>
  <done>STATE.md + ROADMAP.md updated, drift gate exit 0, phase-final-QA.md complete with all 4 sections (Localhost, DEV, Drift gate, Friend re-acceptance signal).</done>
</task>

<task type="auto">
  <name>Task 4: Final commit + handoff</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-PHASE-FINAL-QA.md (Tasks 1-3 results)
  </read_first>
  <files>(no source files — final commit + push only)</files>
  <action>
    1. Commit STATE.md + ROADMAP.md + phase-final-QA artifacts:
    ```bash
    git add .planning/STATE.md .planning/ROADMAP.md .planning/phases/16.2-friend-persona-qa-gap-closure/
    git commit -m "docs(16.2): close phase 16.2 — all 7 friend-persona issues resolved + drift gate green"
    git push origin feature/phase-16-its-uplift-attribution
    ```

    **DO NOT add `Co-authored-by: Claude` to the commit.**

    2. Print phase-completion summary to user:
    ```
    Phase 16.2 complete.

    7 plans shipped:
    - 16.2-01: date-range freeze fix (Item 1)
    - 16.2-02: forecast tooltip multi-model + Highlight binding (Item 2)
    - 16.2-03: cohort tooltip layout + Spline z-order (Items 3+4)
    - 16.2-04: visit-number week/month coverage audit (Item 5)
    - 16.2-05: Prophet Path B revert + pipeline regen + learning entry (Item 6)
    - 16.2-06: CampaignUpliftCard Rule + Axis ticks (Item 7)
    - 16.2-07: phase-final QA + drift gate + STATE/ROADMAP update

    Branch: feature/phase-16-its-uplift-attribution
    Phase 16+16.1+16.2 ship as ONE PR.

    Next: /gsd-ship to open PR.
    ```
  </action>
  <acceptance_criteria>
    - Final commit landed: `git log feature/phase-16-its-uplift-attribution --oneline -1` includes "docs(16.2)"
    - No `Co-authored-by: Claude`: `git log -1 --format='%B' feature/phase-16-its-uplift-attribution | grep -c 'Co-authored-by: Claude'` returns 0
    - Push to remote succeeded: `git rev-parse origin/feature/phase-16-its-uplift-attribution` matches `git rev-parse HEAD`
    - Branch unchanged from C-10 invariant: `git rev-parse --abbrev-ref HEAD` returns "feature/phase-16-its-uplift-attribution"
  </acceptance_criteria>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -q 'feature/phase-16-its-uplift-attribution'</automated>
  </verify>
  <done>STATE+ROADMAP commits pushed; phase 16.2 ready for /gsd-ship.</done>
</task>

</tasks>

<verification>
- Localhost QA × 5 cards × 2 locales (Task 1) — all 7 issues PASS, console clean
- DEV QA (Task 2) — all 7 issues PASS in `ja` locale on DEV preview URL
- STATE.md + ROADMAP.md updated (Task 3) — drift gate passes
- Final commit pushed (Task 4) — branch invariant preserved (C-10), no Co-authored-by
</verification>

<success_criteria>
- ROADMAP SC8: localhost-first Chrome MCP verification at 375×667 in ja AND en across all 5 cards, zero console errors, backend pipeline regen pre-DEV-QA
- ROADMAP SC9: friend-persona re-acceptance gate ready (owner runs the persona test on DEV preview URL post-`/gsd-ship`)
- ROADMAP SC10: planning-docs drift gate passes
- All 7 owner-reported issues regression-checked at the friend-persona viewport
</success_criteria>

<output>
After completion, create `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-07-SUMMARY.md` with:
- Localhost verdict (PASS/FAIL with breakdown)
- DEV verdict (PASS/FAIL with breakdown)
- Drift gate exit code
- Final commit SHA
- Handoff message to user (next: /gsd-ship)
</output>
