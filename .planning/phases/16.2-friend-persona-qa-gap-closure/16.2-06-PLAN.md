---
phase: 16.2-friend-persona-qa-gap-closure
plan: 06
type: execute
wave: 2
depends_on: [16.2-01]
autonomous: false
requirements: [SC-7]
files_modified:
  - src/lib/components/CampaignUpliftCard.svelte
must_haves:
  truths:
    - "CampaignUpliftCard sparkline shows a horizontal dashed `<Rule y={0}>` line — the counterfactual baseline matching the legend chip"
    - "Y-axis renders 3-4 € tick labels at 375px without crowding (e.g., '−€500', '€0', '+€500', '+€1000')"
    - "X-axis renders 4-5 days-since-launch tick labels at 375px (e.g., '0', '7', '14', '21', '28')"
    - "W4 LOCKED Y-label `<p>` ABOVE Chart preserved — the new `<Axis placement='left'>` provides numeric ticks only, NOT the rotated label text"
    - "C-02 invariant preserved: `tooltipContext.touchEvents: 'auto'`"
    - "C-03 invariant preserved: camelCase prop names (strokeOpacity, strokeWidth, strokeDasharray) — kebab-case is silently overridden by layerchart Path.svelte"
  artifacts:
    - path: "src/lib/components/CampaignUpliftCard.svelte"
      provides: "Sparkline with Rule baseline + Y-axis € ticks + X-axis days ticks"
      contains: "<Rule y={0}|placement=\"left\"|placement=\"bottom\""
  key_links:
    - from: "<Chart> sparkline body"
      to: "<Rule y={0}> dashed counterfactual baseline"
      via: "layerchart Rule primitive"
      pattern: "<Rule y={0}"
    - from: "<Svg> children inside <Chart>"
      to: "<Axis placement='left' format=...>"
      via: "layerchart Axis primitive with € formatter"
      pattern: "<Axis placement=\"left\""
---

<objective>
Item 7 — Add the missing chart primitives the friend-owner asked for on `CampaignUpliftCard.svelte`. Owner reported (verbatim 2026-05-05): *"Did the Apr 14, 2026 campaign work?, i do not see any no campaign baseline, nor x and y axis measures"*.

Two gaps in the sparkline:
- (a) **Baseline LINE missing**: The legend chip says "Dashed line = no campaign baseline" but the actual dashed horizontal line at y=0 is not drawn on the sparkline.
- (b) **Axis tick labels missing**: Owner sees the LABEL text ("Cumulative revenue impact (€)" / "Days since campaign launch") but no tick numbers (€0, €200, ... and 0, 7, 14, ...).

Per D-15: add `<Rule y={0} stroke-dasharray="4 4">` inside the sparkline's `<Chart>` block.
Per D-16: add `<Axis placement="left">` with € tick formatter (3-4 ticks fit 375px without crowding).
Per D-17: add `<Axis placement="bottom">` with days-since-launch tick formatter (4-5 ticks fit).
Per D-18: W4 LOCKED Y-label as `<p>` ABOVE Chart MUST be preserved — Item 7 only adds tick MARKS, not rotating an in-Svg label.

Purpose: ROADMAP SC7. Friend-persona acceptance gate cannot pass while the owner can't read magnitudes off the campaign-uplift sparkline she's asked to make a business decision from.

Output: `CampaignUpliftCard.svelte` sparkline renders Rule baseline + Y-axis ticks (€) + X-axis ticks (days). W4 Y-label preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md
@.planning/feedback/16.1-friend-2026-05-05/HANDOFF.md
@.planning/phases/16.1-friend-persona-ux-polish/16.1-CONTEXT.md
@CLAUDE.md
@.claude/CLAUDE.md

<interfaces>
<!-- Current CampaignUpliftCard.svelte sparkline state confirmed via grep 2026-05-05: -->
<!--   Line 288: <Spline y="cum_uplift" class="stroke-2" curve={curveMonotoneX} /> -->
<!--   Line 290: <Tooltip.Root contained="window" class="max-w-[80vw] text-xs"> -->
<!--   Line 291: {#snippet children({ data: pt })} -->
<!--   ... -->
<!--   Line 306: </Tooltip.Root> -->

<!-- The <Chart> block contains: <Svg> + <Spline> + <Tooltip.Root> only. -->
<!-- NO <Axis> primitives. NO <Rule> primitive. This is the gap. -->

<!-- Imports at top of file (verify before editing): -->
<!--   import { Chart, Svg, Spline, Tooltip } from 'layerchart'; -->
<!-- Need to ADD: Axis, Rule -->

<!-- D-18 requires preserving the existing <p class="..."> Y label ABOVE the Chart wrapper. -->
<!-- Look for: <p ...>Cumulative revenue impact (€)</p> or i18n key 'cumulative_revenue_impact_eur' or similar. -->
<!-- That <p> must STAY. The new <Axis placement="left"> provides only the numeric ticks, NOT a rotated label. -->

<!-- Y-axis formatter — use formatEUR or formatEURShort from $lib/format. -->
<!-- Y values are in cents (cum_uplift is cents per 16.1-03), so the formatter likely needs `(v) => formatEURShort(v)` (assuming formatEURShort accepts cents — verify). -->
<!-- If cum_uplift is in EUR (not cents) on this card, use a simple inline formatter `(v) => (v < 0 ? '−€' : '€') + Math.abs(Math.round(v))`. -->

<!-- X-axis formatter — days since launch is integer 0..N. Use `(v: number) => String(v)` or `(v) => 'Day ' + v`. -->
<!-- D-17 picks "Day N" or just "N" — implementer chooses cleanest at 375px (Claude's discretion per CONTEXT.md `<decisions>`). -->

<!-- Layerchart <Rule y={...}> renders a horizontal/vertical reference line. -->
<!-- Pattern (verify via Context7 if uncertain): -->
<!--   <Rule y={0} stroke="..." strokeDasharray="4 4" /> -->
<!--   strokeDasharray MUST be camelCase per C-03 (Path.svelte destructures it). -->
-->

<!-- Layerchart <Axis> at 375px viewport — keep `ticks={3}` or `ticks={4}` for left, `ticks={5}` for bottom to avoid label crowding. -->
<!-- Style: Axis renders to the OUTSIDE of the chart area by default; layerchart will reserve space automatically -->
<!-- (verify nothing in Chart's existing layout will clip ticks — if there's a tight `padding={...}` prop, may need to add `padding={{ left: 36, bottom: 24 }}` to preserve label space). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Axis primitives + Rule baseline to CampaignUpliftCard sparkline</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md (D-15, D-16, D-17, D-18, C-02, C-03)
    - .planning/feedback/16.1-friend-2026-05-05/HANDOFF.md (Item 7 verbatim)
    - .planning/phases/16.1-friend-persona-ux-polish/16.1-CONTEXT.md (W4 LOCKED Y-label decision context)
    - src/lib/components/CampaignUpliftCard.svelte (full file — current state has no Axis, no Rule)
    - src/lib/components/RevenueForecastCard.svelte (lines 215-216, reference Axis usage in another card)
    - src/lib/format.ts (formatEUR / formatEURShort signatures — confirm whether they take cents or EUR)
    - src/lib/i18n/messages.ts (check for existing 'cumulative_revenue_impact_eur' / 'days_since_launch' / 'no_campaign_baseline' keys from 16.1-03)
    - .claude/memory/feedback_layerchart_mobile_scroll.md (C-02 — touchEvents: 'auto')
    - .claude/memory/feedback_svelte5_tooltip_snippet.md (C-03 — camelCase prop names)
  </read_first>
  <files>src/lib/components/CampaignUpliftCard.svelte</files>
  <action>
    Open `src/lib/components/CampaignUpliftCard.svelte`.

    **Step A: update imports**
    Find the existing layerchart import line (top of file, around line 1-30). Currently:
    ```ts
    import { Chart, Svg, Spline, Tooltip } from 'layerchart';
    ```
    Update to:
    ```ts
    import { Chart, Svg, Spline, Tooltip, Axis, Rule } from 'layerchart';
    ```

    **Step B: locate the `<Chart>` / `<Svg>` block** (around lines 280-307 per grep)

    Current shape:
    ```svelte
    <Chart ... tooltipContext={{ ..., touchEvents: 'auto' }}>
      <Svg>
        <Spline y="cum_uplift" class="stroke-2" curve={curveMonotoneX} />
      </Svg>
      <Tooltip.Root contained="window" class="max-w-[80vw] text-xs">
        {#snippet children({ data: pt })}
          ...
        {/snippet}
      </Tooltip.Root>
    </Chart>
    ```

    **Step C: add Axis primitives + Rule INSIDE `<Svg>`, BEFORE `<Spline>`**

    Updated shape:
    ```svelte
    <Chart ... tooltipContext={{ ..., touchEvents: 'auto' }}>
      <Svg>
        <!-- D-16: Y-axis tick marks (€). 3-4 ticks fit at 375px. -->
        <Axis
          placement="left"
          ticks={3}
          format={(v: number) => (v < 0 ? '−€' : '€') + Math.abs(Math.round(v))}
          rule
        />

        <!-- D-17: X-axis tick marks (days since launch). 4-5 ticks fit at 375px. -->
        <Axis
          placement="bottom"
          ticks={5}
          format={(v: number) => String(v)}
          rule
        />

        <!-- D-15: counterfactual baseline — horizontal dashed line at y=0
             matching the legend chip "Dashed line = no campaign baseline". -->
        <Rule y={0} class="stroke-zinc-500" strokeDasharray="4 4" />

        <!-- Existing Spline — must render AFTER Axis + Rule to paint on top -->
        <Spline y="cum_uplift" class="stroke-2" curve={curveMonotoneX} />
      </Svg>
      <Tooltip.Root ...>
        ...
      </Tooltip.Root>
    </Chart>
    ```

    **Notes**:
    - Use `strokeDasharray` (camelCase) not `stroke-dasharray` per C-03 (kebab-case is silently overridden by Path.svelte).
    - The Y formatter is INLINE because `cum_uplift` may be in cents OR EUR — implementer must verify by reading the data shape and adjust the formatter accordingly. If `cum_uplift` is in cents, use `formatEURShort(v)` from `$lib/format`. If in EUR, the inline formatter above is fine.
    - The X formatter assumes `days_since_launch` is the X channel and is integer 0..N. Confirm by inspecting the chart's `x={...}` prop or data shape.
    - If `<Spline>` has a typed `x` accessor that pulls from a non-day field (e.g., a Date), the X-axis formatter must convert appropriately.

    **Step D: verify W4 LOCKED Y-label `<p>` ABOVE Chart is UNCHANGED.**
    Search for the existing label `<p>` element (likely above the `<Chart>` wrapper from 16.1-03 D-18 ship). Pattern:
    ```svelte
    <p class="..." data-testid="...">{t(page.data.locale, 'cumulative_revenue_impact_eur')}</p>
    <div bind:this={scrollerRef} ...>
      <Chart ...>...</Chart>
    </div>
    ```
    DO NOT modify or delete this `<p>`. The new `<Axis placement="left">` provides only numeric tick MARKS — not the label text. (W4 LOCKED rejected in-Svg axis label rotation due to 375px clipping risk.)

    **Step E: verify C-02 preserved**
    Confirm `tooltipContext={{ ..., touchEvents: 'auto' }}` is still present on the `<Chart>` element (not changed to `'pan-x'` or omitted).
  </action>
  <acceptance_criteria>
    - `grep -c '<Rule y={0}' src/lib/components/CampaignUpliftCard.svelte` returns ≥1 (D-15 baseline added)
    - `grep -c 'placement="left"' src/lib/components/CampaignUpliftCard.svelte` returns ≥1 (D-16 Y-axis added)
    - `grep -c 'placement="bottom"' src/lib/components/CampaignUpliftCard.svelte` returns ≥1 (D-17 X-axis added)
    - `grep -c 'strokeDasharray' src/lib/components/CampaignUpliftCard.svelte` returns ≥1 (C-03 camelCase honored on Rule)
    - `grep -c 'stroke-dasharray=' src/lib/components/CampaignUpliftCard.svelte` returns 0 (kebab-case forbidden on layerchart Path-derived components — verify post-edit)
    - `grep -cE 'Axis, Rule|Rule, Axis|, Axis,|, Rule,' src/lib/components/CampaignUpliftCard.svelte` returns ≥1 (imports updated)
    - `grep -c "touchEvents: 'auto'" src/lib/components/CampaignUpliftCard.svelte` returns ≥1 (C-02 preserved)
    - W4 Y-label `<p>` preserved: `grep -cE 'cumulative_revenue_impact|<p[^>]*>{t.*cumulative' src/lib/components/CampaignUpliftCard.svelte` returns ≥1
    - svelte-check baseline preserved (≤7 errors per STATE.md)
  </acceptance_criteria>
  <verify>
    <automated>npm run check 2>&amp;1 | tail -3</automated>
  </verify>
  <done>CampaignUpliftCard sparkline has Rule baseline + Axis left + Axis bottom inside Svg. W4 Y-label preserved. svelte-check green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Localhost-first Chrome MCP verification at 375×667 in ja AND en</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md (C-04)
    - .claude/CLAUDE.md §"Frontend / UI changes: LOCALHOST FIRST"
  </read_first>
  <what-built>
    Rule baseline + Axis left + Axis bottom on CampaignUpliftCard sparkline (Task 1).
  </what-built>
  <how-to-verify>
    1. Dev server on http://localhost:5173.
    2. Chrome MCP at viewport 375×667, locale `ja`.
    3. Scroll to CampaignUpliftCard. Visually verify:
       - W4 Y-label `<p>` ABOVE the Chart still shows "累計売上影響額 (€)" (or "Cumulative revenue impact (€)" in en)
       - Sparkline now shows a HORIZONTAL DASHED line at y=0 spanning the full chart width — the "no campaign baseline"
       - Y-axis (left) shows 3-4 € tick labels (e.g., '−€500', '€0', '+€500')
       - X-axis (bottom) shows 4-5 day tick labels (e.g., '0', '7', '14', '21', '28')
       - The cum_uplift Spline renders ON TOP of Rule + Axis (correct paint order)
       - Legend chip BELOW the chart still says "Dashed line = no campaign baseline" (or 「点線=キャンペーンなしの基準」 in ja) — this is the legend; the new Rule is the actual chart line
    4. Take screenshot.
    5. Switch locale to `en`; repeat step 3-4.
    6. Read console; verify NO errors and NO `invalid_default_snippet` warnings.
    7. Save 2 screenshots to `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-06-screenshots/` (campaign-ja, campaign-en).
    8. State PASS/FAIL in `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-06-VERIFICATION.md`.

    Edge case to check: if the `cum_uplift` data range is entirely positive (no negative uplift), Y-axis ticks may all be positive ('€0', '€500', '€1000'). That's still PASS — the Rule line at y=0 still renders at the bottom of the chart, and the owner can read magnitudes.
  </how-to-verify>
  <acceptance_criteria>
    - 2+ screenshots: `ls .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-06-screenshots/ | wc -l` returns ≥2
    - VERIFICATION.md PASS: `grep -E 'Visual verification: PASS' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-06-VERIFICATION.md` returns ≥1
    - VERIFICATION.md confirms zero console errors: `grep -iE 'console: clean|0 errors' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-06-VERIFICATION.md` returns ≥1
  </acceptance_criteria>
  <resume-signal>Type "approved" if Rule + 2 axes render correctly + W4 label preserved + console clean in both ja+en. Otherwise describe failure mode (e.g., axis ticks crowded, Rule not visible, label removed by accident).</resume-signal>
</task>

</tasks>

<verification>
- Rule baseline + Y-axis ticks + X-axis ticks added per D-15/D-16/D-17
- W4 LOCKED Y-label `<p>` ABOVE Chart preserved per D-18
- C-02 (touchEvents: 'auto') + C-03 (camelCase props) preserved
- Chrome MCP localhost verification at 375×667 in ja AND en (Task 2 gate)
</verification>

<success_criteria>
- ROADMAP SC7(a): horizontal counterfactual baseline visible
- ROADMAP SC7(b): Y € ticks + X days-since-launch ticks visible
- W4 invariant preserved (Y label as <p> ABOVE Chart, not in-Svg axis label)
- localhost-qa-gate.js Stop hook does not block
</success_criteria>

<output>
After completion, create `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-06-SUMMARY.md` with:
- File modified: CampaignUpliftCard.svelte (line ranges)
- Imports added (Axis, Rule)
- Tick formatter strings used (€ format + day format)
- Y-axis tick count chosen + X-axis tick count chosen + rationale
</output>
