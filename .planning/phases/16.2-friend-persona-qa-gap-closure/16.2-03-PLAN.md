---
phase: 16.2-friend-persona-qa-gap-closure
plan: 03
type: execute
wave: 2
depends_on: [16.2-01]
autonomous: false
requirements: [SC-3, SC-4]
files_modified:
  - src/lib/components/CalendarRevenueCard.svelte
  - src/lib/components/CalendarCountsCard.svelte
must_haves:
  truths:
    - "On CalendarRevenueCard tooltip, every model row renders as <label-LEFT> <value-RIGHT> on a single line — no value pushed to next line, no flipped label/value pairs"
    - "On CalendarCountsCard tooltip, model row layout matches above"
    - "All forecast Splines (past + future) render visually IN FRONT OF Bars at every grain — verified via DOM inspection of SVG element source order in both cards"
    - "No `invalid_default_snippet` runtime warning on either card"
  artifacts:
    - path: "src/lib/components/CalendarRevenueCard.svelte"
      provides: "Fixed tooltip model row layout + verified Spline z-order over Bars"
      contains: "flex justify-between|whitespace-nowrap"
    - path: "src/lib/components/CalendarCountsCard.svelte"
      provides: "Mirror of CalendarRevenueCard fix"
      contains: "flex justify-between|whitespace-nowrap"
  key_links:
    - from: "Tooltip.Item label span"
      to: "Tooltip.Item value span"
      via: "flex justify-between (or truncate / flex-shrink-0 / whitespace-nowrap)"
      pattern: "flex justify-between|whitespace-nowrap"
    - from: "<Spline past> + <Spline future> source order"
      to: "<Bars> source order"
      via: "SVG paint order (later in source = on top)"
      pattern: "<Bars[\\s\\S]+?<Spline|<Spline[\\s\\S]+?<Bars"
---

<objective>
Items 3 + 4 (merged — same files) — Fix the visit-cohort tooltip layout regression in `CalendarRevenueCard.svelte` + `CalendarCountsCard.svelte` (Item 3), AND verify/fix the Spline-behind-Bars z-order issue (Item 4) — both items modify the same files, sharing wave conflict so they ship in one plan.

**Item 3** — Owner reported (verbatim 2026-05-05): *"the popup should show the name of the variable on the left side aligned, and value on the right side, somehow the forecast values are opposite, and also the 1 row slided up on the right side. clear bug."* Screenshot `02-tooltip-layout-model-rows-misaligned.png` shows: cohort buckets render correctly (label-LEFT value-RIGHT), but the D-16 model section below shows "○ Naive (DoW avg)" without a value, then "1.214 € ○ SARIMAX" (value on LEFT, label on RIGHT), then "1.038 €" floating below. Layout regression in the model rows.

**Item 4** — Owner reported: *"sometimes the forecast lines go behind the bar graphs, do you know why? you should keep the lines always front."* Source-order inspection (CONTEXT.md `<code_context>`) confirms `<Bars>` at line 364 comes BEFORE `<Spline past>`/`<Spline future>` at lines 401/417 — which means Splines should already render on top. But Area CI bands at lines 386-396 (transparent fill) sit between them; the visible "behind bars" symptom may be the CI band reading as a fill obscuring bars, OR a sub-pixel anti-aliasing artifact, OR ordering between forecast Spline + CI Area + Bars within specific buckets where bars are very tall.

Purpose: ROADMAP SC3 + SC4. Friend-persona acceptance gate cannot pass while two of the most-prominent dashboard cards have visually broken tooltips and forecast lines.

Output: Both Calendar* cards have correct tooltip layout (every row `flex justify-between`) AND verified Spline-on-top z-order at all grains. Honors C-01 (snippet contract) and C-03 (camelCase prop names).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md
@.planning/feedback/16.1-friend-2026-05-05/HANDOFF.md
@.planning/feedback/16.1-friend-2026-05-05/02-tooltip-layout-model-rows-misaligned.png
@CLAUDE.md
@.claude/CLAUDE.md

<interfaces>
<!-- Item 3 — current Tooltip.Item rendering relies on layerchart's built-in Tooltip.Item which auto-applies flex justify-between layout (label-LEFT value-RIGHT). -->
<!-- Per CalendarRevenueCard.svelte:467-473 (current state confirmed via grep): -->
<!--   <Tooltip.Item -->
<!--     label={t(page.data.locale, `forecast_model_${name}` as MessageKey)} -->
<!--     color={FORECAST_MODEL_COLORS[name]} -->
<!--     value={formatEUR(fr.yhat_mean * 100)} -->
<!--   /> -->
<!-- The bug must be in HOW Tooltip.Item handles long labels at narrow viewport (375px max-w-[92vw] ≈ 345px usable). -->
<!-- The model label `t(locale, 'forecast_model_naive_dow')` is "Naive (DoW avg)" in en, similar length in ja. -->
<!-- When label text wraps to 2 lines, layerchart's Tooltip.Item layout breaks (value pushes to a new visual row). -->
<!-- Fix candidates: -->
<!--   (a) Wrap Tooltip.Item with explicit flex container if Tooltip.Item internal layout doesn't support whitespace-nowrap on label -->
<!--   (b) Truncate label via CSS class, OR use shorter label keys (e.g., 'naive_dow' → 'Naive' instead of 'Naive (DoW avg)') -->
<!--   (c) Replace Tooltip.Item with hand-rolled <li class="flex justify-between gap-3"><span class="truncate">...</span><span class="flex-shrink-0 tabular-nums">{value}</span></li> -->
<!-- Implementer chooses based on which approach honors layerchart Tooltip semantics + matches the 16.1-01 pattern with minimal diff. -->

<!-- Item 4 — current Bars/Spline order in CalendarRevenueCard.svelte: -->
<!--   Line 364: <Bars> (in {#each series ...} loop)                            -->
<!--   Line 372: <Spline> (trendData)                                            -->
<!--   Line 386: <Area> (CI band)                                                -->
<!--   Line 401: <Spline past>                                                   -->
<!--   Line 417: <Spline future>                                                 -->
<!-- Source order = paint order. Splines (401, 417) ARE after Bars (364), so they SHOULD render on top. -->
<!-- The owner's "lines go behind bars sometimes" complaint may be resolved by one of: -->
<!--   1. Restructuring the tall-bar regions so transparent CI Area at line 386 doesn't visually wash out the Spline above it -->
<!--   2. Increasing the past Spline strokeOpacity from 0.7 → 0.85 or 1.0 (currently strokeOpacity={0.7} per 16.1-01 D-02) -->
<!--   3. Verifying the issue does NOT reproduce post-Item-1 fix (the freeze may have been masking a stale-render symptom) -->
<!-- Plan: visual verification at 3 sample buckets. If Splines DO render on top (confirmed via DOM/screenshot), Item 4 may be a no-op or just a documentation update. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Item 3 — Fix tooltip model row layout in CalendarRevenueCard + CalendarCountsCard</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md (D-07, D-08, C-01)
    - .planning/feedback/16.1-friend-2026-05-05/HANDOFF.md (Item 3 verbatim)
    - .planning/feedback/16.1-friend-2026-05-05/02-tooltip-layout-model-rows-misaligned.png (visual ground truth — eyeball BEFORE editing per CONTEXT.md `<specifics>`)
    - .claude/memory/feedback_svelte5_tooltip_snippet.md (C-01)
    - src/lib/components/CalendarRevenueCard.svelte (lines 442-479, current Tooltip.Root snippet body)
    - src/lib/components/CalendarCountsCard.svelte (lines 358-395, mirror)
  </read_first>
  <files>src/lib/components/CalendarRevenueCard.svelte, src/lib/components/CalendarCountsCard.svelte</files>
  <action>
    Open `src/lib/components/CalendarRevenueCard.svelte`. Inspect the model row block (lines ~467-473) inside the Tooltip.Root snippet:

    ```svelte
    {#each modelRows as { name, row: fr } (`mr-${name}`)}
      <Tooltip.Item
        label={t(page.data.locale, `forecast_model_${name}` as MessageKey)}
        color={FORECAST_MODEL_COLORS[name]}
        value={formatEUR((fr.yhat_mean as number) * 100)}
      />
    {/each}
    ```

    Replace the `<Tooltip.Item>` with a hand-rolled `<li class="flex justify-between items-center gap-3 py-0.5 text-xs">` row that prevents label wrap from pushing value to a new line:

    ```svelte
    {#each modelRows as { name, row: fr } (`mr-${name}`)}
      <li class="flex items-center justify-between gap-3 py-0.5 text-xs">
        <span class="flex items-center gap-1.5 min-w-0">
          <span
            class="inline-block h-2 w-2 flex-shrink-0 rounded-full"
            style:background-color={FORECAST_MODEL_COLORS[name]}
          ></span>
          <span class="truncate">{t(page.data.locale, `forecast_model_${name}` as MessageKey)}</span>
        </span>
        <span class="flex-shrink-0 whitespace-nowrap tabular-nums">
          {formatEUR((fr.yhat_mean as number) * 100)}
        </span>
      </li>
    {/each}
    ```

    Rationale (record inline as a comment):
    - `flex items-center justify-between gap-3` → label-LEFT, value-RIGHT, single row
    - `min-w-0` on label container + `truncate` on label → label can shrink + ellipsis if too long
    - `flex-shrink-0 whitespace-nowrap tabular-nums` on value → value never wraps, never gets pushed to new line, monospace digits

    Apply byte-equivalent fix to `src/lib/components/CalendarCountsCard.svelte` model rows (lines ~382-389), substituting `formatEUR(... * 100)` with `formatIntShort(fr.yhat_mean as number)` (counts not cents).

    KEEP cohort/visit-seq buckets (the `topRows` block above the divider) using `<Tooltip.Item>` as-is — only the model rows are buggy. Do NOT touch the cohort row block.

    Verify C-01: `{#snippet children({ data: row })}` form preserved on both files.
  </action>
  <acceptance_criteria>
    - `grep -c '{#snippet children' src/lib/components/CalendarRevenueCard.svelte` returns ≥1
    - `grep -c 'let:data' src/lib/components/CalendarRevenueCard.svelte` returns 0
    - `grep -cE 'flex (items-center )?justify-between' src/lib/components/CalendarRevenueCard.svelte` returns ≥1
    - `grep -c 'flex-shrink-0' src/lib/components/CalendarRevenueCard.svelte` returns ≥1
    - `grep -c 'whitespace-nowrap' src/lib/components/CalendarRevenueCard.svelte` returns ≥1
    - `grep -c 'truncate' src/lib/components/CalendarRevenueCard.svelte` returns ≥1
    - Same 6 conditions hold for CalendarCountsCard.svelte
    - svelte-check baseline preserved
  </acceptance_criteria>
  <verify>
    <automated>npm run check 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Both Calendar* cards have hand-rolled model row markup with flex+justify-between+truncate+whitespace-nowrap. Cohort row block unchanged. svelte-check green.</done>
</task>

<task type="auto">
  <name>Task 2: Item 4 — Verify Spline-on-top z-order at sample buckets; reorder if needed</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md (D-09)
    - **NOTE: D-09 in CONTEXT.md says "Move Splines AFTER Bars" — but source-order inspection during planning (2026-05-05) revealed Splines ARE ALREADY after Bars (CalendarRevenueCard: Bars 364, Splines 401/417; CalendarCountsCard: Bars 280, Splines 317/333). D-09 was written from a stale assumption. The owner's "behind the bars" complaint must therefore have a different cause: CI Area band sub-pixel artifact, sub-pixel anti-aliasing, opacity stacking, or a per-bucket bar-height case where bars dwarf the Spline visually. Investigate visually FIRST; only reorder if a concrete repro shows the wrong stacking.**
    - .planning/feedback/16.1-friend-2026-05-05/HANDOFF.md (Item 4 verbatim)
    - src/lib/components/CalendarRevenueCard.svelte (lines 360-440, full Bars + Spline + Area block)
    - src/lib/components/CalendarCountsCard.svelte (mirror)
  </read_first>
  <files>src/lib/components/CalendarRevenueCard.svelte, src/lib/components/CalendarCountsCard.svelte (conditionally)</files>
  <action>
    Source-order inspection confirms current state:
    - CalendarRevenueCard: Bars at line 364 → trendData Spline at 372 → Area CI band at 386 → past Spline at 401 → future Spline at 417. Splines ARE after Bars in source order.
    - CalendarCountsCard: same shape (Bars 280 → Spline 288 → Area 302 → past Spline 317 → future Spline 333).

    SVG paint order = source order (later children paint on top). Splines should already render on top of Bars.

    Action plan:

    1. **Visual repro** (in dev server localhost:5173, viewport 375×667, locale `ja`):
       - Open CalendarRevenueCard with default range that exercises the past-forecast overlay.
       - Identify 3 buckets where bars are TALL (>50% chart height) AND past or future Spline crosses the bar.
       - Take screenshot at high zoom (Chrome DevTools or Chrome MCP take_screenshot at 2x density).
       - Inspect SVG via DOM: confirm `<path>` elements for Spline appear AFTER the `<rect>` Bars in DOM source order.

    2. **If Splines visually render BEHIND bars at any of the 3 sample buckets** (genuinely the bug):
       - Investigate the deeper cause: is the Area CI band (transparent fill) at line 386 causing a visual wash that makes the Spline appear faded INTO the bar? Move `<Area>` block BEFORE `<Bars>` (so CI bands paint behind bars, not on top).
       - OR increase past Spline `strokeOpacity` from 0.7 → 0.85 to reduce the visual fade-into-bar effect.
       - Apply minimal change.

    3. **If Splines DO visually render on top at all 3 sample buckets** (confirmed via screenshot):
       - Document in `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-z-order-investigation.md` that the source order is correct.
       - Owner-reported "lines behind bars sometimes" may have been a transient render artifact from the Item 1 freeze (stale chart state during the hang). Item 1 fix may resolve Item 4 implicitly.
       - In this case, NO code change to CalendarRevenueCard / CalendarCountsCard for Item 4. Note as resolved by Item 1.

    Expected outcome: Most likely path 3 (no code change needed). Path 2 (Area-before-Bars reorder) is the contingency if visual repro persists.

    Write a 1-section investigation note to `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-z-order-investigation.md`:
    ```
    # 16.2-03 Item 4 z-order investigation
    ## Repro buckets (3 samples)
    - Bucket 1: <date> — <observation>
    - ...
    ## DOM inspection
    - Source order: Bars → Spline → ... (confirmed / refuted)
    ## Conclusion
    - <one of: "no fix needed (path 3)", "Area moved before Bars (path 2)", "strokeOpacity bumped to 0.85 (path 2 alt)">
    ## Screenshots
    - 3 sample bucket PNGs at .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-screenshots/
    ```
  </action>
  <acceptance_criteria>
    - Investigation note exists: `test -f .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-z-order-investigation.md` returns 0
    - 3 sample bucket screenshots saved: `ls .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-screenshots/ 2>/dev/null | wc -l` returns ≥3
    - Either: code change applied AND noted in investigation file under "Conclusion" — OR no code change and explicit statement "no fix needed (path 3)"
    - If code change applied: `git diff feature/phase-16-its-uplift-attribution -- src/lib/components/CalendarRevenueCard.svelte` shows minimal diff (1-3 line changes)
    - svelte-check baseline preserved
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-z-order-investigation.md &amp;&amp; grep -c '## Conclusion' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-z-order-investigation.md</automated>
  </verify>
  <done>Investigation note + 3 screenshots exist. Either code change applied (minimal) or "no fix needed" documented with screenshot evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Localhost-first Chrome MCP verification at 375×667 in ja AND en</name>
  <read_first>
    - .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-CONTEXT.md (C-04)
    - .claude/CLAUDE.md §"Frontend / UI changes: LOCALHOST FIRST"
    - .planning/feedback/16.1-friend-2026-05-05/02-tooltip-layout-model-rows-misaligned.png
  </read_first>
  <what-built>
    Item 3 (tooltip model row layout) + Item 4 (Spline z-order verification) on CalendarRevenueCard + CalendarCountsCard.
  </what-built>
  <how-to-verify>
    1. Dev server on http://localhost:5173.
    2. Chrome MCP at 375×667, locale `ja`.
    3. Scroll to CalendarRevenueCard.
    4. Hover any bucket where past forecast lines render — verify tooltip shows:
       - Cohort rows (1st / 2nd / ... / Total) with label-LEFT value-RIGHT
       - Divider
       - Model rows: each with color dot + label-LEFT + value-RIGHT, ALL on a single line, no wrap, no flip
    5. Take screenshot.
    6. Repeat for CalendarCountsCard.
    7. Switch locale to `en`; repeat steps 3-6.
    8. Inspect 3 sample buckets where forecast Spline crosses tall bars — verify Spline visible ON TOP of bar (not behind).
    9. Read console; verify NO `invalid_default_snippet` warnings or other errors.
    10. Save 4 screenshots to `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-final-screenshots/` (revenue-ja-tooltip, revenue-en-tooltip, counts-ja-tooltip, counts-en-tooltip).
    11. State PASS/FAIL in `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-VERIFICATION.md`.
  </how-to-verify>
  <acceptance_criteria>
    - 4+ screenshots: `ls .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-final-screenshots/ | wc -l` returns ≥4
    - VERIFICATION.md PASS: `grep -E 'Visual verification: PASS' .planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-VERIFICATION.md` returns ≥1
    - Owner-reported screenshot does not reproduce: VERIFICATION.md notes "02-tooltip-layout-model-rows-misaligned.png does not reproduce post-fix"
  </acceptance_criteria>
  <resume-signal>Type "approved" if model row layout is consistent, no flipped rows, no wrapping, Splines visible on top, console clean in both ja+en. Otherwise describe failure mode.</resume-signal>
</task>

</tasks>

<verification>
- Item 3: model row layout fixed in both Calendar* cards
- Item 4: Spline z-order verified or fixed minimally (path 2 only if path 3 doesn't hold)
- C-01 + C-03 invariants preserved
- Chrome MCP localhost verification at 375×667 in ja AND en (Task 3 gate)
</verification>

<success_criteria>
- ROADMAP SC3: visit-cohort tooltip layout fixed
- ROADMAP SC4: forecast Splines render in front of bars (verified or fixed)
- localhost-qa-gate.js Stop hook does not block
</success_criteria>

<output>
After completion, create `.planning/phases/16.2-friend-persona-qa-gap-closure/16.2-03-SUMMARY.md` with:
- Item 3 fix: hand-rolled flex row replaces Tooltip.Item for model rows
- Item 4 outcome: which path (2 or 3) was taken; if path 3, explicit "no code change" statement
- Files modified + line ranges
</output>
