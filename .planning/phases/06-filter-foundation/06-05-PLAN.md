---
phase: 06-filter-foundation
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [FLT-01, FLT-02, FLT-03, FLT-04, FLT-05, FLT-06, FLT-07]
must_haves:
  truths:
    - "ROADMAP.md Phase 6 requirements line lists only FLT-01..FLT-04, FLT-07"
    - "ROADMAP.md Phase 7 requirements line includes FLT-05"
    - "ROADMAP.md Phase 8 requirements line includes FLT-06"
    - "REQUIREMENTS.md traceability table shows FLT-05 → Phase 7 and FLT-06 → Phase 8"
    - "Founder opens /on iPhone at 375px and verifies the filter bar end-to-end"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 6/7/8 requirement assignments reflecting D-01 scope amendment"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table for FLT-05/06"
  key_links:
    - from: ".planning/ROADMAP.md Phase 6"
      to: ".planning/REQUIREMENTS.md FLT-01..07"
      via: "requirements line in phase details"
      pattern: "FLT-0[1-7]"
---

<objective>
Close Phase 6 with the two outstanding housekeeping items from D-01: patch ROADMAP.md + REQUIREMENTS.md to move FLT-05 to Phase 7 and FLT-06 to Phase 8; then run a 375px human-verify checkpoint on DEV with the founder's workflow.

Purpose: The scope amendment locked in 06-CONTEXT D-01 ("Phase 6 ships 4 filters, not 6") is documented in CONTEXT but NOT yet reflected in the roadmap or requirements matrix. Downstream phases must see the corrected assignment. The human-verify checkpoint is the final UAT gate — Phase 6's entire value prop is "UX win" and that can only be verified by a human on a real 375px viewport.
Output: Roadmap + requirements diffs committed; human-verify checkpoint signed off by user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-filter-foundation/06-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch ROADMAP.md + REQUIREMENTS.md for FLT-05/06 reassignment</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - /Users/shiniguchi/development/ramen-bones-analytics/.planning/ROADMAP.md (Phases 6/7/8 sections)
    - /Users/shiniguchi/development/ramen-bones-analytics/.planning/REQUIREMENTS.md (traceability table lines ~201–216)
    - /Users/shiniguchi/development/ramen-bones-analytics/.planning/phases/06-filter-foundation/06-CONTEXT.md (D-01 amendment language)
  </read_first>
  <action>
    1. Edit `.planning/ROADMAP.md` Phase 6 block:
       - Change `**Requirements**: FLT-01, FLT-02, FLT-03, FLT-04, FLT-05, FLT-06, FLT-07` to `**Requirements**: FLT-01, FLT-02, FLT-03, FLT-04, FLT-07` (remove FLT-05, FLT-06)
       - In the phase description, append a note: `**Scope amendment (2026-04-15):** FLT-05 (country) moved to Phase 7 and FLT-06 (repeater bucket) moved to Phase 8 because both filters depend on columns/views that don't yet exist on v1.0 — see .planning/phases/06-filter-foundation/06-CONTEXT.md D-01.`
       - Update Phase 6 Success Criteria #1: "A user can pick an arbitrary date range (or a preset), a day/week/month granularity, and any of the 4 dropdown filters" → replace "any of the 4 dropdown filters" with "the 2 available dropdown filters (sales type, payment method)". Remove mention of country and repeater bucket. Leave criteria #2–#4 unchanged.
       - Update Phase 6 `**Plans**: TBD` → `**Plans**: 5 plans` with a bullet list mirroring the 5 PLAN.md files (01-01 through 06-05). Use the same format as Phase 1 / Phase 5 in the roadmap:
         ```
         - [ ] 06-01-PLAN.md — Wave 0 RED scaffold: zod filters schema + customToRange + Guard 6 + filter-bar e2e fixmes
         - [ ] 06-02-PLAN.md — Hand-rolled UI primitives (popover, sheet, checkbox, command) + #popover-root
         - [ ] 06-03-PLAN.md — Migration 0018 transactions_filterable_v + loader refactor to parseFilters + distinct option arrays
         - [ ] 06-04-PLAN.md — FilterBar + DatePickerPopover + FilterSheet + MultiSelectDropdown; delete DateRangeChips; flip e2e stubs
         - [ ] 06-05-PLAN.md — ROADMAP/REQUIREMENTS patch for FLT-05/06 + 375px human-verify checkpoint
         ```
    2. Edit `.planning/ROADMAP.md` Phase 7 block:
       - Change `**Requirements**: DM-01, DM-02, DM-03` to `**Requirements**: DM-01, DM-02, DM-03, FLT-05`
       - Append to Phase 7 Success Criteria: `5. The country dropdown filter (FLT-05) is wired through the existing Phase 6 filter schema (`src/lib/filters.ts`) and the loader now queries a refreshed `transactions_filterable_v` that exposes `wl_issuing_country`; supports "DE only" / "non-DE only" / individual-country multi-select.`
    3. Edit `.planning/ROADMAP.md` Phase 8 block:
       - Change `**Requirements**: DM-04, DM-05, DM-06, DM-07, DM-08` to `**Requirements**: DM-04, DM-05, DM-06, DM-07, DM-08, FLT-06`
       - Append to Phase 8 Success Criteria: `6. The repeater-bucket dropdown filter (FLT-06) is wired through the Phase 6 filter schema against `dim_customer.lifetime_bucket` / `fct_transactions.lifetime_bucket`; supports all / first_timer / 2x / 3x / 4-5x / 6+.`
    4. Edit `.planning/REQUIREMENTS.md` traceability table:
       - Change `| FLT-05 | Phase 6 — Filter Foundation | Pending |` to `| FLT-05 | Phase 7 — Column Promotion | Pending |`
       - Change `| FLT-06 | Phase 6 — Filter Foundation | Pending |` to `| FLT-06 | Phase 8 — Star Schema | Pending |`
    5. Update the Coverage table in ROADMAP.md §"Coverage Summary" / Phase counts if needed (FLT-01..07 count stays 7 but phase assignments shift — double check counts per phase still add to 65).
    6. Do NOT mark any FLT requirement as `[x]` complete yet — that's for `/gsd:verify-work` or `/gsd:end-phase` after checkpoint sign-off.
  </action>
  <verify>
    <automated>grep -c "FLT-05" .planning/ROADMAP.md && grep -n "Phase 7 — Column Promotion" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FLT-05" .planning/ROADMAP.md` shows FLT-05 NOT listed under Phase 6 but listed under Phase 7
    - `grep -n "FLT-06" .planning/ROADMAP.md` shows FLT-06 NOT listed under Phase 6 but listed under Phase 8
    - `grep -E "\\| FLT-05 \\| Phase 7" .planning/REQUIREMENTS.md` matches
    - `grep -E "\\| FLT-06 \\| Phase 8" .planning/REQUIREMENTS.md` matches
    - `grep -n "06-01-PLAN.md" .planning/ROADMAP.md` matches (plan list populated)
    - `grep -c "06-0[1-5]-PLAN.md" .planning/ROADMAP.md` returns 5
    - `grep -n "Scope amendment" .planning/ROADMAP.md` matches
    - No FLT requirement is marked `[x]` yet
  </acceptance_criteria>
  <done>ROADMAP and REQUIREMENTS reflect D-01 scope amendment; all 65 requirement IDs still accounted for.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 375px human-verify checkpoint on DEV</name>
  <what-built>
    Phase 6 filter foundation: sticky FilterBar with DatePickerPopover (presets + custom range) + GrainToggle (day/week/month) + Filters button opening a bottom sheet with sales_type and payment_method multi-selects + Reset all. Full SSR navigation on every change. Chip-scoped KPI tiles honor all 4 filters via transactions_filterable_v.
  </what-built>
  <how-to-verify>
    1. Ensure DEV is up-to-date: `git push origin <branch>` and wait for Cloudflare Pages DEV deploy to go green. Confirm the deploy URL (printed by `gh` or in Cloudflare dashboard).
    2. Open the DEV URL on a real phone (or Chrome DevTools mobile emulation at iPhone SE 375×667). Log in as the founder's account or the seeded test tenant.
    3. **FLT-01 date picker preset path**: Tap the two-line date picker button on the sticky bar. Verify popover opens. Tap `30d`. Expect: popover closes, URL updates to contain `range=30d`, KPI tiles visibly re-render with 30d numbers, button line 1 now reads `30d` and line 2 shows the last-30-days date range.
    4. **FLT-01 custom range path**: Tap the date picker button again. Select custom `From` and `To` (e.g. first day of current month → today). Tap `Apply range`. Expect: URL contains `range=custom&from=...&to=...`, button line 1 reads `Custom`, KPI tiles reflect the custom window.
    5. **FLT-02 grain toggle**: Tap each of day / week / month on the grain toggle. Expect: URL updates with `grain=<value>` on each tap; cohort/LTV charts re-render (or stay the same if they are weekly-only — acceptable, not a bug).
    6. **FLT-03 + FLT-04 sheet flow**: Tap the `Filters` button. Expect: bottom sheet slides up. Verify:
       - Sales type dropdown renders with 1+ options (INHOUSE / TAKEAWAY or whatever DEV data has); tap to select INHOUSE only.
       - Payment method dropdown renders with 1+ options; tap to select one method.
       - Tap `Apply filters`. Expect: sheet closes, URL contains `sales_type=INHOUSE&payment_method=<method>`, chip-scoped KPI tiles (txCount, avgTicket, chip-window revenue) change, fixed tiles (revenueToday / 7d / 30d) DO NOT change.
    7. **Draft-and-apply verification**: Tap `Filters` again. Tick a different option in a dropdown. Tap the backdrop (outside the sheet) to dismiss. Reopen the sheet — expect the draft change discarded (original selection shown).
    8. **Reset all**: Inside the sheet, tap `Reset all filters`. Expect: sheet closes, URL becomes `?range=7d&grain=week` (defaults kept per D-16), all filter tints cleared.
    9. **Active-state tint**: With a non-default filter applied, visually confirm the control has a subtle primary-color tinted border/background. With defaults, no tint.
    10. **Sticky bar height**: Scroll the card stream. Verify the filter bar stays pinned to the top and does NOT exceed ~72px of vertical space (rough visual — no pixel ruler required).
    11. **Empty-dropdown guard**: If DEV data has only one sales_type value (e.g. only INHOUSE), verify the sheet still renders sensibly. If ONE of the distinct arrays is empty server-side, verify that dropdown row is hidden entirely. (If both are populated, this step is non-applicable — note it in the approval message.)
    12. **URL robustness**: Paste `<deploy>/?range=bogus&grain=huh` in the address bar. Expect: page renders normally with defaults (range=7d in memory even if URL keeps `bogus` — acceptable per D-17 coerce-not-redirect).
    13. **Back/forward**: Apply a filter, tap back in the browser, expect prior filter state restored.
    14. Open Chrome DevTools console and confirm no `[transactions_filterable_v]` / `[distinctSalesTypes]` / `[distinctPaymentMethods]` error logs.
  </how-to-verify>
  <files>DEV deploy URL (runtime only — no file edits in this checkpoint)</files>
  <action>Human verification only — follow the how-to-verify steps on a real 375px viewport against the DEV Cloudflare Pages deploy. No code changes expected.</action>
  <verify><automated>echo "checkpoint:human-verify — no automated verify"</automated></verify>
  <done>User responds `approved` after completing every step of how-to-verify, or raises specific gaps.</done>
  <resume-signal>Type `approved` to close Phase 6. If any step fails, describe the issue (step number + symptom) and a gap-closure plan will be created via `/gsd:plan-phase 06 --gaps`.</resume-signal>
</task>

</tasks>

<verification>
- ROADMAP + REQUIREMENTS patches committed
- Human-verify checkpoint signed off
- `bash scripts/ci-guards/run.sh`, `pnpm test:unit -- --run`, `pnpm test:e2e`, `pnpm check` still green on main
</verification>

<success_criteria>
- Phase 6 scope amendment (D-01) reflected in the authoritative planning docs
- Founder-path 375px end-to-end verified on DEV
- Zero open blockers against FLT-01, FLT-02, FLT-03, FLT-04, FLT-07 (the 5 IDs Phase 6 owns after amendment)
- FLT-05 / FLT-06 clearly handed off to Phases 7 / 8 respectively
</success_criteria>

<output>
After completion, create `.planning/phases/06-filter-foundation/06-05-SUMMARY.md` with: patch diffs summary, checkpoint screenshot / approval message, any issues raised during verification that rolled into a follow-up plan.
</output>
