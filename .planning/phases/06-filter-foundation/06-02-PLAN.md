---
phase: 06-filter-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/ui/popover.svelte
  - src/lib/components/ui/sheet.svelte
  - src/lib/components/ui/checkbox.svelte
  - src/lib/components/ui/command.svelte
  - src/lib/components/ui/index.ts
  - src/app.html
  - tests/unit/ui-primitives.test.ts
autonomous: true
requirements: [FLT-01, FLT-03, FLT-04]
must_haves:
  truths:
    - "A Popover renders its content portaled to #popover-root and closes on Escape / backdrop click"
    - "A Sheet slides up from the bottom at 375px with a 24px drag-grabber and dims the backdrop"
    - "A Checkbox toggles on click and Space, renders a primary-tint fill when checked, has 44px touch target via wrapper"
    - "A Command list container renders option rows with role=option aria-selected"
  artifacts:
    - path: "src/lib/components/ui/popover.svelte"
      provides: "Headless Popover primitive, portaled, Svelte 5 runes"
      contains: "open"
    - path: "src/lib/components/ui/sheet.svelte"
      provides: "Bottom slide-up Sheet primitive with backdrop + focus trap + scroll lock"
      contains: "role=\"dialog\""
    - path: "src/lib/components/ui/checkbox.svelte"
      provides: "20px checkbox with 44px hit area wrapper"
      contains: "aria-checked"
    - path: "src/lib/components/ui/command.svelte"
      provides: "role=listbox list container for option rows"
      contains: "role=\"listbox\""
    - path: "src/app.html"
      provides: "#popover-root portal target"
      contains: "id=\"popover-root\""
  key_links:
    - from: "src/lib/components/ui/popover.svelte"
      to: "src/app.html #popover-root"
      via: "teleport/portal append"
      pattern: "popover-root"
    - from: "src/lib/components/ui/index.ts"
      to: "new primitives"
      via: "re-export"
      pattern: "export .* from './(popover|sheet|checkbox|command)'"
---

<objective>
Hand-roll the four net-new shadcn-style UI primitives Phase 6 requires (popover, sheet, checkbox, command) and add the `#popover-root` portal target to `app.html`. No feature components this plan — just primitives matching the existing `src/lib/components/ui/` conventions (Svelte 5 runes, `$props`, `class` merging via `cn()`, `data-slot` attributes).

Purpose: Per 06-UI-SPEC `Component Inventory`, Phase 6 cannot use shadcn-svelte CLI (unreachable per 04-01). These primitives are the substrate for FilterBar/DatePickerPopover/FilterSheet/MultiSelectDropdown in Plan 04. Isolating them in Plan 02 keeps Plan 04 focused on composition, and lets Plan 02 run in parallel with Plan 01 (disjoint file sets).
Output: 4 new primitives + 1 `app.html` edit + `ui/index.ts` re-exports + a minimal vitest suite proving props + ARIA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-filter-foundation/06-UI-SPEC.md
@.planning/phases/06-filter-foundation/06-CONTEXT.md
@src/lib/components/ui/button.svelte
@src/lib/components/ui/toggle-group.svelte
@src/lib/components/ui/index.ts
@src/app.html
@src/lib/utils.ts

<interfaces>
Existing `cn()` helper lives in `src/lib/utils.ts`. All existing primitives use `data-slot` attributes, `class` prop merging via `cn()`, Svelte 5 runes (`let { open = $bindable(false), children, class: className }: Props = $props()`). Match that style literally — do not import from any `bits-ui` / `melt-ui` package (not installed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add #popover-root to app.html + popover.svelte primitive</name>
  <files>src/app.html, src/lib/components/ui/popover.svelte, src/lib/components/ui/index.ts</files>
  <read_first>
    - /Users/shiniguchi/development/ramen-bones-analytics/src/app.html
    - /Users/shiniguchi/development/ramen-bones-analytics/src/lib/components/ui/button.svelte (prop/class pattern)
    - /Users/shiniguchi/development/ramen-bones-analytics/src/lib/components/ui/tooltip.svelte (closest existing floating-UI pattern)
    - /Users/shiniguchi/development/ramen-bones-analytics/src/lib/components/ui/index.ts
    - /Users/shiniguchi/development/ramen-bones-analytics/src/lib/utils.ts (cn helper)
  </read_first>
  <action>
    1. Edit `src/app.html`: add `<div id="popover-root"></div>` immediately before `</body>`. This is the portal target.
    2. Create `src/lib/components/ui/popover.svelte`:
       - Svelte 5 runes, `$props()` destructure with TypeScript `Props` interface
       - Props: `open: boolean = $bindable(false)`, `class?: string`, `children: Snippet`, `trigger: Snippet`
       - Render `{@render trigger()}` inline
       - When `open`, render a second block that teleports to `#popover-root` via a `$effect` that appends a detached `HTMLDivElement` to `document.getElementById('popover-root')` and mounts content inside it. Use `position: fixed` styling (NOT absolute) to escape sticky stacking (Gotcha #8 per UI-SPEC).
       - Backdrop: a transparent `<button>` covering `inset-0 fixed` that sets `open = false` on click (captures outside clicks)
       - Escape key: global `keydown` listener while open; `Escape` → `open = false`
       - `role="dialog"`, focusable container
       - Tailwind classes: `fixed inset-x-4 top-20 max-w-[343px] mx-auto rounded-lg border bg-popover text-popover-foreground shadow-lg p-4` (matches UI-SPEC anchored-below fallback)
       - `data-slot="popover-content"` on the content wrapper
       - Use `cn()` from `$lib/utils` to merge classes
    3. Add `export { default as Popover } from './popover.svelte';` to `src/lib/components/ui/index.ts`.
    4. Smoke-check with `pnpm check` (svelte-check) — no type errors.
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "id=\"popover-root\"" src/app.html` matches
    - `grep -n "data-slot=\"popover-content\"" src/lib/components/ui/popover.svelte` matches
    - `grep -n "role=\"dialog\"" src/lib/components/ui/popover.svelte` matches
    - `grep -n "export { default as Popover }" src/lib/components/ui/index.ts` matches
    - `grep -n "\\\$bindable" src/lib/components/ui/popover.svelte` matches (runes syntax)
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>Popover primitive compiles, portals to #popover-root, closes on Escape + backdrop.</done>
</task>

<task type="auto">
  <name>Task 2: sheet.svelte + checkbox.svelte + command.svelte primitives</name>
  <files>src/lib/components/ui/sheet.svelte, src/lib/components/ui/checkbox.svelte, src/lib/components/ui/command.svelte, src/lib/components/ui/index.ts, tests/unit/ui-primitives.test.ts</files>
  <read_first>
    - /Users/shiniguchi/development/ramen-bones-analytics/src/lib/components/ui/popover.svelte (just created in Task 1)
    - /Users/shiniguchi/development/ramen-bones-analytics/src/lib/components/ui/toggle-group.svelte (closest aria-checked pattern)
    - /Users/shiniguchi/development/ramen-bones-analytics/.planning/phases/06-filter-foundation/06-UI-SPEC.md §"Responsive + Accessibility Contract" and §"Interaction Contract"
    - /Users/shiniguchi/development/ramen-bones-analytics/tests/unit/ (existing test structure)
  </read_first>
  <action>
    1. Create `src/lib/components/ui/sheet.svelte`:
       - Props: `open: boolean = $bindable(false)`, `title?: string`, `class?: string`, `children: Snippet`
       - Bottom slide-up: container `fixed inset-x-0 bottom-0 z-50 rounded-t-xl border-t bg-background max-h-[85vh] overflow-y-auto p-6` with `transform transition-transform` and `translate-y-0` when open, `translate-y-full` when closed
       - 24px drag grabber: `<div class="mx-auto mb-4 h-1.5 w-12 rounded-full bg-muted"></div>` at top
       - Backdrop: `fixed inset-0 bg-black/40 z-40` clickable to close
       - `role="dialog" aria-modal="true"`, `aria-labelledby="sheet-title"` when `title` present
       - Scroll lock while open: `$effect(() => { if (open) document.body.style.overflow='hidden'; else document.body.style.overflow=''; return () => { document.body.style.overflow=''; }; });`
       - Respect `prefers-reduced-motion: reduce`: swap translate for opacity via a media-query-scoped class
       - Escape key closes (same pattern as popover)
       - `data-slot="sheet-content"` on the container
    2. Create `src/lib/components/ui/checkbox.svelte`:
       - Props: `checked: boolean = $bindable(false)`, `label?: string`, `class?: string`, `onCheckedChange?: (v: boolean) => void`
       - Outer wrapper: `<label class="flex items-center gap-3 min-h-11 cursor-pointer">` (44px touch target)
       - Inner visual: `<span class="size-5 shrink-0 rounded border border-input flex items-center justify-center">` with checkmark svg (inline `<svg>`) when `checked`
       - When checked: `bg-primary border-primary text-primary-foreground`
       - Hidden native `<input type="checkbox" bind:checked class="sr-only">` so native events + a11y work
       - `aria-checked={checked}`, `role="checkbox"` on the visual span
       - Fire `onCheckedChange?.(checked)` in an `$effect`
       - `data-slot="checkbox"` on root label
    3. Create `src/lib/components/ui/command.svelte`:
       - Props: `class?: string`, `children: Snippet`
       - Renders `<div role="listbox" aria-multiselectable="true" class={cn('flex flex-col', className)} data-slot="command-list">{@render children()}</div>`
       - No fuzzy search, no keyboard nav in v1 — just a semantic listbox container (UI-SPEC explicit: "minimal — list + checkbox items, no fuzzy search in v1")
    4. Update `src/lib/components/ui/index.ts`: add `export { default as Sheet } from './sheet.svelte';`, `export { default as Checkbox } from './checkbox.svelte';`, `export { default as Command } from './command.svelte';`.
    5. Create `tests/unit/ui-primitives.test.ts` using vitest + @testing-library/svelte (already in project — see existing InsightCard.test.ts for pattern):
       - Test 1: Checkbox renders with `aria-checked="false"` by default; click toggles to `aria-checked="true"`
       - Test 2: Checkbox wrapper has `min-h-11` class (touch target)
       - Test 3: Sheet with `open={true}` renders `role="dialog"` and `aria-modal="true"`
       - Test 4: Sheet with `open={false}` has the close-translate class OR is not in the document
       - Test 5: Command renders `role="listbox"` container
       - Test 6: Popover with `open={true}` + children renders the children (use a `bind:this` or query by test content)
       - Use `container.querySelector` for assertions per project convention ("test isolation uses container.querySelector not screen.getByText" — STATE.md 04-03 decision).
    6. Run the test file → GREEN. Then run `pnpm check` → no type errors.
  </action>
  <verify>
    <automated>pnpm test:unit -- --run tests/unit/ui-primitives.test.ts && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:unit -- --run tests/unit/ui-primitives.test.ts` exits 0 with ≥6 tests passing
    - `pnpm check` exits 0
    - `grep -n "role=\"dialog\"" src/lib/components/ui/sheet.svelte` matches
    - `grep -n "aria-modal=\"true\"" src/lib/components/ui/sheet.svelte` matches
    - `grep -n "min-h-11" src/lib/components/ui/checkbox.svelte` matches
    - `grep -n "role=\"listbox\"" src/lib/components/ui/command.svelte` matches
    - `grep -n "document.body.style.overflow" src/lib/components/ui/sheet.svelte` matches (scroll lock)
    - `grep -nE "export { default as (Sheet|Checkbox|Command) }" src/lib/components/ui/index.ts | wc -l` returns 3
    - No changes to any file outside `files_modified`
  </acceptance_criteria>
  <done>Sheet + Checkbox + Command primitives compile, GREEN tests, ARIA attributes match UI-SPEC.</done>
</task>

</tasks>

<verification>
- `pnpm test:unit -- --run tests/unit/ui-primitives.test.ts` passes
- `pnpm check` passes
- `bash scripts/ci-guards/run.sh` still green (should be — no .from() usage in primitives)
- No existing component usage broken: `pnpm test:unit -- --run` full suite passes
</verification>

<success_criteria>
- 4 primitives + `#popover-root` exist and match existing `src/lib/components/ui/` style literally
- Plan 04 can `import { Popover, Sheet, Checkbox, Command } from '$lib/components/ui'` without further work
- No `bits-ui`/`melt-ui` dependency added
</success_criteria>

<output>
After completion, create `.planning/phases/06-filter-foundation/06-02-SUMMARY.md` with: primitives shipped, ARIA verification, test counts, any style deviations from UI-SPEC (justified).
</output>
