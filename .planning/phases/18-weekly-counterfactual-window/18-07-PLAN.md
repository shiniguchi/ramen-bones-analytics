---
phase: 18-weekly-counterfactual-window
plan: 07
type: execute
wave: 7
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05", "18-06"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [UPL-08, UPL-09]
tags: [phase-final-qa, planning-docs-drift-gate, dev-deploy, manual-verification, e2e]
must_haves:
  truths:
    - "All 6 ROADMAP success criteria for Phase 18 verified end-to-end against DEV (not local-only)"
    - "Migration 0069 confirmed live on DEV (campaign_uplift CHECK includes 'iso_week'; campaign_uplift_weekly_v exists; GRANT to authenticated)"
    - "Pipeline writes iso_week rows on DEV (verified by SELECT count(*) FROM public.campaign_uplift WHERE window_kind = 'iso_week' returning > 0)"
    - "/api/campaign-uplift on DEV returns weekly_history field with rows for the friend's campaign (verified by curl + jq)"
    - "CampaignUpliftCard at DEV URL renders bar chart + new hero date label at 375×667 mobile viewport (Playwright MCP screenshot)"
    - "Tap-to-scrub working on DEV: clicking a non-default bar updates the hero (Playwright MCP click + screenshot diff)"
    - "Bootstrap CI re-fit per week verified: spot-check that the SARIMAX point_eur for week of Apr 27 – May 3 matches the conversation 2026-05-07 manual computation (~−€149) within rounding tolerance (CONTEXT.md Specifics §line 128)"
    - "Mobile vertical-page-scroll-while-finger-on-chart works on DEV (touchEvents 'auto' confirmed live)"
    - "Planning-docs drift gate PASSES: STATE.md frontmatter (progress.completed_phases, completed_plans, last_updated) + ROADMAP.md tickboxes match disk artifacts"
    - ".claude/scripts/validate-planning-docs.sh exits 0"
    - "STATE.md gains a Performance Metrics row + Decisions/Open Todos entries for Phase 18"
    - "ROADMAP.md Phase 18 row marked '[x]' with Plans Complete column updated to 7/7"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated frontmatter (progress.completed_phases, completed_plans, last_updated) + accumulated context entries for Phase 18"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 18 row marked '[x]' with Plans Complete column updated to 7/7; Phase 18 sub-plan list ticked complete"
  key_links:
    - from: ".planning/STATE.md frontmatter"
      to: ".planning/ROADMAP.md table"
      via: "Both must agree per validate-planning-docs.sh"
      pattern: "completed_phases"
    - from: "DEV /api/campaign-uplift weekly_history"
      to: "Plan 02 pipeline writer"
      via: "End-to-end smoke confirms data flows from cumulative_uplift.py → campaign_uplift table → campaign_uplift_weekly_v → API JSON"
      pattern: "weekly_history"
---

<objective>
Phase-final QA + planning-docs hygiene for Phase 18. This plan is the gate before `/gsd-ship` per `.claude/CLAUDE.md` workflow.

Three concerns:

1. **End-to-end QA on DEV** — every artifact from Plans 01..06 must work against the live DEV Supabase + Cloudflare Pages deployment. This is the "decision-maker lens" check from CLAUDE.md QA standards: would the friend-owner see Phase 18 working correctly? Six rounds: schema, pipeline, API, hero, bar chart, mobile-page-scroll.

2. **Planning-docs drift gate** — STATE.md frontmatter + ROADMAP.md tickboxes must agree with disk artifacts. `.claude/scripts/validate-planning-docs.sh` is the source of truth (per `.claude/CLAUDE.md` "Planning-docs drift gate (mandatory before /gsd-ship)").

3. **Document lessons learned** — append to STATE.md Decisions + Open Todos + Performance Metrics for Phase 18.

Wave 7 — depends on ALL prior plans (18-01..06).

Purpose: Without phase-final QA, the gate sometimes passes locally but breaks on DEV. Without planning-docs sync, `/gsd-ship` will refuse to ship per the drift-gate. Without documented lessons, the next milestone reuses no learnings.

Output: 2 modified planning files, evidence of round-trip QA (Playwright MCP DEV screenshots, curl + jq output, supabase-dev MCP queries), `validate-planning-docs.sh` exit 0.

`autonomous: false` — multiple human-verify checkpoints (mobile QA, ja translation review, drift gate confirmation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@docs/workflow.md
@.planning/phases/18-weekly-counterfactual-window/18-CONTEXT.md
@.planning/phases/18-weekly-counterfactual-window/18-RESEARCH.md
@.planning/phases/18-weekly-counterfactual-window/18-PATTERNS.md
@.planning/phases/18-weekly-counterfactual-window/18-01-SUMMARY.md
@.planning/phases/18-weekly-counterfactual-window/18-02-SUMMARY.md
@.planning/phases/18-weekly-counterfactual-window/18-03-SUMMARY.md
@.planning/phases/18-weekly-counterfactual-window/18-04-SUMMARY.md
@.planning/phases/18-weekly-counterfactual-window/18-05-SUMMARY.md
@.planning/phases/18-weekly-counterfactual-window/18-06-SUMMARY.md
@.claude/scripts/validate-planning-docs.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [Phase-Final QA]: End-to-end round-trip on DEV</name>
  <what-built>
    All 6 ROADMAP success criteria for Phase 18 implemented across 6 plans:
    - Plan 01: migration 0069 — campaign_uplift CHECK + weekly_v wrapper view.
    - Plan 02: cumulative_uplift.py — compute_iso_week_uplift_rows() + bucket_dates_by_iso_week().
    - Plan 03: /api/campaign-uplift — weekly_history payload field.
    - Plan 04: CampaignUpliftCard hero rewrite — Decision A weeks-since-launch tier source.
    - Plan 05: bar chart + CI whiskers + tap-to-scrub — Decision B path documented.
    - Plan 06: i18n keys + ModelAvailabilityDisclosure compatibility check.
  </what-built>
  <how-to-verify>
    Per `.claude/CLAUDE.md` "Final QA & Definition of Done" + adversarial QA standard. Six rounds:

    ### Round A — Schema sanity (UPL-08 success criterion 1)
    Use supabase-dev MCP (or psql to DEV) to assert:
    ```sql
    -- 1. CHECK constraint includes iso_week
    SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'campaign_uplift_window_kind_check';
    -- Expected: contains 'iso_week'

    -- 2. Wrapper view exists
    SELECT count(*) FROM information_schema.views WHERE table_name = 'campaign_uplift_weekly_v';
    -- Expected: 1

    -- 3. View body filters iso_week
    SELECT pg_get_viewdef('public.campaign_uplift_weekly_v'::regclass) LIKE '%iso_week%';
    -- Expected: t

    -- 4. Pipeline wrote rows (Plan 02 nightly run completed, OR forecast-refresh.yml manual dispatch ran)
    SELECT model_name, count(*) FROM public.campaign_uplift WHERE window_kind = 'iso_week' GROUP BY model_name ORDER BY model_name;
    -- Expected: 5 rows (one per model: sarimax, prophet, ets, theta, naive_dow), each count >= 1.
    ```

    If `count = 0` for `iso_week`, the pipeline hasn't run since the migration. Trigger it:
    ```bash
    gh workflow run forecast-refresh.yml --ref feature/phase-18-weekly-counterfactual-window
    # Wait ~5-10 min, then re-query.
    ```

    ### Round B — API sanity (UPL-08 success criterion 2)
    ```bash
    # Curl the DEV branch URL with auth.
    curl -s -H "Cookie: <auth-cookie>" "https://<branch-url>/api/campaign-uplift" | jq '.weekly_history'
    # Expected: non-empty array of {iso_week_start, iso_week_end, model_name, point_eur, ci_lower_eur, ci_upper_eur, n_days: 7}.

    # Most recent week's sarimax point_eur should be in the −€500 to +€500 range
    # for the friend's campaign (confirms bootstrap re-fit landing realistic numbers).
    curl -s -H "Cookie: <auth-cookie>" "https://<branch-url>/api/campaign-uplift" \
      | jq '.weekly_history[-1] | {iso_week_end, point_eur, ci_lower_eur, ci_upper_eur}'
    ```

    **Bootstrap-correctness spot check (CONTEXT.md Specifics §128):**
    The 2026-05-07 conversation manually computed sarimax = −€149 for the week of Apr 27 – May 3. Compare:
    - If DEV has run the pipeline post-migration AND the data covers Apr 27 – May 3:
      `point_eur` for `iso_week_end = '2026-05-03'` should be ≈ −€149 ± rounding (within ±€10).
    - The CI bounds will differ from any "subtract daily cumulative bounds" derivation — that's the whole point. Ensure CI half-width is wider for N=7 than the equivalent N=30 bounds (sanity that the bootstrap re-fit ran on the 7-day slice).

    ### Round C — Hero rendering (UPL-08 success criterion 3, 5)
    Playwright MCP against the DEV branch URL:
    ```
    mcp__playwright__browser_navigate → https://<branch-url>
    # Login.
    mcp__playwright__browser_resize → 375x667 (mobile)
    mcp__playwright__browser_screenshot → plan-18-07-mobile-en.png
    ```
    Verify:
    - Hero text "Week of [Mon date] – [Sun date]" — NOT "Since April 14th" (UPL-08 SC3).
    - Maturity-tier × CI-overlap matrix copy resolves correctly (e.g., "Apr 27 – May 3 added/reduced/no detectable lift €X").
    - ModelAvailabilityDisclosure renders correctly when expanded (UPL-08 SC5 — backtest pills + per-model maturity text).

    ### Round D — Bar chart rendering (UPL-08 success criterion 4)
    Same DEV viewport. Verify:
    - Bar chart visible below hero.
    - One bar per fully-completed ISO week since campaign launch.
    - CI whisker visible at each bar.
    - Color coding: at least 1 gray bar (CI straddles 0) OR 1 green/red bar — depends on the actual data; spot-check colors against the API response numbers.
    - Dashed y=0 baseline visible.

    ### Round E — Tap-to-scrub (UPL-08 success criterion 4)
    ```
    mcp__playwright__browser_screenshot → "before tap"
    mcp__playwright__browser_click → first non-default bar
    mcp__playwright__browser_screenshot → "after tap"
    ```
    Diff the two screenshots. The hero number AND date label MUST change.

    ### Round F — Mobile scroll (UPL-08 success criterion 6)
    On the 375×667 viewport with the bar chart in view, scroll the parent page vertically.
    The page MUST scroll vertically (touchEvents: 'auto' confirmed live).
    If the chart blocks scroll → memory rule violated; check Plan 05 implementation.

    Report PASS / FAIL per round. State explicitly which UPL-08 / UPL-09 success criteria are satisfied.
  </how-to-verify>
  <verify>
    <automated>cd /Users/shiniguchi/development/ramen-bones-analytics && gh run list --workflow=deploy.yml --limit 1 --json conclusion 2>/dev/null | grep -q '"conclusion":"success"' && echo "Latest deploy: success"</automated>
  </verify>
  <resume-signal>Type "approved" once all 6 rounds PASS. If any round PARTIAL/FAIL, fix before resuming. Document any merge-deferred items explicitly (e.g. "Round F PARTIAL — chart blocks scroll on iOS Safari; fix in 18.1 follow-up").</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update planning docs (STATE.md + ROADMAP.md) + drift-gate green</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/STATE.md (full file — particularly the frontmatter `progress.completed_phases` / `completed_plans` / `last_updated` + "Decisions" + "Open Todos" + "Performance Metrics" sections)
    - .planning/ROADMAP.md (full file — particularly the "Phase 18: Weekly Counterfactual Window" entry around line 437-448 + the Progress table at line 451-475 + Coverage Summary at line 476-485 with v1.4 row)
    - .claude/scripts/validate-planning-docs.sh (full script — understand what assertions it makes)
    - All 7 SUMMARYs in .planning/phases/18-weekly-counterfactual-window/ (read each — extract Decisions, Open Todos, performance numbers)
  </read_first>
  <action>
    1. Update `.planning/STATE.md` frontmatter:
       - `progress.completed_phases`: 20 → 21 (Phase 18 closes)
       - `progress.completed_plans`: 104 → 111 (104 + 7 plans for Phase 18)
       - `progress.percent`: recompute (111 / 116 if no other phase added; verify total)
       - `last_updated`: today's ISO date (2026-05-07 or whenever Plan 07 actually ships)
       - `last_activity`: same
       - `status`: planning → complete (or whatever the post-phase value is per the gsd_state convention used by Phase 17 closer; check 17-10-SUMMARY.md or `git log` for the prior pattern)

    2. Update `.planning/STATE.md` body:
       - "Current Position" section: Phase 18 → "Complete" or move to next phase placeholder.
       - Add a Decisions row for **Decision A (Plan 04)**: "maturity tier sourced from chronological weeks-since-launch (NOT per-week n_days). Rejected option (b) drop-tiering because the 7-key i18n set provides richer copy variation."
       - Add a Decisions row for **Decision B (Plan 05)**: "Per-bar fill via [chosen path = Option A snippet | Option B three filtered Bars | Option C manual rect via chartCtx], picked after Context7 verification on 2026-05-07."
       - Add a Decisions row for **Bootstrap CI re-fit per week**: locked from CONTEXT.md.
       - Add an Open Todos row for the deferred items from CONTEXT.md (cumulative_since_launch writer cleanup, per-day CI bands, multi-campaign UX, partial launch-week bar — flagged for v1.5 / future cleanup).
       - Add Performance Metrics rows for Plans 18-01..18-07 (model used, context %, plan duration if measured).
       - Status section: "Phase 18 complete — v1.4 Weekly Campaign Read shipped (single-feature milestone)".

    3. Update `.planning/ROADMAP.md`:
       - Phase 18 entry around line 437-448:
         - Replace `**Plans**: TBD via /gsd-plan-phase` with the 7-plan ticked list:
           ```
           **Plans**:
             - [x] 18-01-PLAN.md — migration 0069: campaign_uplift iso_week CHECK + weekly_v wrapper (UPL-08)
             - [x] 18-02-PLAN.md — pipeline writer: compute_iso_week_uplift_rows + bootstrap CI re-fit per 7-day slice (UPL-08)
             - [x] 18-03-PLAN.md — /api/campaign-uplift weekly_history payload field (UPL-08)
             - [x] 18-04-PLAN.md — CampaignUpliftCard hero rewrite (Decision A weeks-since-launch tier) (UPL-08, UPL-09)
             - [x] 18-05-PLAN.md — bar chart + CI whiskers + tap-to-scrub (Decision B [path]) (UPL-08, UPL-09)
             - [x] 18-06-PLAN.md — i18n keys (uplift_week_label etc.) + ModelAvailabilityDisclosure compat check (UPL-09)
             - [x] 18-07-PLAN.md — phase-final QA + planning-docs drift gate (UPL-08, UPL-09 sign-off)
           ```
       - Progress table at line 451-475:
         - Phase 18 row update: `Phase 18. Weekly Counterfactual Window | v1.4 | 7/7 | Complete | YYYY-MM-DD`.
       - Coverage Map (around v1.4 entries near line 562):
         - Confirm UPL-08 + UPL-09 already mapped to Phase 18 (lines 561-562 already have this; no change needed).

    4. Run `.claude/scripts/validate-planning-docs.sh`. Confirm exit 0. If non-zero, the script prints the drift; fix in STATE.md or ROADMAP.md until it passes.

    5. Commit:
    ```bash
    git add .planning/STATE.md .planning/ROADMAP.md
    git commit -m "docs(18-07): close Phase 18 — STATE + ROADMAP synced; drift gate green (UPL-08, UPL-09)"
    git push origin feature/phase-18-weekly-counterfactual-window
    ```
  </action>
  <verify>
    <automated>cd /Users/shiniguchi/development/ramen-bones-analytics && bash .claude/scripts/validate-planning-docs.sh && echo "VALIDATOR PASS"</automated>
  </verify>
  <done>
    - STATE.md frontmatter updated (completed_phases, completed_plans, last_updated, percent).
    - STATE.md body has new Decisions + Open Todos + Performance Metrics rows for Phase 18.
    - ROADMAP.md Phase 18 entry has the 7-plan ticked list.
    - ROADMAP.md Progress table row for Phase 18 = "7/7 | Complete | YYYY-MM-DD".
    - `.claude/scripts/validate-planning-docs.sh` exits 0.
    - Commit pushed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [Final Sign-Off]: Confirm phase ready for /gsd-ship</name>
  <what-built>Phase 18 implementation complete + planning docs synced + drift gate green.</what-built>
  <how-to-verify>
    1. Confirm all preceding tasks PASSED.
    2. Confirm `git status` is clean (or only the planning-docs commit pending push, which Task 2 should have pushed).
    3. Confirm CF Pages branch deploy for the latest commit succeeded.
    4. Confirm DEV verification screenshots are saved (referenced in 18-07-SUMMARY.md).
    5. Optional: review the 18-XX-SUMMARY files for any flagged items needing user decision before ship.

    State PASS / FAIL.
  </how-to-verify>
  <verify>
    <automated>cd /Users/shiniguchi/development/ramen-bones-analytics && git status --porcelain | wc -l | grep -q "^[[:space:]]*0$" && echo "Working tree clean" && bash .claude/scripts/validate-planning-docs.sh && echo "VALIDATOR PASS"</automated>
  </verify>
  <resume-signal>Type "approved" once Phase 18 is ready for /gsd-ship. The next command should be `/gsd-ship 18`.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| Local planning docs → ship gate | Drift between STATE.md / ROADMAP.md / disk artifacts blocks /gsd-ship per `.claude/CLAUDE.md`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-18-16 | Repudiation (incorrect ROADMAP tickboxes leading to false-shipped status) | ROADMAP.md updates | mitigate | `validate-planning-docs.sh` is the source of truth; CI also runs it on PRs touching SUMMARY/ROADMAP/STATE. Manual + automated double-check. |

</threat_model>

<verification>

After all 3 tasks:

1. **Phase-final QA:** all 6 rounds PASS or documented PARTIAL with merge-deferred resolution.
2. **Drift gate:** `validate-planning-docs.sh` exits 0.
3. **Working tree:** clean, commits pushed.
4. **DEV deploy:** latest commit deployed successfully.

</verification>

<success_criteria>

Plan 18-07 is complete when all of the following are TRUE:

- [ ] Round A schema sanity PASSED on DEV.
- [ ] Round B API sanity PASSED on DEV (weekly_history non-empty + bootstrap-spot-check matches conversation 2026-05-07 manual compute within ±€10).
- [ ] Round C hero rendering PASSED on DEV at 375×667 (Week of [Mon] – [Sun] visible).
- [ ] Round D bar chart rendering PASSED on DEV (bars + whiskers + colors + baseline).
- [ ] Round E tap-to-scrub PASSED on DEV (Playwright click + screenshot diff).
- [ ] Round F mobile scroll PASSED on DEV (touchEvents:'auto' confirmed live).
- [ ] STATE.md frontmatter updated (completed_phases, completed_plans, last_updated).
- [ ] ROADMAP.md Phase 18 row reflects 7/7 Complete.
- [ ] `validate-planning-docs.sh` exits 0.
- [ ] All 6 ROADMAP success criteria for Phase 18 (UPL-08 + UPL-09) signed off.
- [ ] User confirmed final sign-off with "approved".

</success_criteria>

<output>
After completion, create `.planning/phases/18-weekly-counterfactual-window/18-07-SUMMARY.md` capturing:
- All 6 round results (PASS/FAIL/PARTIAL with notes)
- Bootstrap spot-check (sarimax point_eur for 2026-05-03 vs the conversation 2026-05-07 manual compute of −€149)
- DEV screenshots paths (mobile + tap-to-scrub before/after)
- STATE.md / ROADMAP.md diff summary
- validate-planning-docs.sh output
- Final UPL-08 / UPL-09 sign-off statement
- Any merge-deferred items (PARTIAL rounds + planned 18.1 follow-ups)
- Phase 18 lessons learned (1-3 bullets) for v1.5 retrospective
</output>
</content>
</invoke>
