---
phase: 04-mobile-reader-ui
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - src/routes/+page.server.ts
  - src/routes/+page.svelte
  - src/lib/components/CohortRetentionCard.svelte
  - src/lib/components/LtvCard.svelte
  - src/lib/components/GrainToggle.svelte
  - tests/unit/cards.test.ts
autonomous: true
requirements: [UI-05, UI-06, UI-07]
must_haves:
  truths:
    - "Cohort retention card renders ≤4 overlaid LayerChart line series"
    - "Grain toggle [Day|Week|Month] inside cohort card header, default Week, URL-synced via ?grain="
    - "LTV card renders LayerChart bars, one per cohort (last 4), persistent italic caveat footer always visible"
    - "Cohorts with cohort_size < 5 dropped from chart via SPARSE_MIN_COHORT_SIZE (D-14 fallback: show anyway if all sparse)"
    - "Neither card accepts a `range` prop (D-04/Pitfall 6) — chip-independent, enforced by `@ts-expect-error` unit test"
    - "LTV caveat shows {N} months computed server-side from first cohort_start"
  artifacts:
    - path: "src/lib/components/CohortRetentionCard.svelte"
      provides: "LayerChart Spline chart + grain toggle header + sparse filter + touch tooltip"
    - path: "src/lib/components/LtvCard.svelte"
      provides: "LayerChart Bars + italic caveat footer"
    - path: "src/lib/components/GrainToggle.svelte"
      provides: "shadcn toggle-group segmented Day/Week/Month URL-synced"
  key_links:
    - from: "src/routes/+page.server.ts"
      to: "retention_curve_v, ltv_v"
      via: ".from('retention_curve_v').eq('grain', grain) + .from('ltv_v').eq('grain', grain)"
      pattern: "from\\(['\"](retention_curve_v|ltv_v)"
    - from: "src/lib/components/CohortRetentionCard.svelte"
      to: "SPARSE_MIN_COHORT_SIZE"
      via: "import from $lib/sparseFilter"
      pattern: "SPARSE_MIN_COHORT_SIZE"
---

<objective>
Land the two chart cards: cohort retention curve (LayerChart Spline, ≤4 cohorts, touch tooltip, local grain toggle) and LTV-to-date (LayerChart Bars + persistent italic caveat). Chip-INDEPENDENT by design — neither component receives `range`.

**Sequential after 04-03:** This plan extends `+page.server.ts`, `+page.svelte`, and `tests/unit/cards.test.ts` on top of 04-03's KPI work. It runs in wave 4 (not parallel with 04-03) to avoid file-write conflicts on shared files.

Purpose: deliver UI-05/06/07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-mobile-reader-ui/04-CONTEXT.md
@.planning/phases/04-mobile-reader-ui/04-UI-SPEC.md
@.planning/phases/04-mobile-reader-ui/04-RESEARCH.md
@src/lib/sparseFilter.ts
@src/lib/components/EmptyState.svelte
@supabase/migrations/0012_leaf_views.sql

<interfaces>
From `supabase/migrations/0012_leaf_views.sql` (confirm exact columns at read time):
- `retention_curve_v`: cohort_start, grain, period, retention_rate, cohort_size, cohort_age_weeks
- `ltv_v`: cohort_start, grain, ltv_cents, cohort_size
- `cohort_mv` is LOCKED — use wrapper views only (CI Guard 1)

CohortRetentionCard props:
```ts
{ data: RetentionRow[]; grain: 'day'|'week'|'month' }  // NO range prop
```
LtvCard props:
```ts
{ data: LtvRow[]; monthsOfHistory: number }            // NO range prop
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend loader with retention_curve_v, ltv_v, and monthsOfHistory derivation</name>
  <read_first>
    - supabase/migrations/0012_leaf_views.sql
    - src/routes/+page.server.ts
    - src/lib/dateRange.ts
  </read_first>
  <behavior>
    - Parallel fetches for `retention_curve_v` and `ltv_v` filtered by `grain`
    - `monthsOfHistory` = whole months between min(cohort_start) and today, computed server-side via date-fns `differenceInMonths`
    - Neither query uses `range` — full history
    - Per-query errors → empty arrays, logged, do not throw
  </behavior>
  <action>
    1. Read `supabase/migrations/0012_leaf_views.sql` to confirm exact column names.

    2. Add to `+page.server.ts` load (alongside kpi queries from 04-03 — merge into same Promise.all):
    ```ts
    import { differenceInMonths, parseISO } from 'date-fns';

    const retentionP = locals.supabase
      .from('retention_curve_v')
      .select('cohort_start,period,retention_rate,cohort_size,grain')
      .eq('grain', grain);

    const ltvP = locals.supabase
      .from('ltv_v')
      .select('cohort_start,ltv_cents,cohort_size,grain')
      .eq('grain', grain);
    ```
    Add to Promise.all. Wrap each in a `.then(r => r.data ?? []).catch(e => { console.error(e); return []; })` pattern so one failure does not poison the page.

    3. Derive `monthsOfHistory`:
    ```ts
    const firstCohort = (ltvData[0]?.cohort_start ?? retentionData[0]?.cohort_start) ?? null;
    const monthsOfHistory = firstCohort ? differenceInMonths(new Date(), parseISO(firstCohort)) : 0;
    ```

    4. Return `retention: retentionData, ltv: ltvData, monthsOfHistory` in data shape.

    5. Guard: verify no `cohort_mv` or `transactions` references added.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; bash scripts/ci-guards.sh &amp;&amp; grep -q "retention_curve_v" src/routes/+page.server.ts &amp;&amp; grep -q "ltv_v" src/routes/+page.server.ts &amp;&amp; grep -q "monthsOfHistory" src/routes/+page.server.ts &amp;&amp; ! grep -E "from\\(['\"](cohort_mv|transactions)" src/routes/+page.server.ts</automated>
  </verify>
  <done>
    Loader returns retention + ltv + monthsOfHistory; guards green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GrainToggle + CohortRetentionCard + LtvCard + wire into +page.svelte + flip 5 unit todos + add 1 new sparse-fallback test</name>
  <read_first>
    - .planning/phases/04-mobile-reader-ui/04-RESEARCH.md (Code Examples: LayerChart Retention Curve + LTV Bars)
    - .planning/phases/04-mobile-reader-ui/04-UI-SPEC.md (Card Layout Specifics, chart line palette)
    - src/lib/sparseFilter.ts
    - src/lib/components/ui/toggle-group (installed by 04-01)
  </read_first>
  <behavior>
    - GrainToggle: 3 segments, active uses `text-blue-600 bg-blue-50`, URL-sync via `goto` with `?grain=` preserved alongside `?range=`
    - CohortRetentionCard: groups rows by `cohort_start`, drops sparse cohorts (cohort_size < 5), slices last 4, renders LayerChart Spline per cohort with palette `['#2563eb','#0891b2','#7c3aed','#db2777']`
    - CohortRetentionCard: renders EmptyState(card='cohort') if data is empty
    - CohortRetentionCard: fallback — if all visible cohorts sparse, show sparse anyway and render hint `Cohort sizes are small — retention lines may swing a lot. Give it a few more weeks of data.`
    - CohortRetentionCard component props MUST be typed `{ data: Row[]; grain: Grain }` — NO range. Enforced by a `@ts-expect-error` unit test (see action step 6 below), NOT by a source grep.
    - LtvCard: renders LayerChart Bars with last 4 cohorts, ltv_eur = ltv_cents/100, persistent italic footer: `Based on {N} months of history — long-term LTV not yet observable.` (or the `< 1 month` variant)
    - LtvCard: renders footer ALWAYS even if data is empty (footer must be outside the `{#if}` branch)
  </behavior>
  <action>
    1. Spike LayerChart 2.x API first (budget 10 min): run `npm view layerchart version` and grep `node_modules/layerchart/dist/index.d.ts` for exported component names (`Chart`, `Svg`, `Axis`, `Spline`, `Bars`, `Tooltip`, `Highlight`). Adjust imports if names differ from 04-RESEARCH example.

    2. Create `src/lib/components/GrainToggle.svelte` using shadcn `toggle-group` primitive from `src/lib/components/ui/toggle-group`. Props: `{ grain: 'day'|'week'|'month' }`. On change, call `goto` updating `?grain=` while preserving `?range=`. Each segment `min-h-11`.

    3. Create `src/lib/components/CohortRetentionCard.svelte` following 04-RESEARCH Code Example (Retention Curve section). Key rules:
       - `let { data, grain }: { data: Row[]; grain: 'day'|'week'|'month' } = $props();` — NO range prop
       - Group by `cohort_start`, filter by `cohort_size >= SPARSE_MIN_COHORT_SIZE`, fallback to all if empty, slice last 4
       - Import palette constant inline
       - Header row: `<h2>Cohort retention</h2><GrainToggle {grain} />`
       - If all visible cohorts sparse → render sparse hint caption above chart
       - Use shadcn `tooltip` for LayerChart Tooltip

    4. Create `src/lib/components/LtvCard.svelte` following 04-RESEARCH Code Example (LTV Bars). Key rules:
       - `let { data, monthsOfHistory }: { data: Row[]; monthsOfHistory: number } = $props();` — NO range
       - Caveat computed via `$derived`: `< 1` → "less than a month of history" variant
       - Caveat `<p class="mt-2 text-xs italic text-zinc-500">` ALWAYS rendered, outside `{#if}`
       - Bar color `fill-blue-600 fill-opacity-85`

    5. Wire into `src/routes/+page.svelte` below KPI tiles:
    ```svelte
    <CohortRetentionCard data={data.retention} grain={data.grain} />
    <LtvCard data={data.ltv} monthsOfHistory={data.monthsOfHistory} />
    ```

    6. Flip 5 existing seed-todos to green tests in `tests/unit/cards.test.ts`:
       - `CohortRetentionCard does NOT accept a range prop` — write a test that includes a `// @ts-expect-error` line attempting to pass a `range` prop. `svelte-check`/`vitest` with typecheck will catch it if the component's props type ever adds `range`. This replaces any source-grep approach.
       - `CohortRetentionCard drops cohorts where cohort_size < 5` — fixture with 4 large + 1 sparse cohort. Assert via the exported pure helper `pickVisibleCohorts(data)` in `sparseFilter.ts` (see step 7) that only 4 cohorts are visible.
       - `CohortRetentionCard renders at most 4 series` — fixture with 6 cohorts, assert `pickVisibleCohorts` returns 4.
       - `LtvCard renders persistent italic caveat footer` — render `LtvCard` with `data=[]`, assert the italic footer node is still present in the DOM.
       - `LtvCard uses same grain URL param as cohort card` — render GrainToggle with grain='month', simulate change, assert URL contains `?grain=month`.

    7. Add a NEW test (not a seed-todo flip) asserting the sparse-fallback branch: fixture of 3 cohorts all `cohort_size=3` (all sparse); assert `pickVisibleCohorts` returns all 3 AND the "cohort sizes are small" hint is rendered in the DOM when `CohortRetentionCard` is rendered with that fixture. Extract `pickVisibleCohorts(data: Row[]): Row[]` into `src/lib/sparseFilter.ts` so the test can exercise the logic without relying on jsdom to render LayerChart SVG.

    **Todo-count math for verify:** plan 04-01 seeds 15 todos. 04-02 flipped 1 → 14. 04-03 flipped 4 → 10. This task flips 5 → **5 remaining**. The sparse-fallback test in step 7 is a NEW test, not a todo flip.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:unit 2>&amp;1 | tee /tmp/p04-04.log &amp;&amp; grep -qE "[^0-9]5 todo" /tmp/p04-04.log &amp;&amp; bash scripts/ci-guards.sh</automated>
  </verify>
  <done>
    Cohort + LTV cards render with LayerChart, 5 seed-todos flipped + 1 new sparse-fallback test added, 5 todos remain. `@ts-expect-error` test enforces absence of `range` prop on CohortRetentionCard (no source-grep).
  </done>
</task>

</tasks>

<verification>
- `npm run build` green, `npm run test:unit` green
- CI guards green
- Manual: open `/?grain=day` then `/?grain=month` — cohort chart re-renders, LTV re-renders with new grain
- Changing `?range=30d` does NOT alter cohort or LTV chart (chip-independent verification)
</verification>

<success_criteria>
UI-05, UI-06, UI-07 complete. Chip-scope bleed (Pitfall 6) structurally prevented by prop types + `@ts-expect-error` test.
</success_criteria>

<output>
Create `.planning/phases/04-mobile-reader-ui/04-04-SUMMARY.md`.
</output>
