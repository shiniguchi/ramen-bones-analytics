---
phase: 04-mobile-reader-ui
plan: 05
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - src/routes/+page.server.ts
  - src/routes/+page.svelte
  - src/lib/components/FrequencyCard.svelte
  - src/lib/components/NewVsReturningCard.svelte
  - tests/unit/cards.test.ts
  - tests/e2e/layout.spec.ts
  - .github/pull_request_template.md
  - scripts/verify-viewport.mjs
autonomous: false
requirements: [UI-08, UI-09, UI-10, UI-11]
must_haves:
  truths:
    - "FrequencyCard renders 5 rows (1/2/3–5/6–10/11+) as plain divs, bar width proportional to customer_count (D-18)"
    - "NewVsReturningCard renders stacked horizontal bar + legend with revenue tie-out (returning + new + cash)"
    - "NewVsReturningCard IS chip-scoped (D-19a exception) — receives `range` and aggregates over chip window"
    - "PR template includes 375px screenshot checklist item (UI-11)"
    - "All remaining unit todos flipped to green tests"
    - "Human checkpoint: friend opens DEV URL on real iPhone Safari and confirms dashboard readable"
  artifacts:
    - path: "src/lib/components/FrequencyCard.svelte"
      provides: "Plain-div horizontal bar list, 5 buckets, zinc-500 bars"
    - path: "src/lib/components/NewVsReturningCard.svelte"
      provides: "Stacked horizontal bar + legend with tie-out values"
    - path: ".github/pull_request_template.md"
      contains: "375"
    - path: "scripts/verify-viewport.mjs"
      provides: "Playwright screenshot script at 375px (stretch automation)"
  key_links:
    - from: "src/routes/+page.server.ts"
      to: "frequency_v, new_vs_returning_v"
      via: "parallel queries; new_vs_returning_v filtered by business_date range"
      pattern: "from\\(['\"](frequency_v|new_vs_returning_v)"
    - from: "src/lib/components/NewVsReturningCard.svelte"
      to: "chip window"
      via: "range prop → aggregation window"
      pattern: "range"
---

<objective>
Close out Phase 4: frequency card, new-vs-returning card (the D-19a chip-scoped exception), PR template 375px gate, and a human checkpoint where the friend opens the DEV URL on a real iPhone. Flips remaining RED unit todos to GREEN.

Purpose: deliver UI-08 + UI-10 + UI-11, complete the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-mobile-reader-ui/04-CONTEXT.md
@.planning/phases/04-mobile-reader-ui/04-UI-SPEC.md
@.planning/phases/04-mobile-reader-ui/04-RESEARCH.md
@src/lib/components/EmptyState.svelte
@src/lib/format.ts
@supabase/migrations/0012_leaf_views.sql

<interfaces>
From `supabase/migrations/0012_leaf_views.sql`:
- `frequency_v`: bucket (text), customer_count (int) — chip-INDEPENDENT
- `new_vs_returning_v`: business_date, segment ('new'|'returning'|'cash_anonymous'), revenue_cents, tx_count — aggregates over business_date range

NewVsReturningCard props (D-19a exception — this IS the one chip-scoped analytics card):
```ts
{ data: Array<{segment: 'new'|'returning'|'cash_anonymous'; revenue_cents: number}> }
```
The loader aggregates rows within chip window BEFORE passing to the component, so the component itself stays stateless.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Loader frequency + new_vs_returning queries, FrequencyCard + NewVsReturningCard, wire into page</name>
  <read_first>
    - supabase/migrations/0012_leaf_views.sql
    - .planning/phases/04-mobile-reader-ui/04-RESEARCH.md (Code Examples: Frequency card, New vs Returning)
    - .planning/phases/04-mobile-reader-ui/04-UI-SPEC.md (Card Layout: Frequency, New vs returning)
    - src/routes/+page.server.ts
  </read_first>
  <behavior>
    - Loader fetches `frequency_v` once (chip-independent) and `new_vs_returning_v` filtered by `business_date BETWEEN from AND to` where from/to come from chipToRange(range)
    - For `new_vs_returning_v`, loader sums `revenue_cents` GROUP BY segment to produce `[{segment:'returning', revenue_cents: N}, ...]`
    - Tie-out invariant verified in test: `returning + new + cash === kpi_daily_v.revenue` over same range (D-19)
    - FrequencyCard renders one `<li>` per bucket with `div` bar width = `customer_count / max * 100%`
    - NewVsReturningCard renders stacked `<div class="flex h-3">` with three segments, legend below with EUR integer values
    - Both cards render EmptyState when data empty/zero
  </behavior>
  <action>
    1. Extend `+page.server.ts` Promise.all with:
    ```ts
    const freqP = locals.supabase.from('frequency_v').select('bucket,customer_count').then(r => r.data ?? []).catch(() => []);
    const nvrP  = locals.supabase
      .from('new_vs_returning_v')
      .select('segment,revenue_cents,business_date')
      .gte('business_date', chipW.from).lte('business_date', chipW.to)
      .then(r => r.data ?? []).catch(() => []);
    ```
    Sum nvr rows GROUP BY segment into `[{segment, revenue_cents}]` shape. Preserve order: returning, new, cash_anonymous (ensure all 3 keys present even if zero).

    Return `frequency: freqData, newVsReturning: nvrShaped` in PageData.

    2. Create `src/lib/components/FrequencyCard.svelte` per 04-RESEARCH Code Example (Frequency). Plain divs only — no LayerChart import. Bucket labels pinned: `1 visit`, `2 visits`, `3–5 visits`, `6–10 visits`, `11+`. Map from DB bucket values (confirm exact values in migration; may already be these strings).

    3. Create `src/lib/components/NewVsReturningCard.svelte` per 04-RESEARCH Code Example. Colors from UI-SPEC: returning `bg-blue-600`, new `bg-indigo-300`, cash `bg-zinc-200`. Legend uses `formatEUR(cents)` (integer, no decimals).

    4. Wire into `src/routes/+page.svelte`:
    ```svelte
    <FrequencyCard data={data.frequency} />
    <NewVsReturningCard data={data.newVsReturning} />
    ```

    5. Flip remaining unit todos in `tests/unit/cards.test.ts`:
       - `FrequencyCard uses plain divs not LayerChart (D-18)` — grep the component source; assert it does NOT import from 'layerchart'
       - `NewVsReturningCard IS chip-scoped (D-19a exception)` — document via test naming; assert loader passes chip-filtered data (integration-ish — test a helper function `shapeNvr(rows)` with a fixture)
       - `NewVsReturningCard tie-out: returning + new + cash === revenue (D-19)` — fixture with known rows, assert sum equals expected
       - `FreshnessLabel muted ≤30h, yellow >30h, red >48h (D-10a)` — 3 cases
       - `Per-card error fallback does not throw whole page (D-22)` — render KpiTile with value=null, assert EmptyState visible, no thrown error

    All remaining todos should be flipped — after this task, 0 todos should remain.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:unit 2>&amp;1 | tee /tmp/p04-05.log &amp;&amp; grep -q "0 todo" /tmp/p04-05.log &amp;&amp; ! grep -q "todo" /tmp/p04-05.log || grep -q "15 passed" /tmp/p04-05.log; bash scripts/ci-guards.sh &amp;&amp; ! grep -q "from 'layerchart'" src/lib/components/FrequencyCard.svelte</automated>
  </verify>
  <done>
    All 9 analytics cards render, 15 unit tests green, 0 todos.
  </done>
</task>

<task type="auto">
  <name>Task 2: PR template 375px gate + optional Playwright screenshot script</name>
  <read_first>
    - .planning/phases/04-mobile-reader-ui/04-UI-SPEC.md (Viewport Contract)
    - .planning/phases/04-mobile-reader-ui/04-VALIDATION.md
    - tests/e2e/layout.spec.ts
  </read_first>
  <action>
    1. Create `.github/pull_request_template.md`:
    ```markdown
    ## Summary
    <!-- What and why -->

    ## 375px verification (UI-11 — MANDATORY for any `src/` change)

    - [ ] Opened CF Pages preview URL in Chrome DevTools at iPhone SE (375×667)
    - [ ] No horizontal scroll on `/`
    - [ ] Screenshot attached below
    - [ ] `npm run test:unit` green
    - [ ] `npm run test:e2e` green (or skipped with reason)
    - [ ] `bash scripts/ci-guards.sh` green

    ## 375px screenshot
    <!-- paste screenshot -->
    ```

    2. Create `scripts/verify-viewport.mjs` — a small Playwright script (not a test) that launches Chromium at 375×667, navigates to `$PLAYWRIGHT_BASE_URL`, takes `docs/screenshots/375-dashboard.png`, exits non-zero if `document.documentElement.scrollWidth > 375`. Invoke manually: `node scripts/verify-viewport.mjs`.

    3. Extend `tests/e2e/layout.spec.ts` with a screenshot assertion: `await expect(page).toHaveScreenshot('dashboard-375.png', { maxDiffPixelRatio: 0.05 });` — optional, behind env flag `E2E_SCREENSHOTS=1` so CI doesn't flake.
  </action>
  <verify>
    <automated>test -f .github/pull_request_template.md &amp;&amp; grep -q "375" .github/pull_request_template.md &amp;&amp; test -f scripts/verify-viewport.mjs &amp;&amp; node --check scripts/verify-viewport.mjs</automated>
  </verify>
  <done>
    PR template + screenshot script land; UI-11 structurally enforced.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Friend opens DEV URL on real iPhone</name>
  <files>DEV deployment (CF Pages preview URL)</files>
  <action>
    Human verification checkpoint after automated deployment. Claude does NOT execute this task — it waits for the friend's sign-off signal.
  </action>
  <verify>Friend types "approved" after completing the verification steps below.</verify>
  <done>Friend has signed off AND made at least one real business decision from the numbers on the dashboard (Core Value gate from PROJECT.md).</done>
  <what-built>
    Full Phase 4 dashboard deployed to CF Pages DEV: login → dashboard with 5 KPI tiles + cohort retention curve + LTV bars + frequency list + new-vs-returning stacked bar + sticky chip bar + freshness label.
  </what-built>
  <how-to-verify>
    1. Push to main — CF Pages auto-deploys.
    2. Share DEV URL with the friend (founder's friend per PROJECT.md).
    3. Friend opens on their iPhone Safari, logs in with seeded credentials.
    4. Verify:
       - Dashboard loads at 375px with no horizontal scroll
       - All 9 cards visible and readable (text ≥12px)
       - Tapping a chip (e.g. 30d) updates Transactions + Avg ticket + NewVsReturning but NOT cohort/LTV/frequency
       - Grain toggle on cohort card switches Day/Week/Month
       - Freshness label shows a reasonable "Last updated Xh ago"
       - Logout button works, redirects to `/login`, can log back in
    5. Friend makes ONE real business decision from the numbers (Core Value gate).
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass, or describe issues (which card, what broke).
  </resume-signal>
</task>

</tasks>

<verification>
- `npm run build` green, `npm run test:unit` green (15 passing, 0 todos), CI guards green
- PR template enforces 375px screenshot
- Friend sign-off received (checkpoint)
</verification>

<success_criteria>
Phase 4 goal met: the friend opens the dashboard on their phone and reads revenue, cohorts, LTV, frequency, and new-vs-returning at 375px with preset date chips.
</success_criteria>

<output>
Create `.planning/phases/04-mobile-reader-ui/04-05-SUMMARY.md`.
</output>
