---
phase: 04-mobile-reader-ui
plan: 09
type: execute
mode: gap_closure
gap_closure: true
wave: 2
depends_on:
  - 04-06
  - 04-08
files_modified:
  - .planning/phases/04-mobile-reader-ui/04-HUMAN-UAT.md
  - scripts/seed-demo-data.sql
  - tests/e2e/dashboard-happy-path.spec.ts
autonomous: false
requirements:
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - UI-09
  - UI-10
  - UI-11
must_haves:
  truths:
    - "A human UAT checklist (04-HUMAN-UAT.md) exists and covers the 5 adversarial-QA blind spots from Gap D"
    - "A seed-demo-data.sql script populates DEV with at least 2 weeks of cohort transactions for the test restaurant"
    - "A Playwright happy-path spec loads / against seeded data and asserts all 9 cards render without console errors"
    - "Phase 4 dashboard loads in DEV with seeded data, no hydration errors, all 9 cards visible (human checkpoint)"
  artifacts:
    - path: ".planning/phases/04-mobile-reader-ui/04-HUMAN-UAT.md"
      provides: "iPhone-at-375px UAT checklist for Phase 4 sign-off"
      contains: "freshness label"
    - path: "scripts/seed-demo-data.sql"
      provides: "Idempotent SQL that seeds the test restaurant with cohort-shaped transactions"
      contains: "insert into public.transactions"
    - path: "tests/e2e/dashboard-happy-path.spec.ts"
      provides: "Playwright spec asserting all 9 cards render with no console errors"
  key_links:
    - from: "tests/e2e/dashboard-happy-path.spec.ts"
      to: "src/routes/+page.svelte"
      via: "Playwright nav to / against seeded DEV data"
      pattern: "page.goto.*'/'"
    - from: "scripts/seed-demo-data.sql"
      to: "public.transactions"
      via: "INSERT statements producing >=2 weeks of cohort data"
      pattern: "transactions"
---

<objective>
Close Phase 4 Gap D: every Phase 4 unit and e2e test ran against empty-state paths, which is exactly why Gap A escaped — the chart code was never exercised with data. Add a happy-path adversarial QA layer: a seeded demo dataset, a Playwright spec that exercises the full dashboard against that data, and a human UAT checklist for the 5 blind spots called out in 04-VERIFICATION.md (freshness label, all 9 cards have data, chip scoping flips correct cards, grain toggle swaps labels, no console errors).

This plan must run AFTER 04-06 (charts must render against layerchart 2.x) and 04-08 (DEV migrations must be applied + drift guard live). Otherwise the happy-path spec would either crash on layerchart or query views that don't exist.

Purpose: Make "the dashboard works on real data" a verified, repeatable claim.
Output: Seeded data, e2e happy-path spec, human UAT checklist, signed-off iPhone screenshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-mobile-reader-ui/04-VERIFICATION.md
@.planning/phases/04-mobile-reader-ui/04-CONTEXT.md
@.planning/phases/04-mobile-reader-ui/04-UI-SPEC.md
@src/routes/+page.server.ts
@src/routes/+page.svelte
@playwright.config.ts

<interfaces>
The 9 cards expected on `/` (per 04-CONTEXT.md D-02 + plans 04-03..04-05):
1. Revenue · Today (KpiTile, fixed window)
2. Revenue · 7d (KpiTile, fixed window)
3. Revenue · 30d (KpiTile, fixed window)
4. Tx count (chip-scoped)
5. Avg ticket (chip-scoped)
6. Cohort retention (LayerChart Spline)
7. LTV (LayerChart Bars)
8. Visit frequency (HTML bars)
9. New vs returning (HTML stacked bar; chip-scoped — D-19a)

Plus the freshness label under the chip bar (D-10) — not a card but a required UI element.

Test restaurant id (DEV): `ba1bf707-aae9-46a9-8166-4b6459e6c2fd`
Test user (provisioned in 04-07 land): `iguchise@gmail.com`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/seed-demo-data.sql for the test restaurant</name>
  <files>scripts/seed-demo-data.sql</files>
  <read_first>
    - supabase/migrations/0010_cohort_mv.sql (cohort_mv shape — what columns must come out)
    - supabase/migrations/0011_kpi_daily_mv_real.sql (kpi_daily_mv source columns)
    - supabase/migrations/0012_leaf_views.sql (retention/ltv/frequency/new_vs_returning view definitions)
    - public.transactions schema (find it in 0001 / 0003 — column list, NOT NULL constraints, business_date column)
    - .planning/phases/04-mobile-reader-ui/04-VERIFICATION.md §"State that needs cleanup" (test restaurant id)
  </read_first>
  <action>
    1. Create `scripts/seed-demo-data.sql` — IDEMPOTENT SQL that:
       - Targets the test restaurant id `ba1bf707-aae9-46a9-8166-4b6459e6c2fd` (declare it as a top-of-file `\set` variable or inline literal)
       - Begins with a guarded delete: `delete from public.transactions where restaurant_id = 'ba1bf707-aae9-46a9-8166-4b6459e6c2fd' and source_tx_id like 'demo-%';` (only deletes our seeded rows, never real data)
       - Inserts ~14 days × ~6 transactions/day of cohort-shaped data:
         - At least 4 distinct cohorts (4 different `card_hash` values whose first visit lands in 4 different weeks)
         - At least one cohort with `cohort_size >= 5` (otherwise UI sparse filter D-14 hides it)
         - Mix of repeat visits per card_hash so retention curves have non-trivial values at period 1 and period 2
         - Some `card_hash IS NULL` rows (cash_anonymous bucket for new-vs-returning tie-out, per Phase 3 D-14)
         - business_date spread across the last 14 days (use `now() - interval 'X days'` so the data stays "fresh" relative to whenever the seed runs)
         - net_cents values in the 800..3500 range (€8..€35 — realistic ramen tickets)
       - Ends with a sanity assertion (commented or via DO block):
         ```sql
         do $$
         declare
           tx_count int;
         begin
           select count(*) into tx_count from public.transactions
             where restaurant_id = 'ba1bf707-aae9-46a9-8166-4b6459e6c2fd' and source_tx_id like 'demo-%';
           if tx_count < 60 then
             raise exception 'seed-demo-data: expected >= 60 demo rows, got %', tx_count;
           end if;
         end $$;
         ```
    2. Apply to DEV: `psql "$SUPABASE_DB_URL" -f scripts/seed-demo-data.sql` (or via supabase CLI / DB MCP). Capture row count.
    3. Refresh the materialized views so cohort_mv and kpi_daily_mv pick up the seed:
       ```sql
       select public.refresh_analytics_mvs();
       ```
       (Function exists per Phase 3 plan 03-05.)
    4. Verify the views now return data for the test restaurant via JWT-claim-set queries (use the test_* SECURITY DEFINER helpers from Phase 3 03-04). Specifically:
       - `kpi_daily_v` returns ≥ 14 rows for the test restaurant
       - `cohort_v` (or whatever wrapper Phase 3 named it) returns ≥ 4 cohorts
       - `ltv_v` returns ≥ 4 cohorts
       - `frequency_v` returns ≥ 1 row
       - `new_vs_returning_v` returns ≥ 14 rows
  </action>
  <verify>
    <automated>test -f scripts/seed-demo-data.sql &amp;&amp; grep -q "demo-" scripts/seed-demo-data.sql &amp;&amp; grep -q "ba1bf707-aae9-46a9-8166-4b6459e6c2fd" scripts/seed-demo-data.sql</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/seed-demo-data.sql` exists
    - Re-running the script against DEV is idempotent (row count identical between consecutive runs)
    - After seeding + MV refresh: `kpi_daily_v` rows ≥ 14, distinct cohorts in cohort wrapper ≥ 4 (verified manually, recorded in commit)
    - All seeded rows have `source_tx_id` prefixed `demo-` so they are distinguishable from real data
  </acceptance_criteria>
  <done>Demo dataset is in DEV, MVs refreshed, all 9 cards' upstream views return non-empty results for the test restaurant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Playwright happy-path spec asserting all 9 cards render against seeded DEV data</name>
  <files>tests/e2e/dashboard-happy-path.spec.ts</files>
  <read_first>
    - tests/e2e/charts-with-data.spec.ts (just-created sibling from 04-06 — reuse fixture/auth pattern if applicable)
    - tests/e2e/layout.spec.ts (existing baseline — assertion patterns, viewport)
    - src/routes/+page.svelte (find data-testid attributes for each of the 9 cards; if missing, this task adds them)
    - playwright.config.ts (mobile chromium 375x667)
  </read_first>
  <behavior>
    - Test 1: signed-in user lands on `/` (uses storageState or programmatic sign-in helper)
    - Test 2: all 9 card headings are visible (Revenue Today, Revenue 7d, Revenue 30d, Tx count, Avg ticket, Cohort retention, LTV, Visit frequency, New vs returning)
    - Test 3: each KPI tile shows a non-empty number (regex `/€\s?\d/`)
    - Test 4: cohort card svg contains ≥ 1 `<path>` (Spline drew a line)
    - Test 5: LTV card svg contains ≥ 1 `<rect>` (Bars drew a bar)
    - Test 6: freshness label is visible and matches `/last updated|updated.*ago/i`
    - Test 7: page console.error count == 0
    - Test 8: clicking the `30d` chip changes the URL to `?range=30d` AND keeps cohort/ltv cards unchanged (chip-independence per D-04)
    - Test 9: clicking the cohort card's grain toggle from `Week` to `Day` changes the x-axis labels (chip-local control per D-12)
  </behavior>
  <action>
    1. Add data-testid attributes to each of the 9 card components if not already present:
       - `data-testid="kpi-revenue-today"`, `data-testid="kpi-revenue-7d"`, `data-testid="kpi-revenue-30d"`
       - `data-testid="kpi-tx-count"`, `data-testid="kpi-avg-ticket"`
       - `data-testid="cohort-card"` (already added in 04-06 if Path B was needed)
       - `data-testid="ltv-card"` (already added in 04-06)
       - `data-testid="frequency-card"`, `data-testid="nvr-card"`
       - `data-testid="freshness-label"`
    2. Auth setup: write a Playwright `globalSetup` (or use `storageState`) that signs in as `iguchise@gmail.com` once and reuses the session. The credentials come from env (`TEST_USER_EMAIL` / `TEST_USER_PASSWORD`). Skip the test gracefully if env vars are missing.
    3. Create `tests/e2e/dashboard-happy-path.spec.ts` implementing all 9 behaviors above. Use explicit `await expect(...).toBeVisible({ timeout: 5000 })` calls; do not rely on implicit waits.
    4. Run `npx playwright test dashboard-happy-path` against DEV with seed loaded — must pass.
    5. Run the full e2e suite to confirm no regressions: `npx playwright test`.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/dashboard-happy-path.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/dashboard-happy-path.spec.ts` exists with ≥ 9 assertions
    - `grep -c "data-testid" src/lib/components/` ≥ 9 across the dashboard components
    - `npx playwright test tests/e2e/dashboard-happy-path.spec.ts` exits 0
    - `npx playwright test` (full suite) exits 0
    - Spec includes an explicit `consoleErrors.length === 0` assertion (regression guard for Gap A and any future hydration crash)
  </acceptance_criteria>
  <done>Happy-path spec exists and passes against seeded DEV data, exercising every card and the freshness label with zero console errors.</done>
</task>

<task type="auto">
  <name>Task 3: Author 04-HUMAN-UAT.md adversarial QA checklist</name>
  <files>.planning/phases/04-mobile-reader-ui/04-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/04-mobile-reader-ui/04-VERIFICATION.md §"Gap D" (the 5 blind spots are the source of truth for this checklist)
    - .planning/phases/04-mobile-reader-ui/04-CONTEXT.md (D-01..D-22 — the design contracts being verified)
  </read_first>
  <action>
    Create `.planning/phases/04-mobile-reader-ui/04-HUMAN-UAT.md` with this exact structure:
    ```markdown
    ---
    status: pending
    phase: 04-mobile-reader-ui
    purpose: Adversarial human QA of the mobile reader dashboard against seeded DEV data
    runner: human, on a real iPhone (or iOS Simulator) at 375px viewport
    prerequisite: scripts/seed-demo-data.sql applied to DEV + refresh_analytics_mvs() executed
    ---

    # Phase 4 — Human UAT Checklist

    Run this against the DEV deployment (NOT localhost) on a real iPhone or iOS Simulator at 375×667. Sign in as the seeded test user. Take a screenshot at each `[ ]` step.

    ## Setup (one-time per session)
    - [ ] Confirm DEV has seeded data: visit Supabase SQL editor and run `select count(*) from public.transactions where source_tx_id like 'demo-%';` — must return ≥ 60
    - [ ] Confirm MVs are fresh: `select public.refresh_analytics_mvs();`
    - [ ] Sign in to the DEV deployment URL as `iguchise@gmail.com`

    ## Section A — Freshness signal (closes Gap D blind spot 1)
    - [ ] Freshness label is visible directly under the sticky chip bar
    - [ ] Label text matches "Last updated Xh ago" (X is a number)
    - [ ] If `MAX(ingested_at)` is < 30h ago, label is muted gray (default)
    - [ ] (Optional) Force-stale test: temporarily push transactions.ingested_at back 36h via SQL; reload; confirm label flips yellow. Revert.

    ## Section B — All 9 cards have data (closes Gap D blind spot 2)
    - [ ] Revenue · Today shows a € number (not "No transactions in this window")
    - [ ] Revenue · 7d shows a € number
    - [ ] Revenue · 30d shows a € number
    - [ ] Tx count card shows a number
    - [ ] Avg ticket card shows a € number with decimals
    - [ ] Cohort retention card shows ≥ 1 line (curve) on the LayerChart svg
    - [ ] LTV card shows ≥ 1 bar on the LayerChart svg
    - [ ] Visit frequency card shows the 5 buckets (1, 2, 3–5, 6–10, 11+) with non-zero rows
    - [ ] New vs returning card shows a stacked bar with all 3 segments labeled

    ## Section C — Chip scoping flips correct cards (closes Gap D blind spot 3)
    - [ ] Default chip is `7d`
    - [ ] Tap `30d`: Tx count and Avg ticket numbers change
    - [ ] Tap `30d`: New vs returning bar changes (D-19a — the ONE chip-scoped analytics card)
    - [ ] Tap `30d`: Revenue Today / 7d / 30d tiles do NOT change (they are fixed-window per D-06)
    - [ ] Tap `30d`: Cohort retention curves do NOT change
    - [ ] Tap `30d`: LTV bars do NOT change
    - [ ] Tap `30d`: Visit frequency bars do NOT change
    - [ ] URL contains `?range=30d` after the tap

    ## Section D — Grain toggle swaps cohort labels (closes Gap D blind spot 4)
    - [ ] Cohort card's grain toggle defaults to `Week`
    - [ ] Tap `Day` in the grain toggle: cohort labels in the legend / x-axis change to daily granularity
    - [ ] Tap `Month`: labels switch to monthly
    - [ ] LTV bars do NOT change when grain toggle changes (LTV is weekly-only in v1, per D-12)

    ## Section E — Console errors (closes Gap D blind spot 5)
    - [ ] Open Safari Web Inspector (or Chrome DevTools remote)
    - [ ] Reload the page
    - [ ] Console shows ZERO red errors
    - [ ] Specifically: no `scale.copy is not a function` (Gap A regression guard)
    - [ ] Specifically: no `restaurant_id` undefined / `not provisioned` redirect loop (Gap B regression guard)

    ## Section F — Layout sanity (UI-11 PR template)
    - [ ] Single column at 375px (no horizontal scroll)
    - [ ] Header has "Ramen Bones" + logout glyph
    - [ ] Chip bar is sticky on scroll
    - [ ] No text smaller than 12px

    ## Sign-off
    - [ ] All sections above passed
    - [ ] Screenshots attached to the PR / commit
    - [ ] Tester signs: ___________________
    - [ ] Date: ___________________

    ---

    If any item fails, do NOT mark Phase 4 complete. File a new gap in `04-VERIFICATION.md` and re-run `/gsd:plan-phase 4 --gaps`.
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/04-mobile-reader-ui/04-HUMAN-UAT.md &amp;&amp; grep -q "freshness label" .planning/phases/04-mobile-reader-ui/04-HUMAN-UAT.md &amp;&amp; grep -q "Gap A regression guard" .planning/phases/04-mobile-reader-ui/04-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all 6 sections (Setup, A, B, C, D, E, F + Sign-off)
    - Contains the 5 Gap D blind spots verbatim (freshness label, 9 cards data, chip scoping, grain toggle, no console errors)
    - References the Gap A and Gap B regression guards explicitly
    - Marked `status: pending` in frontmatter (executor flips to `passed` after Task 4 sign-off)
  </acceptance_criteria>
  <done>04-HUMAN-UAT.md exists with all 5 Gap D blind spots covered as checklist items.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human iPhone UAT sign-off against DEV</name>
  <what-built>
    Tasks 1-3 produced:
    - DEV is seeded with ≥60 demo transactions for the test restaurant
    - MVs refreshed via refresh_analytics_mvs()
    - Playwright happy-path spec passes against seeded data
    - 04-HUMAN-UAT.md checklist is ready to walk through

    Combined with 04-06 (layerchart 2.x), 04-07 (auth hook SECURITY DEFINER), and 04-08 (migration drift guard), the dashboard should now be fully usable on a real iPhone.
  </what-built>
  <how-to-verify>
    1. Open the DEV deployment URL on a real iPhone (or iOS Simulator) at 375×667
    2. Sign in as `iguchise@gmail.com`
    3. Walk through every checkbox in `.planning/phases/04-mobile-reader-ui/04-HUMAN-UAT.md` Sections A–F
    4. Take a screenshot of the home dashboard at full scroll (top → bottom)
    5. Open Safari Web Inspector and confirm zero console errors during initial load AND while interacting with chips + grain toggle
    6. If any checkbox fails, STOP, file a new gap in 04-VERIFICATION.md, and do not mark Phase 4 complete

    Specifically pay attention to:
    - Gap A regression: chart cards must render without "$scale.copy" error
    - Gap B regression: signed-in user must NOT be redirected to /not-provisioned
    - D-19a invariant: chip change must move new-vs-returning but NOT cohort/ltv/frequency
  </how-to-verify>
  <resume-signal>Type "approved" with screenshot attached, OR describe the failing checklist item(s) and which gap section they belong to.</resume-signal>
</task>

</tasks>

<verification>
1. `scripts/seed-demo-data.sql` applied to DEV, idempotent
2. `npx playwright test tests/e2e/dashboard-happy-path.spec.ts` exits 0
3. `npx playwright test` (full suite) exits 0
4. `.planning/phases/04-mobile-reader-ui/04-HUMAN-UAT.md` exists and covers all 5 Gap D blind spots
5. Human iPhone UAT signed off with screenshot
6. Phase-level gates: `npm run build` + `npm run test:unit` + `npx playwright test` + `npm run test:guards` all green
</verification>

<success_criteria>
- DEV dashboard loads on a real iPhone with seeded data, all 9 cards render, no console errors
- Migration-drift guard is green (Gap C closed)
- Auth hook SECURITY DEFINER guard is green (Gap B closed)
- LayerChart 2.x charts render against non-empty data (Gap A closed)
- Human UAT checklist signed off (Gap D closed)
- Phase 4 can finally be marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-mobile-reader-ui/04-09-SUMMARY.md` with: seed row count, full Playwright test report, link to attached UAT screenshots, sign-off date + tester, final phase-gate summary (build/unit/e2e/guards all green).
</output>
