---
phase: 07-column-promotion
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - scripts/ingest/types.ts
  - scripts/ingest/normalize.ts
  - tests/ingest/normalize.test.ts
  - tests/ingest/idempotency.test.ts
autonomous: true
requirements: [DM-03]
must_haves:
  truths:
    - "TxRow interface carries wl_issuing_country and card_type"
    - "normalize.ts exports canonicalizeCardType and populates both new fields during invoice-grain reduction"
    - "canonicalizeCardType output for every entry in canonical-card-types.json matches the expected canonical bucket (TS↔SQL identity)"
    - "Re-running the loader against the same CSV produces zero diffs in transactions — including zero diffs on wl_issuing_country and card_type"
    - "Worldline fallback path maps (wl='', pos='Visa') → 'visa' from the POS entry"
    - "Wave 0 normalize.test.ts canonicalizeCardType block + idempotency.test.ts assertions are unskipped and GREEN"
  artifacts:
    - path: "scripts/ingest/normalize.ts"
      provides: "canonicalizeCardType helper + extended TxRow construction"
      contains: "canonicalizeCardType"
    - path: "scripts/ingest/types.ts"
      provides: "TxRow interface extended with wl_issuing_country + card_type"
  key_links:
    - from: "scripts/ingest/normalize.ts TxRow reducer"
      to: "canonicalizeCardType(first.wl_card_type, first.card_type)"
      via: "first-row-of-invoice-group (matches existing tip/sales_type pattern)"
      pattern: "canonicalizeCardType\\("
    - from: "scripts/ingest/upsert.ts (unchanged)"
      to: "public.transactions insert payload"
      via: "spread of TxRow minus invoice_number (normalize.ts:42-45 pattern already handles new fields)"
      pattern: "\\.\\.\\.rest"
---

<objective>
Extend the CSV loader to persist wl_issuing_country and card_type onto future ingests, using the same canonical mapping the migration 0019 backfill used. Add canonicalizeCardType TS helper (identity-paired with public.normalize_card_type). Flip Wave 0 normalize.test.ts canonical tests and idempotency.test.ts assertions to GREEN.

Purpose: Satisfy DM-03 — new ingests fill both new columns, re-runs are zero-diff.
Output: TxRow + normalize.ts updated; canonicalizeCardType exported; tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-column-promotion/07-CONTEXT.md
@.planning/phases/07-column-promotion/07-RESEARCH.md
@.planning/phases/07-column-promotion/07-02-SUMMARY.md
@scripts/ingest/types.ts
@scripts/ingest/normalize.ts
@scripts/ingest/upsert.ts
@tests/ingest/fixtures/canonical-card-types.json
@tests/ingest/fixtures/sample.csv

<interfaces>
<!-- From 07-RESEARCH "Loader Integration — Exact Insertion Point" -->

Change 1 — types.ts:46-58 (TxRow interface adds):
  wl_issuing_country: string | null;   // char(2) or null
  card_type: string;                    // canonical, never null ('unknown' min)

Change 2 — normalize.ts:126-138 (TxRow construction), first-row-of-invoice-group pattern:
  wl_issuing_country: (first.wl_issuing_country || '').trim() || null,
  card_type: canonicalizeCardType(first.wl_card_type, first.card_type),

Change 3 — upsert.ts:42-45: ZERO CODE CHANGE (spread handles new fields).
Change 4 — parse.ts: ZERO CHANGE (29-column header validator already covers both source columns).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TxRow + implement canonicalizeCardType + wire into reducer</name>
  <files>
    scripts/ingest/types.ts,
    scripts/ingest/normalize.ts,
    tests/ingest/normalize.test.ts
  </files>
  <read_first>
    - scripts/ingest/types.ts lines 46-58 (TxRow interface)
    - scripts/ingest/normalize.ts lines 15-20 (normalizePaymentMethod — where canonicalizeCardType sits next to)
    - scripts/ingest/normalize.ts lines 84-142 (invoice-grain reducer; TxRow construction at 126-138; first-row convention at line 118)
    - scripts/ingest/upsert.ts lines 42-45 (verify no change needed)
    - tests/ingest/fixtures/canonical-card-types.json (from 07-02)
  </read_first>
  <behavior>
    (These are the tests already drafted in 07-01 — unskip and make pass):
    - canonicalizeCardType unit tests: 13+ cases from canonical-card-types.json all map to expected buckets
    - Additionally: data-driven test that loads canonical-card-types.json and asserts every entry
    - TxRow snapshot: reducer output for a sample.csv invoice row includes wl_issuing_country + card_type with correct canonical values
    - Worldline fallback test: fixture row T-FALLBACK (wl_card_type='', card_type='Visa') → TxRow.card_type === 'visa'
  </behavior>
  <action>
    1. Edit scripts/ingest/types.ts — add to the TxRow interface (07-RESEARCH exact lines 46-58):
       ```ts
       wl_issuing_country: string | null;
       card_type: string;
       ```
       Place them after `sales_type` and before `invoice_number` to match 07-RESEARCH Change 1.

    2. Edit scripts/ingest/normalize.ts — add `canonicalizeCardType` between line 20 (end of normalizePaymentMethod) and line 26 (start of toStagingRows). Implementation per 07-RESEARCH Focus Q2:
       ```ts
       const CARD_TYPE_CANONICAL: Record<string, string> = {
         'visa': 'visa',
         'mastercard': 'mastercard', 'mc': 'mastercard', 'master card': 'mastercard',
         'amex': 'amex', 'american express': 'amex',
         'maestro': 'maestro',
         'girocard': 'girocard', 'ec': 'girocard', 'ec karte': 'girocard', 'ec-karte': 'girocard',
       };

       export function canonicalizeCardType(
         wl: string | null | undefined,
         pos: string | null | undefined,
       ): string {
         const raw = (wl ?? '').trim() || (pos ?? '').trim();
         if (!raw) return 'unknown';
         const key = raw.toLowerCase().replace(/\s+/g, ' ');
         return CARD_TYPE_CANONICAL[key] ?? 'other';
       }
       ```
       Add `// D-03 + D-04: Worldline first, POS fallback, canonical-set mapping.` comment.

    3. Edit scripts/ingest/normalize.ts reducer at lines 126-138 — add the two new TxRow fields per 07-RESEARCH Change 2:
       ```ts
       wl_issuing_country: (first.wl_issuing_country || '').trim() || null,
       card_type: canonicalizeCardType(first.wl_card_type, first.card_type),
       ```
       Place after `sales_type: first.sales_type,` and before `invoice_number: invoice,`.

    4. Unskip the normalize.test.ts canonicalizeCardType describe block from 07-01. Add a data-driven test that imports canonical-card-types.json and loops `expected = canonicalizeCardType(input, null)` (the Task-1 fixture models wl-only inputs; for POS fallback coverage, the existing Worldline-fallback case in the block already covers `('','Visa') → 'visa'`).

    5. Verify scripts/ingest/upsert.ts requires ZERO change — the destructure at line 42 (`const { invoice_number, ...rest } = r;`) already spreads new fields into the insert payload. If your read of the file shows otherwise, STOP and raise.

    6. Verify scripts/ingest/parse.ts requires ZERO change — the 29-column header validator already covers card_type (line 28), wl_card_type (line 34), wl_issuing_country (line 36).

    Per D-07: this task is the normalization-coverage + Worldline-fallback test locked requirement.
  </action>
  <verify>
    <automated>npm run test -- --run tests/ingest/normalize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - canonicalizeCardType exported from normalize.ts
    - All canonical-card-types.json entries pass the data-driven test
    - TxRow interface has wl_issuing_country + card_type
    - npm run typecheck (or `tsc --noEmit`) passes
    - upsert.ts + parse.ts unchanged
    - normalize.test.ts GREEN, previously-skipped block now running
  </acceptance_criteria>
  <done>
    canonicalizeCardType lives next to normalizePaymentMethod and is byte-identical in output to public.normalize_card_type on all shared fixture entries. Reducer populates both new TxRow fields.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotency integration — zero-diff on re-ingest including new columns</name>
  <files>tests/ingest/idempotency.test.ts</files>
  <read_first>
    - tests/ingest/idempotency.test.ts (existing shape from 07-01 extension — is it hitting real Supabase or mock? see 07-RESEARCH Open Question 2)
    - scripts/ingest/index.ts (loader entry + report shape: transactions_new, transactions_updated)
  </read_first>
  <behavior>
    - Two sequential loader runs against an identical CSV → run 2 report has transactions_new === 0 AND transactions_updated === 0
    - After run 2, select one of the new sample.csv fixture invoices (T-VISA) and assert wl_issuing_country + card_type match expected values
    - Pre-and-post snapshot equality for a sampled invoice's wl_issuing_country + card_type between run 1 and run 2
  </behavior>
  <action>
    Unskip the idempotency assertions drafted in 07-01. Confirm the test already uses the sample.csv fixture (which now has the 6 new rows from 07-01) and that the new fixture invoices survive the round trip unchanged.

    If the idempotency test hits a real Supabase (per 07-RESEARCH Open Q2), confirm migration 0019 is applied to the test DB before the test runs. Document in this plan's SUMMARY whether the test uses real DB or mock — so Wave 3 / Phase 8 know.

    Assert the Worldline-fallback fixture row (T-FALLBACK) lands in transactions.card_type = 'visa' via POS path — this covers D-07 fallback requirement.

    Assert the blackout-style fixture row (T-UNK) lands in transactions.wl_issuing_country = NULL and card_type = 'unknown'.
  </action>
  <verify>
    <automated>npm run test -- --run tests/ingest/idempotency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - idempotency.test.ts GREEN with the new assertions active
    - Second run reports transactions_new=0 AND transactions_updated=0
    - T-FALLBACK row confirms POS fallback path
    - T-UNK row confirms NULL country + 'unknown' card_type
    - Full suite `npm run test -- --run` green
  </acceptance_criteria>
  <done>
    DM-03 satisfied. Loader is idempotent including the two new columns. Wave 0 DM-* tests fully GREEN.
  </done>
</task>

</tasks>

<verification>
- `npm run test -- --run` — full suite green
- `npm run typecheck` — no TS errors
- `bash scripts/ci-guards.sh` — still passes
- Manual DEV spot-check: pick one recently-ingested invoice and SELECT its wl_issuing_country + card_type; confirm they are populated (not NULL for a card invoice)
</verification>

<success_criteria>
- TxRow interface extended, canonicalizeCardType exported
- Loader writes both new columns on every ingest
- Re-running loader is zero-diff
- Worldline fallback path proven by test
- TS canonicalizeCardType and SQL normalize_card_type agree on every entry in canonical-card-types.json
</success_criteria>

<output>
After completion, create `.planning/phases/07-column-promotion/07-03-SUMMARY.md` with:
- Confirmation idempotency.test.ts uses real DB vs mock
- Sample TxRow output for one of the new fixture invoices (before/after columns visible)
- Loader report JSON from run 1 + run 2 showing zero diffs
</output>
