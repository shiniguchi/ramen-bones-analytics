---
phase: 07-column-promotion
plan: 04
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/lib/filters.ts
  - src/lib/components/CountryMultiSelect.svelte
  - src/lib/components/FilterSheet.svelte
  - src/routes/+page.server.ts
  - tests/unit/filters-country.test.ts
  - tests/unit/country-multiselect.test.ts
  - tests/integration/filter-country-loader.test.ts
autonomous: true
requirements: [FLT-05]
must_haves:
  truths:
    - "src/lib/filters.ts accepts a `country` param via csvArray() and parseFilters round-trips meta-sentinels and ISO-2 codes"
    - "CountryMultiSelect component wraps MultiSelectDropdown and enforces mutual exclusion of meta-options vs specific countries (D-05)"
    - "+page.server.ts fetches distinct countries from transactions_filterable_v, prepends ['__de_only__','__non_de_only__'], appends '__unknown__'"
    - "+page.server.ts translates filters.country into the correct Supabase query: eq / or / is null / in — per D-05 + D-06 semantics"
    - "FilterSheet renders the CountryMultiSelect block with draft-state wiring mirroring sales_type/payment_method"
    - "All 3 Wave-0 FLT-05 tests are unskipped and GREEN"
  artifacts:
    - path: "src/lib/filters.ts"
      provides: "zod country param on the filters schema"
    - path: "src/lib/components/CountryMultiSelect.svelte"
      provides: "wrapper around MultiSelectDropdown with meta-option mutual-exclusion rules"
    - path: "src/lib/components/FilterSheet.svelte"
      provides: "country dropdown slot with draft state"
    - path: "src/routes/+page.server.ts"
      provides: "distinctCountriesP loader + country WHERE-clause composition"
  key_links:
    - from: "FilterSheet.svelte countryDraft"
      to: "CountryMultiSelect.svelte selected prop"
      via: "bind:selected"
      pattern: "bind:selected=\\{countryDraft\\}"
    - from: "+page.server.ts load fn"
      to: "transactions_filterable_v.wl_issuing_country"
      via: "q.eq / q.or / q.is / q.in based on meta-sentinel detection"
      pattern: "wl_issuing_country"
    - from: "src/lib/filters.ts schema"
      to: "parseFilters URL → typed object"
      via: "csvArray()"
      pattern: "country: csvArray"
---

<objective>
Wire FLT-05 country filter through the Phase 6 filter infrastructure. Extend the zod schema, build a thin CountryMultiSelect wrapper around the existing MultiSelectDropdown, integrate into FilterSheet, and translate the filter into correct Supabase WHERE clauses in +page.server.ts. Flip all 3 Wave-0 FLT-05 tests GREEN.

Purpose: Ship the user-visible payoff of Phase 7 — the country filter.
Output: Country dropdown live in the dashboard at 375px with DE only / Non-DE only / individual multi-select + Unknown bucket.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-column-promotion/07-CONTEXT.md
@.planning/phases/07-column-promotion/07-RESEARCH.md
@.planning/phases/07-column-promotion/07-02-SUMMARY.md
@src/lib/filters.ts
@src/lib/components/MultiSelectDropdown.svelte
@src/lib/components/FilterSheet.svelte
@src/routes/+page.server.ts

<interfaces>
<!-- From 07-RESEARCH "FilterSheet Meta-Option Integration" -->

Schema delta (filters.ts, after line 41):
  country: csvArray(),

Meta sentinels (string constants, must start with __ to never collide with ISO-2):
  __de_only__, __non_de_only__, __unknown__

distinctCountriesP loader (mirror distinctPaymentMethodsP at +page.server.ts:213-231):
  SELECT wl_issuing_country FROM transactions_filterable_v
  → unique + sort + prepend ['__de_only__','__non_de_only__'] + append '__unknown__'

WHERE-clause composition (from 07-RESEARCH, exact shape):
  if filters.country?.length > 0:
    if includes('__de_only__'):      q.eq('wl_issuing_country','DE')
    elif includes('__non_de_only__'): q.or('wl_issuing_country.is.null,wl_issuing_country.neq.DE')
    else:
      hasUnknown = includes('__unknown__')
      specific = country.filter(c => !c.startsWith('__'))
      if hasUnknown && specific.length: q.or(`wl_issuing_country.is.null,wl_issuing_country.in.(${specific.join(',')})`)
      elif hasUnknown:                   q.is('wl_issuing_country', null)
      elif specific.length:              q.in('wl_issuing_country', specific)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema + CountryMultiSelect component + unskip unit tests</name>
  <files>
    src/lib/filters.ts,
    src/lib/components/CountryMultiSelect.svelte,
    tests/unit/filters-country.test.ts,
    tests/unit/country-multiselect.test.ts
  </files>
  <read_first>
    - src/lib/filters.ts lines 40-55 (csvArray + parseFilters pattern)
    - src/lib/components/MultiSelectDropdown.svelte (full file — understand props + toggling)
    - 07-RESEARCH "FilterSheet Meta-Option Integration" section
    - tests/unit/filters-country.test.ts (from 07-01 — scaffold to unskip)
    - tests/unit/country-multiselect.test.ts (from 07-01 — scaffold to unskip)
  </read_first>
  <behavior>
    (Tests already drafted in 07-01; unskip and pass them.)
    filters.ts:
      - parseFilters({country:'__de_only__'}).country === ['__de_only__']
      - parseFilters({country:'DE,AT,FR'}).country === ['DE','AT','FR']
      - parseFilters({country:'__unknown__,DE'}).country === ['__unknown__','DE']
      - parseFilters({}).country === undefined

    CountryMultiSelect.svelte:
      - Selecting '__de_only__' while ['AT','DE'] selected → ['__de_only__']
      - Selecting 'AT' while ['__de_only__'] selected → ['AT']
      - Selecting '__non_de_only__' while ['__de_only__'] selected → ['__non_de_only__']
      - Meta-options render above a visual separator; specific options below
      - Unknown label 'Unknown' mapped from '__unknown__'
      - Country labels mapped via small ISO-2 → name table (at minimum DE,AT,CH,FR,NL — populate from Task 0 ground-truth in 07-02-SUMMARY)
  </behavior>
  <action>
    1. Edit src/lib/filters.ts — add `country: csvArray(),` after the payment_method line (~line 41). parseFilters automatically picks it up. No other changes.

    2. Create src/lib/components/CountryMultiSelect.svelte — thin Svelte 5 runes wrapper around MultiSelectDropdown:
       - Props: `options: string[]`, `selected: string[]` (bind:), label hint
       - Derived state via $derived: split options into metaOptions (startsWith '__' AND in the sentinel whitelist) and specificOptions (everything else)
       - Label map: `{ __de_only__: 'DE only', __non_de_only__: 'Non-DE only', __unknown__: 'Unknown', DE: 'DE (Germany)', AT: 'AT (Austria)', CH: 'CH (Switzerland)', FR: 'FR (France)', NL: 'NL (Netherlands)' }` — extend from 07-02-SUMMARY distribution
       - onToggle handler enforcing D-05 mutual exclusion:
         - if clicking '__de_only__' or '__non_de_only__': set selected = [clicked]
         - if clicking a specific (non-'__') option: strip all meta sentinels from selected, then toggle the clicked one
       - Render: either reuse MultiSelectDropdown and prepend meta buttons, OR render a Popover wrapping two lists separated by an `<hr>`. The simpler path is the second — MultiSelectDropdown is generic and mutual-exclusion logic doesn't belong in it (per 07-RESEARCH "wrap, not extend").

    3. Unskip tests/unit/filters-country.test.ts — remove `.skip` from describe. Should pass immediately.

    4. Unskip tests/unit/country-multiselect.test.ts — remove `.skip`. Use @testing-library/svelte to simulate toggles per the behavior above.
  </action>
  <verify>
    <automated>npm run test -- --run tests/unit/filters-country.test.ts tests/unit/country-multiselect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - filters.ts has `country: csvArray()` line
    - CountryMultiSelect.svelte exists, <150 lines
    - Both unit tests GREEN
    - npm run check (svelte-check) passes
    - ISO-2 → label map covers every country from 07-02-SUMMARY
  </acceptance_criteria>
  <done>
    Filter schema and UI wrapper land. Mutual exclusion provably enforced.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: +page.server.ts distinctCountriesP + WHERE composition + FilterSheet integration + unskip SSR test</name>
  <files>
    src/routes/+page.server.ts,
    src/lib/components/FilterSheet.svelte,
    tests/integration/filter-country-loader.test.ts
  </files>
  <read_first>
    - src/routes/+page.server.ts:213-231 (distinctPaymentMethodsP pattern)
    - src/routes/+page.server.ts full load fn (find where sales_type/payment_method WHERE clauses are applied to `q`)
    - src/lib/components/FilterSheet.svelte lines 25-26, 31-32, 54, ~98 (draft-state pattern; insertion point after payment-method block)
    - 07-RESEARCH "FilterSheet Meta-Option Integration" SSR snippet
    - tests/integration/filter-country-loader.test.ts (from 07-01 — scaffold to unskip)
  </read_first>
  <behavior>
    - +page.server.ts load fn returns `distinctCountries` prop to +page.svelte
    - When filters.country is undefined → no WHERE clause added to q
    - When filters.country=['__de_only__'] → q.eq('wl_issuing_country','DE') called
    - When filters.country=['__non_de_only__'] → q.or('wl_issuing_country.is.null,wl_issuing_country.neq.DE') called
    - When filters.country=['__unknown__'] → q.is('wl_issuing_country', null) called
    - When filters.country=['DE','AT'] → q.in('wl_issuing_country', ['DE','AT']) called
    - When filters.country=['__unknown__','DE'] → q.or('wl_issuing_country.is.null,wl_issuing_country.in.(DE)') called
    - FilterSheet receives distinctCountries as a prop and renders <CountryMultiSelect> bound to countryDraft
  </behavior>
  <action>
    1. Edit src/routes/+page.server.ts — add `distinctCountriesP` mirroring `distinctPaymentMethodsP` at lines 213-231. Exact shape from 07-RESEARCH:
       ```ts
       const distinctCountriesP = locals.supabase
         .from('transactions_filterable_v')
         .select('wl_issuing_country')
         .then(r => {
           const rows = (r.data ?? []) as Array<{ wl_issuing_country: string | null }>;
           const real = [...new Set(
             rows.map(x => x.wl_issuing_country).filter((v): v is string => !!v)
           )].sort();
           return ['__de_only__', '__non_de_only__', ...real, '__unknown__'];
         })
         .catch((e: unknown) => { console.error('[distinctCountries]', e); return [] as string[]; });
       ```
       Add `distinctCountriesP` to the `Promise.all(...)` block next to distinctPaymentMethodsP, destructure the result, and pass `distinctCountries` in the returned props object.

    2. In the same file, find where filters.sales_type / filters.payment_method are applied to the Supabase query `q` and add the country composition block directly after, matching 07-RESEARCH exactly (reproduced in <interfaces> above). Every branch must use the Supabase client's sanitized methods — NO string interpolation outside the single `q.or(...)` template, whose array elements come from `SELECT DISTINCT` (not user text), so FLT-07 / ci-guards Guard 6 stays satisfied.

    3. Edit src/lib/components/FilterSheet.svelte:
       - Add `distinctCountries: string[]` to Props
       - Add `let countryDraft = $state(filters.country ?? [])` near the other draft states (~line 25-26)
       - Add `countryDraft` to the apply/reset handlers matching salesTypeDraft + paymentMethodDraft logic (~line 31-32, 54)
       - After the payment-method block (~line 98), insert:
         ```svelte
         {#if distinctCountries.length > 0}
           <CountryMultiSelect
             options={distinctCountries}
             bind:selected={countryDraft}
           />
         {/if}
         ```
       - Import CountryMultiSelect at the top of the script block
       - Wire `country: countryDraft.length ? countryDraft : undefined` into the apply() filter payload

    4. Also update wherever +page.svelte passes props to FilterSheet to forward the new distinctCountries prop.

    5. Unskip tests/integration/filter-country-loader.test.ts. Should now pass against the mocked Supabase chain.

    6. Manual 375px verification via Chrome MCP against DEV (per project mandatory QA):
       - Open DEV URL, open FilterSheet, confirm country dropdown renders
       - Toggle DE only → confirm only DE rows appear in the KPI cards
       - Toggle Non-DE only → confirm non-DE + unknown rows surface
       - Toggle Unknown specifically → confirm NULL bucket
       - Toggle DE + AT specifically → confirm both render
       - Screenshot at 375px, paste into SUMMARY
  </action>
  <verify>
    <automated>npm run test -- --run tests/integration/filter-country-loader.test.ts && npm run test -- --run && npm run check && bash scripts/ci-guards.sh</automated>
  </verify>
  <acceptance_criteria>
    - Integration test GREEN
    - Full test suite GREEN
    - svelte-check GREEN
    - ci-guards GREEN (no dynamic SQL, view-only access preserved)
    - Manual 375px verification captured in SUMMARY
    - DEV dashboard renders the country filter and round-trips URL state correctly
    - All 4 Phase 7 ROADMAP success criteria satisfied
  </acceptance_criteria>
  <done>
    FLT-05 live on DEV. Phase 7 complete.
  </done>
</task>

</tasks>

<verification>
- `npm run test -- --run` full suite GREEN
- `npm run check` (svelte-check) GREEN
- `bash scripts/ci-guards.sh` GREEN
- Manual DEV 375px verify: country filter dropdown appears, meta-options mutually exclusive, DE-only / Non-DE-only / individual selection all produce correct KPI changes
- URL state round-trip: selecting DE only produces `?country=__de_only__` in the URL and reloading the page restores the selection
</verification>

<success_criteria>
- filters.ts accepts country param
- CountryMultiSelect component enforces D-05 mutual exclusion
- +page.server.ts translates country filter per D-05 + D-06 semantics
- FilterSheet renders the new dropdown at 375px without horizontal scroll
- All 3 Wave-0 FLT-05 tests unskipped and GREEN
- Phase 7 ROADMAP success criterion 5 (country filter) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-column-promotion/07-04-SUMMARY.md` with:
- 375px screenshot(s) from Chrome MCP on DEV
- URL state round-trip examples for each meta-option
- Final Phase 7 status — all 5 ROADMAP success criteria mapped to their proving artifact
</output>
