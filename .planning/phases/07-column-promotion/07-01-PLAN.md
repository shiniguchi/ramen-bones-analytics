---
phase: 07-column-promotion
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/ingest/schema.test.ts
  - tests/ingest/backfill.test.ts
  - tests/ingest/normalize.test.ts
  - tests/ingest/idempotency.test.ts
  - tests/unit/filters-country.test.ts
  - tests/unit/country-multiselect.test.ts
  - tests/integration/filter-country-loader.test.ts
autonomous: true
requirements: [DM-01, DM-02, DM-03, FLT-05]
must_haves:
  truths:
    - "Failing tests exist for every Phase 7 requirement before any production code lands"
    - "Running the test suite shows RED for schema, backfill, normalization, idempotency, country filter schema, country multi-select UI, and country SSR loader wiring"
    - "Extended normalize.test.ts fixture rows cover all canonical card-type buckets"
  artifacts:
    - path: "tests/ingest/schema.test.ts"
      provides: "DM-01 schema assertion via information_schema.columns"
    - path: "tests/ingest/backfill.test.ts"
      provides: "DM-02 backfill row-match spot-check"
    - path: "tests/unit/filters-country.test.ts"
      provides: "FLT-05 zod schema acceptance of country param including meta-sentinels"
    - path: "tests/unit/country-multiselect.test.ts"
      provides: "FLT-05 meta-option mutual-exclusion logic"
    - path: "tests/integration/filter-country-loader.test.ts"
      provides: "FLT-05 SSR WHERE-clause translation"
  key_links:
    - from: "tests/ingest/normalize.test.ts"
      to: "scripts/ingest/normalize.ts canonicalizeCardType"
      via: "direct import (import { canonicalizeCardType })"
      pattern: "canonicalizeCardType"
    - from: "tests/unit/filters-country.test.ts"
      to: "src/lib/filters.ts"
      via: "import { parseFilters }"
      pattern: "country"
---

<objective>
Wave 0 RED scaffold for Phase 7. Lands failing/skipped tests for every Phase 7 requirement before any production code is touched. Mirrors Phase 2/3/4/5/6 TDD entry patterns.

Purpose: Pin the behavioral contract. Waves 1-3 flip these tests GREEN one-by-one.
Output: 5 new test files + 2 extended existing test files, all committed RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-column-promotion/07-CONTEXT.md
@.planning/phases/07-column-promotion/07-RESEARCH.md
@tests/ingest/normalize.test.ts
@tests/ingest/idempotency.test.ts
@tests/ingest/fixtures/sample.csv
@src/lib/filters.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED scaffold for DM-01/DM-02/DM-03 (backend + loader)</name>
  <files>
    tests/ingest/schema.test.ts,
    tests/ingest/backfill.test.ts,
    tests/ingest/normalize.test.ts,
    tests/ingest/idempotency.test.ts,
    tests/ingest/fixtures/sample.csv
  </files>
  <read_first>
    - tests/ingest/normalize.test.ts (existing pattern, note the `expect(staging.length).toBe(24)` pin)
    - tests/ingest/idempotency.test.ts (existing pattern + whether it hits a real DB or mock — see 07-RESEARCH Open Question 2)
    - tests/ingest/fixtures/sample.csv (see RESEARCH: T-3 rows 5-7, T-11 rows 18-20 — invoice-grain wl_* columns)
    - scripts/ingest/normalize.ts:18-20 (normalizePaymentMethod location — canonicalizeCardType goes next to it)
    - supabase/migrations/0008_transactions_columns.sql (baseline transactions columns)
  </read_first>
  <behavior>
    schema.test.ts (DM-01):
      - queries information_schema.columns for `public.transactions`
      - asserts `wl_issuing_country` exists with data_type='character' and character_maximum_length=2
      - asserts `card_type` exists with data_type='text'
    backfill.test.ts (DM-02):
      - seeds 3 invoices into stg_orderbird_order_items with known wl_issuing_country + wl_card_type + card_type
      - one row: wl_card_type='Visa', wl_issuing_country='DE'  → expects (DE, visa)
      - one row: wl_card_type='', card_type='Mastercard', wl_issuing_country='AT' → expects (AT, mastercard) via POS fallback
      - one row: wl_card_type='', card_type='', wl_issuing_country='' (blackout-style) → expects (NULL, unknown)
      - asserts t.wl_issuing_country and t.card_type match expectations after running the migration backfill block
    normalize.test.ts (DM-03 canonical + fallback), extend existing:
      - new unit tests importing `canonicalizeCardType` from '../../scripts/ingest/normalize'
      - cases: ('Visa', null) → 'visa' ; ('VISA', '') → 'visa' ; ('MasterCard', null) → 'mastercard' ;
        ('mc', null) → 'mastercard' ; ('Master Card', null) → 'mastercard' ; ('girocard', null) → 'girocard' ;
        ('EC', null) → 'girocard' ; ('american express', null) → 'amex' ; ('Maestro', null) → 'maestro' ;
        ('', 'Visa') → 'visa' (POS fallback) ; ('', '') → 'unknown' ; (null, null) → 'unknown' ;
        ('Diners', null) → 'other'
      - extend sample.csv with 5 new invoice-grain rows covering visa/mastercard/girocard/unknown/other + one POS-fallback row
      - bump the `expect(staging.length).toBe(24)` pin to the new row count
    idempotency.test.ts (DM-03), extend existing:
      - after two sequential loader runs, assert report.transactions_new === 0 AND report.transactions_updated === 0
      - assert that for at least one re-ingested row, transactions.wl_issuing_country and transactions.card_type are both unchanged (read before/after, deep-equal)
  </behavior>
  <action>
    Create tests/ingest/schema.test.ts and tests/ingest/backfill.test.ts using the existing Vitest+Supabase patterns already used in tests/ingest/. Use `it.skip` or `it.fails` wrappers on cases whose production code does not exist yet, OR leave them as plain `it()` so they fail loud — pick `it.skip` with a TODO comment linking to 07-02-PLAN so the suite stays CI-green while still being traceable.

    Extend tests/ingest/normalize.test.ts with the 13 canonicalizeCardType unit cases. The import of `canonicalizeCardType` will fail to resolve until 07-03 — that IS the RED state. Mark the new describe block `describe.skip('canonicalizeCardType (Wave 2)', …)` so CI stays green but the skip appears in the vitest output.

    Extend tests/ingest/fixtures/sample.csv with 5 new invoice-grain rows (copy the 29-column structure from T-3 block). Each new invoice adds 1 line-item row only (single-line invoices are valid per the reducer). Invoice numbers: T-VISA, T-MC, T-GIRO, T-UNK, T-OTHER, T-FALLBACK (6 rows total). Bump `expect(staging.length).toBe(24)` → `toBe(30)`.

    Extend tests/ingest/idempotency.test.ts with a new assertion block inside the existing "two sequential runs" test: after the second run, run a Supabase select on transactions filtered to one of the new invoice numbers and snapshot-compare wl_issuing_country + card_type between the two runs. Mark new assertions skip-guarded on the existence of the wl_issuing_country column (query information_schema first; if missing, skip).

    Per D-07, idempotency + normalization coverage + Worldline fallback are non-negotiable — this task LANDS those tests. Canonical bucket coverage aligns with D-04.

    File:line refs: see 07-RESEARCH Focus Q2 for canonicalizeCardType shape, Focus Q1 for DISTINCT ON backfill shape.
  </action>
  <verify>
    <automated>npm run test -- --run tests/ingest/schema.test.ts tests/ingest/backfill.test.ts tests/ingest/normalize.test.ts tests/ingest/idempotency.test.ts 2>&1 | grep -E "(skip|fail|pass)"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist on disk
    - Vitest output shows the new describe blocks as skipped or failing (not green)
    - sample.csv row count bumped and existing sample.csv assertions updated consistently
    - No production files (scripts/ingest/**, supabase/migrations/**) modified
  </acceptance_criteria>
  <done>
    RED scaffold landed for DM-01/DM-02/DM-03. Running vitest shows the new tests in skip/fail state pinned to Wave 1-2 TODOs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED scaffold for FLT-05 (filter schema + UI + SSR)</name>
  <files>
    tests/unit/filters-country.test.ts,
    tests/unit/country-multiselect.test.ts,
    tests/integration/filter-country-loader.test.ts
  </files>
  <read_first>
    - src/lib/filters.ts (existing csvArray pattern at lines 40-41; parseFilters at ~line 48)
    - src/lib/components/MultiSelectDropdown.svelte (the primitive being wrapped)
    - src/lib/components/FilterSheet.svelte (draft-state pattern, salesTypeDraft/paymentMethodDraft around lines 25-26, 31-32, 54)
    - src/routes/+page.server.ts:213-231 (distinctPaymentMethodsP pattern to mirror for distinctCountriesP)
    - 07-RESEARCH "FilterSheet Meta-Option Integration" section for sentinel list
  </read_first>
  <behavior>
    filters-country.test.ts (FLT-05 zod schema):
      - parseFilters({ country: '__de_only__' }) → { country: ['__de_only__'] }
      - parseFilters({ country: '__non_de_only__' }) → { country: ['__non_de_only__'] }
      - parseFilters({ country: 'DE,AT,FR' }) → { country: ['DE','AT','FR'] }
      - parseFilters({ country: '__unknown__,DE' }) → { country: ['__unknown__','DE'] }
      - parseFilters({}) → { country: undefined } (no filter)
      - parseFilters({ country: '' }) → { country: undefined } (empty string = no filter)

    country-multiselect.test.ts (FLT-05 UI mutual exclusion):
      - Mounting CountryMultiSelect with options ['__de_only__','__non_de_only__','DE','AT','__unknown__'] and selected=[]
      - Toggling '__de_only__' → selected becomes ['__de_only__']
      - Then toggling 'AT' → selected becomes ['AT'] (meta cleared, per D-05)
      - Then toggling '__non_de_only__' → selected becomes ['__non_de_only__'] (specific cleared)
      - Toggling '__de_only__' while '__non_de_only__' is selected → selected becomes ['__de_only__'] (meta-options are mutually exclusive with each other)

    filter-country-loader.test.ts (FLT-05 SSR WHERE translation):
      - Mocks the Supabase client's .from('transactions_filterable_v').eq/.or/.is/.in chain
      - Asserts filters.country=['__de_only__'] calls .eq('wl_issuing_country', 'DE')
      - Asserts filters.country=['__non_de_only__'] calls .or('wl_issuing_country.is.null,wl_issuing_country.neq.DE')
      - Asserts filters.country=['__unknown__'] calls .is('wl_issuing_country', null)
      - Asserts filters.country=['DE','AT'] calls .in('wl_issuing_country', ['DE','AT'])
      - Asserts filters.country=['__unknown__','DE'] calls .or('wl_issuing_country.is.null,wl_issuing_country.in.(DE)')
      - Asserts filters.country=undefined adds no WHERE clause
  </behavior>
  <action>
    Create the 3 test files. Use `describe.skip('FLT-05 …', …)` wrappers so CI stays green until Wave 3 flips them.

    filters-country.test.ts — standard Vitest unit test, import { parseFilters } from '../../src/lib/filters'. The country field does not yet exist on the schema; the tests will fail compile/runtime until 07-04. Mark the describe block `.skip` with a TODO pointing to 07-04.

    country-multiselect.test.ts — use @testing-library/svelte (already in the repo per Phase 6) to mount the not-yet-existing CountryMultiSelect component. Skip-guarded until 07-04.

    filter-country-loader.test.ts — mock Supabase client chain (pattern from Phase 6 existing loader tests). Assert the exact WHERE-clause shape from 07-RESEARCH's loader snippet. Skip-guarded.

    All 3 files land with describe.skip so `npm run test -- --run` stays green but the skipped-test count increases by 3. Each skip has a comment: `// TODO(07-04): unskip when CountryMultiSelect + filters.ts country field land`.
  </action>
  <verify>
    <automated>npm run test -- --run tests/unit/filters-country.test.ts tests/unit/country-multiselect.test.ts tests/integration/filter-country-loader.test.ts 2>&1 | tee /tmp/phase7-wave0.log && grep -q "skipped" /tmp/phase7-wave0.log</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test files exist
    - Vitest shows +3 skipped describe blocks, 0 new failures
    - No production src/lib/** files modified
  </acceptance_criteria>
  <done>
    FLT-05 RED scaffold landed and committed. Full suite stays green via .skip, and Wave 3 has a clear unskip target.
  </done>
</task>

</tasks>

<verification>
- `npm run test -- --run` — full suite green (new tests skipped, not failing)
- `git diff --stat` — touches only tests/** and tests/ingest/fixtures/sample.csv
- No scripts/ingest/**, supabase/migrations/**, or src/** files modified
</verification>

<success_criteria>
- 5 new test files + 2 extended existing test files committed
- Full suite green with skipped-test count bumped
- Every Phase 7 requirement (DM-01/02/03, FLT-05) has a named failing/skipped test ready for its downstream wave to unskip
</success_criteria>

<output>
After completion, create `.planning/phases/07-column-promotion/07-01-SUMMARY.md` listing the test files created, the skip markers, and the wave each one will be flipped in.
</output>
