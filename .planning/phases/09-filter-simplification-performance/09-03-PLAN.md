---
phase: 09-filter-simplification-performance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0020_visit_attribution_mv.sql
  - supabase/migrations/0022_transactions_filterable_v_is_cash.sql
  - .planning/phases/08-visit-attribution-data-model/08-CONTEXT.md
autonomous: false
requirements: [VA-01, VA-02, VA-11, VA-12, VA-13]
gap_closure: true

must_haves:
  truths:
    - "supabase db push completes cleanly against DEV — migrations 0020, 0021, 0022 all apply"
    - "visit_attribution_mv materializes with tx_id as text sourced from transactions.source_tx_id"
    - "transactions_filterable_v exposes is_cash via a valid LEFT JOIN on (restaurant_id, source_tx_id)"
    - "phase8-visit-attribution.test.ts passes against TEST after the edits"
    - "Phase 9 dashboard cold-start smoke (UAT Test 1) succeeds against DEV"
  artifacts:
    - path: "supabase/migrations/0020_visit_attribution_mv.sql"
      provides: "visit_attribution_mv with correct text tx_id"
      contains: "t.source_tx_id"
    - path: "supabase/migrations/0022_transactions_filterable_v_is_cash.sql"
      provides: "transactions_filterable_v with is_cash joined on source_tx_id"
      contains: "va.tx_id = t.source_tx_id"
    - path: ".planning/phases/08-visit-attribution-data-model/08-CONTEXT.md"
      provides: "Corrected D-04 documenting tx_id text"
      contains: "tx_id text"
  key_links:
    - from: "visit_attribution_mv.tx_id"
      to: "transactions.source_tx_id"
      via: "MV SELECT (0020 line 12)"
      pattern: "t\\.source_tx_id\\s+as\\s+tx_id"
    - from: "transactions_filterable_v.is_cash"
      to: "visit_attribution_mv.tx_id"
      via: "LEFT JOIN predicate (0022 line 18)"
      pattern: "va\\.tx_id = t\\.source_tx_id"
---

<objective>
Fix the composite-PK type mismatch bug in Phase 8 migration 0020 and the Phase 9 view in migration 0022. Both reference a non-existent `transactions.id` column; the real row identity is `source_tx_id text` (per migration 0003). Unblock `supabase db push` against DEV, which unblocks Phase 9 UAT tests 2-9.

Purpose: Phase 9 UAT blocker — 8 tests cannot run until migrations land on DEV.
Output: Patched 0020 + 0022 migrations, corrected Phase 8 D-04 decision doc, and a clean `supabase db push` + passing integration test against TEST, then DEV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-filter-simplification-performance/09-UAT.md
@.planning/debug/09-migration-0020-tx-id.md
@supabase/migrations/0003_transactions_skeleton.sql
@supabase/migrations/0020_visit_attribution_mv.sql
@supabase/migrations/0022_transactions_filterable_v_is_cash.sql
@.planning/phases/08-visit-attribution-data-model/08-CONTEXT.md

<interfaces>
<!-- Authoritative transactions schema (from 0003_transactions_skeleton.sql) -->
<!-- NO surrogate id uuid column. PK is composite. -->

```sql
create table public.transactions (
  restaurant_id  uuid not null references public.restaurants(id),
  source_tx_id   text not null,
  occurred_at    timestamptz not null,
  card_hash      text,
  gross_cents    integer not null,
  net_cents      integer not null,
  created_at     timestamptz not null default now(),
  primary key (restaurant_id, source_tx_id)
);
```

Key rules:
- `tx_id` in `visit_attribution_mv` MUST be sourced from `t.source_tx_id` (type: text).
- The existing unique index `visit_attribution_mv_pk (restaurant_id, tx_id)` is correct once `tx_id` is text — no index change needed.
- Wrapper view `visit_attribution_v` just passes `tx_id` through — no change needed.
- Migrations 0020-0022 are UNPUSHED to DEV, so in-place edits are preferred over superseding migrations (keeps history clean).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix migration 0020 — t.id to t.source_tx_id, uuid to text in test helper</name>
  <files>supabase/migrations/0020_visit_attribution_mv.sql</files>
  <action>
Apply two exact edits to `supabase/migrations/0020_visit_attribution_mv.sql`:

**Edit 1 (line 12) — MV select column:**

OLD:
```
  t.id                          as tx_id,
```

NEW:
```
  t.source_tx_id                as tx_id,
```

**Edit 2 (line 49) — test_visit_attribution RETURNS TABLE column type:**

OLD:
```
  tx_id         uuid,
```

NEW:
```
  tx_id         text,
```

DO NOT change anything else. Specifically:
- Leave the unique index `visit_attribution_mv_pk (restaurant_id, tx_id)` alone (line 28-29) — correct as-is.
- Leave the wrapper view `visit_attribution_v` alone (lines 35-42) — passes `tx_id` through.
- Leave the refresh function (lines 71-82) alone.
- Leave comments/headers alone.

Rationale: `public.transactions` has no `id` column; the PK is composite `(restaurant_id, source_tx_id text)` per migration 0003. Since 0020 has never been applied anywhere, an in-place edit is preferred over a superseding migration (per planning constraint).
  </action>
  <verify>
    <automated>grep -nE "t\.id|tx_id\s+uuid" supabase/migrations/0020_visit_attribution_mv.sql; echo "Expected: no matches above. Post-fix greps:"; grep -n "t.source_tx_id" supabase/migrations/0020_visit_attribution_mv.sql; grep -n "tx_id         text" supabase/migrations/0020_visit_attribution_mv.sql</automated>
  </verify>
  <done>
- Line 12 reads `t.source_tx_id                as tx_id,`
- Line 49 reads `tx_id         text,`
- No remaining `t.id` or `tx_id uuid` in the file (parameter type `uuid` in function signature line 46/66 stays — that's `rid uuid`, unrelated).
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix migration 0022 — JOIN predicate t.id to t.source_tx_id</name>
  <files>supabase/migrations/0022_transactions_filterable_v_is_cash.sql</files>
  <action>
Apply one exact edit to `supabase/migrations/0022_transactions_filterable_v_is_cash.sql`:

**Edit (line 18) — LEFT JOIN predicate:**

OLD:
```
  ON va.restaurant_id = t.restaurant_id AND va.tx_id = t.id
```

NEW:
```
  ON va.restaurant_id = t.restaurant_id AND va.tx_id = t.source_tx_id
```

DO NOT change anything else. The `restaurant_id` half of the predicate is already correct and scopes the JOIN so `source_tx_id` uniqueness is preserved. After Task 1 lands, `va.tx_id` is `text` and `t.source_tx_id` is `text` — types align, JOIN compiles.

Rationale: Same composite-PK bug as 0020. Without this fix, 0022 fails with the same SQLSTATE 42703 and the Phase 9 dashboard cannot read `is_cash`.
  </action>
  <verify>
    <automated>grep -n "t\.id" supabase/migrations/0022_transactions_filterable_v_is_cash.sql; echo "Expected: no matches. Post-fix grep:"; grep -n "va.tx_id = t.source_tx_id" supabase/migrations/0022_transactions_filterable_v_is_cash.sql</automated>
  </verify>
  <done>
- Line 18 reads `  ON va.restaurant_id = t.restaurant_id AND va.tx_id = t.source_tx_id`
- No remaining `t.id` in the file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct Phase 8 D-04 decision doc — tx_id text, not uuid</name>
  <files>.planning/phases/08-visit-attribution-data-model/08-CONTEXT.md</files>
  <action>
Apply one exact edit to `.planning/phases/08-visit-attribution-data-model/08-CONTEXT.md` line 20:

OLD:
```
- **D-04:** MV columns: `restaurant_id uuid`, `tx_id uuid`, `card_hash text`, `is_cash boolean`, `visit_seq integer` (NULL for cash), `business_date date`.
```

NEW:
```
- **D-04:** MV columns: `restaurant_id uuid`, `tx_id text` (sourced from `transactions.source_tx_id` — `transactions` has no surrogate `id`; PK is composite `(restaurant_id, source_tx_id)`), `card_hash text`, `is_cash boolean`, `visit_seq integer` (NULL for cash), `business_date date`.
```

Doc-only correction — no code impact. Ensures future readers of the Phase 8 decisions know why `tx_id` is `text`.
  </action>
  <verify>
    <automated>grep -n "tx_id uuid" .planning/phases/08-visit-attribution-data-model/08-CONTEXT.md; echo "Expected: no matches"; grep -n "tx_id text" .planning/phases/08-visit-attribution-data-model/08-CONTEXT.md</automated>
  </verify>
  <done>
- D-04 line documents `tx_id text` with a parenthetical explaining the composite PK.
- No remaining `tx_id uuid` in the file.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify locally against TEST — db push + phase 8 integration test</name>
  <files>tests/integration/phase8-visit-attribution.test.ts</files>
  <action>
Run the verification sequence against TEST Supabase (never DEV first) to confirm the three preceding edits are correct before pushing to DEV.

Commands (in order, stop on first failure):

1. **Apply migrations to TEST:**
   ```
   supabase db push --db-url "$TEST_DB_URL"
   ```
   Expected: migrations 0020, 0021, 0022 all apply cleanly. No SQLSTATE 42703.

2. **Run Phase 8 integration test:**
   ```
   npm run test:integration -- tests/integration/phase8-visit-attribution.test.ts
   ```
   Expected: all tests pass. The test already types `tx_id: string` in every cast (per debug doc), so the `text` type change is already aligned.

3. **If (1) or (2) fails:** stop, report the exact error, do NOT proceed to the DEV checkpoint. Return to Tasks 1-3 to fix.

If `TEST_DB_URL` env var is not set, read from `.env.test` or ask the user to export it. Do not fall back to pushing to DEV without TEST verification — the whole point of this task is to catch a second bug before it lands.
  </action>
  <verify>
    <automated>supabase db push --db-url "$TEST_DB_URL" && npm run test:integration -- tests/integration/phase8-visit-attribution.test.ts</automated>
  </verify>
  <done>
- `supabase db push` completes against TEST with no errors.
- `phase8-visit-attribution.test.ts` all-green.
- Ready to hand off to checkpoint for DEV push + UAT re-run.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Checkpoint — push to DEV and confirm UAT Test 1 passes</name>
  <files>.planning/phases/09-filter-simplification-performance/09-UAT.md</files>
  <action>
Human-verify checkpoint. Claude has already automated the edits (Tasks 1-3) and TEST verification (Task 4). This checkpoint pushes to DEV and confirms the Phase 9 UAT cold-start smoke test passes.

Automated prep steps for Claude to run before pausing:
1. `supabase db push --db-url "$DEV_DB_URL"` — expect clean apply of 0020/0021/0022.
2. Start dev server: `npm run dev` (background).
3. Emit the dashboard URL for the user to open.

Then PAUSE for the user to perform the visual/functional verification in `<how-to-verify>`. On "approved", update `.planning/phases/09-filter-simplification-performance/09-UAT.md`:
- Test 1 `result: issue` → `result: passed`, remove the `reported:` block.
- Update summary counts: `issues: 0`, `passed: 1`.
- Do NOT change Tests 2-9 status — those will be re-run by the UAT workflow, not this plan.
  </action>
  <what-built>
- 0020 patched: `t.id` → `t.source_tx_id` (line 12); `tx_id uuid` → `tx_id text` (line 49).
- 0022 patched: JOIN predicate uses `t.source_tx_id`.
- 08-CONTEXT.md D-04 corrected.
- TEST verified: `supabase db push` clean, `phase8-visit-attribution.test.ts` green.
- DEV: migrations 0020/0021/0022 applied by Claude before checkpoint pause.
  </what-built>
  <how-to-verify>
1. Confirm DEV migration push output (shown by Claude above) has no errors and all three migrations are listed as applied.

2. Open the dev dashboard URL (Claude will print it). Log in as the seeded test restaurant.

3. Confirm UAT Test 1 (cold-start smoke) passes:
   - Dashboard loads — no 500 page, no error toast.
   - Exactly 2 KPI tiles render (Revenue + Transactions) with live numbers (not empty state, not "column is_cash does not exist").
   - No errors in the browser console or server logs referencing `transactions_filterable_v`, `visit_attribution_mv`, `t.id`, or SQLSTATE 42703.

4. Sanity check via DB MCP (or psql): `SELECT COUNT(*) FROM public.visit_attribution_mv;` returns > 0 against DEV.

5. Reply with "approved" if all 4 checks pass. On issue, paste the exact error and Claude will iterate.
  </how-to-verify>
  <verify>
    <automated>grep -q "result: passed" .planning/phases/09-filter-simplification-performance/09-UAT.md && grep -qE "issues: 0" .planning/phases/09-filter-simplification-performance/09-UAT.md</automated>
  </verify>
  <done>
- DEV db push applied 0020/0021/0022 cleanly.
- UAT Test 1 visually confirmed passing by the user.
- 09-UAT.md Test 1 updated to `result: passed`, summary counts updated.
- Phase 9 UAT is unblocked — Tests 2-9 can be re-run by the standard UAT workflow.
  </done>
  <resume-signal>Type "approved" once DEV is green and UAT Test 1 passes, or report the exact error for another gap-closure iteration.</resume-signal>
</task>

</tasks>

<verification>
Full verification chain:

1. **Static grep checks (post-edit):**
   - `grep -rn "t\.id" supabase/migrations/0020_visit_attribution_mv.sql supabase/migrations/0022_transactions_filterable_v_is_cash.sql` → no matches
   - `grep -n "tx_id uuid" supabase/migrations/0020_visit_attribution_mv.sql .planning/phases/08-visit-attribution-data-model/08-CONTEXT.md` → no matches
   - `grep -n "t.source_tx_id" supabase/migrations/0020_visit_attribution_mv.sql supabase/migrations/0022_transactions_filterable_v_is_cash.sql` → matches present in both

2. **Migration application (TEST):**
   - `supabase db push --db-url "$TEST_DB_URL"` → exit 0

3. **Integration test (TEST):**
   - `npm run test:integration -- tests/integration/phase8-visit-attribution.test.ts` → all green

4. **Migration application (DEV):**
   - `supabase db push --db-url "$DEV_DB_URL"` → exit 0

5. **UAT Test 1 (DEV):**
   - Cold start dashboard → live KPI tiles, no errors
</verification>

<success_criteria>
- [ ] Migration 0020 line 12 reads `t.source_tx_id as tx_id`
- [ ] Migration 0020 line 49 reads `tx_id text` in the test helper RETURNS TABLE
- [ ] Migration 0022 line 18 reads `va.tx_id = t.source_tx_id`
- [ ] 08-CONTEXT.md D-04 documents `tx_id text` with composite-PK rationale
- [ ] `supabase db push` against TEST applies 0020/0021/0022 without error
- [ ] `phase8-visit-attribution.test.ts` passes against TEST
- [ ] `supabase db push` against DEV applies 0020/0021/0022 without error
- [ ] UAT Test 1 (dashboard cold-start smoke) passes on DEV
- [ ] UAT Tests 2-9 can now be executed (no longer blocked by prior-phase)
</success_criteria>

<output>
After completion, create `.planning/phases/09-filter-simplification-performance/09-03-SUMMARY.md` capturing:
- What was edited (exact lines in 0020, 0022, 08-CONTEXT.md)
- Which UAT tests are now unblocked
- Any deviations from plan (e.g., if TEST_DB_URL was unavailable and a workaround was used)
- Confirmation that 09-UAT.md Test 1 transitioned from `issue` to `passed`
</output>
