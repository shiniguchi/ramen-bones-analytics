# v1.4 Requirements Archive

**Milestone:** v1.4 Weekly Campaign Read
**Archived:** 2026-05-07
**Status:** All requirements shipped ✅

## v1.4 Requirements

### UPL-08 — Weekly Counterfactual Pipeline ✅ v1.4

**Description:** Pipeline computes per-ISO-week counterfactual uplift with bootstrap CI re-fit on the 7-day slice; persists weekly history rows (one per fully-completed Mon–Sun week since campaign launch).

**Covered by:** Phase 18 (Plans 18-01, 18-02, 18-03)

**Validation:**
- Migration 0069 extends `window_kind` CHECK + creates `campaign_uplift_weekly_v`
- `compute_iso_week_uplift_rows()` writes rows with independent bootstrap CI (1000 paths, seed 100_000+k)
- API returns `weekly_history[]` on `/api/campaign-uplift`
- Spot-check: sarimax `point_eur` for `iso_week_end=2026-05-03` = −€149.04 (spec: −€149 ± €10, delta €0.04 ✅)

**Notes:** Bootstrap CI is independently re-fit per 7-day slice — never derived by subtracting daily cumulative bounds (correlated samples). This is a hard invariant documented in CONTEXT.md and enforced by a test asserting the seed-namespace disjoint property.

---

### UPL-09 — Weekly Bar Chart Hero ✅ v1.4

**Description:** Dashboard `CampaignUpliftCard` replaces cumulative-since-launch hero with last-completed-week read + bar-chart history (CI whiskers, color-coded by significance, tap-to-scrub).

**Covered by:** Phase 18 (Plans 18-04, 18-05, 18-06, 18-07)

**Validation:**
- Hero: "Week of 4月27日 – 5月3日" (not "Since April 14th") confirmed in DEV round C ✅
- Bar chart: 2 bars rendered, CI whiskers visible, gray color (CI straddles 0), dashed baseline + legend ✅
- Tap-to-scrub: bar 1 → Apr 27 week; bar 0 → Apr 20 week ✅
- Mobile: `touchEvents:'auto'`, page scrolls with chart in view ✅
- Overflow fix: no CI whisker bleed outside 100px chart box ✅
- i18n: `uplift_week_label`, `uplift_bar_chart_caption`, `uplift_history_x_axis_label` × 5 locales ✅

**Notes:** Maturity tier now sourced from weeks-since-launch (Decision A), not `n_days`. Per-week rows always have `n_days=7` which would always resolve to "early" under the old n_days-based derivation. Decision A fixes this.

---

## Traceability Table

| Req ID | Description | Phase | Status |
|--------|-------------|-------|--------|
| UPL-08 | Weekly counterfactual pipeline | 18 | ✅ Complete |
| UPL-09 | Bar chart hero + tap-to-scrub | 18 | ✅ Complete |

## Notes on Changed Requirements

- **UPL-09 scope extension from UPL-05/06:** UPL-09 supersedes the cumulative-since-launch display surface (UPL-05 "cumulative headline" and UPL-06 "CI overlap labeling") on the card. The underlying `cumulative_since_launch` rows still exist in the DB and pipeline (backwards-compat deferred), but the card no longer surfaces them.
