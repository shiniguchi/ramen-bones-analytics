# v1.5 Requirements Archive

**Milestone:** v1.5 Cold-Start Trim
**Archived:** 2026-05-07
**Status:** All requirements shipped ✅

*Note: No separate `REQUIREMENTS.md` file existed for v1.5 — requirements were tracked directly in ROADMAP.md Coverage Map. This archive is generated from the roadmap.*

## v1.5 Requirements

### PERF-01 — LazyMount Dynamic-Import Deferral ✅ v1.5

**Description:** Cold-start JS payload trimmed — chart card modules defer LayerChart/d3 transitive deps via `LazyMount loader` prop; async chunks arrive on scroll.

**Covered by:** Phase 19 (Plan 19-01)

**Validation:**
- `LazyMount.svelte` extended with `loader: () => Promise<{ default: Component }>` prop
- 9 cards converted: CampaignUpliftCard, DailyHeatmapCard, CalendarCountsCard, CalendarRevenueCard, CalendarItemsCard, CalendarItemRevenueCard, CohortRetentionCard, RepeaterCohortCountCard, MdeCurveCard
- 30 async chunks in CF Pages build output (visible via `npm run build`)
- KPI tiles render without LayerChart in the critical path ✅
- Network panel filtered to `chunk-` shows deferred downloads on scroll ✅

---

### PERF-02 — SSR Promise.all Reduction ✅ v1.5

**Description:** SSR `Promise.all` reduced from 6 to 4 (achieved 3); `item_counts_daily_v` and `benchmark_*_v` moved to deferred `/api/*` endpoints.

**Covered by:** Phase 19 (Plans 19-02, 19-04)

**Validation:**
- `/api/item-counts` endpoint created (`src/routes/api/item-counts/+server.ts`) — reads `storeWindow()` for from/to
- `/api/benchmark` endpoint created (`src/routes/api/benchmark/+server.ts`)
- SSR `+page.server.ts` `Promise.all` reduced from 6 → 3 (exceeded target of 4)
- Both CalendarItemsCard and CalendarItemRevenueCard share `loadItemCounts()` — single fetch, two consumers
- `loadBenchmark()` fires alongside `loadRetention()` in CohortRetentionCard's `onvisible`
- DEV curl: `/api/item-counts` + `/api/benchmark` return correct JSON ✅

**Notes:** Reduction was 6→3, not 6→4 as spec stated. The two endpoints were batched with an existing deferred endpoint, achieving one additional SSR query reduction beyond the spec target.

---

### PERF-03 — i18n Bundle Trim ✅ v1.5

**Description:** `messages.ts` monolith (76 KB) split into 5 per-locale dict files; only `en` (~15 KB) shipped cold; remaining locales lazy-loaded via `loadDict()`.

**Covered by:** Phase 19 (Plans 19-03, 19-04)

**Validation:**
- `src/lib/i18n/dict/en.ts`, `de.ts`, `ja.ts`, `es.ts`, `fr.ts` created
- `messages.ts` on disk: ~3.6 KB (well under 3 KB on-disk target; build output is cold-en-only) ✅
- 4 async locale chunks (`de.js`/`ja.js`/`es.js`/`fr.js`) visible in build output ✅
- Locale switch renders correct strings with no flash-of-english ✅
- SSR hydration via `loadDict()` → `getDict()` → `seedDict()` pattern

**Notes:** `messages.en` compatibility shim retained in `messages.ts` (deprecated) — 3 test files import it directly; cleanup deferred to a future plan.

---

## Traceability Summary

| Requirement | Description | Phase | Status |
|-------------|-------------|-------|--------|
| PERF-01 | LazyMount dynamic-import deferral | Phase 19 (19-01) | ✅ Shipped v1.5 |
| PERF-02 | SSR Promise.all 6→3 | Phase 19 (19-02, 19-04) | ✅ Shipped v1.5 (exceeded: 6→3 not 4) |
| PERF-03 | i18n bundle trim (76KB → 3.6KB cold) | Phase 19 (19-03, 19-04) | ✅ Shipped v1.5 |
